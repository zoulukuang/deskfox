# 应用身份命名规则(macOS Bundle ID / Windows AppId)

> 应用身份在两个平台上都决定:数据目录归属 / URL Scheme handler / 系统是否识别为同一应用(决定升级是替换还是共存)。
> 改身份 = 系统视为全新应用(权限重置、数据目录易主、旧版变孤儿)。**改动须谨慎,merge 上游时勿误覆盖**。
>
> **关联规范**:本文规定**身份标识**(AppId / Bundle ID),发布渠道语义与版本号规则见 [`版本号与发布渠道规范.md`](./版本号与发布渠道规范.md)(3-tier:稳定版 / 预览版 / 本地测试)。

---

## 当前规则

### macOS Bundle ID(reverse-DNS,2026-04-30 起,`bundle-id-debrand` 落地)

| 档 | Bundle ID | 用途 | 应用程序网格搜索 |
|---|---|---|---|
| **prod** | `ai.deskfox.app` | 正式发布 | ✅ 可见 + 可搜(无后缀) |
| **beta** | `ai.deskfox.app.beta` | 测试版 | ✅ 可见 + 可搜(`.beta` 类 Chrome Beta) |
| **dev** | `ai.deskfox.app.dev` | 开发自用 | ⚠️ 网格搜索过滤 `.dev` 后缀,Cmd+Space / Launchpad / 第三方 launcher 仍可见 |

**配置位置**:`packages/branding/tauri-overrides/{prod,beta,dev}.json` 各自的 `identifier` 字段。

**base `tauri.conf.json` 仍是上游 `ai.opencode.desktop.dev`** — 这是 anomalyco/opencode 上游 contract,**不动**(P1 隔离,merge upstream/dev 不冲突)。三档全在 fork 自己的 override 里覆盖。

### Windows AppId(Inno Setup GUID,2026-04-30 起,`win-tri-env-appid` 落地)

> Windows 没有 "Bundle ID" 概念,等价物是 Inno Setup `AppId`(GUID)。系统通过 AppId 识别"装新版要不要替换旧版"以及控制面板"应用与功能"里的注册项归属。

| 档 | AppId | OutputBaseFilename | 控制面板"应用与功能"显示名 | 安装目录 |
|---|---|---|---|---|
| **prod** | `{F9F6F6C5-D865-468C-BCE5-BF0ECA24A763}`(沿用现有,锁死) | `DeskFox-<版本>-setup.exe` | DeskFox | `Program Files\DeskFox` |
| **beta** | `{86413DCA-EA81-415A-A309-473EBFD78990}` | `DeskFox-Beta-<版本>-setup.exe` | DeskFox Beta | `Program Files\DeskFox Beta` |
| **dev** | `{4C5D29F2-3BBB-49A2-B248-B74B716F8EA1}` | `DeskFox-Dev-<版本>-setup.exe` | DeskFox Dev | `Program Files\DeskFox Dev` |

三档独立 GUID + 独立安装目录 → Win 端三档可在同一台机器**同时安装并存**,各自有独立的卸载入口、开始菜单分组、快捷方式。

**配置位置**:
- `packages/branding/installer/DeskFox.iss` — `#ifndef AppEnv` 兜底 `prod` + `#if/#elif/#else` 三档切换 `AppId` / `AppName` / `OutputBase` 三个 `#define`,默认 `prod`,可命令行 override
- `packages/branding/scripts/pack-installer.ps1` — `-Env dev|beta|prod` 参数(默认 `prod`),通过 `ISCC /DAppEnv=...` 注入

---

## 设计原则(Mac / Win 共通)

1. **三档身份独立 → 同一台机器可共存安装**
   - Mac:三档全部已落地(2026-04-30 `bundle-id-debrand`)
   - Win:三档全部已落地(2026-04-30 `win-tri-env-appid`)

2. **prod 身份一旦发布,任何情况禁止改**
   - Mac:`ai.deskfox.app` 锁死(改了 = 系统识别成新 app,旧用户数据/权限丢)
   - Win:`{F9F6F6C5-D865-468C-BCE5-BF0ECA24A763}` 锁死(改了 = installer 视为新 app,已装用户的版本不会自动升级,会变成"装两份")

3. **base 配置不动,override 全在 fork-only 文件**
   - Mac:`tauri.conf.json` 不动(上游 contract),`packages/branding/tauri-overrides/*.json` override
   - Win:`packages/branding/installer/DeskFox.iss` 是 fork-only(本身不存在上游),用 `#ifdef` / ISCC `/D` 切档,不另立文件

4. **reverse-DNS 与域名对齐(仅 Mac)**:`ai.deskfox.app` ↔ user 持有的 `deskfox.ai` 域名。未来做 URL Scheme handler / Universal Link / OAuth callback 时,Apple 要求 `apple-app-site-association` 校验 reverse-DNS 与域名所有权一致;现在对齐 = 未来 0 迁移成本。Win 端 GUID 与 DNS 无关,无此要求。

5. **完全切断 anomalyco/opencode 命名空间共享(Mac)**:不用 `ai.opencode.*` 前缀(那是上游)。用户即使同 Mac 装两个软件,TCC / 数据目录 / URL Scheme 完全独立。

6. **`ai.` 前缀不用 `com.`(Mac)**:符合 AI / agent 类工具命名习惯(对照 `ai.cursor.app`、上游 `ai.opencode.*` 都用此前缀)。

---

## 维护规则

### Mac

- **merge upstream/dev 时**:base `tauri.conf.json` 的 `identifier` 字段会回到上游(`ai.opencode.desktop.dev`),**这是预期行为,接受 theirs**;三档 override 在 `packages/branding/tauri-overrides/` 里,跟上游 0 路径冲突,自动保留
- **扩新档**(如未来加 `nightly`):新建 `packages/branding/tauri-overrides/nightly.json`,`identifier` 用 `ai.deskfox.app.nightly` 形式延续 reverse-DNS 树
- **绝不在 base `tauri.conf.json` 里改 identifier**:违反 P1 隔离,merge upstream 必冲突
- **绝不用回 `ai.opencode.*` 前缀**:那是上游命名空间,共享会出 4 类问题(详见 [`docs/features/bundle-id-debrand/3-changelog.md`](../features/bundle-id-debrand/3-changelog.md) 触发原因段)
- **改 Bundle ID 须重打安装包并通知 user 重装**:旧 ID 的 `/Applications/<App>.app` 不会自动清(macOS 按 ID 识别),TCC 权限重置一次(实测 macOS 14+ 自动放行用户目录,无干预)

### Windows

- **prod GUID `{F9F6F6C5-D865-468C-BCE5-BF0ECA24A763}` 锁死,任何情况禁止改** — 改了等于"重新发布一款新软件",已装用户的版本不会自动升级,会变成"装两份",必须先卸载旧版
- **dev / beta GUID 落地后也不应该改**(原则同上,虽然实际影响仅限开发者本地)
- **扩新档**(如未来加 `nightly`):`DeskFox.iss` 加新 `#elif AppEnv == "nightly"` 分支 + 新 GUID,同时 `pack-installer.ps1` 加 `-Env nightly` 选项
- **Inno Setup `#ifdef` 用法注意**:Inno Setup 预处理器不支持 `else if`,要用 `#elif`;或者由 ISCC `/D AppEnv=...` 命令行注入参数,在 `.iss` 内部 `#ifdef AppEnv` + `#elif AppEnv == "..."` 链式判断

---

## 历史溯源

- **2026-04-30 早**(macOS):三档全用上游 base `ai.opencode.desktop.dev`,prod / beta / dev 不区分,踩坑发现 `.dev` 后缀触发 macOS 26 应用程序网格搜索隐藏 → `prod-bundle-id-fix`(`7618346fe`)给 prod / beta 各加独立 identifier(prod 去 `.dev`),但仍带 `opencode` 字眼
- **2026-04-30 晚**(macOS):`bundle-id-debrand`(`3fd5ceaf5`)完整品牌切割,三档全去 `opencode` 改 `ai.deskfox.app` 系列,与上游 0 命名空间共享。本规则文档同笔落地(原文件名 `Bundle-ID-命名规则.md`,标题"Bundle ID 命名规则(macOS)")
- **2026-04-30 晚**(本次扩规则):分支策略 v2 讨论中发现 Mac/Win 不对称(Mac 三档共存,Win 单一 AppId 不能共存),决定补 Win 三档 AppId。本文档同笔扩展为"应用身份命名规则",涵盖两端
- **2026-04-30 晚**(Windows 落地):`win-tri-env-appid` — `DeskFox.iss` 加 `#if AppEnv` 三档分支(beta GUID `{86413DCA-...}` / dev GUID `{4C5D29F2-...}` 新生成,prod GUID 不变);`pack-installer.ps1` 加 `-Env` 参数;v2 切换后第一笔 feat 落地

详见:
- [`docs/features/bundle-id-debrand/3-changelog.md`](../features/bundle-id-debrand/3-changelog.md) — Mac 完整设计 rationale
- [`docs/features/win-tri-env-appid/3-changelog.md`](../features/win-tri-env-appid/3-changelog.md) — Win 三档 AppId 实施细节
- [`docs/features/分支策略-v2/1-spec.md`](../features/分支策略-v2/1-spec.md) 附录 D — Win 三档 AppId 时机决策的上下文
