# Features 索引

> 规范 v2(2026-04-27)起,每个 feature 一个目录,含 1-spec / 2-plan / 3-changelog 三文档。
> 详情见 [`CLAUDE.md`](../../CLAUDE.md) 的"完整文档链路"段。
>
> **分支名注**:2026-05-21 起本仓主分支 `dev` → `main`(详 `rename-dev-to-main` feat)。以下 feat 描述中"dev 分支"字眼(historical entries)指向当前 `main`,描述作为历史快照不回填。

## 主分支命名空间(2026-05-21 起)

| 命名空间 | token | 含义 |
|---|---|---|
| Git 分支 | `main` | 本仓主分支(原 `dev`,2026-05-21 重命名)|
| Git 分支 | `feat/<name>` | feat 分支,合 main 即销毁 |
| Git 分支 | `upstream/dev` | 上游 anomalyco/opencode 主分支(他们仍叫 `dev`)|
| Installer channel | `prod` | 稳定版(发布物,无版本号后缀)|
| Installer channel | `beta` | 测试版(预发,保留 AppId 备用,日常不主动 ship)|
| Installer channel | `dev` | 预览版(可对外发,版本号 `-dev` 后缀)|

**两个命名空间不撞**:`main` 是分支维度,`dev`(channel)是 installer 维度。同一个 `main` 分支可以 build 出 prod / beta / dev 三种 installer。

| feat-id | 状态 | 简介 |
|---|---|---|
| [chat-tilde-del-fix](./chat-tilde-del-fix/) | done | 聊天渲染单波浪号误判删除线(REQ-098,2026-08-07,Medium)— GFM 内置 del 定界是 `~~?`,同一行两个「数字~数字」区间即被闭合成 `<del>`(`4.80~5.05 … 5.20~5.35` → 中间整段被划,金融/数值场景高频)。修法:`packages/ui/src/context/marked.tsx` + `packages/web/src/components/share/content-markdown.tsx` **各加一次** tokenizer override 收紧成只认 `~~`(两包 `marked.use()` 各自实例,改一处不传导);⚠ 非匹配必须返 `undefined`,返 `false` 会回退内置实现(已实测)。触 ui/web 路径黑名单 → **R4 override 已审批**。[feat: chat-tilde-del-fix] |
| [oauth-loopback-bind](./oauth-loopback-bind/) | done | OAuth callback server 绑 0.0.0.0(REQ-019,2026-08-07,Tiny→Medium)— `digitalocean.ts:187` / `openai/codex.ts:305` / `mcp/oauth-callback.ts:155` 三处 `listen(port, cb)` 缺 hostname,违反 R6;上游自家 `plugin/xai.ts:502` 已是正确写法 → 遗漏而非设计。fork 侧先改不等上游,带 FORK marker;配套裸-listen 守卫测试 + `UPSTREAM-MERGE-GUIDE.md` §5.6 复查 grep(长期价值)。**⚠ redirect URI 三处保持不动**(改了会 `redirect_uri mismatch`)。触 `packages/opencode/` 黑名单 → **R4 override 已审批**。[feat: oauth-loopback-bind] |
| [tray-health-status](./tray-health-status/) | done | 托盘反映后台健康状态(REQ-099,2026-08-07,Medium)— 看门狗 3 态(`restarting/ready/gave-up`)只发 renderer,托盘无表示;顺带查出 `setTrayStatus` **自覆盖真 bug**(`buildMenu()` 重建时 label 写死"就绪"并重赋 `statusItem`,预留接口"调了也没用")。修法:模块级 `currentStatusLabel` + `mapWatchdogStatusToTray` 纯函数 + 主进程 emit 回调直接接线(不新增 IPC)+ 三态图标脚本生成(⚠ mac `setTemplateImage` 只保留 alpha,徽标须挖洞否则两态 md5 相同)。不触黑名单、无需 R4。[feat: tray-health-status] |
| [session-heal-stat-timeout](./session-heal-stat-timeout/) | in-progress | session 列表自愈 stat 超时+闩(REQ-079 含 076,2026-08-02,Small)— 离线卷死路径 `fs.stat` 挂死 + 每次 `Session.list` 全量重扫拖慢侧栏。`confirmedMissingByNodeFs` 可注入 statFn/timeoutMs + 3s 竞速(超时=检查出错→false 保守不动);新 `healStaleSessionDirectoriesOnce` 进程级闩(key 含 worktree,改名自动重扫;fromDirectory 主路径不走闩)。3 新单测 + 既有 6 回归 + 干净 env project 簇 139 全绿。**触 `packages/opencode/` 路径黑名单 → R4 override 待 user 审批 commit**(复核报告见 3-changelog;session.ts 实际改动仅调用点换名 2 行)。[feat: session-heal-stat-timeout] |
| [model-capability-ui](./model-capability-ui/) | in-progress | 模型能力-UI 对齐(REQ-026,2026-08-02,Medium)— 视觉模型静默降级三层治理:① user config getbot 7 视觉模型补 modalities + 删 4 死 ID(仓外,有备份);② 粘贴/拖图前端拦截(三态判定纯函数 `model-capability.ts`,仅明确 false 拦,unknown 放行走后端 ERROR 兜底);~~③ 选择器行内 📷/🧠 徽标~~(**已撤,2026-08-02 follow-up**:真机截图实锤徽标挤爆 w-72 弹层行宽致样式全乱,user 拍板去掉;拦截与数据补齐保留,恢复徽标可从 `ce948764fd` 取回)。4 单测 + typecheck 绿;上游 2 文件 FORK marker / 0 R4;getbot 真发图端到端阶段验。[feat: model-capability-ui] |
| [imbot-agent-schema-upgrade](./imbot-agent-schema-upgrade/) | in-progress | imbot agent 自动升级(REQ-094,2026-08-02,Medium)— 原「已有 agent.imbot 完全跳过」致发版新 spec 对存量用户永不生效。抽纯逻辑 `imbot-agent.ts`:spec 加 `_schemaVersion:3`,三分支升级(无→注入 / 低版本→覆盖管理键 `_schemaVersion`/`description`/`permission` 保留 model/prompt/tools 等用户键 / 同高版本→零写盘);形状异常防御不覆盖。6 单测 + typecheck 绿;fork-only 非黑名单 / 0 R4;真机 V2 残留升级 T7 端到端阶段验。[feat: imbot-agent-schema-upgrade] |
| [feishu-retry-feedback](./feishu-retry-feedback/) | in-progress | 飞书 LLM 重试期反馈(REQ-093,2026-08-02,Medium)— 核心自动重试(503/overloaded,无上限)期间 bot 沉默「像死了」。纯 adapter 层:dispatcher 消费 `session.status` retry(计数/onRetry 回调/**retry 重置 fastfail 窗口**,消除边播报边硬杀矛盾)+ pipeline 节流播报「正在自动重试第 N 次」(首条立即/≥90s/≤3 条)+ fastfail 判死后防御式 `session.abort` 终止后台重试 + friendlyErrorReply 重试终态 pattern。核心 0 改动;11 新单测,adapter 全量 792 pass;真机 mock 503 + REQ-086 联动 blocker 随发版验收。[feat: feishu-retry-feedback] |
| [startup-sidebar-feedback](./startup-sidebar-feedback/) | in-progress | 启动期 sidebar 点击无响应(REQ-092,2026-08-02,Medium)— navigateToProject not-ready 快路径(跳过 SDK await 链直接路由,点击即响应)+ SessionSkeleton 门条件抽纯函数补 ready 维度(启动期亮 skeleton)。REQ-037 已随 Electron 换基座消解,采方向①只补反馈不阻 click。4 单测 + typecheck 绿;上游 2 文件 FORK marker / 0 R4;CDP e2e(T5/T6)本批端到端阶段,REQ-037 轻触回归真机随发版。[feat: startup-sidebar-feedback] |
| [feishu-session-project-visibility](./feishu-session-project-visibility/) | in-progress | 飞书 session 桌面端项目列表缺失(REQ-086,2026-08-02,Medium)— 根因:adapter 建 session 恒用 `resolveWorkspace(account.workspace)`,未设 workspace 回退全局 imbot-workspace → projectID 不等于任何桌面项目 → scope=project 不可见。修法:绑定成功后当前有打开项目且账号未设 workspace 则自动注入为该项目目录(成功页明示会话进项目列表 + 文件落 `_deskfox/`);编辑页空态提示「不进项目列表」;毗邻 bug 一并修:saveAccount 重绑沿用清单漏 model/workspace → re-OAuth 即丢设置。6 单测(T1-T6 含 bug-repro)+ typecheck 绿;0 上游黑名单 / 0 R4;真机 T7/T8 + REQ-093 联动 blocker 随发版验收。[feat: feishu-session-project-visibility] |
| [first-launch-onboarding](./first-launch-onboarding/) | in-progress | 首次启动默认工作区(REQ-083 v4,2026-07-13,Medium)— 首启在系统 Documents 下建 `New DeskFox/` 放一份介绍文档 `关于 DeskFox 你该知道的几件事.md`(base64 二维码内嵌单文件),自动打开为工作区 + 该文档作首个 tab。主进程 `deskfox/onboarding.ts`(纯决策 `decideOnboarding` + IO 壳,写失败降级不阻塞)+ `electron-store` marker gate(重启不重复/删不重建/已存在不覆盖);自动打开复用现成 deep link `open-project`(加可选 `file` 参,renderer `handleDeepLinks` 开首 tab),免新 IPC。base64 内嵌经实测(`rewriteAssetSrc` 素通 `data:` + DOMPurify 默认放行 img data:)采单文件。10 主进程单测 + deep-link 用例 + typecheck 绿;上游侵入 3 文件(FORK marker)/ 0 R4。**首启端到端 + Win + TCC 兜底 + 设置 UI 待真桌面 QA/follow-up**。[feat: first-launch-onboarding] |
| [quick-ask-enter-align](./quick-ask-enter-align/) | done | 「加入聊天」浮窗快捷键对齐主输入框(REQ-082,2026-07-13,Medium 纯前端)— 选区浮窗提交键从 `Cmd/Opt+Enter`(Win `Ctrl+Enter`)对齐成裸 `Enter` 提交 / `Shift+Enter` 换行 / IME 组合守卫 / `Esc` 取消。两个同款浮窗一起改(`host.tsx` fork-only + `file-tabs.tsx` 上游);新建共享纯函数 `utils/ime.ts` `isImeComposingEvent`(prompt-input 的私有 `isImeComposing` 未导出不可复用);i18n `shortcutHint` 模板串改文案(zh/zht/en)并移除 `shortcut` 传参。4 单测 + typecheck 绿;上游侵入 1 文件(续用 FORK marker)/ 0 R4。[feat: quick-ask-enter-align] |
| [project-close-heal-race](./project-close-heal-race/) | done | 右键项目「关闭」失效修复(2026-07-08,Tiny)— REQ-072(`e4d815eb5`)的"折叠竞态自愈"效应把 `projects.list()` 当依赖追踪,关闭当前项目时 list 先变、路由还没切走,效应误判"被折叠"又 `open` 补回 → 关不掉(Win/Mac 同炸)。修法:`isListed` 包 `untrack`,自愈只由路由进入/boot 完成驱动;效应抽 fork-only `project-restore.ts` 可单测,6 单测(3 复现+3 回归)+ 变异验证 + CDP 真机三路径验收;`package.json` test 加 `--conditions=browser`(bun 默认解析 solid server 构建,createEffect 是 no-op,effect 单测跑不动;521 全量全绿)。 |
| [win-anchor-hide-case-fold](./win-anchor-hide-case-fold/) | in-progress | REQ-069/072 的 Windows 适配(阶段二,Medium)— 兑现 REQ-069 spec 明列「Windows 差异项留阶段二」的两个「Mac 上看不见、Win 上必现」缺口。**缺口1**:`.deskfox` 锚目录在 Win 资源管理器不隐藏(mac 点目录天然隐藏)→ 实现 `anchor.ts` `hideAnchorDir` win32 分支(`attrib +h`,可注入 setHidden 便于单测)+ 接入 `writeAnchor`(写成功才隐藏,唯一建锚点单点收口,project.ts 不碰)。**缺口2**:Win 文件系统大小写不敏感但 relocate 链用裸 `===` 比路径 → 持久化 worktree(历史/深链/手输大小写不受控)与后端 realpath 规范化路径差一位即匹配漏判→relocate 静默失效退「打不开+forget」→ 新 fork-only `same-directory.ts`(Win 风格折叠小写,POSIX 不折叠=Mac 零回归),统一 `server.tsx` 持久化匹配层 + `home.tsx`/`layout.tsx` relocate 取 id 处。触黑名单 `packages/core/anchor.ts`(fork-only 平台薄层)→ R4。缺口3(UNC/网络盘 `mountRootOf` Win 加固)记 backlog。core anchor 27 测 + app same-directory/server 20 测全绿 + `attrib +h` 真机验证 + typecheck 全绿。[feat: win-anchor-hide-case-fold] |
| [project-continuity-v2026-8-4](./project-continuity-v2026-8-4/) | in-progress | 切项目/改名连续性 + stale-path 簇收官(v2026.8.4,Medium)— REQ-072 会话侧栏改项目维度(改名/挪位/复制后旧会话跟随):后端 `session.ts` `list` 单文件加 global 门控(有真实身份按 project_id 列 / global 哨兵回落 directory 守大杂烩,FORK marker,scope=project plumbing 全上游原生)+ 前端 `session-load.ts` 传 `scope:project` + `tabs.tsx` 去重改 server+session.id;REQ-071 输入框草稿切项目丢失 = 上游自带再水合时序 bug 定点修(不动 keyed/持久化格式,CDP 复现后择候选,回贡上游);REQ-070 stale-path mac 物理盘 QA(承接 068 unreachable + 061 M5,纯 U 盘真机验收无代码)。触上游黑名单 `session.ts` → R4 squash 变体。 |
| [feishu-desktop-session-sync](./feishu-desktop-session-sync/) | spec | 飞书↔桌面同 session 协同呈现(v2026-07-05-2,Large)— REQ-073 六项 + REQ-055 昵称底座,全落 fork-only adapter `adapter-feishu-lark`,预期 0 上游侵入/0 R4。① 停自动归档(删 `message-pipeline.ts` 4 处 `archiveSession`,飞书会话进侧栏)② 跨重启接续(查找回读已落盘 `chatSessionStore`,现只读内存 Map)③ 桌面续聊同 session 不回推飞书(解归档后前端免费)④ title 加 `[botName]` 前缀(抽共用 `getOrCreateSession` helper 统一 4 处近乎逐字重复的查找/创建块)⑤ 授权双端反向失效(`plugin.ts` 订阅 `permission.replied` + `permission-card.ts` `handleExternalResolve`,后端 0 改动)⑥ 群/合并转发发送者昵称(新 `contact-name-resolver.ts` open_id→昵称底座 + graceful 回落)。含 3 项「施工前待钉死」需真飞书凭证/真机(auth 实验/端点实测/并发写)。1-spec 待 user 审签(Large)。 |
| [req069-folder-identity](./req069-folder-identity/) | in-progress | 非 git 文件夹稳定项目身份(v2026.6.25,Large)— 隐藏锚文件 `.deskfox/id` 使普通文件夹改名/挪位后仍认得同一项目;连续性令牌全序 `remote > .git/opencode > 锚id > root`;复制对齐 git(副本共享身份,M7 副本检测作废);M6 锚丢失软恢复(反查 ProjectDirectoryTable);M8 feature flag 默认关灰度。6 单元(U1-U6),触 `packages/core`+`packages/opencode` 黑名单,走 R4 squash 配额变体。1-spec 待 user 审签(R8/Large)。 |
| [retire-tauri-build-residue](./retire-tauri-build-residue/) | done | 清退 Tauri 构建残留(2026-06-21,Medium)— 换基座后死代码/broken 测试清理。删 10 个 Tauri 残留文件(1735 行):旧构建脚本 `build-deskfox.{sh,ps1}`/`pack-installer.{sh,ps1}`/`pack-preview-dev.sh`(被 `build-deskfox-electron.*` 取代,无活调用)+ **broken 测试 `updater-config.test.ts`**(readFileSync 已删的 `minisign.pub`/`src-tauri/*.rs`,全环境 0pass/1fail,且在 `test:ci` 里 → CI branding 包此前一直红)+ DEPRECATED `verify-updater-artifacts.ts` + `tauri-overrides/*.json`(无活链路读)。重写 `lo-bundle-strip.test.ts` 守护对象迁到 `build-deskfox-electron.*` + `verify.ts`(LO 保护逻辑零丢失)+ 清 ~10 处配套注释(历史溯源注释保留)。branding 47 pass(CI 转绿)/ typecheck 3/3 / 6 脚本 bash -n 过。全 fork-only;0 改上游 / 0 R4 / 改的代码文件均注释。 |
| [local-channel-win](./local-channel-win/) | done | local 第 4 档 Win 侧落地 + 双端杀进程三档定稿(2026-06-21)— Win wrapper 补 `-Env local`(本地测试版,独立身份 appId `.local` + 隔离 `opencode-local.db`,永不发布、始终 `--dir`)对齐 Mac;跟苹果端同事协同把 build 前杀进程定稿成**三档矩阵**(打 local 只杀「DeskFox 本地版」不误伤正在用的正式版 / 打发布档杀 `DeskFox`+预览版+Beta 三档因共享 `opencode.db` 不能共存,排除 local、不通杀 `electron`/`opencode-cli`、精确匹配),覆盖 CLAUDE.md 权威 + Win/Mac wrapper + Windows SOP + `/ship`。配套 `verify-fileviewer.py` 全自动文件预览验证入仓(纯 CDP 点侧边栏开项目,修 `[...querySelectorAll].map` spread 让 CDP 求值挂起的坑→for-loop)。双端协作纪律沉淀记忆 `feedback_dual_end_rule_alignment`。全 fork-only;0 改上游 / 0 R4 / 0 黑名单。 |
| [v2026.8.3](./v2026.8.3/) | in-progress | 供应商/模型连通性修复批 + GUI 残余补丁(2026-06-18,Large,版本级 4 单元)— U1 REQ-066 Build 摘要行从 text part 尾部抽到首个 text part 正文前 + Collapsible 默认收起(改上游 message-part.tsx,FORK marker)/ U2 REQ-052 供应商连/断后 `refreshProviders()` 强制失效 providers query(server-sync 层,布局无关)/ U3 REQ-054 getbot `mergeGetbotModels` 纯函数 + 脚本写回 + 应用内「刷新模型」按钮清幽灵 / U4 收敛 `layout.tsx` 双 ToastRegion 为单一(检查更新「每点必双」根因 = Kobalte 单例被两 region 各渲一遍)。**验收覆盖对齐发货布局**:U2/U3 原 e2e 用 `enableNewLayout` 强开 v2 = 测了 DeskFox 不发货的 settings-v2(默认 v1 经典布局 + FORK 隐藏 v2 开关);两笔 fix 把锚点补到 v1 `settings-providers.tsx` + e2e 改测 v1(本地版真机 CDP 实测 v1 面板 4 供应商均渲出 `data-provider-id`)。17 文件 / +983 −60 / typecheck 22/22 · app 459 · ui 30 · e2e 17 全绿 / 上游仅 message-part(FORK)/ 0 R4。**未 push 未合 main**;真桌面 U4 动态 + 视觉对齐待 user 验。 |
| [ship-win-oss-robustness](./ship-win-oss-robustness/) | done | Electron Windows 首发 ship 健壮性 + 跨引擎升级桥踩坑(2026-06-16,Small + 1 prod 发版 + bug-repro)— `/ship` 换 Electron 后首次真发版 DeskFox **2026.8.0**(Win prod,SkipBump 与 Mac 8.0 同波次)暴露的坑修复:① `deploy-electron-updater.sh` 启动 `set -a; source config.env` 自加载 OSS 凭据(原 plain 赋值不 export 到子进程致"缺 OSS 凭据")② `upload-asset-to-oss.sh` ossutil 跨平台(uname→mac/win/linux 二进制 + OSSUTIL_BIN 覆盖 + 平台化下载;原硬编码 mac 路径 Win 上 Exec format error)③ config.env 补 OSS_ACCESS_KEY_ID/SECRET(值在 alibaba-cdn.md)④ ship.md 加**步骤 7c 迁移桥**。**bug-repro:正式版「检查更新」永远"已是最新"** —— 存量正式版是最后一个 Tauri 版,查 Tauri 端点(latest.json+minisign),而 8.0 只喂了 electron 端点(latest.yml),两条更新链互不相见;修法跑 `bridge-electron-updater.ps1` 用 Tauri 私钥签 Electron NSIS + 部署 Tauri 格式 manifest 到 Tauri 端点。**核心经验**:prod 每次 ship 必同时喂 electron + Tauri 两条更新源(直到 Tauri 用户迁完)。纯 fork-only;0 改上游 / 0 R4。 |
| [deskfox-verify-chain](./deskfox-verify-chain/) | done | DeskFox verify 链:一条命令、一个判定、按改动自动选层(2026-06-15,Medium,Electron 基座)— 把现成验收零件(typecheck+oxlint 静态 / `smoke.py` CDP 冒烟 / 冷启动健康 / 发布物校验)编排成 `bun run packages/branding/smoke/verify.ts`,**单一退出码** 🟢0/🟡2/🔴1。四层正交:L0 静态 / L1 冷启动(`--cold` 杀全部→真冷启→`cold-start.py` 监控,抓 sidecar 预热竞态)/ L2 交互(连 CDP 9222 跑 probe boot/providers/panels/settings/files)/ L3 发布物(`--release`/`--build` 校验 dist-deskfox,**electron-updater 口径**:latest.yml sha512 与 exe 实算一致 + 版本非 0.0.0 + .blockmap + LO 进包,不复用过时的 Tauri `verify-updater-artifacts.ts`)。`--changed` 按 git 改动自动选 probe;自动探活 9222 复用在跑的 dev(剔 boot 免打断会话)、没起后台拉跑完只杀自己拉起的。冷启动脚本自包含入仓(源 OPENCODE-PLAN 诊断工具)+ **修其 conn-refused 预热误报**(终态连上才算瞬时)。立 R9 验收铁律入规范(v7 + callout):🔴/L0 不过不进 merge,🟡 须 user 确认。刻意不注册 `package.json` npm 别名(黑名单 + 避上游合并摩擦)。纯 fork-only;0 改上游 / 0 R4 / 0 黑名单。三文档为事后补齐。 |
| [win-plugin-dist-rebuild-guard](./win-plugin-dist-rebuild-guard/) | done | Windows 打包插件 dist 陈旧致 media-gen "Bun is not defined" 修复(2026-06-15,Tiny,bug-repro)— 装完 dev 包冷启动日志 `[media-gen] generate server start failed: ReferenceError: Bun is not defined`,媒体生成功能坏。根因:media-gen 源码 6/12 已 Node 化(node-serve.ts),但 dist 陈旧仍含 `Bun.serve`;飞书那次（`dev-feishu-binding-missing`）的「先重建+Bun 守卫」只加进 `.sh`(Mac),Windows 的 `.ps1` §3.5a 仍只查存在不重建 → 陈旧 dist 混入。修:`.ps1` §3.5a 镜像 `.sh` —— 打包前调 `build-{feishu,media-gen}-plugin.ps1` 先重建 + `Bun.serve` 守卫（残留即 throw）。端到端验证:重出安装包→重装→冷启动 `[media-gen] generate server: http://127.0.0.1:51737` 成功，`Bun is not defined`=0，冒烟全 PASS。副带发现冷启动脚本 is_transient 漏 ERR_CONNECTION_REFUSED 致误报 FAIL（另仓另修）。仅 1 个 fork 构建脚本改动；0 上游侵入。 |
| [electron-icon-stale-fix](./electron-icon-stale-fix/) | done | Electron 打包 icon.ico 陈旧致 256 校验失败修复(2026-06-15,Tiny,bug-repro)— `electron-builder --win` dev 档报 `Icon must be at least 256x256, provided 128x128` 中断。根因:`win.icon` 引用的 `icon.ico`(gitignored 现场产物)在 `build-deskfox-electron.ps1` 里是「仅缺失才生成」,磁盘残留旧 128 ico(早于 6/14 补的 256 源图)被静默复用(同飞书 dist 陈旧反模式:只查存在不查新鲜度)。修:第 2 步改「每次从 ico-source 全量重生成」+ 校验含 ≥256(失败前置暴露)。验证:重生成 dev/icon.ico 含 256 → `electron-builder --dir` 越过 icon 嵌入步骤完整跑通,win-unpacked exe 新鲜产出。`.sh`(Mac icns)同类守卫留作 Mac 侧建议。仅 1 个 fork 构建脚本改动;0 上游侵入。 |
| [fox-blue-theme](./fox-blue-theme/) | done | Fox Blue 主题(2026-06-15,Medium-light,Electron 基座)— 基于默认主题 OC-2 复制新主题 Fox Blue(浅色),把选中态底色(文件树/会话列表/设置左导航,三处共用 `--surface-base-active`)+ 开关 ON(`switch.css` 选择器,原近黑 `--icon-strong-base`)改成品牌 logo 蓝 Cool Blue `#7295c4`;追加 文件树「所有文件」文字色对齐会话列表(`.text-text-weak`→`--text-strong` #171717)+ 列表项 hover 三档蓝反馈(hover 0.12 / 选中 0.32 / 选中+hover 0.44)。**0 上游源文件改动**:新主题靠 `context.tsx` 的 `import.meta.glob` 自动发现,只加 `themes/fox-blue.json`(OC-2 纯克隆,仅 name/id 不同)+ fork CSS 集中蓝覆盖(scope `html[data-theme="fox-blue"][data-color-scheme="light"]`,深色暂=OC-2)。`fox-blue.test.ts` clone 完整性守 merge 漂移;CDP 实算 10 验收点全绿;真桌面 QA 待 user。遗留:打包卡一个无关的 icon 256px 校验(另记)。 |
| [session-list-load-more-30](./session-list-load-more-30/) | done | 会话列表「加载更多」每次 +30(2026-06-15,Tiny)— user 要求侧边栏点「加载更多」每次新加载 30 条(原 +15)。`sidebar-workspace.tsx` 两处 `loadMore` 的 `limit += 15` → `+= 30`;底层 limit store 机制不变。typecheck 26/26;真机 QA 待 user。 |
| [settings-panel-cleanup](./settings-panel-cleanup/) | done | 通用设置面板精简 + 终端入口 + 菜单本地化(2026-06-15,Medium,Electron 基座)— user 看 dev 包提 5 项:① 删「高级」整组(文件树/导航控件/命令面板/服务器状态/Custom agents)② 删「New layout and designs」开关(默认 `false` 经典布局不变,只删展示)③ 「终端字体/捏合缩放」简繁体补译(原显英文)④ 标题栏 DEV 徽标前加「打开终端」图标(`command.trigger("terminal.toggle")` 切集成终端,gate `params.dir`)⑤ macOS 原生菜单跟随全局语言。**任务 5 做透**:打通 渲染进程 `useLanguage()` → IPC `set-menu-locale` → 主进程 `rebuildMenu()`,复用 fork `desktop-menu-i18n` 翻译表;role 项也翻译(带 label 用翻译表覆盖、纯系统 role About/Hide/Quit 用带 `app.getName()` 译名),仅有真实译文才覆盖 → 英文/未翻译语言零回归。删 UI 只删展示不动底层字段(零回归)。新测 `desktop-menu-i18n.test.ts` 守 DESKTOP_MENU 全 label 有 zh/zht 译文(防再泄漏英文)。typecheck 26 / app 434 / desktop main 69 / 新测 4 全绿;dev 包真机验。全 fork-only + 上游 additive/删除型 FORK marker;0 R4 / 0 黑名单。 |
| [dev-feishu-binding-missing](./dev-feishu-binding-missing/) | done | Electron dev 包飞书显示"未绑定"根治(2026-06-15,Medium,bug-repro,Electron 换基座 follow-up)— user 质疑 dev 包看不到 3 个绑定账号。**逐层取证**:数据完好在共享 `~/.opencode/feishu-config.json`(非身份隔离,后端 `os.homedir()`=真实 home)→ 插件在包里 → 但 `feishu-plugin-server.json` 指的 port 死了 → `server.log` 实锤 `ReferenceError: Bun is not defined`。**真因**:Electron 边车跑 Node(旧 Tauri 是 Bun),飞书插件 HTTP server 用 `Bun.serve`;源码 6/12 已适配(`node-serve.ts` 用 node:http + `server.ts` 改调 `serveFetch`),但 bundled `dist/plugin.js` 是 6/9 陈旧产物仍含 `Bun.serve` → Node 下插件 server 起不来 → 列账号失败 → UI 回退"未绑定"(media-gen 同病)。症结:`build-deskfox-electron.sh` 打包前只检查 dist 存在、不重建,陈旧 dist 静默混入。**修复**:① 重建两插件 dist(`Bun.serve` 清零)② §3.5a 改"先重建"+ post-build Bun 守卫(残留即 fail,本次回归防线)。端到端验证:server 活监听 / `Bun is not defined`=0 / WSS 3/3 全连 / `/accounts` 返回 3 账号 / UI 真机确认。仅 1 个 fork-only 构建脚本改动;0 上游侵入 / 0 R4。遗留另记:prod/dev 插件注册 exclusive takeover 互争 + 飞书 multipart 上传 Node 兼容。 |
| [project-avatar-save](./project-avatar-save/) | done | 编辑项目上传头像保存后侧边栏不显示(2026-06-12,Medium,bug-repro)— 所有项目所有端复现。根因:侧边栏渲染数据 `layout.tsx` 的 `enrich()` 解析 icon override 时只读 `childStore.icon`(commit `aa07f38b07` 引入单源),而编辑对话框对无 id / global 项目走 `globalSync.project.meta` 把 override 只写进 `childStore.projectMeta.icon` ——这块"只写不读"死数据致上传头像永久不显示。修:fork-only 纯函数 `project-icon-override.ts`(override 解析也读 projectMeta)+ enrich FORK 接线 + dialog 两路径统一写 canonical childStore.icon。后端往返已验无责(`project.test.ts` 32/32 + migration 在)。新测 4/4 含 bug-repro / app 839 绿 / typecheck 16。真机 QA 待 user。 |
| [llm-stream-idle-timeout](./llm-stream-idle-timeout/) | done | LLM 直连流式请求 SSE 空闲超时默认 120s(2026-06-11,Medium)— 多会话永久"思考中"根治:上游 `chunkTimeout`(#16366 wrapSSE)默认关闭 + `timeout` schema 文档"默认 5min"从未实现 = SSE 死流(NAT/LB 静默驱逐)无限挂,留 `tokens.output=0` 残骸。fork-only `provider/stream-timeout.ts`(默认 120s + `effectiveChunkTimeout` 纯函数)+ provider.ts 3 处 FORK 接线 + config schema `chunkTimeout` 可 `false` 关闭、timeout 描述修正。不给总 timeout 补默认(误杀长回复)。新测 7/7 含 bug-repro(停滞 ReadableStream);上游核实未修,待真机冒烟后提上游 PR。 |
| [home-empty-onboarding-copy](./home-empty-onboarding-copy/) | done | 首页 home 品牌化改造:常驻引导 + wordmark + 钢蓝 CTA(2026-06-09,Medium)— 进首页不管有无最近项目,常驻显示:DeskFoX.Ai **wordmark SVG**(半透明 0.5,新 fork 组件 `deskfox-wordmark.tsx`)+ 加粗中灰标题「你的专属 AI 工作助理已就绪」+ 一行副标题 + **钢蓝 #7295C4 实心 CTA「打开文件夹」**(色取自 logo)。列宽固定 552px 居中;去掉「最近项目」头部重复打开按钮。套设计稿 `OPENCODE-PLAN/首次引导/onboarding-final.html` + `品牌设计/SVG/wordmark.svg`。**纠偏**:初版误塞「空状态」分支,真机验收后提为常驻、`home.empty.*`→`home.welcome.*`、`Switch`→`Show`。品牌色走 index.css 自有 CSS 变量(R3 合规,不动上游 button/text token)。全 17 语言 i18n。迭代用独立 HTML 预览 + Edge 无头截图秒级调样。i18n 测试 15 绿 / typecheck 通过 / 0 R4。真机视觉 QA 待 user。 |
| [dev-independent-version-line](./dev-independent-version-line/) | done | DEV 预览版版本号从稳定版独立出来(2026-06-09,Medium)— 原 `installer-versions.json` prod/dev 共用同一平台版本号(只靠文件名/渠道区分),dev 号被 prod 绑死。user 拍板 **Dev 领先模式**:加 `dev-<plat>` 扁平复合 key 独立号线,新功能先进 dev 预览(`--bump minor` 领先),稳定后 prod 追上同波次,同 Chrome/VSCode Insiders。格式仍 `YYYY.次.补` 纯数字(Mac CFBundleShortVersionString + updater 约束,不加 `-dev` 后缀)。扁平复合 key 而非嵌套 → prod 读取零改动 + bump 的 grep/sed 不互撞。改 bump/build/pack 脚本 .sh+.ps1 + 治理文档 §3.2bis/§3.5/§4.2。Mac 端实测全过(含 sed 双向不误伤);**Win .ps1 同事 2026-06-10 实测三步通过,双端齐活**。 |
| [telemetry-usage-stats](./telemetry-usage-stats/) | spec | 匿名使用统计接入主力 Tauri 端(2026-06-06,Medium)— 数据安全前提下最小采集:`app_open`/`update_downloaded`/`update_applied` 3 事件 + version/os(大类)/arch(大类)/install_id(匿名)+ 城市级地理(IP 用完即丢不持久化)。opt-out 默认开(设置→通用 开关)。架构走 **Rust 侧原生**(WebView 加载不了 Node SDK)+ 修现有 Plausible 后端 + 删 2026-04 遗留 SDK 冗余 update_check。放弃使用时长(不用心跳),用 DAU+启动次数代理。**待 user 审签**。|
| [prevent-sleep](./prevent-sleep/) | done | 防止电脑休眠保障飞书远程随时可用(2026-06-06,Medium)— DeskFox 是常驻服务,飞书消息走官方 SDK WSS 长连接 push,电脑一休眠长连接断 → 远端发消息收不到。给用户一个**持久化开关**(挂飞书桥接设置栏目 + 托盘勾选项双入口同步),开启后阻止系统休眠但**允许屏幕关闭**,开一次以后启动默认保持。核心用 `keepawake` crate 跨平台(Win/Mac/Linux,`display=false`+`idle/sleep=true`),新建 `prevent_sleep.rs` + `lib.rs` ≤10 行注入,其余 fork-only。**否决**智能按连接触发(逻辑死结)+ 自启动(另立 feat)。已知 native 边界:Win 现代待机电池供电防休眠被忽略 / 笔记本合盖默认睡(均写入提示)。 |
| [win-ship-命令](./win-ship-命令/) | done | Windows /ship 一键发版 SOP 入仓 + 台账填实(2026-06-03,Medium)— 对齐 Mac `macos-ship-命令` 既定架构(command 各端本机 gitignored 避冲突 / SOP 知识入仓)。补 Win 侧缺失的 SOP 入仓文档;ship.md 加**步骤 3.5 填实台账**(用 code-review 摘要 + `git log` 填实 `installer-versions.md`,杜绝空占位回流 main)。不强行跨平台合并 command(尊重既定决策 + Win 端拿不到 Mac 完整 SOP 无法验证)。0 改上游 / 0 R4 / 纯文档 + 本机 command。|
| [lo-bundle](./lo-bundle/) | in-progress | LibreOffice 预捆绑安装包(2026-06-03,Medium)— 精简版 LO 内置进 DeskFox Windows installer,用户安装后无需二次下载即可渲染 Office 文档。`prepare-lo-bundle.ps1` 一次性 bundle 准备 + `DeskFox.iss` 条件编译 + `office-installer.ts` bundled 路径检测(优先级仅次于 env var)。|
| [sidecar-watchdog-respawn](./sidecar-watchdog-respawn/) | done | sidecar 看门狗自动重启 + 插件截流(2026-06-04,Medium,REQ-049 Layer③+①)— 起因 6/03 内置 agent 经 claude-code 插件跑重型 Workflow,海量 stream-json 事件撑爆 sidecar(exit 0x80000003)→ 主进程不重启、前台卡死、连累飞书白屏。**Layer③**(本仓):`server.rs` 轮询式看门狗(5s ping health,连续 3 次失败判定死/挂 → 同 port+password 重启 + 熔断防 restart storm)+ `lib.rs` `shutting_down` 防误重启 + `layout.tsx` 前台重连提示;**Layer①**(deskfox-plugins 仓):`firehose-guard.ts` 纯函数截 reasoning/大工具结果/大入参,答案与执行不变。全 FORK 标记,两层独立可逆。cargo check + typecheck 17/17 + 单测绿(Tauri lib 测试 exe 受沙箱 DLL 限制不计);行为验收(杀 sidecar→重启)待 release 真桌面。|
| [lo-bundle-macos](./lo-bundle-macos/) | done | LibreOffice 预捆绑 macOS 适配(2026-06-03,Medium)— 对称 Windows lo-bundle,将 LO 25.8.7 arm64 打入 DeskFox.app/Contents/Resources/libreoffice/。`prepare-lo-bundle.sh` + `build-deskfox.sh` 条件注入 Tauri resources + `office-installer.ts` darwin bundled 路径检测。|
| [electron-replatform-macos](./electron-replatform-macos/) | in-progress | Electron 换基座 macOS 端适配(2026-06-14,Medium)— 阶段0 验证基座 mac 可用(electron-vite build + electron-builder --mac + 启动后端 HTTP 401 全链路通,纯验证)+ 阶段1 固化 dev 构建脚本 `build-deskfox-electron.sh`(`--publish never` + 绕 Clash 代理 + 删 `native/` 死引用;版本注入复用远程 dev-independent-version-line 的 config 自读方案)。**关键事实**:opencode 后端走内嵌 Node(utilityProcess.fork),无需外部 sidecar 二进制。**运行时功能平移补全(2026-06-14)**:防睡眠持久化+启动恢复 / HTML 预览右键加聊天 / macOS 内置 LibreOffice 注入+构建资源门槛 / desktop 单测全局 electron mock 设施。阶段2/3 已落地(签名公证见 `electron-macos-sign-notarize`、updater+迁移桥见 `electron-macos-updater-bridge`,2026.8.0 已发版)+ 阶段4 ship.md 换基座(见 `macos-ship-命令` follow-up)。0 R4(deskfox config 黑名单已被远程 `1a708ebab4` 豁免)。|
| [electron-replatform-windows](./electron-replatform-windows/) | in-progress | Electron 换基座 Windows 端打包链适配(2026-06-14,Medium)— 对称 macOS,把 Win 打包链从 Tauri 迁到 Electron。新增 `build-deskfox-electron.ps1`(对称 mac `.sh`:版本预检 + icon.ico 现场生成 + 杀进程 + plugin/LO 就绪门槛 + electron-vite build + electron-builder `--win` + post-build 验证;绕 Clash 代理 + `--publish never`)。config 加 `targetPlat==="windows"` LO `extraFiles` 注入分支(`libreoffice-bundle/windows` → app 根 `libreoffice/`)。**后端零改动**:Win LO 用 `extraFiles` 落 app 根,正好命中后端**现有**探测路径 `dirname(execPath)\libreoffice`(原计划改后端 `office-installer.ts` 加 `resources` 段被 pre-commit 黑名单拦截 → 反转配置层方案,R1 三级跳,**避开 R4 override**)。验收(Win 真机):A1 出 `win-unpacked\DeskFox Dev.exe`(231MB)/ A2 冷启动后端 HTTP 401 / A4 门槛硬失败 / desktop 81 pass / typecheck 绿。运行时三修复(防睡眠/HTML 右键/mock 基建)随分支白送,视觉·native 待真机 QA。**0 R4 / 0 上游侵入 / 0 黑名单改动**。|
| [electron-macos-sign-notarize](./electron-macos-sign-notarize/) | done | Electron macOS 包 Developer ID 签名 + 公证(2026-06-15,阶段2,Medium)— 换基座后 deskfox config `mac.identity=null` 未签名致嵌套 soffice 被 SIGKILL + 无法分发/升级。接 electron-builder 原生签名公证(env 驱动,密钥不入仓):config mac 段镜像上游范式(hardenedRuntime/entitlements/notarize)+ identity 剥 `Developer ID Application:` 前缀(electron-builder 要求);build 脚本 `--sign`/`--notarize` flag。**核心坑**:osx-sign 逐文件签搞不定 LibreOffice 多可执行(soffice 报 subcomponent uno not signed → 回落 adhoc)→ `codesign --deep` 预签源 LO + `mac.signIgnore` 跳过(同时 28min→6min)。验证 TC-1~4+6 全绿(soffice --version 退出 0、deep verify、health 401);TC-5 公证随 /ship 实发验通(.app + .dmg 均 Notarized,.dmg 需补 `notarytool submit`)。0 R4。详见 [reference_electron_macos_signing]。|
| [electron-macos-updater-bridge](./electron-macos-updater-bridge/) | done | Electron macOS updater 部署 + Tauri→Electron 升级桥(2026-06-15,阶段3,Large)— **Part A** `deploy-electron-updater.sh` 加 `--platform mac`(latest-mac.yml + zip/dmg → `electron/<ch>/`,Electron 自更新);**Part B** 新 `bridge-electron-updater.sh`(存量 Tauri mac 用户迁移):`COPYFILE_DISABLE=1 tar` 防 AppleDouble + minisign 签(实测 minisign 直签 Tauri 公钥可验,免 tauri CLI)+ Tauri latest.json → 老 `v1/latest/<ch>/darwin` 端点 + 版本号防呆(>2000)。A dev 真部署验证 + B 本地 B-1~5 全绿;**2026.8.0 /ship 首次 prod 真部署两条源全线上生效**。0 R4。详见 [reference_minisign_tauri_bridge]。|
| [libreoffice-user-install-fail-win](./libreoffice-user-install-fail-win/) | in-progress | 内置 LibreOffice 干净机器报「User installation could not be completed」修复(2026-06-07,Tiny,bug-repro)— 剥皮脚本 `prepare-lo-bundle.{sh,ps1}` 整删了 `presets/`(LO 首次建用户 profile 的初始模板源,~200KB)→ 干净机器 100% 必现;2026-06-03 只补 extensions 留骨架漏了 presets,故 2026.7.0 仍复现。Win/Mac 双端独立锁定同因(Mac A/B/C 对照 + Win C 实验),两端删除清单移出 presets 即修复。代码已合 main,落地待两端各重做 bundle + 重发版。防回归 `lo-bundle-strip.test.ts`(接 CI `branding#test:ci`)。|
| [help-icon-community-link](./help-icon-community-link/) | done | REQ-039 左下角"?"帮助入口加链接 → DeskFox 社区页(2026-06-02)— sidebar footer 设置齿轮下方的"?"图标早已接好 `onOpenHelp`,本需求只把 `platform.openLink` 目标 `https://opencode.ai/desktop-feedback` → `https://deskfox.ai/#community`(顺手修一处上游 brand leak)。Tauri 端 `openLink` 走 `shellOpen` 真外部浏览器打开,符合需求。Tiny 1 行 + FORK marker / `layout.tsx:2363` / typecheck 17/17 / 0 R4 / 上游侵入 1 行(已 marker)。error.tsx 另一处同链接不在范围未动。|
| [file-tabs-project-level](./file-tabs-project-level/) | done | 话题/文件查看器解耦:文件 tab 改项目级(REQ-042,2026-06-02,Medium)— REQ-041 后续(同 `feat/mirror-layout` 分支独立一笔)。文件查看器 tab 从「会话级」(`sessionTabs[项目/会话ID]`,切话题换整套)解耦为「项目级」(`projectTabs[项目]`,切话题不变、切项目才换);审查/上下文 tab 内容仍由 session 层按会话提供。user 拍板**始终保持当前标签** + 项目级粒度。**底层绑定审计**:OpenCode 原「会话记得看过哪些文档」只驱动 UI + 内存选区记录,无 AI 上下文/无持久化现场恢复消费者 → 按元原则不为假想需求单独维护 per-session 并行状态。实现:新增 `store.projectTabs` + `tabs()` 存储 key 改 `projectTabKey()`(提纯文件 `session-key.ts` 便于单测);旧 sessionTabs 不迁移自然废弃。验证:typecheck + projectTabKey 6 单测 + 全套 786 单测 + e2e 14 全过;真桌面 QA 通过 2026-06-02。纯前端 / 0 改后端 / revert 不影响 REQ-041。 |
| [feishu-account-workspace](./feishu-account-workspace/) | in-progress | 飞书账号级 workspace(2026-06-07,Medium)— 每个飞书账号绑一个**专用真实项目目录**作 agent workspace,目的是让飞书 Agent 远程参与该项目开发(人不在电脑前时通过飞书写代码)。**核心机制**:`workspace?` 字段贯通 schema→pipeline(`workspaceDir()` helper,9 处 `query.directory`+PermissionCard+ATTACH 白名单根全跟随)→account-store/server 白名单;飞书收的文件/图片落 `<ws>/_deskfox/feishu/{files,images}`(`_deskfox/` 下划线前缀可见约定 + `.gitignore` 自动维护,未来多 IM 共用此根);改 workspace = 换新家从零开始(靠 `onAccountsChanged`→`syncAccounts` 重建 pipeline hot 生效,无需删 chatSessionStore)。GUI 文件夹选择器(Rust `feishu_pick_workspace_dir` + `blocking_pick_folder`)+ P4「对话记忆跟着目录走」提示 + A1 安全提示。决策:安全档沿用 v3 极简档(权限给够,危险操作发飞书审批)/ 多账号可绑同一 workspace 不拦。**测试**:单元 +18 断言(T1-T11/T18)723 pass / typecheck 17/17 / cargo check 0 新增 warning / release exe build 成功;**T12-T17 native(原生选择器/hot 生效/`_deskfox` 落点/ATTACH 发回)待 user 真机 QA**。0 改上游 / 0 R4 / fork-only。 |
| [feishu-edit-dialog-ux](./feishu-edit-dialog-ux/) | in-progress | 飞书账号编辑弹窗 UX:默认模型简化 + 身份/目录可见性(2026-06-08,Medium)— 三 Ask:**①** 去掉"跟随默认"勾选,首次进入默认预选 **OpenCode Zen + "自动(始终用免费模型)"**;"自动"= 哨兵 `{opencode, __auto_free__}`,**存哨兵不存具体 model**,pipeline 发请求前 `effectiveModel()` 实时解析 OpenCode Zen 第一个免费模型(`pickFirstFreeModel`,判据 `!cost||cost.input===0`),opencode 换免费模型也自动跟上、解析不到回退全局默认;免登录开箱即用(上游 `provider.ts` 未认证只留免费模型 + `apiKey:public`);**vision 预检改走 effectiveModel 修哨兵误判**。**②** 标题带飞书账号名 `<botName>(cli_xxx)`。**③** 账号列表 + 弹窗空态显示**真实生效 workspace 绝对路径**(adapter 回 `workspaceEffective` → Rust 透传;默认路径收口 `deskfox-dir` 单一真相源)。抽 `feishu-edit-account-model` helper(Logic 清单)。**测试**:helper T1-T6 12 + pickFirstFreeModel T7/T15 + resolveWorkspace T8 全绿;typecheck 16/16 / adapter 740 / app 826 / media-gen 140 / cargo check clean;**T9-T15 View+native(渲染/标题/列表/哨兵往返/免登录回复/vision)待 user 真机 QA**。几乎全 fork-only(上游仅 i18n 3 文件)/ 0 R4。 |
| [iconbar-left-decouple](./iconbar-left-decouple/) | done | 界面完整重排:图标栏锚左 + 话题/文件查看器解耦(REQ-041,2026-06-02,Large)— 在 REQ-040 镜像版基础上把图标条拆到最左、会话面板锚最右、main 夹中间(`图标条│文件树│审查│聊天│会话列表`)。user 拍板**彻底解耦**:删两套悬停预览(layout peek + sidebar-project HoverCard 卡)+ `aim.ts` 整删 + 删图标控折叠;点图标只切项目、会话栏开合只归顶部 sidebar.toggle 按钮。**净删 507 行**(+161/−668),减法重构。深改 4 个上游文件(R1 必须深改分支),提高未来 merge 冲突面(已知情)。跨文件 context 字段(sidebarHovering/clearHoverProjectSoon)保签名传恒定避免连锁。**UI 微调并入**:titlebar 两组互换 + 文件树 tab [所有文件][更改] + 工具组文件树开关↔项目打开下拉互换 + e2e-mock 隐藏 DebugBar(聊天移右与 dev 浮层重叠修复)。验证:typecheck 17/17 + 单测 780 + e2e 14 + CDP DOM/开合验证全过;真桌面 QA 通过 2026-06-02。 |
| [mirror-layout](./mirror-layout/) | superseded | 五栏镜像翻转(REQ-040,2026-06-02,Medium)— 纯前端镜像:`图标条│会话列表│聊天│审查│文件树`→`文件树│审查│聊天│会话列表│图标条`,3 封装单元整体平移 + flex-row-reverse。**作为 REQ-041 的中间基线**(commit `a24ff35a0`),最终布局被 [iconbar-left-decouple](./iconbar-left-decouple/) 取代(图标栏从最右挪回最左)。0 改后端 / 0 改上游逻辑。 |
| [macos-ship-命令](./macos-ship-命令/) | done | macOS `/ship` 一键发版命令(2026-06-01,Medium)— 发版 SOP 串成全自动 fire-and-forget,区别于 Win:**签名+公证内置 + 「不公证不推送」硬门禁**。步骤 0-8(前置检查/code-review 高危才停/杀进程/pack-installer 打包签名公证/公证门禁 stapler+spctl/chore 分支 bump/tag ship-mac-prod-/gh release/Gitee 镜像/报告)。公证撞苹果超时→停留本地→`/ship resume` 补公证续发(不重 build)。双轮验证是 ship 前置门槛(不进流程);触发即授权;绝不 push main/合 feat→main。skill 在本机 `.claude/commands/ship.md`(gitignored,避免与 Win /ship 冲突),SOP 知识入仓本 feat。复用 pack-installer/build-deskfox(签名公证)/mirror-asset-to-gitee/3-notarize。0 改上游/0 R4。 |
| [macos-codesign-notarize](./macos-codesign-notarize/) | done | macOS Developer ID 签名 + 公证集成进 build-deskfox.sh(2026-06-01,Medium)— Sequoia 15+ 对 unsigned+quarantine 报「已损坏」,签名+公证是唯一可靠分发。三处改动:§1.8 source 本机签名配置(Tauri 自动签 sidecar+.app)/ §3.6 build 后 API Key 公证 .dmg + staple(NOTARIZE_OK)/ §4 提示三态。公证用直接 API Key 不用 keychain profile(非交互 shell 读钥匙串报 User interaction not allowed)。无 config.env 优雅降级产 unsigned。**端到端实证**:Tauri 自动签生效(.app Developer ID + flags=runtime + timestamp),本次公证撞苹果服务超时(30min;我方流程已证正确 — history 同款包 05-29 曾 Accepted,05-30 提交卡 2 天 = 苹果故障),留待恢复后 3-notarize.sh 补。顺修提示 bug(公证失败误报已公证)。证书/凭据在本机 ~/.deskfox-signing/ 绝不入仓,隐私 grep 0 命中。0 改上游 / 0 R4。[bug-repro: 公证失败时 build 提示误报已公证] |
| [package-verify-script](./package-verify-script/) | done | 打包产物自动化验证脚本(2026-06-01,Medium)— A 层包完整性 + B 层 sidecar headless 冒烟,零 GUI / 零焦点干扰。对称 `build-deskfox.sh`,`-Env` 三档,`SCRIPT_DIR` 自推导无硬编码。验 .app 结构 / arm64 / 三档 Bundle ID / Gatekeeper / media-gen catalog 内联数据(15 model id 全内联 + 标签 + 0 外部读取)+ sidecar `serve` 真监听 + HTTP 响应 + 日志无 fatal + plugin.js ESM 加载。**起因**:Mac WKWebView 无 CDP 促成此 headless 路线(GUI 黑盒 `e2e-tauri-mac` 占用 user 电脑、不覆盖 catalog)。prod 1.14.33 实跑 23/23 / 0 改上游 / 0 R4 / fork-only。 |
| [test-gate-and-spec-cases](./test-gate-and-spec-cases/) | done | 测试纪律升级:R8 测试用例清单 + R9 分支内验收闸 + 单元测试自动 backstop(2026-06-01,Medium)— 起源:catalog 数据/代码分层 merge 后才发现 UI 标签不一致,暴露"测试范围未在动工前定 + 绿了再 merge 未成硬门槛 + pre-push 单测 gate 一直悬空"。**R8**:Medium+ 的 1-spec 动工前列逐条可勾选测试用例(验什么/层级/预期,运行时·native 风险点显式列入)。**R9**:开发完按 R8 跑全套+全绿、问题在 feat 分支内解决,才向 user 提 merge。**pre-push backstop**:push 含 main 时跑 fork 自家包单测(media-gen/adapter-feishu-lark/app,~25s,闸只跑 fork 不碰上游因根 test 故意禁)。**顺手纠偏**:删《自动化测试规范》操作流程里与三铁律矛盾的过时「ff merge dev → push 双远端」。改 `.husky/pre-push` + 测试规范(v5)+ CLAUDE.md(R5 段 + 修订 v3.2)。R8 dogfood 7 条用例全过(语法/三包绿/闸 pass+fail 双路径/范围/文档无残留/双文档一致)。0 改上游 / 0 R4 / 无运行时行为变化。 |
| [catalog-capability-label-sync](./catalog-capability-label-sync/) | done | catalog.ts CAPABILITY_LABEL 对齐真 UI(2026-06-01,Tiny)— media-catalog-data-extract CDP 验证时发现 `catalog.ts` 把 tts/asr 标成旧词「配音/转写」,而真 UI(前端 `media-creation-store.ts` CREATION_MODES)+ 插件工具描述(`index.ts`)早已用「语音合成/语音识别」。catalog.ts 是全仓唯一掉队副本且无生产引用(仅测试)。改:tts→语音合成 / asr→语音识别 + 注释标明真相源在前端 CREATION_MODES、此处为须同步的插件侧副本;同步更新 catalog-xiaomi.test.ts 断言 + cdp-catalog-verify.ts 注释。无运行时行为变化(CAPABILITY_LABEL 不驱动 UI)。typecheck pass / 140 测试 pass / 0 改上游 / 0 R4。跨包 type+label 双副本以「同改」注释维系,彻底单一真相源另立专项。 |
| [media-catalog-data-extract](./media-catalog-data-extract/) | done | media-gen 模型目录 数据/代码分层 阶段 1(2026-06-01,Medium)— 「媒体模型适配数据/代码分层 + 开源 registry」三阶段路线第一步。把 `catalog.ts` 的 `BUILTIN_CATALOG` 数据本体抽到纯数据 `catalog.data.json` + 配可移植 `catalog.schema.json`(JSON Schema 契约,阶段 2 开源 registry 复用);类型/`CAPABILITY_LABEL`/`*_KEY` 常量留 `.ts`。**运行时零行为变化**:Bun.build 内联 JSON 进单文件 `dist/plugin.js`,import 后原样导出,dispatch/registry 零改动,不做运行时校验。原内联踩坑注释(尤其 `xiaomi-mimo-v2.5-asr` 的 Omni-当-ASR + 6/30 下线预警)落到可选 `note` 字段随数据走。**测试**:新增 `catalog-data.test.ts` 12 用例(结构/id 唯一/capability 枚举与代码一致/providerKey/默认唯一/无未知字段/参数类型 + schema↔代码防漂移)/ media-gen 全包 140 pass / typecheck pass / dist 重建验证内联。**纯 fork**:0 改上游 / 0 R4 / catalog.ts 净 -142 行。决策底稿 `OPENCODE-PLAN/需求池/媒体模型适配-数据代码分层与开源registry.md`。 |
| [feishu-llm-timeout-surface](./feishu-llm-timeout-surface/) | done | 飞书 bot 收到消息但不回的根因修复 — LLM 超时 / 空响应 surface(2026-06-01,Medium)— 起源:2026-05-31 用户撞 bot 收到消息但完全不回,根因链路 `LLM hang → dispatcher 30 分钟 timeout → 走 partial 路径返空 → runOpencode 拉 session.messages 找不到 useful → 返空字符串 → handle() 静默 return → 飞书侧 0 reply`。**4 处改动**:① `prompt-dispatcher.register()` 返回类型 `Promise<string>` → `Promise<DispatchResult>`(reply + source 字段),timeout 无 partial 时 reject 带 timeoutMs 诊断;② `runOpencode` 所有"返空字符串"分支改 throw + timeout-partial 兜底(partial 优先于 fallback);③ `friendlyErrorReply` 新增 5 类 pattern(timeout / 429 / 5xx / no-useful / session-state);④ `handle()` / `handleMergeForward()` empty reply 兜底发 `EMPTY_REPLY_FALLBACK`(belt-and-suspenders)。**顺手修一个 pre-existing bug**:`register()` 二次调用同 sessionID 时 superseded reject 永不触发(finalize 闭包 `get()` 拿不到 waiter 早 return → OLD promise hang 永不 reject + 内存泄漏)。**测试**:新建 `prompt-dispatcher.test.ts` 10 case(含 [bug-repro] 标记 + Logic 清单补齐)/ `friendly-error.test.ts` +7 case / `message-pipeline.test.ts` +4 e2e case。adapter-feishu-lark 全包 646 pass(+24)/ typecheck 17/17 / 0 改上游 / 0 R4。配套诊断工具 `OPENCODE-PLAN/诊断工具/DeskFox-tools/DeskFox-Diag.ps1` v6.3 飞书桥接专项 6 子段同期落地。 |
| [window-title-deskfox-fix](./window-title-deskfox-fix/) | done | 主窗口标题品牌泄漏修复(2026-05-29,Tiny)— `windows.rs:56` `.title("OpenCode")` 上游硬编码改为读 `app.config().product_name`(走 tauri-overrides 按 env 注入 `DeskFox` / `DeskFox Dev` / `DeskFox Beta`),与 productName / installer / bundle identifier 三档品牌字符串对齐。1 文件 / +3 行 / FORK marker / 启动验证 PID MainWindowTitle 真值由 `OpenCode` → `DeskFox Dev`。 |
| [e2e-chat-loop](./e2e-chat-loop/) | done | 聊天主循环 Phase 1 mock e2e 套件(2026-05-29,Medium)— 3 case 覆盖 user 视角:新 session 发消息→user msg→AI 回复 / sidebar 新 session 出现 / busy 期 progress 显示。接手 WIP commit `11ada4f02`(3 case fixme + mock 不通),修通 + 重写 chat-mock 路由 + SSE 改 `addInitScript` 路线。**5 条关键踩坑沉淀**:① Playwright `**/foo` glob 不匹配带 `?directory=` query → mock 路由全 RegExp 化 `/\/foo(\?|$)/` ② `route.fulfill({body})` 不接 async generator → SSE 必走浏览器端 `window.fetch` patch + `ReadableStream` 注入 ③ assistant message mock 最小 shape = `parentID` + `tokens` + `cost` 三件套缺一不可(parentID 缺 → SessionTurn 不渲染 / tokens 缺 → ErrorBoundary 全屏崩)④ `session.created` event 必须带顶层 `directory` 字段否则 per-project sync 收不到 ⑤ `data-session-id` 在 sidebar + recents 两处都 render,断言用 `.first()`。**纯 fork**:0 改产品源码 / 0 R4 / mock helper 可被未来 chat 相关 spec(followup / abort / retry)直接复用。 |
| ~~[startup-sidebar-ready-gate](./startup-sidebar-ready-gate/)~~ | reverted | ~~启动期 sidebar 项目切换"无响应"修复~~ — **2026-05-29 user 实测撞 2 个 bug 已 revert**:① 第一次点击未选 project tile 只变灰不切换,需第二次才能切 ② gate 状态(opacity-60)下鼠标 hover/move 误触发 sortable drag,DragOverlay ghost 飞出 sidebar 区。怀疑 ready-gate 加的 `onPointerDown` 没 `event.stopPropagation()` 跟 `use:sortable` 启动 drag tracking 冲突 + reactive cascade 阻断 pointerup cleanup。原 backlog "启动期-sidebar-点击无响应" 重新打开。 |
| [e2e-tauri-phase2-mac](./e2e-tauri-phase2-mac/) | done | Phase 2 真桌面 e2e Mac 端**基础设施 + user-flow + 项目注入三层落地**(2026-05-28,Medium)— 跟 Win 端 `e2e-tauri-phase2-real-desktop` 用例 / saveDialog 思路对齐,底层走 GUI 黑盒(macOS WKWebView 无 CDP)。**saveDialog mock 方案 ② 降级**(env var + 新 Tauri command `read_e2e_save_path_env`,跟 Win ① 共存)。**架构**:`packages/app/e2e-tauri-mac/` 平级独立,helpers/ 4 文件(osascript / cliclick / screencapture / window-bounds)+ fixtures.ts spawn `.app/Contents/MacOS/DeskFox` + env 注入 + hydrate poll + `pkill -f "DeskFox Dev.app"` 精确杀残留(主进程 + sidecar)。**`test:e2e:tauri` 顶层 dispatch** 按 platform 自动路由。**测试现状**:全量套件 **3 passed + 1 skip / 1.3min**:① `smoke-mac.spec.ts` 2 case pass(链路通 / 窗口 / 截屏阈值)② **`command-palette-flow-mac.spec.ts` 真 user-flow pass**(Cmd+K 弹命令面板 → typeUnicode "new" 过滤 → Escape 关闭 5 phase byte 序列 / 截屏证实搜索框"NEW"+过滤"New session"命中)③ `md-to-word-real-mac.spec.ts` test.fixme — 项目主页 ≠ Win session view,Cmd+K 行为不同(留 backlog 2 个后续方案:扩 deep_link file 参数 / file tree click)。**关键技术突破** — ① `clickToFront(titleBarAnchor)` helper(物理点击窗口标题,osascript activate 不可靠 macOS Sequoia 实证)② `anchorOf(bounds, relX, relY)` 比例锚点 ③ **deep_link 注入项目跟 Win 看齐**(`open -a "<dev .app>" "opencode://open-project?directory=..."`,显式指定 .app 绕开 LaunchServices 默认 handler 被 prod `/Applications/DeskFox.app` 抢占问题)。**7 条实施期间踩坑沉淀**:① cliclick.type 中文吞掉(helper 加 ASCII 强校验 throw)② macOS IME 把 osascript keystroke 中文 unicode 转拼音首字母("会话"变"AA",改 ASCII "new" 绕开)③ dev .app default state 进 imbot-workspace ④ webview hydrate 检测阈值算法 ⑤ Claude 视觉 Read 截图 + 1.89 倍坐标换算 + cliclick 物理点击突破 ⑥ deep_link 注入项目 + LaunchServices prod 抢占绕开 ⑦ 项目主页 vs session view 行为差异。Medium ~2200 行(三文档 ~830 + 代码 ~1370),0 改 Win 端 / 上游 fork-only 3 文件 +27 行。**纯 fork**:0 R4 / 0 上游侵入。typecheck 17/17 / Rust cargo check 0 errors / Phase 1 e2e 13 pass 无回归 / packages/app 738/0 unit 无回归。 |
| [media-gen-xiaomi](./media-gen-xiaomi/) | done | 小米 MiMo 接 provider — REQ-030 第三家(2026-05-28,Large)— 4 档:标准 TTS(`mimo-v2.5-tts` 9 个预设音色)/ 语音克隆(`mimo-v2.5-tts-voiceclone` DataURL 参考音)/ 语音设计(`mimo-v2.5-tts-voicedesign` 文字描述声线)/ 转写(`mimo-v2.5` Omni 多模态 7.8s 短音频)。**REQ-030 首次加新 capability 类型** `tts_clone` / `tts_design`(user 拍板授权);MiMo 是 VoiceClone/VoiceDesign 独家(阿里/MiniMax 都没)。**架构差异**:① 跟阿里/MiniMax 都不一样,所有能力**同一 endpoint**(`POST /v1/chat/completions`)走 OpenAI chat 兼容协议 ② Auth 用 `api-key:` header **不是** `Authorization: Bearer` ③ Audio in/out 都 base64:VoiceClone 输入要 **DataURL** 格式(`data:audio/wav;base64,xxx`,probe 实测踩坑)/ VoiceDesign 输入**不能**含 `audio.voice` 字段(踩坑)/ ASR 走 mimo-v2.5 多模态而非 mimo-v2.5-asr(Token Plan 未暴露)。**by-provider switch 第三家继续不抽 MediaAdapter**(§0.1 既定方向)。**前端 capability 类型 3 处副本同步**(`media-creation.ts` / `creation-input.ts` / `media-creation-store.ts`),UI 工具栏加"声线描述"输入框(仅 voiceDesignHint 模型显示)。Token Plan TTS 三档限免不消耗额度。**测试**:65 unit(8 文件)+ 24 前端 buildCreationInput / app 全包 744/744 / 17 typecheck pass / 4-case CDP 自测脚本(`cdp-creation-xiaomi-all.ts`)/ probe 阶段真打 4 API 全通真生成音频。**纯 fork**:0 改上游 / 0 R4 / 4 commits / ~2300 行(代码 ~1300 + 测试 ~800 + 文档 ~800)。新增 3 条踩坑沉淀准备进 `OPENCODE-PLAN/knowledge-base/接 AI 媒体供应商-踩坑实录.md`(DataURL 字段 / VoiceDesign 必须 absent voice / Omni 替 ASR + Token Plan asr 直 model 未暴露)。 |
| [e2e-tauri-phase2-real-desktop](./e2e-tauri-phase2-real-desktop/) | done | Phase 2 真桌面 e2e 启用(2026-05-28,Medium+)— 老分支 `feat/e2e-real-tauri-webdriver`(2026-05-08 架子)cherry-pick 6 文件 `packages/app/e2e-tauri/` 进当前 main(避 3 周 / 457 commits 漂移 + 30+ 治理文档冲突),实施 **saveDialog mock 方案 ①**(`page.exposeFunction` 0 产品代码侵入 — `packages/desktop/src/index.tsx saveFilePickerDialog` 加 ~13 行 hook 检测 `window.__deskFoxE2eSavePath`,生产 undefined fall through native dialog),并修老分支 2 个 bug(URL 编码 `encodeURIComponent` → base64 / `E2E_PROJECT_DIR` 改本仓 `D:/project/opencode-fork` 稳保 .md)+ 改 md-to-word-real spec 用 Ctrl+K 命令面板替代文件树点击(更稳)。**核心 spec 真桌面 1.2min 全链路 pass**:Source CLAUDE.md → 真启动 DeskFox + CDP 连入 + mock 函数注入 + Ctrl+K + Enter + 右键 + 导出 Word + **saveDialog mock 拦截走 mock 路径** + docx 真生成 22.5KB + word/document.xml 103,588 chars + 段落+文本 run 正常。补齐测试金字塔第三层(unit / Phase 1 mock e2e / Phase 2 真桌面 e2e)。**纯 fork**:0 改上游 / 0 R4 / ~13 行真产品代码 + 6 个新 e2e 文件 + 3 文档。`test:e2e:tauri` 独立 script 不进 pre-push 闸(build release 太重,留独立手动 / 后续 CI 触发)。Follow-up:smoke#2 race fix / Mac CDP 路径 / 更多场景 / docx 视觉效果优化。 |
| [media-gen-minimax](./media-gen-minimax/) | done | MiniMax 接 provider — REQ-030 第二家(2026-05-28,Large)— image-01 / Hailuo-2.3 / speech-2.8-hd 三能力接入,UI 零改动 catalog/registry 自动让 minimax 三档亮起。**关键决策**:by-provider switch 不抽 MediaAdapter(§0.1 竖切打穿,第三家再抽);dispatch 重构成 `dispatchAlibaba` + `dispatchMinimax` 函数,alibaba 既有代码 0 行为改。**关键差异**:① MiniMax tts 返回 hex 内联(非 URL)→ engine 写 tmpdir + 返回 `file://` URL,asset-save 加 file:// 分支(~10 行)兼容,引擎→server 契约不变。② MiniMax 同模型双后缀计费分流:`-hd` 走 Token Plan 配额(speech-2.8-hd 实测 73KB 真音频出声),`-turbo` 走积分计费(没积分 0/0)— 客服简称会省略后缀,**真 model id 必从厂商 FAQ + 实测确认**。③ 错误码 2013 vs 2056 是探活金钥(2013=model 名错,2056=对但配额);`status_msg (0/0 used)` ≠ 配额耗尽,是套餐无此能力。**测试**:60 单测全过 / typecheck 17/17 / 真用户 CDP e2e 3/3 路由命中 / 真 API probe image+tts 实出资产。**真桌面 QA**:文生图 + TTS user 验通,视频是套餐限制(Plus 不含视频,要 Max)非代码问题。**纯 fork**:0 改上游 / 0 R4 / 11 commits / ~1200 行(含测试+CDP+文档)。**深坑沉淀**:8 条经验进 [`OPENCODE-PLAN/knowledge-base/接 AI 媒体供应商-踩坑实录.md`](../../../OPENCODE-PLAN/knowledge-base/接 AI 媒体供应商-踩坑实录.md)。 |
| [creation-edit-asr-no-file](./creation-edit-asr-no-file/) | done | 创作模式 `@<路径>` 文件引用没传给图编辑/ASR(Tiny+ bug-repro,2026-05-28)— user 报「图片编辑需要先提供一张图片」/「转写需要先提供一个音频文件」,但 `+号` 附件 base64 图能用、纯文本 TTS/文生图 也能用。根因:`prompt-input.tsx submitCreation` 只识别 `type:"image"` part 的 `dataUrl`,完全忽略 `type:"file"` part(`@<path>` 引用),且 `input.audioUrl` 整段代码从未赋值过 → ASR 永远拿不到音频。修法:helper extract `buildCreationInput`(纯函数,进 Logic 清单)—— ① `@<path>` file part 按 capability 路由进 `refFile`(`image_edit`/`video_i2v`)/`audioUrl`(`asr`);② 路径走 `absolutePath` 拼绝对(`build-request-parts.ts` 同款规则,Win 盘符/UNC/POSIX 兼容);③ prompt 只取 `text` part(剔除 `@<path>`/`@<agent>` 字面避免污染厂商 API);④ 兼容旧 +号 base64 附件;⑤ file part 优先级高于 base64 附件。**18 bug-repro 单测**(absolutePath 4 + 主战场 5 + 兼容/边界 9)/ app 全套 **729/0**(原 711 + 18,零回归)/ typecheck 过 / 0 改上游 / 纯前端。commit:本笔(grep `[feat: creation-edit-asr-no-file]`) |
| [npm-registry-cn-mirror](./npm-registry-cn-mirror/) | done | 国内用户 sidecar npm 走国内镜像(2026-05-28)— 根因:sidecar 装 `@opencode-ai/plugin`(及用户自装/自写插件)默认走 `registry.npmjs.org`,北京实测 12s vs 国内镜像 0.7~1.8s;连不上则反复失败 + 首次项目交互空等 `waitForDependencies` 超时 →「每次开都卡一下」(慢非坏,功能不受影响)。方案 B:给 sidecar 注入 `npm_config_registry` env(`npm-config.ts:18` 带 `env:{...process.env}` → 一次覆盖「后台装包+用户装插件」两条路径,可逆、国际用户不注入保持官方)。**区域区分**:首启时区 UTC+8 即时猜主镜像(零阻塞)+ 后台探活(官方≤2.5s 判国际用官方 / 否则并发取最快镜像 / 全挂回落官方)写 14 天 TTL 缓存自愈。**验证过镜像清单**:npmmirror(主)/huaweicloud/tencent/huaweicloud-repo/cnpmjs(清华/中科大实测 404 淘汰)。**纯 fork**:0 改上游 TS,新建 `npm_registry.rs` + `cli.rs`/`lib.rs` 各 ≤5 行注入(FORK marker)。6 单测(纯逻辑独立 rustc 跑通——crate `cargo test` 在 Win 是 `0xc0000139` 通病)/ release 出 DeskFox.exe / 运行时日志验证注入触发。异步化首次交互留 backlog。commit:本笔(grep `[feat: npm-registry-cn-mirror]`) |
| [file-tree-test-client-env](./file-tree-test-client-env/) | done | file-tree.test.ts Windows 测试修复(Tiny,2026-05-27)— 远程同步代码后 app 单测在 Win 跑出 1 fail(`Client-only API called on the server side`)。根因:bun test 解析 solid-js **服务端构建**,`import ./file-tree` 连带加载 `@opencode-ai/ui/context-menu`(Kobalte)→ `@kobalte/core` 求值期调 client-only API 抛错(纯测试环境问题,真实 app 有浏览器正常)。测试已 mock 其余 UI 组件独缺 context-menu,补一条同款 mock 隔离。上游测试 #12468 加 FORK marker。app 711/0(原 708/1)。commit `b073c5fa5` |
| [feishu-merge-forward-image-400](./feishu-merge-forward-image-400/) | done | 合并转发图片 graceful 降级(2026-05-27)— 真机发现合并转发子图下载全 400(飞书错误码 234043:**官方不支持读取合并转发内子消息资源**,平台硬限制不可修;原机制单测 mock fetch 掩盖、真实从没成功)。改 graceful:flatten `maxImages=0`(不建下载列表+不渲染假的"已展开识别",加 `imageCount`)+ **停掉死下载**(省 5×400)+ prompt 告知 LLM 只看文字 + **回复头部诚实提示**「📷 合并转发里的 N 张图我读不了(飞书限制),需识别请直接转发图」。飞书 622 pass + 真机 UX user 验通。commit `aadbd245c` |
| [feishu-win-test-compat](./feishu-win-test-compat/) | done | 飞书单测 Windows 兼容修复(Tiny,2026-05-27)— 飞书 image-recognition/merge-forward 那批在 Mac 验"622 全过"、Win 跑 607/15 fail(media-gen 合并前体检发现,实证 origin/main 自带非 media-gen 引入)。① **真 Win 代码 bug**:`image-downloader.ts` 路径越界 assert `resolve(p)+"/"` 在 Win(反斜杠路径)前缀永不匹配 → 飞书图片下载全失败,改 `+path.sep`;② classifyAttachment 测试 `ROOT="/tmp/.."` Unix 路径 → 改 `resolve(...)`;③ defaultFilePath 断言写死 `/` → 改 `join(...)`;④ dedup 2 个 TTL 刷新测试时序裕度太窄(Win ~16ms 粒度)→ 放大 ttl/sleep。修后 622 pass / 0 fail(Win)。commit `b6e2f2f4a` |
| [media-gen-bundle](./media-gen-bundle/) | done | media-gen 创作插件打包进安装包(同飞书机制,2026-05-27)— 从"靠开发仓绝对路径加载"升级为软件内置部分:Tauri resources ship + DeskFox.iss [Files] 装到 `{app}\plugin\media-gen` + `feishu_plugin_install.rs` 通用化加 `ensure_media_gen_plugin_in_config` 开机注入。装完连 Alibaba (China) 填 key 即自动可用、零手动配。注入逻辑通用化(`inject_plugin` 从末两段自动推标识,签名不变测试零改)+ `media_gen_and_feishu_coexist` 单测。顺带修 build-deskfox 清理块 PS5.1 编码 bug(Get-Content/Set-Content 默认 ANSI → .NET UTF-8 无 BOM,否则写坏含中文 opencode.jsonc 致 setup hook 注入全废)。真机端到端验通(build→清理→注入→/healthz+8模型7能力→文生图落项目根)。commit `b977f4130` |
| [window-resizable](./window-resizable/) | done | 修窗口无法鼠标拖拽改大小(Tiny,2026-05-27)— 真因:`tauri-plugin-window-state` 持久化恢复 fullscreen/maximized,残留 `.window-state.json` 的 `fullscreen:true` 反复恢复 → 窗口钉死全屏抓不到边。修:`constants.rs` window_state_flags 减 MAXIMIZED+FULLSCREEN + `windows.rs` 去 `.maximized(true)` 改窗口态(1280×800 / min 800×600 / center)+ 清残留状态文件。~8 行 / 2 上游文件 FORK marker。native 拖拽 user 真桌面验通。commit `44180a261` |
| [media-creation-mode](./media-creation-mode/) | done | 创作模式(2026-05-26~27,REQ-030)— 否决"AI 自动调工具"改显式模式菜单:底部统一菜单(Chat + 文生图/图片编辑/文生视频/图生视频/语音合成/语音识别/专业翻译)+ 左侧随模式换模型/音色 + 输入即生成。结果融入聊天滚动流(轻量卡:提示词+输入缩略图+小结果+已保存路径+打开)+ 产出落当前项目根 `creations/`。fork-frontend(prompt-input/message-timeline 注入)+ 本地生成服务(Bun.serve 51737 R6 SSE)+ 模块级 store,引擎复用 media-gen-alibaba。CDP 真机端到端自测 + user 桌面 QA。14 commits。无模型引导 / 通用引擎留 backlog(见 OPENCODE-PLAN 后续路线 doc) |
| [media-gen-alibaba](./media-gen-alibaba/) | done | 多模态生成第一竖切(REQ-030)— 阿里(DashScope/通义万相)全能力适配。新建 `packages/media-gen` opencode plugin(**0 改上游**),6 工具:文生图 / 改图 / 文生视频 / 图生视频 / 翻译 / 语音合成 / 语音识别。8 模型实测(`wanx2.1-t2i-turbo/plus`、`qwen-image-edit`、`wanx2.1-t2v/i2v-turbo`、`qwen-mt-turbo`、`qwen-tts`、`paraformer-v2`),3 协议(异步 task / 同步 chat / 同步 multimodal)。**关键决策**:先竖切不抽象(通用层等第二家逼出)+ 0 Rust(边车进程调不到 Tauri 命令)+ 本地文件 OSS 上传(ASR)/base64(改图)+ 改图 `wanx2.1-imageedit`→`qwen-image-edit` 纠偏(下载结果图用 Read 工具亲眼验证)+ `tool-shim` 避 effect 进 bundle。Large ~1500 行全新 fork-only / 16 单测 / typecheck 净 / dist 417K 冒烟 6 工具 / 真厂商人眼验 / **0 R4 / 0 上游侵入** / 8 commits。已证伪 `wan2.6-t2i`(url error)`wanx2.1-imageedit`(换背景无效)。follow-up:拖拽附件改图、OSS 持久化、`/media` 兜底、转 minimax 抽通用层。完整规划见 REQ-030 |
| [feishu-merge-forward](./feishu-merge-forward/) | done | 飞书 user 合并转发消息识别(2026-05-26)— **最具鲁棒性方案落地**:D1=C 全图展开 MAX_IMAGE_COUNT=5(继承 S1-S5)/ D2=SDK `client.im.v1.message.get` + Promise.race 30s timeout / D3=MAX_SUB_MESSAGES=50 末尾省略 / D4=嵌套 1 层递归 + depth≥2 占位 / D5=群聊带 sender(open_id 前 6 位)+ p2p 不带 / D6=中文 user 视角占位 + 元信息(`[文件: x.docx 1.1MB]` / `[语音 12s]` / `[分享: 群 oc_xxx]`)。**R1-R4 鲁棒补强**:30s timeout / `sortByCreateTime` R2 / 0 子消息+拉取失败+深度超限 友好兜底 / vision 预检 textOnly 降级。**关键解耦**:`merge-forward-flatten.ts` 纯函数 helper(`flattenMergeForward` / `renderSubMessage` / `sortByCreateTime` / `hasAnyImage`,8 种 msg_type 占位)+ `merge-forward-fetcher.ts` SDK 调用层 + `message-pipeline.ts` `handleMergeForward` 独立路径 + `expandNestedMergeForward` 嵌套展开(`  ↳ ` 缩进 + 占位 replace);runOpencode `imageOpts.imageParts[]` 多 file part 注入。**5 经验沉淀** changelog:① global image index 必须独立计数器(初版 `images.length+1` 截断后停增)② 嵌套递归不能放纯函数(IO 不允许)③ SDK timeout 用 Promise.race 兜底(SDK 不支持 AbortController)④ 子消息资源访问用**子消息自己的 message_id**(API 二分跟 image-recognition 同款风险,提前规避)⑤ SDK get_message 普通 JSON 0 multipart 风险(只 image.create/file.create multipart 撞 axios+form-data 链)。**测试**:622/622 全过(基线 574 + M 37 + F 11)/ 16/16 typecheck / 0 R4 / 0 上游侵入。Medium 1.5d / 1891 行 / 10 文件。**留 backlog**:file/audio/video/sticker 多模态展开 / 嵌套≥3 层 / sender_id→用户名缓存 / 结构化 JSON pattern D |
| [feishu-image-recognition](./feishu-image-recognition/) | done | 飞书 user 发图给 bot 识别(2026-05-26)— **D 方案落地**:image 下载到 `~/.opencode/imbot-workspace/feishu-images/<chatId>/`,`FilePartInput.url=file://<absolutePath>` 由 opencode-cli `prompt.ts:1230` 自动 readFile + base64 inline 给 LLM(0 临时 server / 0 R6 / 0 改上游)。**最终落地范围**:text + image + post 三 messageType(post = 图+文字混合);**vision 预检**:收图前查 `model.capabilities.input.image`,不支持的 model 直接友好提示换模型,避免卡死"识别中..."(10min TTL cache)。**S1-S5 加固**:20MB 大小硬限 / 30s fetch timeout / mime 白名单(7 种 image)/ 路径越界 assert / error 不带 token。**测试**:I1-I5 + S1-S5 + U5 + U1 + U2-U4 共 57 新增 case(`parseMessageContent` / `extractVisionSupport` / `isVisionCacheFresh` 三个 helper extract);全包 574 pass / 0 fail / 16/16 typecheck / 0 R4 / 0 上游侵入。**5 个 fix follow-up 踩坑沉淀** changelog:① `/images/` 端点对 user 上传图返 400(必用 `/messages/{id}/resources/`)② post messageType 不是 image(图文混合默认走 post)③ vision capability ≠ attachment ④ plugin → pipeline → downloader 循环 import(downloader 自己重算 IMBOT_WORKSPACE 路径)⑤ sidecar binary 时间戳陷阱;Medium 1.5d / 1687 行 / 8 文件;留 backlog:merge_forward(合并转发) / file / audio / video / sticker / 多图聚合 |
| [md-editing-iter-3](./md-editing-iter-3/) | done | MD 编辑态语义样式增强(白领基线,2026-05-25,iter-1/iter-2 后第 3 迭代,14 commits,6 轮矫正)。**最终 copy 方案**:GitHub MD CSS heading 比例(h1=2em / h2=1.5em / h3=1.25em,公众号 / 知乎 / 简书 / Notion 训练 10 年)+ iA Writer opacity 0.7 标记符弱化 + GitHub Primer 单蓝 accent 仅给链接(`var(--text-interactive-base)`,跟 markdown.css 预览侧统一)+ inline code 无 chip(源模式靠反引号自识别)+ Strikethrough 划线 + heading 无下划线。**矫正链 6 轮**:② var(--primary) 未定义 bug + Option A 现代办公配色;③ 删 monospace chip 反 fenced code block token 灾难;④ 加 strikethrough + Prec.high 尝试;⑤ 去 default `fallback:true` + Prec.high 翻 CSS cascade 反 default heading/link underline;⑥ 扩 codeLanguages(Python/SQL/JSON/YAML/CSS)+ HTML 解决 user 反馈"代码块都该有高亮"割裂;⑦ Cmd+Shift+E keybind workaround Tauri WebView + SolidJS Portal 互操作限制(GUI 自测 Workaround)。**自测路径完整**:Playwright e2e 视觉断言 + 滚动 tour(12-22s)+ 真 .app + Python Quartz CGEvent 滚动 + screencapture 兜底(20 张视觉档案 `/tmp/iter3-final-shots/`)。Medium ~+800 行 / 9 文件 / 4 单测 H1-H4 / 16/16 typecheck / e2e 全过 / 真 .app 视觉验证 PASS / **2 R4**(@lezer/highlight + 6 个 @codemirror/lang-*)。8 语言完整高亮(JS/TS/Python/SQL/JSON/YAML/HTML/CSS);Bash/Diff/Mermaid 单色(无官方 lang-* 已知边界)。10 个深坑沉淀到 3-changelog 经验段(用户画像优先 / CSS 变量验证 / lezer-markdown tag 范围 / CM6 fallback 全或无语义 / CSS cascade 与 Prec.high / e2e 断言阈值 / mock vs prod 差异 / Tauri WebView Portal 限制 / vite minifier 反转 / cliclick t: vs kp:)|
| [imbot-workspace-rename-followup](./imbot-workspace-rename-followup/) | done | imbot-workspace-rename 后 follow-up:user 升级后老 chatSessionStore 里 session ID 还绑老 directory(`feishu-workspace`)→ LLM 用老 system prompt + 老 cwd → emit ATTACH 用老路径 ENOENT。修法:plugin 启动时 `applyStaleSessionsCleanup` 一次性清 chatSessionStore(marker 文件 `.imbot-workspace-rename-cleanup-applied` 幂等),让 plugin 重建 session 用新 directory。Trade:user 失去 multi-turn memory(one-time)。Medium- 净 +280 行 / 2 文件 / 6 单测 T1-T6(5 mock + 1 真 fs)/ 517 测试 0 fail / 16/16 typecheck / 0 R4 / 0 上游侵入 |
| [imbot-workspace-rename](./imbot-workspace-rename/) | done | home base 路径 `~/.opencode/feishu-workspace/` → `~/.opencode/imbot-workspace/` — 对齐 ADR "imbot 跨 IM 共享 home base" 语义,未来 telegram/钉钉 接入时不再叫"feishu-workspace" 困惑。**改名 only**(其他 feishu-*config/sessions/server/dedup 真 feishu-specific 保留)。`migrateLegacyWorkspace` helper 启动时自动 mv 老路径(原子 rename),T1-T6 6 case 全覆盖(5 mock + 1 真 fs)。Medium 净 +180 行 / 511 测试 0 fail / 16/16 typecheck / 0 R4 / 0 上游侵入。配套 ADR 同步路径(OPENCODE-PLAN 仓 separate commit)|
| [im-account-agent-workspace-binding](./im-account-agent-workspace-binding/) | **rejected-superseded** | 🛑 **已否决,不实施**(2026-05-25)。研究阶段做完代码审计(硬编码真 9 处而非 7,line 号漂移 ±40,ChatSessionStore 路径变了,enableAutoGroupCreate 已删),user 跟架构师角色讨论后**全盘推翻**原 per-account workspace 绑定方案。**取代架构**:所有 IM 共享 home base workspace + 唯一 `imbot` agent,定制能力放 `<home-base>/.opencode/agent/imbot.md` 单文件,**0 代码改动**。否决理由 6 个(抽象层错位 / 1:1 锁死 / user-facing primitive 错位 / systemPrompt 不可靠 / GUI 选 agent 是安全漏洞 / 解决未验证需求)。1-spec 保留作审计快照。详 [`OPENCODE-PLAN/架构决策/im桥接-imbot单一架构.md`](../../../OPENCODE-PLAN/架构决策/im桥接-imbot单一架构.md) + 配套 user 指南 [`docs/governance/imbot-定制指南.md`](../governance/imbot-定制指南.md) |
| [feishu-group-new-cmd-and-mention-rename](./feishu-group-new-cmd-and-mention-rename/) | done | 飞书桥接"高级能力"面板全面整顿(2026-05-25):① `/new` 群里启用条件 — 私聊永远允许,群里只在 `requireMention=false`(免@ 模式)时允许,清 session 含"影响所有成员"提示 ② GUI checkbox 语义反转 + 默认改关 — "群里需要 @ 后再响应"(默认勾)→ "允许 AI 免@ 读取群里所有信息"(默认不勾),i18n key 改名 `requireMention.*` → `allowReadAll.*` ③ **彻底删 `enableAutoGroupCreate` 死开关全栈**(`feishu-group-slash-command` 后已无引用):config-schema / store / server / Tauri AccountSummary / Wire types / GUI utils / dialog checkbox / i18n key 全删,换 1 行 info paragraph 说明 `/group` 用法。Medium 净 -2 行 / 16 文件 / 505 测试 0 fail / 16/16 typecheck / Rust cargo check 过 / 0 R4 / 0 上游侵入 |
| [feishu-group-slash-command](./feishu-group-slash-command/) | done | 把建群从自然语言改成 `/group <群名>` 显式命令 — 删 LLM marker 路径 + 缩窄自然语言 regex 为白名单短语(中文 14 + 英文 4)+ 查询后缀排除兜底 + 命中降级为引导文案 + 群聊禁用一致 `/new`;`/group` 路径 0 调 LLM 解耦 provider(claude-code / imbot / default 都一样能建群);删 `parseCreateGroupShortForm` 半结构化路径。Medium 净 -378 行 / 505 测试 0 fail / 16/16 typecheck / 0 R4 / 0 上游侵入 |
| [upstream-rename-sst-to-anomalyco](./upstream-rename-sst-to-anomalyco/) | done | forward-looking docs 统一改 `sst/opencode` → `anomalyco/opencode`(2026-05-24 user 拍板对齐:sst 已 transfer 到 anomalyco,GitHub 自动 redirect,以后讲"opencode 官方/上游"一律指 anomalyco)— 27 文件 / 净 +2 行(主要 1:1 文本替换 + CLAUDE.md 1 行历史注);历史 archive / 旧 feat changelog / `docs/STATUS.md` / `改动日志.md` / `docs/installer-versions.md` 保留原 sst 字眼作为历史快照不动;memory 加 `reference_upstream_opencode_owner.md` 防未来 agent 再犯"二级 fork"误判 |
| [e2e-pre-push-gate](./e2e-pre-push-gate/) | done | 测试纪律自动化最后一公里 — `.husky/pre-push` 在原 bun 版本 + typecheck 后追加 Phase 1 e2e gate 段:**只在 push 含 main 时触发**(feat 分支 push 仍只 typecheck,不付 ~24s e2e 成本);vite mock 必须已在 :3000(不自动 spawn,避开 Win Git Bash 跨平台 pid 管理坑,fail 时清楚指引);通过 `git push --no-verify` + `[override-pre-push: 理由]` tag 跳过;3 种场景手动验证全过(无 vite+main→fail / 无 vite+feat→skip e2e / vite+main→11 passed 24.1s)|
| [e2e-vite-warmup](./e2e-vite-warmup/) | done | R5 v4 flaky 48h 内修首次履约 — vite mock 冷启动 + Playwright 多 worker 并行 → 第 1 批 2-3 spec timeout 偶发 fail;双保险方案:① `vite/e2e-mock.js` server.warmup.clientFiles 8 个核心 SolidJS 组件(启动时主动预编)② 新建 `e2e/global-setup.ts` 跑测试前真 chromium 加载 `/` 一次让 module 编完(`playwright.config.ts` 接 globalSetup);4 次连跑(含真冷启动)全 11 passed / 0 flaky / 20.4-24.0s;Tiny+ 58 代码 + 80 文档;同时删 `OPENCODE-PLAN/需求池/e2e-vite-cold-start-flaky.md` backlog(闭环)|
| [e2e-bug-repro-3case](./e2e-bug-repro-3case/) | done | Phase 1 e2e 首批 bug-repro 示范用例落地 — A6 large-file-preview-guard(100MB+ txt → FileTooLarge 卡 + 2 按钮)/ A5 chat-drop-overlay-stuck-fix(dragover 激活 → drop+stopPropagation → window capture-phase 兜底清 overlay)/ A4 auto-save-debounce-flush(D1 降级路径:invoke write_text_file → memfs 同步 + mtime 自增 + 0 误 toast + mtime 冲突保护);实施中暴露 Phase 1 fixture infra 4 bug(mock timing / shape 转换 / memfs.list root prefix / Playwright glob query string)全数顺手修;~640 行(266 代码 + 24 fixture/memfs + 350 文档),Medium 4h 实施;全套 e2e 11 passed / 1 skipped / 14s,0 回归;R5 v4 bug-repro 提级到 Phase 1 e2e 的首批履约 |
| [e2e-phase1-mock-mode](./e2e-phase1-mock-mode/) | done | 自动化测试 Phase 1 基础设施落地 — Vite mock plugin(条件激活 `--mode e2e-mock`)+ 内存 fs MemFS(170 行,mtime 严格自增 + watcher event + cross-process via `window.__deskfoxE2eMemfs`)+ Tauri invoke dispatch 22 命令(fs 11 / 外部 app 3 / 飞书 8 stub + override 表)+ bootstrap mock(4 query:config / provider / path / project)+ workspace entry + 7 个 Playwright fixture helpers + `mock-foundation.spec.ts` 端到端 smoke 8/8 assertions;**治理同步升 R5 v3→v4**(Medium feat 强制 ≥ 1 Phase 1 e2e / bug-repro 提级到 reactive 层 / View 清单硬门槛即时生效);1280 行新代码 + 5.5 工作日实际(原估 15-21d,节省 10-15d);Stage ② 5 spec 全过 + 0 回归 + 10.5s 全套(<2 min 远超 A1)|
| [auto-save-debounce-flush](./auto-save-debounce-flush/) | done | 文件查看器编辑态 debounce auto-save + flush(REQ-001 dirty tab 修复,Obsidian 风混合方案 C)— 1s debounce 后自动落盘;`<Show keyed>` 切 tab unmount 时 `onCleanup` flush(snapshot 模式+`setStoredContent` 同步更新 store 避免 race);窗口关 emit `deskfox-flush-before-close` event 前端转 DOM custom event flush;`markSelfWriting` 500ms 窗口拦自写触发的"AI 修改了此文件"误 toast;9 单测 |
| [abandon-cloud-build-workflows](./abandon-cloud-build-workflows/) | done | 云端 build workflow 永久废止 — 3 个 release workflow yml(`release-deskfox.yml` / `release-mac-deskfox.yml` / `release-mirror-gitee-deskfox.yml`)从 "DISABLED 2026-05-11 待复活" 改 "OFFICIALLY ABANDONED 2026-05-21";所有 ship 走唯一本地路径(pack-installer → commit bump → tag → push → gh release create [--prerelease] → Gitee API → mirror-asset-to-gitee);治理 doc §四/§五/§六/§七 全面更新 |
| [ship-scripts-naming-fix](./ship-scripts-naming-fix/) | done | ship 脚本 4 个对齐新命名规则(installer-naming-cleanup 后续) — `pack-installer.{ps1,sh}` path echo / .dmg rename 用 NumericVer(strip env suffix);`mirror-asset-to-gitee.{ps1,sh}` tag regex 加 `dev` 识别(Tier 2 预览版),Mac 顺手补 asset 自动定位。本次首次跑 Tier 2 ship 时暴露的 4 个脚本 bug |
| [installer-naming-cleanup](./installer-naming-cleanup/) | done | installer 文件名去重 — `OutputBaseFilename` 模板用 `NumericAppVersion`(strip env suffix)替 `AppVersion`,Tier 2/beta 产物从 `DeskFox-Dev-2026.5.21.1-dev-setup.exe` 简化为 `DeskFox-Dev-2026.5.21.1-setup.exe`;env 只通过 OutputBase 前缀体现,版本号保持纯数字;Tier 1 prod 产物名不变(历史一致);治理 doc §3.4 新增命名规则段 |
| [3tier-versioning-governance](./3tier-versioning-governance/) | done | 3-tier 发布渠道 + 版本号体系正式立规 — Tier 1 稳定版(prod 无后缀,GitHub Release latest)/ Tier 2 预览版(dev `-dev` 后缀,GitHub Release prerelease,可对外发)/ Tier 3 本地测试版(`-NoBundle` raw exe,不发布)。新治理 doc `docs/governance/版本号与发布渠道规范.md`(9 段 centerpiece)+ 5 处 cross-link(README × 2 / 应用身份-命名规则 / CLAUDE.md / pack-installer.{ps1,sh} 头注)|
| [rename-dev-to-main](./rename-dev-to-main/) | done | 主分支 `dev` → `main` 重命名(2026-05-21,与 installer channel `dev` 命名空间解耦)— Phase A 文档全面更新 + Phase B 实际 git 操作(local rename / push origin main / GitHub default branch / 删 origin/dev / Gitee 同步)全部落地 |
| [installer-version-env-suffix](./installer-version-env-suffix/) | done | 安装包版本号按 env 加后缀 + N 序列独立(B2 口径)— bump 脚本加 `-Env dev\|beta\|prod` 参数,prod 无后缀 `2026.5.21.1` / beta `2026.5.21.1-beta` / dev `2026.5.21.1-dev`;N 计数器**平台 × env 双维度独立**;Inno Setup `VersionInfoVersion` 用 ISPP `Copy(...)` strip 后缀给 PE header(N.N.N.N 数字格式);双端 parity(ps1 + sh)|
| [large-file-preview-guard](./large-file-preview-guard/) | done | 大文件预览统一防护(REQ-025)— 4 层防御:L1 入口闸门(`get_file_size` 新 Tauri 命令 + load() pre-check)+ L2 媒体改 localasset 不进 JS 内存(顺手修原图片"不可预览"bug)+ L3 阈值兜底(text/markdown/html 10MB / office 200MB / media+binary ∞ / default 100MB)+ L4 UX 兜底组件(用本机软件打开 / 打开所在文件夹);office 50MB→200MB 决议(常见 PPT 含图含视频);canEdit() 加 tooLarge 守卫防数据丢失;19 单测 |
| [chat-drop-overlay-stuck-fix](./chat-drop-overlay-stuck-fix/) | done | 拖拽接收浮层卡死修复 — file-tree 行 onDrop 调 stopPropagation 杀掉 document bubble drop 致浮层 setDraggingType(null) 不执行;window 级 capture-phase drop + dragend 双兜底,ONLY 清状态不参与 drop 处理;R5 bug-repro 测试先行(DOM 事件流前置假设)|
| [chat-input-focus-follow](./chat-input-focus-follow/) | done | 聊天输入框焦点跟随 — 聊天对话区右键 + MD/HTML viewer 选区"加入聊天"后光标到末尾 + 输入框 focus,user 可立刻继续打字(模块级 singleton ref + rAF;Tiny+ 4 文件 53 行) |
| [chat-selection-menu](./chat-selection-menu/) | done | 聊天对话区右键选区菜单 — 替换 WebView2 原生为 DeskFox 自家两项(添加到聊天 / 复制),输入面板跟文件查看器一致 + 红色 overlay 选区高亮;helper extract 模式 + 12 单测 |
| [file-tree-multi-drag-to-chat](./file-tree-multi-drag-to-chat/) | done | 文件树多选(Shift/Ctrl)拖到聊天窗口接通 — attachments 加 `application/x-deskfox-paths` MIME 消费,abs→rel 转换走 helper extract 模式 + 10 单测;0 R4 |
| [file-tree-llm-write-refresh](./file-tree-llm-write-refresh/) | done | AI 创建新文件后右侧文件树自动浮现 — `file.edited` watcher 加父目录刷新分支,修"loaded:true + expanded:false 缓存目录里新文件需 F5 才出现"bug;R5 bug-repro 测试先行 |
| [html-viewer-ux-polish](./html-viewer-ux-polish/) | done | HTML 预览 UX 优化 — 去顶部 toolbar + iframe 内右键桥接(postMessage)弹 DeskFox 菜单 + 左键点 iframe 内菜单消失 + 阈值 2MB→10MB + CodeMirror HTML 编辑高亮;R4 第 6 笔本季(超配,user 授权)|
| [html-viewer-allow-scripts](./html-viewer-allow-scripts/) | done | HTML 预览 iframe 开 `allow-scripts` — 解决 PPT/Slides 翻页按钮等内嵌 JS 失效问题(反转 md-office-improvements spec A1.9,跨 origin 论证 MDN 警告不适用)|
| [file-tree-dnd](./file-tree-dnd/) | done | 文件树拖放移动 — 全功能(核心+多选+剪切粘贴+撤销+外部拖入) |
| [getbot-接入](./getbot-接入/) | done | GetBot 模型聚合 Provider 接入(热门首位 + 推荐标 + 内置 apiKey 流程) |
| [规范-v2](./规范-v2/) | done | 三文档分离 + diff 阈值放宽 + 注册型扩展点出黑名单 |
| [加聊天-preview-fix](./加聊天-preview-fix/) | done | 文件查看器右键加聊天后选中文字未传到模型 — synthetic text 里带 preview |
| [查看器-自动刷新](./查看器-自动刷新/) | done | 模型 Edit/Write 后查看器不更新 — client 加 file.edited 监听 + 编辑态 dirty 守卫 |
| [禁自动升级](./禁自动升级/) | done | 关闭 opencode 官方自动升级入口和通道,防止 DeskFox 被覆盖 |
| [启用自动升级](./启用自动升级/) | done | 反向操作:启用 DeskFox 自家更新通道(Layer1/2 翻转,Layer3 保留) + Windows Inno→NSIS 切换 + minisign 密钥三次重生成(**2A00 带密码**,根治 Win 空密码传不进子进程) + Win 签名密码注入 + LO bundle 注入。**Win 本机验收全绿(2026-06-06)**:TC-1/2/4/5/7/8/9/10/12(CDP + 产物验签 + 后端 manifest + typecheck/单测/e2e),含 LO 190MB 包验签 8/8;Win 主体已合 main。**Mac 适配**(`feat/macos-updater-adapt`):`verify-updater-artifacts.ts` 加 `--target darwin` + `build-deskfox.sh` `2.6` 段(LO 重签后基于最终 `.app` 重打包 `.app.tar.gz`+`tauri signer sign`)+ `pack-installer.sh` 列产物 + `deploy-updater-manifest.sh` 接 `/ship` 步骤 7.5(OSS 上传 + finalize + SCP 部署 `updates.deskfox.ai`);prod 真 build(2A00)**verify darwin 8/8**。剩 TC-3(mac 真升级)/ TC-6(win 真安装)/ TC-11(Inno 共存)硬件阻塞,延后真机/真实发版验 |
| [installer-打包](./installer-打包/) | done | DeskFox 打成 Windows installer(不签名)— Inno Setup 路线;顺手修 icon pipeline bug 让 exe 嵌入多分辨率狐狸 |
| [icon-pipeline-deep-fix](./icon-pipeline-deep-fix/) | done | Tauri winres 实只读 icons/dev/icon.ico、无视 prod.json override — apply-icons 同步覆盖 dev/ + 新设计资源全量更新 |
| [macos-打包](./macos-打包/) | done | DeskFox 打 macOS .app + .dmg(arm64,不签名)— Phase 1 scaffolding + Phase 2 user Mac 实战打通 |
| [claude-code-loop-fix](./claude-code-loop-fix/) | done | 修 claude-code plugin 选 Claude 模型时 step loop 不 break、UI 卡死 — 兜底块用 step-finish part 替代 finish 顶层字段(R4 override,case 1) |
| [build-pipeline-sidecar-fix](./build-pipeline-sidecar-fix/) | done | Windows build-deskfox.ps1 加 sidecar 自动 build 步骤 — 时间戳判断 + non-baseline 优先(绕 clash 代理下 bun-baseline 下载失败) |
| [plugin-cwd-channel](./plugin-cwd-channel/) | done | opencode 加 _opencode providerOptions namespace 暴露 cwd 给 spawn-based plugin(R4 第 3 笔本季特批,不扣下季度配额) |
| [installer-versioning](./installer-versioning/) | done | installer 版本号规则 YYYY.M.D.N(年.月.日.当天第几版)+ bump 脚本 + pack-installer 一键脚本 + docs/installer-versions.md 版本日志 |
| [md-viewer-typography](./md-viewer-typography/) | done | 文件查看器看 .md 时排版升级 — 标题阶梯(字号+加粗双轨,h1/h2=700,h3-h6=600)/ 行内代码芯片 / 引用块底色 / HR 显形 / 表头底色,scoped 在 data-context="file-viewer" 下,聊天侧零影响 |
| [bump-script-encoding-fix](./bump-script-encoding-fix/) | done | bump-installer-version.ps1 加 UTF-8 BOM 修中文 placeholder 乱码(PS 5.1 源解析陷阱)|
| [macos-右键选区-修复](./macos-右键选区-修复/) | done | macOS WKWebView 文字上右键选区丢失 — 选区历史栈挑最长 + overlay div 替代 stale 的 CSS Custom Highlight + 关闭菜单时统一清原生/Pierre 选区 |
| [加聊天-option-enter](./加聊天-option-enter/) | done | 文件查看器"+ 添加到聊天窗口"对话框 macOS 加 Option+Enter 提交快捷键 + 底部提示文案平台化(Tiny) |
| [macos-pack-installer](./macos-pack-installer/) | done | macOS 一键打 .app/.dmg 脚本(pack-installer.sh + bump-installer-version.sh)+ icon.icns 加 .gitignore + 4 个 sh +x 权限(Tiny) |
| [office-installer-macos](./office-installer-macos/) | done | LibreOffice 自动安装 macOS 适配 — DMG 下载 + hdiutil 挂载 + cp -R 到 ~/Applications + soffice 检测路径(Tiny,fork-only,纯增量) |
| [prod-bundle-id-fix](./prod-bundle-id-fix/) | done | macOS prod / beta Bundle ID 独立 override(prod=ai.opencode.desktop,beta=...beta),修应用程序网格搜不到 .dev Bundle ID 应用的问题(Tiny) |
| [bundle-id-debrand](./bundle-id-debrand/) | done | 完整品牌切割 — Bundle ID 三档全去 `opencode` 字眼,改 `ai.deskfox.app` 系列(prod/.beta/.dev),与 anomalyco/opencode 上游 0 命名空间共享(Tiny) |
| [分支策略-v2](./分支策略-v2/) | done | dev 单一稳定主干 + 上游同步分离 + 三档环境正交分支 — feat → dev 一次性 merge 切换(`fae01d2a8`,187 文件 / +19721 行),废除 4.3 节"禁止直 push 到 dev",origin 双 push 临时改单推 gitee |
| [win-tri-env-appid](./win-tri-env-appid/) | done | Windows 三档 AppId 同机共存 — `DeskFox.iss` 加 `#if AppEnv` 切档(beta `{86413DCA-...}` + dev `{4C5D29F2-...}` 新生成,prod GUID 锁死不变);`pack-installer.ps1` 加 `-Env` 参数;Mac/Win 三档共存能力对齐 |
| [数据目录-deskfox-隔离](./数据目录-deskfox-隔离/) | spec | DeskFox 与上游 opencode 共用全部数据目录(auth/sessions/config/cache/install_id),两档实施:Phase 1 install_id 独立(fork-only,3 行)/ Phase 2 全量隔离(改上游 R3);从隐私协议 v0.5 待办挪入。**2026-05-04 评估暂搁(deferred)** — 当前主受众非工程并行使用场景,99% 用户 0 收益;触发条件(并发冲突 / schema 破坏 / 商业化 / 10w+ 用户)未出现前继续 backlog,详见 1-spec.md "评估决策" 段 |
| [post-sync-build-fix](./post-sync-build-fix/) | done | sync/upstream-2026-05-02 merge 后两个 latent build bug 浮出 — Windows symlink 落空(`.d.ts` 转真文件 + triple-slash reference,R4 override)+ Bun baseline runtime ~190MB 下载绕过(build-deskfox.ps1 直接 build --single 跳 predev.ts) |
| [dev-typecheck-fix](./dev-typecheck-fix/) | done | dev typecheck 555 错根因 — sync abort 后 node_modules symlinks 没 rollback 到 lock,同 codebase 看到两份 effect 类型;`bun install` 修(0 代码),并把 abort 后必跑 reconcile 写进 UPSTREAM-MERGE-GUIDE §5.0 / §7 / TL;DR |
| [zod-schema-bridge](./zod-schema-bridge/) | done | 上游 sync prep — office-pdf-ref 从 schema enum 抽离到 fork-only vendor MIME 协议(`application/x-deskfox-pdf-ref`),producer + consumer 都改走 `@opencode-ai/shared/office-pdf-protocol`;`Content` schema 上 fork 字段值依赖归零,以后上游改 Content 0 冲突;R4 override 第 2 笔本季 |
| [updater-disable-adapter](./updater-disable-adapter/) | done(rolled back) | 上游 sync prep — 禁自动升级走法从"条件 spread 藏 UI"改成"上游 sentinel + menu 灰显"风格;**翻车** UX 回归(立即检查按钮变可点击 + 假 toast),被 `updater-disable-adapter-rollback` 撤回;constants.rs 注释更新保留 |
| [updater-disable-adapter-rollback](./updater-disable-adapter-rollback/) | done | 撤回 `updater-disable-adapter` 的 createPlatform sentinel + menu 灰显改动,恢复 conditional spread 模式(method undefined when off → controls 自动 disable);constants.rs 注释保留;UPSTREAM-MERGE-GUIDE §4.4 表格 + 教训段沉淀:**改 method 暴露策略前先 grep `disabled={!platform.<method>}` 类多重信号 callsite** |
| [sync-2026-05-03-aborted](./sync-2026-05-03-aborted/) | done | 上游 sync 实战 abort 复盘 — 8 个 conflict 全 resolve 后撞 SDK 双路径互斥(httpapi vs hono 二选一);过程中踩 `bun.lock` 自由 resolve 坑(`poe-oauth: *` → mcp-oauth bug);全 lessons 沉淀到 UPSTREAM-MERGE-GUIDE §4.7(lock 处理方法学)+ §7(2 行新踩坑)+ TL;DR(2 条);决策走 office-routes-effect-httpapi(下笔 spec) |
| [office-routes-effect-httpapi](./office-routes-effect-httpapi/) | done | fork 4 个 office routes(`/file/office-pdf` + `/office-tooling/{status,install,progress}`)从 Hono 迁到 Effect HttpApi PublicApi,httpapi-mode SDK 含 fork office method;实施在 sync-2026-05-03-2 期间(新建 `file-office.ts` 集中 schema + groups/handlers FORK block);老 Hono routes 暂留(共存,follow-up 删) |
| [win-bun-install-fix](./win-bun-install-fix/) | done | Windows `bun install` 不再被 `tree-sitter-powershell` postinstall 阻断 — 上游 `trustedDependencies` 列了它但 fork+upstream 都只用 wasm(native binding 0 用),且 `tree-sitter-powershell@0.25.10` 不发 Win prebuilds → 必走 fallback 编译 → Windows 没 VS Build Tools 报错。fix:root `package.json` 删该 entry(R4 第 3 笔本季,超 1 笔配额,user 明确授权);typecheck 15/15 + DeskFox.exe build 通过;pre-push 安全网恢复 |
| [sync-2026-05-03-2](./sync-2026-05-03-2/) | done | 本季 sync 实战成功 — 462 commits / 1157 文件 / +58k/-53k merge 完成;早上 abort 的 SDK 双路径互斥被 office-routes-effect-httpapi 解锁;`bun.lock` 走新 §4.7 take theirs 绕开 mcp-oauth 坑;9 个 fork 文件现场调整(import path adapt + Instance→InstanceState.context 适配);typecheck 15/15 + DeskFox.exe build 通过;0 R4 |
| [release-mac-ci](./release-mac-ci/) | done | GitHub Actions 自动打 macOS .dmg(arm64,不签名)— 新增 `release-mac-deskfox.yml`,push `ship-mac-(prod\|beta)-*` tag 触发,与 Win workflow 独立;复用 mac 端现有 build-deskfox.sh + workflow 内自带 .dmg 重命名 |
| [repo-migration-deskfox](./repo-migration-deskfox/) | done | 主仓从 `yuesoue/opencode-for-office-deskfox`(无 fork 关系)迁到 `zoulukuang/deskfox`(真 fork from anomalyco/opencode;owner 名 2026-05-04 由 `yuesoue` 改名得来,详见 user-rename-zoulukuang);修品牌诚信 + 解锁上游 PR 工作流;dev + 全 tag push,4 处 docs URL 更新,老仓 dev 加 deprecation banner;Mac 端 origin 切换 / Gitee 镜像迁 / 老仓 archive 留 backlog |
| [user-rename-zoulukuang](./user-rename-zoulukuang/) | done | GitHub user 改名 `yuesoue` → `zoulukuang`(对齐 Gitee 用户名);本地 origin / origin-legacy URL 切到 zoulukuang;5 处活跃 docs URL 更新;老仓 deprecation banner URL 改名;**`yuesoue` 占位空号 2026-05-04 评估弃做**(项目小 / 老仓 0 stars/forks,squat 实际风险低,接受不防)(Tiny) |
| [gitee-release-mirror](./gitee-release-mirror/) | done | GitHub Release publish 后同步到 Gitee Release — **混合方案**:workflow `release-mirror-gitee-deskfox.yml` 镜像元数据(tag/name/body,30s)+ 本地脚本 `mirror-asset-to-gitee.ps1` 上传 .exe 附件(5.5s,76Mbps);3 次实测后发现 GitHub US runner 上行 Gitee CN 50MB 被 GFW 节流到 30min timeout,pivot 让本地 IP 干上传活;首次实测 `ship-prod-2026.5.3.1` 全链路通(workflow 创 release_id=669251 + 本机秒传 .exe);**2026-05-04 续笔**:补 Mac `mirror-asset-to-gitee.sh`(~230 行 bash,镜像 .ps1 逻辑,自动定位 .dmg + GitHub fallback 下载 + jq 解析,跨平台 stat,文件 mode 100755),Mac 协作者拉 dev 实测 `ship-mac-prod-2026.5.4.1`(GitHub draft 待 publish 后跑) |
| [readme-deskfox-自家版](./readme-deskfox-自家版/) | done | GitHub 主仓 README 从上游 OpenCode 完整重写为 DeskFox 自家版 — **英文主 `README.md` + 中文 `README.zh.md`**(2026-05-04 修订,因国外申请数字签名期间英文更合适;原计划中文主)— 匹配 deskfox.ai 站点定位 / 品牌 / URL;删 21 个上游 locale README;复制 1 个 logo SVG + 4 张产品截图(hero + PDF/PPT/视频 预览)到 `docs/assets/`;走 feat 分支迭代 + GitHub 上预览审稿(DeskFox 自家 markdown preview 不渲染本地图,backlog);merge dev 后删 feat 分支;后续修订英文化直接 dev |
| [actions-node24-bump](./actions-node24-bump/) | done | 两 workflow yml(release-deskfox + release-mac-deskfox)4 个 actions 升级 Node 20→24:`checkout@v4→v5` / `cache@v4→v5` / `upload-artifact@v4→v6` / `action-gh-release@v2→v3`;清掉 GitHub 6 月 2 日 force Node 24 前的 deprecation 警告;mac + win dispatch dev 双端实测全过(5m37s + 18m19s),Node 警告完全消失(Tiny chore,8 行 / 2 文件)|
| [filetree-ctrlc-textsel-fix](./filetree-ctrlc-textsel-fix/) | done | 修聊天气泡 / md 查看器选文本后 Ctrl+C 失效 — `useFileTreeShortcuts` 的 B 路径在中性区(activeElement=body)+ 文件树 selection 非空时误抢,`event.preventDefault()` 阻断原生 copy 但 `clipboard.setCopy` 又不写 OS 剪贴板,净效果"键失灵";加一道 `hasTextSelectionOutsideFileTree()` 闸,文本选区在文件树外时让原生走;A 路径与 B 路径设计意图(文件树焦点 / 文件树有选中无文本选区)均保留;Ctrl+X 一并修好(Tiny,fork-only,~25 行 / 1 文件,0 R4)|
| [file-tree-ux-polish](./file-tree-ux-polish/) | done | 文件树 UX 一揽子优化(5 项 / 5 笔 commit / Medium / 0 上游侵入)— ① 自动刷新(LLM busy→idle 边沿触发递归 refresh)② 节点右键菜单 4 组重整(删打印 + 加复制路径/刷新 + 文案去 .md 但默认值保留 markdown)③ 空白菜单 2 组重整 + 修刷新只刷根的递归 bug(新加 `refreshAllExpanded` 扫所有 expanded 子目录)④ 默认面板展开 + tab "all"(决议 D 不迁老用户)⑤ 键盘 ↑↓/Enter/F2/Delete/Backspace(macOS)— 复用现有 shouldTrigger gate;rebase 远端 ctrlc-textsel-fix 0 冲突 |
| [menu-i18n](./menu-i18n/) | done | 文件树菜单 / 对话框 / toast 接入 i18n 框架 — `fileTree.*` 命名空间 49 个 key(en/zh/zht 三本同步,其他 14 种自动 fallback en);file-tree.tsx + dialog-file-tree.tsx 全部硬编码中文 → `language.t()`;rebase 到 file-tree-ux-polish 后 audit 加减 key(删 print 加 copyPath/copyPathSuccess);Medium 324 行 / 6 文件 / 0 R4 / 0 上游侵入 |
| [viewer-ctrlc-fix](./viewer-ctrlc-fix/) | done | 文件查看器非 .md 内容(`@pierre/diffs` shadow DOM)Ctrl+C 失效修复 — 加 window-capture keydown 复用既有 `pickBestRecentSelection()` shadow-aware 选区机制 + `navigator.clipboard.writeText`;编辑态 / input / textarea / contenteditable 全让原生路径;Tiny ~36 行 / 1 文件 / 0 R4 / 0 上游侵入 |
| [imbot-permission-minimal](./imbot-permission-minimal/) | done | imbot v2 务实档(~30 条 ask)→ v3 极简档(8 条 ask)— read 只拦 .env/.ssh、bash 只拦 8 条真不可逆(rm -rf / git --force / aws s3 rb / aws ec2 terminate / dd/mkfs/fdisk / shutdown)、webfetch 撤回 allow,其他全沿用 build default;user 信任"飞书 IM 消息流可见"= 看得到 LLM 在做什么,把可逆操作信任度调高;Medium / ~60 行净 / 6 单测调整 + 2 新 / 0 R4 / 0 上游侵入 |
| [imbot-windows-delete-cmds](./imbot-windows-delete-cmds/) | done | v3.1 micro-patch — imbot v3 实测发现 Windows 漏洞:LLM 在默认 PowerShell shell 跑 `rm -rf` 时实际调 `Remove-Item -LiteralPath ...`(不是 unix rm),绕过 `bash["rm -rf *"]: ask` pattern,目录被真删。补 4 条 Win 风格 pattern(Remove-Item / rmdir / del / rd),覆盖跨 shell 调用;`rmdir *` 在 PowerShell 里是 `Remove-Item -Recurse` function alias 能删非空目录,从 v3 砍掉名单移回 ask;Tiny / 4 pattern + 3 测试调整 + 1 新测试 / 0 R4 / 0 上游侵入 |
| [md-office-improvements](./md-office-improvements/) | done | MD 查看器办公场景综合优化 — 一笔大 feat / 4 Phase / 8 项 scope:Phase 1 共建 Tauri protocol localasset:// + 本地图 + 音视频 + HTML 预览 / Phase 2 Frontmatter 隐藏 + Callout + 脚注 / Phase 3 Mermaid 动态加载(runtime 0 网络)/ Phase 4 MD 内链跳转 + 文件树焦点同步。吸收 6 个需求池条目;Large(~660 行净代码 / 11 文件 / 7 R4 override);新依赖 marked-alert + marked-footnote + mermaid;DeskFox.exe 32.27→34.79MB(+2.5MB);post-release 修 5 轮 user 反馈 + 4 项 polish 完成 |
| [md-editing-enhance](./md-editing-enhance/) | in-progress | .md 编辑体验增强(Tier B 全套)— 表 1+2 共 11 项编辑增强:列表续延(普通/编号/任务)+ Ctrl+B/I/K 格式化 + 拖图/截图自动 ![](path) + Ctrl+Enter 任务勾选 + 块引用续延 + Heading 折叠 + 智能 URL 粘贴 + 表格 Tab 跳格 + Ctrl+F/H 搜索替换(`@codemirror/search` 即插)+ 修保存后双提示框 bug(toast id 跟踪 + dismiss);Medium(~340 行净代码 / 5 文件 / **0 R4 override** — 全在 app 层);DeskFox.exe 34.79→34.79MB(+40KB);吸收需求池 obsidian-md编辑体验 + 保存后双提示框 + 保存后提示优化 三个条目 |
| [right-click-stale-selection-fix](./right-click-stale-selection-fix/) | done | .md 文件查看器右键残留旧选区修复 — `handleSelectionContextMenu` + `pickBestRecentSelection` 历史栈机制(2026-04-29 为 macOS WebKit Shadow DOM collapse 加的)被全局套到 light DOM 上,引入两个副作用:① 点空白后旧选区从栈里复活(`selectionchange` 不 fire "选空"事件)② 算法只比 length 不比 recency,选 A(长)→ 选 B(短)永远命中 A。fix:`renderMarkdown` 改挂新 `handleLightDomContextMenu`(直接读 `window.getSelection()`,无栈),`renderDefault`(Pierre Shadow DOM)路径一字未改 → Mac 零回归(Tiny ~30 行 / 1 文件 / 0 R4 / 0 上游侵入)|
| [setup-version-badge](./setup-version-badge/) | done | 设置面板左下角版本牌改 DeskFox 自家文案 + installer 版本号 — 原"OpenCode Desktop / v1.14.33"(走上游 i18n key + 上游 package.json `version` 字段)改为"DeskFox for Windows / v2026.5.6.2"(平台名根据 `platform.os` 选 Windows/macOS/Linux,版本号读新 fork-only `packages/branding/installer-versions.json`)。bump 脚本(`.ps1` + `.sh`)各加 step 3 — surgical regex 替换对应平台 key value(保留缩进/key 顺序),前端 `import` JSON 渲染。0 上游冲突(`package.json version` 字段不动 → 上游 sync 0 冲突);平台名 hardcode 不走 i18n(专有名词行业惯例不翻译);dead i18n key `app.name.desktop` 留 17 份字典里不删(上游 sync 兼容)。Mac bump.sh 待 Mac 端首次 bump 自然 trigger 验证(Tiny ~50 行 / 5 文件 / 1 新 JSON / 0 R4 / 0 上游侵入)|
| [md-export-pdf-word](./md-export-pdf-word/) | done | .md 导出 Word(.docx)— `@jinzhongjia/markdown-docx@1.0.4` + viewer 右键菜单 + 6 项极致优化(A1 代码块 / A2 Mermaid / A5 本地图 / B1 emoji / B2 错误友好 / C1 选区显菜单);v1 PDF 路线 drop(Tauri WKWebView `window.print()` silent issue #5330);v2 实测 4 笔根治(代码块切分 single-paragraph + 修 inline code 误并 / emoji 字体 fallback / Mermaid foreignObject patch + scale 3x + 5s 等待 / Replace 报错修);Medium(17 笔 commit / ~700 行净代码 / 1 R4 override — `bun.lock` 加 fflate dep)|
| [menu-always-show-with-disabled](./menu-always-show-with-disabled/) | done | .md 查看器右键菜单始终显示 4 项,选区相关项("添加到聊天窗口" / "复制")按 `mdMenu().text.trim()` 灰显 disabled,"编辑" / "导出 Word" 不依赖选区始终可用 — 替代之前的"选了文字 4 项 / 没选 1 项"两套行为;UX 一致性 / user 一眼看到全部能做的事;Tiny(1 笔 commit / 净 -12 行 / 0 R4 / 0 黑名单)|
| [md-context-menu-i18n](./md-context-menu-i18n/) | done | .md 右键菜单完整接入 i18n,跟随软件语言切换 — 之前只有"导出 Word"接 i18n,其他项硬编码中文(英文 locale 用户看到中英混杂)。加 5 个 `fileViewer.menu.*` key(addToChat / copy / input.placeholder / input.shortcutHint / input.submit)+ 复用 `common.edit` / `common.cancel`,zh/zht/en 三本字典同步,其他 14 种语言自动 fallback en;Tiny(1 笔 / 净 +29 行 / 4 文件 / 0 R4 / 0 黑名单)|
| [tests-md-to-word-integration](./tests-md-to-word-integration/) | done | **MD → Word 端到端集成测试**(基础设施实证 — 用 D 系列 + e2e 基础设施跑通 `exportMdAsDocx` 主入口完整流程,11 测试全 pass)。复用 D4 Tauri invoke mock + 加 saveDialog 控制 + showToast mock,真跑 markdown-docx / docx / fflate / 所有 fork helper。覆盖 11 场景:取消对话框 / 完整流程参数校验 / docx 是合法 zip(含 word/document.xml)/ defaultFileName 拼 .docx / i18n.title 传 dialog / invoke 失败友好错误(EACCES → 无写入权限 + [详细])/ **代码块 syntax 高亮 token 切分**(>const< 单 run)+ MdCode 段落样式 + `<w:br/>` 软换行 / **emoji `<w:rFonts>` Segoe UI Emoji 覆盖** / 无 mdFileDir 跳本地图 / 有 mdFileDir 走 read+write 双 invoke / mermaid 块无 viewerEl 不抛错。踩坑:syntax 高亮把代码切 token 不再连续字符串(改 assert `>token<`)。`md-export-docx.ts` 成为 Logic 清单**首个 helper + 集成双层覆盖**的范本。测试 531 → 543(+11 全 pass)| Medium ~230 行 / 1 文件 / 0 R4 / 0 上游侵入 |
| [e2e-mock-infrastructure](./e2e-mock-infrastructure/) | done | **e2e 基础设施 mock 路径建立 + 5 个真 e2e 跑通** — V2 双清单 View 清单门槛的前置依赖搞定。3 选 1 中选 Playwright `page.route` 拦截路径(不改前端 / 不依赖 sidecar / 跑得快)。新建 `e2e/fixtures.ts`(~50 行,`installServerMock` + `mockedPage` 扩展 fixture)拦截 4096 所有请求(/health 200 ok / GET 空数组 / POST 200 ok)。扩 `smoke.spec.ts`(baseline + mock 路径 2 测试,实测 body 渲染 345 字符,真 UI 出来)。新建 `i18n-sidebar.spec.ts`(3 测试):**实证 i18n drift 补全的真实 UI 渲染**(`No projects open` / `Open a project to get started` / `Getting started` / `free models`)+ 0 fatal console error 守门。踩坑 networkidle 永不 idle(改 domcontentloaded + 2s wait)+ strict mode 多 match(`.first()`)。**V2 双清单逻辑闭环完成** — Logic 严格守门 / View 基础设施已建门槛可生效。e2e 6 总 / 5 pass / 1 skip(上游 todo.fixme)/ 18.5s | Medium ~190 行 / 3 文件 / 0 R4 / 0 上游侵入 |
| [governance-test-dual-list-c](./governance-test-dual-list-c/) | done | 测试治理规范升级到**双清单**(选项 C)— D 系列实施完成后 user 选 C(B 仅记录数字 / A 修单清单语义 / **C 双清单**)。决策 2 单清单 → Logic 清单(行覆盖 80%)+ View 清单(e2e ≥ 1 happy path)。原 4 文件分配:Logic 4(`md-export-docx` / `markdown-editor-extensions` / `dialog-settings-version` / `file-tabs-helpers`)+ View 2(`dialog-settings.tsx` / `file-tabs.tsx`)。**helper extract 模式正式入规则**(组件抽出 helper 进 Logic / 原组件留 View)。**View 清单硬门槛延后**(等 e2e 基础设施 setup 完成,opencode sidecar 或前端 mock mode);承认基础设施未 ready 但清单已建,长期逼着 e2e 走通(否则 View 清单永远空话)。CLAUDE.md R5 v3.1 + governance v2 + 需求池长期规划 v1.2 同步修订(Tiny ~120 行 / 3 文件 / 0 R4 / 0 上游侵入)|
| [tests-codemirror-fixture-d3](./tests-codemirror-fixture-d3/) | done | **D 系列第 2 笔(D3)** — CodeMirror Mock View + 6 Command + handlePasteHook 测试。**关键洞察**:Command 只用 `view.state` + `view.dispatch`,不需真 EditorView(避开 happydom 兼容)。Mock view 工厂用 `EditorState.create + simplified dispatch`,~30 行 setup 解锁全部 Command 测试。export 6 个 Command + handlePasteHook,新建 `markdown-editor-extensions-commands.test.ts`(~310 行 / 36 测试):toggleBold/Italic 各 2 / insertLink 2 / toggleTaskCheck 8 含 Tier B 空 `[]` 放宽 + 缩进 + 多行光标定位 / continueList 9 含编号递增 + 空 bullet 退出 + 选区返 false / tableTab 5 / handlePasteHook 8 含 URL+preventDefault+stopPropagation 校验 + uri-list 多行 # 注释跳过 + clipboardData null 防御。`markdown-editor-extensions.ts` 估算覆盖率从 ~17% 升到 **~75%**(接近 80% 门槛,剩 3 个异步路径 readFileAsBase64/handleImageDrop/主入口)。测试 437 → 473(+36 全 pass)| Medium ~310 行 / 2 文件 / 0 R4 / 0 上游侵入 |
| [tests-tauri-invoke-mock-d4](./tests-tauri-invoke-mock-d4/) | done | **D 系列第 1 笔(D4)** — Tauri invoke mock 模式建立 + `md-export-docx.ts` 推到 100%。新建 `md-export-docx-inline.test.ts`(~180 行 / 22 测试,单独文件因 `mock.module` 污染)用 `mock.module("@tauri-apps/api/core")` + `invokeImpl` 可变状态 + `invokeCallLog` 断言模式;覆盖 `inlineLocalImages` 早返回(3:mdFileDir 缺/空 md/纯文字)+ 外链跳过(8:http/https/data:/blob:/file:/localasset:/协议相对/锚点)+ 本地替换(9:成功/title保留/不识别扩展/**invoke 失败保留原文**/**percent-encoded 解码**/多张并发/混合外链+本地/absolute 路径/invoke 参数 root="")+ alt 保留(2:空/中文)。`md-export-docx.ts` 8/8 helpers ≈ 100%(关键模块清单首个 100%)。mock 模式可复用到后续 D3 异步路径。测试 415 → 437(+22 全 pass)| Tiny ~180 行 / 2 文件 / 0 R4 / 0 上游侵入 |
| [tests-markdown-editor-extensions](./tests-markdown-editor-extensions/) | in-progress | `markdown-editor-extensions.ts` 易测部分覆盖(R5 决策 2 关键模块清单第 2 个文件)— **未达 80% 门槛**(实际 ~17% 行覆盖,**透明报告**)。给 7 个 helper 加 export(LIST_PATTERNS/TASK_PATTERN/URL_PATTERN/IMAGE_EXT_PATTERN/timestampName/depthOf/PHRASES)+ 写 42 测试(LIST_PATTERNS 4 子模式 12 / TASK_PATTERN 含 `[]` 空放宽 4 / URL_PATTERN 4 / IMAGE_EXT 3 / timestampName 5 / depthOf 8 含 Win 反斜杠 / PHRASES 4 含 zh-zht 简繁差异校验)。剩 11 项(6 个 CodeMirror Command 需 EditorView fixture / 3 个异步 IO 需 Tauri invoke mock / 主入口 markdownEditorExtensions)转独立 backlog,需 setup CodeMirror 测试基础设施 + Tauri mock。测试 373 → 415(+42 全 pass)| Medium ~280 行 / 2 文件 / 0 R4 / 0 上游侵入 |
| [tests-md-export-docx-coverage](./tests-md-export-docx-coverage/) | done | `md-export-docx.ts` 覆盖率推到 80% 门槛(R5 决策 2 关键模块清单**首个达标**)— 给 4 个 helper(`mergeCodeBlockParagraphs` / `base64ToBytes` / `bytesToBase64` / `splitRunsForEmoji` / `patchForeignObjects`)加 export 注解,扩展测试 +26(base64 5 / emoji 7 / mergeCode 7 含 inline code bug 防护 / foreignObject 7 用 happydom DOMParser SVG fixture)。覆盖 7/8 helpers ≈ 87.5%。剩 `inlineLocalImages` 转 backlog(需 Tauri invoke mock)。测试 347 → 373(+26 全 pass)。3 个关键模块(markdown-editor-extensions / dialog-settings / file-tabs)继续 backlog,单独 feat 推进 | Medium ~210 行 / 2 文件 / 0 R4 / 0 上游侵入 |
| [e2e-smoke-探路](./e2e-smoke-探路/) | done | Playwright e2e 架子探路 + smoke baseline 建立 — 链路验证(vite dev server + chromium headless),baseline 只测"链路通"(HTTP / HTML / URL),**故意不测 UI 内容**(web 版需要 opencode server 跑在 4096,无 sidecar 时 body 为空)。踩坑:首次跑报 chromium 没装(`bunx playwright install chromium` 修);body 内容为空(降级 baseline)。新增 `e2e/smoke.spec.ts`(~30 行 / 1 测试 pass,10.2s)+ `e2e/README.md`(~70 行,现状 / 跑法 / 后续 3 选 1 接入路径 A 启 opencode-server / B mock mode / C fixture replay)。后续接入路径转独立 backlog,推荐 B(mock mode)先做。0 上游侵入(不动 todo.spec.ts / playwright.config.ts)(Tiny ~100 行 / 2 新文件 / 0 R4)|
| [i18n-history-drift-补全](./i18n-history-drift-补全/) | done | zh/zht 历史 i18n 漂移补全(`tests-mac-recent-feats` feat 揭露)+ 守门升级到全字典 — 23 key 缺失:command.project.* / session.child.* / session.review.noBranch* / settings.general.section.advanced / settings.general.row.shell.* (4) / settings.general.row.show{FileTree,Navigation,Search,Status,Terminal}.* (10) / sidebar.empty.* (2)。zh/zht 各加 23 行翻译跟随已有风格(zh 用"会话/项目/默认"/ zht 用"工作階段/專案/預設"等),`i18n-completeness.test.ts` 守门范围从"关键 namespace(fileViewer/common)"升级到**全字典 100% 覆盖** + 加 dead key 检查;以后任何 sync / feat 加 en key 不补 zh/zht → CI 立即 fail。测试 346 → 347(+1 dead key 守门测试)(Medium ~100 行 / 3 文件 / 0 R4 / 0 上游侵入)|
| [tests-mac-recent-feats](./tests-mac-recent-feats/) | done | 为最近 3 个 Mac 端推的 feat 补 unit 测试(R5 测试纪律首笔实施 / 测试金字塔 70% unit 起步)— `md-export-pdf-word` + `menu-always-show-with-disabled` + `md-context-menu-i18n`。新建 `i18n-completeness.test.ts`(7 测试,关键 namespace `fileViewer.*` / `common.*` 守门 zh/zht 100% 覆盖 en + 11 个核心 key 显式断言 + shortcutHint 模板校验)+ `md-export-docx.test.ts`(19 测试,`mimeFromPath` 9 种扩展名/大小写/Win 反斜杠/URL 风格/边界 + `friendlyError` 9 类常见错误中文化)。`md-export-docx.ts` 加 2 处 export 提升可测性(0 行为变化)。**揭露真实问题**:zh / zht 缺 en 上游 ~20 个非关键 key(转 backlog `i18n-history-drift-补全`)。测试增量 320 → 346(+26 全 pass);关键模块清单 `md-export-docx.ts` 覆盖率 0% → ~25% ⬆(Medium ~220 行 / 3 文件 / 0 R4 / 0 上游侵入)|
| [automation-test-governance](./automation-test-governance/) | done | 自动化测试治理规范固化(**只定纪律不启动开发**)— user 拍板 1A 2B 3A 4A 5B 后:① 新 feat 必含至少 1 个测试(Tiny 例外)② 关键模块覆盖率 ≥ 80%(初版 4 文件:markdown-editor-extensions / dialog-settings / file-tabs / md-export-docx)③ 70/20/10 金字塔比例 ④ 修 bug 必先写复现测试 + 同 commit + `[bug-repro: ...]` tag ⑤ Claude 自审起步,后期接 mutation testing。新建 `docs/governance/自动化测试规范.md`(~280 行),CLAUDE.md 加 R5 测试纪律 + v3 修订记录;需求池长期规划保留 backlog 等 user 决定第 1 期启动时机(Medium ~300 行 / 2 文件 / 0 R4 / 0 上游侵入)|
| [build-script-skip-baseline](./build-script-skip-baseline/) | done | Mac `build-deskfox.sh` 跳 Bun baseline runtime 下载 — 跟 Win `build-deskfox.ps1`(2026-05-02 commit `696bbcc00`)同等修法补 Mac 侧。原本调 `predev.ts` 走 `build --single --baseline`,Bun.compile 拉 ~190MB `bun-darwin-arm64-baseline` 运行时,2026-05-07 GitHub Actions macos-latest runner build 卡 57 分钟触发 1h 超时,`ship-mac-prod-2026.5.7.1` 0 release 产出。改成 `cd packages/opencode && bun run build --single`(无 `--baseline`,复用本机 bun runtime 零下载)+ `RUST_TARGET → BUILD_DIR_NAME` 映射 + cp 到 sidecar 路径(commit `8f22c4a27`);Tiny(1 笔 / +27 / -6 / 1 文件 / 0 R4 / 0 上游侵入)|
| [claude-md-iron-rules](./claude-md-iron-rules/) | done | CLAUDE.md 加 dev 分支三条铁律(强约束,2026-05-08 立)— ① 永不直接 dev 开发(都开 feat 分支)② 所有 → dev 合并必经 user 同意 ③ 所有 dev → 远端 push 必经 user 同意。每层把关给 user 一次刹车机会。例外:开 feat 分支 / feat 分支内 commit / feat 分支 push origin agent 可自主。加在"默认仓库约定(分支策略 v2)"段开头作为顶层规则;Tiny(纯 doc / 1 文件 / +8 行 / 0 R4)|
| [md-export-word-iter-2](./md-export-word-iter-2/) | done | **MD → Word 导出第二轮迭代 — 全面保真度提升**:HTML 标签全转 Word 元素 / GFM Alerts 5 类彩色独立块 / blockquote 同段视觉一体 / 远程图片走 Tauri reqwest + sniff 字节头 + SVG 转 PNG / 表格全边框 + header 灰底 / mermaid 居中 / **数学公式走 LaTeX→MathML(KaTeX)→OMML(mml2omml)→Word 原生公式可编辑** / 字号行距 Word default / 目录 Ctrl+点击跳转(heading bookmark + #anchor 转 internal hyperlink + _rels 改) / `==高亮==` 黄底 / GFM emoji shortcode `:rocket:` 等 80+。viewer 侧也加 `<mark>` / emoji / heading anchor / 嵌套 link 渲染。Sentinel 占位 + 后处理 docx XML 双夹策略,15+ helper 全有单测(72 → 147)。后端加 `fetch_url_base64` Tauri 命令,加 katex + mathml2omml 2 个 npm 包(净 +1MB)。已知 deferred:`\int_0^∞` 等 nary 转 OMML 后 `<m:e>` 留空,Word 显占位框(库 bug,3 次修补失败,Backspace 可删)+ save dialog 默认按钮在"否"(Tauri-plugin-dialog 限制),详见需求池(Large ~2600 行 / 6 文件 / 2 npm 依赖 / 0 R4 / 0 上游侵入)|
| [md-editing-iter-2](./md-editing-iter-2/) | done | **.md 编辑体验第二轮基础体验补齐** — iter-1(`md-editing-enhance`)交付 Tier B 全套后 user 反馈 3 项基础空白:① 长行不软换行(出水平滚动条)② 选区底色不明显(--surface-raised-stronger 跟当前行色阶接近,看不出选中没)③ 写到第几行不知道。fix:加 `EditorView.lineWrapping` + `drawSelection()` extension(行尾空白也绘连贯)+ index.css 选区色换 GitHub 蓝半透明 `rgba(56,139,253,0.35)`;新组件 `cm-status-bar.tsx` 显示行/列/选中字符数,`code-mirror-view` 加 `onCursorChange` prop + mount 初始回调,`file-tabs.tsx` 编辑态挂状态栏;i18n 三语补 `fileViewer.editor.statusBar.{line,col,sel}`;7 单测覆盖光标计算(空文档/单行/多行/中文/选区/跨行/反向)。AI 改写选区(原 spec 项 4)D4=B 拆出独立后续 feat。0 npm 依赖(drawSelection 来自既有 `@codemirror/view`),跨链路保证不影响 viewer 预览 + Word 导出(Tiny ~150 行 / 8 文件 / 0 R4 / 0 上游侵入)|
| [feishu-bridge](./feishu-bridge/) | done | DeskFox 飞书 / Lark 桥接 — system tray 常驻 + OAuth Device Flow 扫码绑定 + WSS 长连接收消息 + opencode plugin 跑 LLM → 回写飞书。X1 plugin 自带 server 架构(单 IM 简洁,N 个 IM 时再演进 channel registry);per-account model + bot 名后台刷新 + openId 脱敏 + 消息 ack reaction;37 笔 commit / 189 单测 / Phase 2 闭环 user 实测通过 |
| [macos-docx-viewer-fix](./macos-docx-viewer-fix/) | done | 修 Mac 端 .docx/.xlsx/.pptx 在文件查看器打不开伪 regression — 根因 `build-deskfox.sh` 只在 sidecar 不存在时 build,导致 packages/opencode/src 改动 3 周不进 sidecar binary;补上跟 Win 端 b9581b76e 同等的时间戳判断(Tiny,1 文件,0 R4,0 上游侵入)|
| [feishu-bridge-ship-packaging](./feishu-bridge-ship-packaging/) | done | 让飞书桥接真正进 installer — 之前 Mac/Win 都跑不了 fresh install(开发者手动改 user config 才能跑);bun build adapter-feishu-lark plugin → 单 3.4MB .js → tauri.conf.json bundle.resources 打进 .app/.exe → DeskFox setup hook 注入 user opencode 配置;双端 build script 同步;Medium,5 新文件 + 4 改动 / 0 R4 / 0 上游侵入 |
| [feishu-plugin-install-win-path](./feishu-plugin-install-win-path/) | done | 补完 `feishu-bridge-ship-packaging` Win 端实际仍跑不通的两个 bug — Win user config 路径选错(`dirs::config_dir()` 返 `%APPDATA%\Roaming\` 跟 sidecar 实读 `~/.config/` 不重叠;Mac 那笔 follow-up 基于"xdg-basedir 在 Win 用 APPDATA"的错误假设)+ plugin URL 反斜杠 + UNC `\\?\` 前缀(Node import / opencode plugin loader 不接受);抽 `to_file_url` helper + 4 单测;Win user 实测扫码绑定通(Tiny ~53 行 / 1 文件 / 0 R4 / 0 上游侵入) |
| [feishu-bridge-newuser-onboarding](./feishu-bridge-newuser-onboarding/) | done | 全新用户拿 .dmg 装完即用 happy path 加固 — A1 plugin 路径失效自愈(user 在 .dmg 挂载点双击后拖 Applications 不再卡死,5 unit test)+ A4 default model 缺失检测(前端 warning 卡片 + 后端 friendlyErrorReply 关键字识别 → 中文可操作指引,7 unit test)+ A3 .dmg 拖拽引导背景图(Swift CoreGraphics 脚本生成 660x400 PNG,显式 dmg layout);Medium,3 笔代码 + 1 笔 docs / 0 R4 / 0 上游侵入 |
| [feishu-bridge-empty-reply-ghost](./feishu-bridge-empty-reply-ghost/) | done | 修飞书桥接 5 条丢失 reply 两个独立 bug:① opencode agent loop 在工具调用 / 多步回复尾部追加 0-token 空 placeholder ghost,plugin 倒序找 last assistant 取到 ghost → 空 reply(占 4/5 case);② `promptTimeoutMs` 默认 5min 对长 agent 任务不够,7m18s 长回复触发硬超时 → plugin 返空(占 1/5 case)。修:抽 `findLastUsefulAssistant` helper 跳 ghost + timeout 5min → 30min;13 单测覆盖 7 场景;Medium ~25 行核心代码 / 1 测试文件 / 0 R4 / 0 上游侵入。Layer 2 message.updated 完成信号重写留独立 backlog |
| [feishu-installer-bundle-plugin](./feishu-installer-bundle-plugin/) | done | 补完 `feishu-bridge-ship-packaging` Win 端最后一个 ship-blocker — Inno Setup `.iss` `[Files]` 段没列 plugin/feishu-bridge/ 目录,prod installer 装完没 plugin/ → DeskFox 启动 resolve_plugin_dir 返 None → 飞书桥接 UI 显示"未启动"。加 2 行 Source 把 `plugin/feishu-bridge/{package.json,dist/plugin.js}` 打入 install dir;副带 bump 2026.5.10.1。教训沉淀:Tauri NSIS(默认/Mac)和 Inno Setup(本仓 Win 自定义)两套配置,资源声明要同步动(Tiny ~5 行 / 1 文件 / 0 R4 / 0 上游侵入)|
| [feishu-server-loopback-bind](./feishu-server-loopback-bind/) | done | 修飞书桥接 plugin server 默认绑 `0.0.0.0` 暴露 LAN 端口的安全 + UX 双 bug — `Bun.serve()` 没传 hostname 默认绑所有网卡,触发 Win Firewall 弹"Bun 是否允许公网访问"对话框 + 同 WiFi 任何人能扫到端口。1 行 fix 加 `hostname: "127.0.0.1"`(Tiny 5 行 / 1 文件 / 0 R4 / 0 上游侵入)|
| [network-bind-safety-guard](./network-bind-safety-guard/) | done | `feishu-server-loopback-bind` 教训沉淀 — CLAUDE.md 加 R6 网络监听安全规则 + `.husky/pre-commit` §4.5 闸,任何新增 `Bun.serve` / `*.listen()` 不显式 loopback 直接 commit block,例外走 `[network-bind-public]` tag override;测试文件豁免;实测假 `Bun.serve` 被 block + 安全版通过(Tiny 46 行 / 2 文件 / 0 R4 / 0 上游侵入)|
| [feishu-bridge-completion-signal-rewire](./feishu-bridge-completion-signal-rewire/) | reverted | Layer 2 重构尝试(dispatcher 切到 `message.updated` + `time.completed` 强信号)→ 实测发现根本误判 opencode 行为(一次 user prompt 产生**多条** assistant message,锁定 first 只发开场白)→ revert 回 Layer 1。沉淀教训 + Layer 2.1 设计方向。Medium 实施 ~270 行新 dispatcher + 17 单测 + 简化 pipeline,然后整体 revert(0 R4 / 0 上游侵入 / 净改动 0)|
| [feishu-bridge-system-prompt-disable-question](./feishu-bridge-system-prompt-disable-question/) | done | 修 LLM 调 `question` 工具反问用户后死锁 bug(飞书 user 看不到 GUI 对话框,agent loop 永远等)— promptAsync body 加 `system: <禁用 question 工具>` 让 LLM 不用反问类工具,信息不足直接答或写"请补充"。临时止血,真互动版(form 卡片 + synthetic message 注入)是 OpenClaw 对齐 roadmap #5,Large 后续做(Tiny 23 行 / 1 文件 / 0 R4 / 0 上游侵入)|
| [office-installer-mirror-cascade](./office-installer-mirror-cascade/) | done | 修 LibreOffice 自动安装"download failed: HTTP 403"用户机直接死的 bug — `pickFastestMirror` 只返 winner 一个,挂了无 fallback。改 `rankMirrors` 返完整排序列表 + `startInstall` 加 cascade 循环(任一 mirror 失败自动切下一个,5 个全失败才报错聚合);UI 切换瞬间显示"镜像 X 失败,改用 Y…"。User 反馈源自飞书桥接外另一台 Windows 机正式 installer 用户。Tiny 64/-32 / 1 文件 / R4 第 5 笔本季(超 3 笔配额,延续 office 引擎 fork-only override 链)/ 0 上游侵入 |
| [feishu-bridge-permission-card](./feishu-bridge-permission-card/) | done | 修飞书 user 任务被 opencode 权限 GUI 对话框卡死的死锁 — 拦截 `permission.asked` Bus event,渲染飞书 CardKit 交互卡片,user 在飞书显式选[允许一次/始终允许/拒绝],plugin 调 `client.postSessionIdPermissionsPermissionId` SDK 回写 opencode 解锁。**保持 user 显式批准 trust 边界**,非 auto-allow。点击后 delete+send 替换为已确认状态卡片(header 绿/红/灰)。findLastUsefulAssistant 加 parentID 约束防 reject 后回放前轮答案。Medium 7 文件 / +1198 行 / 51 单测 / 5 笔 commit(踩 4 坑陆续修)/ 0 R4 / 0 上游侵入 |
| [pack-installer-rebuild-step](./pack-installer-rebuild-step/) | done | 修 `pack-installer.ps1` 顺序错位 SOP bug — bump 跟 ISCC 之间没 rebuild exe,导致 installer 文件名(`.11.1`)跟 exe UI 显示版本号(`.10.1`)mismatch(`installer-versions.json` 经 vite **烧进 bundle**,bump 后不重 build 不传导)。加 step 1.5 自动调 build-deskfox.ps1 + 新 `-SkipBuild` flag(CI 场景手动跳)。Tiny ~25 行 / 1 文件 / 0 R4 / 0 上游侵入 |
| [win-ship-local-pack-switch](./win-ship-local-pack-switch/) | done | 停掉所有 GitHub Actions 自动出安装包 + Gitee 镜像 workflow(Win build / Mac build / Gitee mirror 三条),改"本地打包 + 手动上传 GitHub Release + API 创 Gitee release + 跑 mirror-asset-to-gitee.ps1"。决策起源 ship 2026.5.11.1 时 Win build 撞 sidecar build deadlock(50min stdout 静默 + 60min job timeout cancel)+ 顺手发现 Gitee mirror 长期 silent fail。Mac 端 user 拍板一起停(维持 CI 需持续投精力)。复活方式注释段内置(Tiny 85/-32 / 3 文件 / 0 R4 / 0 上游侵入)|
| [feishu-bridge-imbot-agent](./feishu-bridge-imbot-agent/) | done | 飞书桥接默认 agent `build` → `imbot` — 收紧 unattended 远程触发场景危险工具默认权限。新加 `imbot` agent(setup hook 自动注入到 user opencode.jsonc,idempotent)同 build 能力同 system prompt 但 bash/edit/write/apply_patch/webfetch + 敏感目录 read 默认 ask;saveAccount 新绑账号默认走 imbot,主 GUI 仍走 build 0 影响。安全闭环:LLM 经 webfetch 拉到 prompt injection 网页被诱导用 bash exfil 时,user 在飞书看到权限卡能即时拒绝。Medium ~120 行 / 5 文件 / 7 单测 / 0 R4 / 0 上游侵入(per-agent 走上游 schema 原生支持)|
| [mac-pack-installer-and-gitee-rename](./mac-pack-installer-and-gitee-rename/) | done | 修补 [macOS] 2026.5.11.1 .dmg 文件名(`DeskFox_1.14.33_aarch64.dmg` → `DeskFox-2026.5.11.1_aarch64.dmg`,对齐 Win 命名)+ Gitee 镜像仓 2026-05-11 改名 `opencode-for-office-deskfox` → `deskfox`(对齐 GitHub)。GitHub release asset 重传 + Gitee 老仓 release 删除 + 新仓重建。`pack-installer.sh` 已存在(rename 逻辑早就有,5.11.1 误绕过)— 本笔文档化为 Mac ship 唯一入口 SOP。legal docs(zh/en)+ memory(reference_push_remotes + MEMORY.md)Gitee URL 同步改名,历史 changelog 保留不改 |
| [win-ship-imbot-5.11.2](./win-ship-imbot-5.11.2/) | **撤回** | Win 端补 ship 5.11.2 把 `feishu-bridge-imbot-agent` 安全 agent 推给 Win 用户 — **装出来撞 castError 启动崩,GitHub draft + Gitee release 已删,被 5.11.4 替代**。原意:5.11.1 ship 时序(10:24)撞 imbot merge(13:14)之前,Mac 5.11.1(13:53)含 imbot 而 Win 5.11.1 不含,**安全 regression(相对 Mac)**。崩因:vite chunking 非确定性,详见 5.11.4 changelog |
| [win-ship-prod-5.11.4](./win-ship-prod-5.11.4/) | done | 5.11.1/2/3 三次 ship 全撞 **vite chunking 非确定性 bug**(同源码不同时刻 build 行为不同,启动期 `castError({})` 撞死)。**5.11.4 workaround** = 用 known-working `target/release/DeskFox.exe`(b268ce694 dev tip 那次 build 实测 OK)直接 ISCC 重打 installer,**不重新 vite build** 避免再撞非确定性。接受 mismatch:Windows 控制面板 + installer 文件名 = 5.11.4 / DeskFox UI 左下角 = v2026.5.11.2(bundle 没动)。Tiny ship chore,根因未真修留 backlog(vite manualChunks 显式分块 / SDK falsy-error fallback 改 Error 而非 {}) |
| [imbot-permission-pragmatic](./imbot-permission-pragmatic/) | done | imbot v1 → v2 务实档 — v1 把 bash/edit/write/apply_patch/webfetch 全 ask 实测 "寸步难行"(opencode bash always 按 arity prefix 颗粒度,user 点 5+ 次 always 仍要点)。v2 重设计:webfetch 全 ask + read 敏感目录 ask 不变(prompt injection 攻击瓶颈),bash 默认 allow + 30+ 删除/不可逆 pattern ask(rm/git --force/git reset --hard/*delete*/*uninstall*/docker rm/aws s3 rm/shutdown/dd/mkfs 等),edit/write/apply_patch 不设走 build defaults(bash echo >> file 等价能力)。permission-card timeout 5→30min 对齐 promptTimeoutMs。攻击面防御等价 v1(攻击瓶颈在 read 不在 bash),user 体验大幅改善。cargo 16/16 + adapter feishu 262/262 + typecheck 16/16。user 升级 v1→v2:删 ~/.config/opencode/opencode.jsonc 的 .agent.imbot 块,重启 DeskFox。Medium ~30 行 spec + 2 单测 / 2 核心文件 / 0 R4 / 0 上游侵入(per-pattern bash 走上游 Wildcard.match 原生)|
| [wss-text-dedup](./wss-text-dedup/) | done | wss-client.ts 加同 text 短期 dedup(同 chatId + 同 trimmedText 10s 内重复 skip)— 防飞书 IM 连击/retry 推**不同 message_id 但内容一致**的多条消息触发 LLM 重复执行 + 弹多张权限卡。复用现有 DedupCache(ttl=10s,maxEntries=200),仅 text 类型生效。imbot v2 实测中 user 发看 /Users/openclaw/.ssh/known_hosts一次但 sidecar 收 2 条独立 message(messageId 不同 SDK dedup 拦不住)导致弹 2 张卡,本笔解决。Tiny,1 文件 +27/-1 行 / 0 新单测(复用 DedupCache 19 测试)/ 0 R4 / 0 上游侵入 |
| [sdk-falsy-error-fallback-fix](./sdk-falsy-error-fallback-fix/) | done | 兜底 SDK `client.gen.ts:102/220` 的 `finalError \|\| ({} as unknown)` falsy fallback 抛空 `{}` → SolidJS `castError` 转 "Unknown error" 空错误页的真凶 — 5.11.x ship 翻车真因 surface fix。在 fork-only `createSdkForServer`(`packages/app/src/utils/server.ts`)的 fetch 边界包一层把 falsy reject 转有效 Error,SDK 看到的 error 永远 truthy → 那个 fallback 永远不触发。同时 console.error 留 `[FETCH-FALSY-REJECTION]` URL + originalError 证据链。**不直接改 SDK**(在 pre-commit 黑名单 + auto-generated 文件下次 regen 被覆盖)。Tiny,1 文件 +44/-3 行,R2 FORK marker / 0 R4 / 0 上游侵入。根因(vite chunking 非确定性 / fetch 为何抛 falsy)未真修,留 backlog 等下次撞时 console 抓到具体证据再查 |
| [dedup-cache-persist](./dedup-cache-persist/) | done | DedupCache 加 persistPath 持久化(JSON 落盘 + 100ms debounce flush + 原子 rename + corrupt 不 crash)— sidecar 重启后 load 上次 mark 过的 message_id,防飞书 WSS 重连后 server 重推老 message 时 in-memory cache 失忆把老 message 当新 message 重复 process。实测起源:user 飞书发 2 条"你是谁" 但 sidecar 收到一条 user 看不到的"看 ~/.ssh/known_hosts"触发 LLM + 弹卡。P5 verify(user 飞书 IM 端没显示这条 user message)确认服务端重推。wss-client 第一层 dedup 配 ~/.opencode/feishu-wss-dedup.json;textDedup 不持久化(10s TTL 落盘无意义)。dedup 测试 19 → 30(+11);adapter-feishu-lark 278 → 289;typecheck 16/16。P2-P4(plugin 多 instance)留 backlog。Medium ~218 行 / 3 文件 / 11 新单测 / 0 R4 / 0 上游侵入 |
| [feishu-plugin-dedup-decision](./feishu-plugin-dedup-decision/) | done | 把"opencode.jsonc 累积多个 feishu-bridge plugin entry → multi-instance → 双推 user message"的根因诊断 + **不做产品层防御**的决策**显式写下**。`imbot-permission-pragmatic v2` + `wss-text-dedup` + `dedup-cache-persist` 后 user 仍撞双推,深查定位:开发机三档来回切换累积 4 个 feishu-bridge entry(空格/raw target/prod/`%20`),opencode loader 并发 import → 3 个独立 plugin instance(3 plugin server port + 3 WSSClient 同 appId connect 飞书),server 给不同 connection 推同 user message 分配不同 message_id → SDK dedup 拦不住 → bot 弹两张卡。**决策不做产品层防御**(普通用户单装永不撞 + L1/L2/L3 三层修法都 over-engineering)。改动:① `feishu_plugin_install.rs` 头加 15 行决策注释列出 L1/L2/L3 候选方案防未来 agent 重复实施 ② `build-deskfox.{sh,ps1}` 加 post-build 清开发机 jsonc 多余 entry(下次 DeskFox 启动 setup hook 自动 inject 当前 .app)③ 本三文档。前一个 verdict 误判"飞书 server 行为,接受现状"被 user 拒绝事后拦截哲学,深查后才定位真凶。Tiny,3 代码文件 +55/-0 + 3 docs / 0 R4 / 0 上游侵入 / 测试豁免(Tiny + bash/ps1 函数 + Rust 注释 = 0 业务逻辑改动)|
| [build-script-json-fallback](./build-script-json-fallback/) | done | `feishu-plugin-dedup-decision` 立的 post-build jsonc 清理只查 `.jsonc`,user 用 `.json`(无 c)的开发者享受不到 — 补 fallback,Win + Mac 双端 build-deskfox 脚本对称跑两种文件名 cleanup;**只影响开发者**(普通用户不跑 build-deskfox 脚本,setup hook 逻辑不动);Tiny / ps1 + sh 各 +6 行 / 单 changelog / 0 R4 / 0 上游侵入 / 测试豁免(对齐 dedup-decision 决策)|
| [mac-ship-prod-5.12.1](./mac-ship-prod-5.12.1/) | done | Mac 端跟 Win 5.12.1 同步出 prod .dmg + 双平台分发(GitHub + Gitee,**反转 2026-05-06 立的"Mac 不跑 Gitee"memory**)。包内容完全等于 Win 5.12.1。Build:bump macos 5.11.1→5.12.1 + `pack-installer.sh --env prod --no-bump` + 手动 rename(--no-bump 时 NEW_VERSION 空跳过 rename — 需求池后续修)+ cp 兜底 .app/MacOS/DeskFox(shasum 验证一致)。产物 `DeskFox-2026.5.12.1_aarch64.dmg` 64.6MB。Tag `ship-mac-prod-2026.5.12.1`(加 mac 前缀避免跟 Win `ship-prod-2026.5.12.1` 撞)。需求池新加 `prod-首次启动-sidecar-idle.md` + `imbot-agent-自动升级.md` + pack-installer-no-bump-rename。Tiny ship chore / 0 R4 / 0 上游侵入 / 测试豁免 |
| [sdk-falsy-empty-body-fix](./sdk-falsy-empty-body-fix/) | done | 补 [sdk-falsy-error-fallback-fix](./sdk-falsy-error-fallback-fix/)(2026-05-12 surface fix)未盖的路径 ② — fetch 成功但 response 4xx/5xx + body 空字符串时 SDK `client.gen.ts:220` 触发 `finalError \|\| ({} as string)` fallback,throw `{}` → SolidJS castError → "出了点问题 / 原因: {}"。5.21.1-dev Mac 首次 Tier 2 ship 翻车真凶。修法:`wrapFetchWithFalsyGuard`(extract export 函数为单测铺路)在 baseFetch 成功后加 layer 2 检查,`!response.ok` + `clone().text() === ""` 时主动抛 Error 截断。顺手修 5-12 layer 1 的 `Object.keys` 错判 Error 为 empty 的 latent bug。Medium-,3 文件 / ~155 行 / 13 单测全过 / 0 R4 / 0 上游侵入 |
| [frontend-stale-session-fallback](./frontend-stale-session-fallback/) | done | 接力 [sdk-falsy-empty-body-fix](./sdk-falsy-empty-body-fix/) 把 5.21.x ship 翻车修到产品级闭环 — `directory-layout.tsx` 启动 createResource 调 sync.session.sync(id) 时如果 sidecar 401/404(stale session id 来自累积的 workspace state),navigate 去掉 stale id 降级到 directory 根("未选择项目" + 最近项目列表),不让 ErrorBoundary 兜底。修法只在启动恢复路径兜底,不动 sync.session.sync 内部(其他 caller 期望错误 surface)。新 helper `isStaleSessionError`(`packages/app/src/utils/stale-session-error.ts`)识别 wrapFetchWithFalsyGuard 抛的 401/404 格式,5xx/403/network/AbortError 不视为 stale。Tiny+,3 新 1 改 文件 / ~115 净行 / 10 单测全过 / 0 R4 / 0 上游侵入 |
| [feishu-pipeline-401-fix](./feishu-pipeline-401-fix/) | done | 修飞书桥接 reply 全静默 bug — 用户视角:发飞书消息没回复(实测 3 条全 empty reply)。**Mac 端复现 / Win 端不复现**的真因:sidecar build 时 `git branch --show-current` fallback baked CHANNEL=`dev`(2026-05-21 改名前主分支名)→ 命中 `HTTPAPI_DEFAULT_ON_CHANNELS` → 走上游不成熟 effect-httpapi 路径,撞**两个 sub-bug**:① Authorization 多 security `{basic, authToken}` 实现成 AND 而非 OR → SDK 只带 basic 就 401 ② `StepFinishPart.reason` schema 必填但 writer 写入 undefined → 编码 400。修法走方案 A:build script 锁 `OPENCODE_CHANNEL=prod` 让 sidecar 走 Hono legacy stack(`c.json` 直出不做 Schema encode + basic auth 单 mechanism 内置 OR fallback)双 bug 全规避,**0 上游 override**;副作用 = DB 文件改名(`opencode-dev.db` → `opencode.db`),配 `lib.rs:initialize()` 头部加 `migrate_pre_prod_db()` 一次性幂等 hook(cp `.db`/`.db-wal`/`.db-shm` 3 文件,源保留作回退)。Medium ~157 行 / 3 文件 / 6 unit + curl e2e / 0 R4 / 0 上游侵入 |
| [mac-ship-dev-2026.5.21.1-dev](./mac-ship-dev-2026.5.21.1-dev/) | done | **Mac 端首次 Tier 2 预览版**(对应 Win 端 `win-ship-dev-2026.5.21.1-dev` 同期发车)— bump commit `b6916bd90`(2026-05-22,installer-versions.json macos `5.12.1` → `5.21.1-dev`)直到 2026-05-23 才补完对外发布(因 bump 后撞 `sdk-falsy-empty-body-fix` + `frontend-stale-session-fallback` 翻车修复 + `feishu-pipeline-401-fix` 核心修复决议一起 ship)。**ship tag on HEAD `f1bd32503`**(含 post-bump feishu fix,user 实测过的 build),走完整 4 步 SOP:`gh release create --prerelease` GitHub Release(prerelease)+ `curl POST /releases` Gitee API 创建 release id=689661 + `mirror-asset-to-gitee.sh` 上传 .dmg(82 Mbps / 6s)。产物 `DeskFox-Dev-2026.5.21.1_aarch64.dmg` 64.5MB / Bundle ID `ai.deskfox.app.dev`。Tiny ship chore / 0 R4 / 0 上游侵入 / 测试豁免(用户三轮验证 stale state + 干净启动 + 二次幂等 + 飞书 reply 回归全过)|
| [feishu-bridge-light](./feishu-bridge-light/) | done | 飞书桥接增强三件套(2026-05-23 落地,opt-in 自动建群默认关)— **(1) `/new` 私聊清话题**(群聊禁用避免共享 chatId 误清全员;stripMentions 处理 @bot 形态;清三处:disk + chatToSession + sessionToChat)**(2) `[ATTACH:path]` 文件回传**(AI 在 reply 嵌 marker → 路径白名单 `~/.opencode/feishu-workspace/` 子树内防 LLM 把 `/etc/passwd` 发出去;按扩展名分流 `image.create`(10MB)/ `file.create`(30MB,file_type 受限枚举,docx 等走 stream 兜底);单 marker 失败不阻断其它,warning append 不漏 marker)**(3) `[CREATE_GROUP:name]` 自动建群**(opt-in `enableAutoGroupCreate` 默认 false + permission-card 二次确认双门控防 prompt injection;仅私聊触发;`chat.create + user_id_list` 拉人 + `chat.link` 拿 share_link 团队群失败降级 chat_id;system prompt 按 opt-in 动态拼避免"哑回复")。**架构决策**:confirm-card.ts 独立成模块而非内嵌 permission-card(语义/类型分离 + 复用 controller 架构模式);system prompt 改成 `getSystemPrompt()` 动态拼。**v1 草稿 15 处假设错配修订**:chatSessionStore.delete 已存在不需新增 / 图片走 image.create 非 file.create / chat.create({data,params}) 真实签名 / chat.link 单独调拿邀请链接 / user_id_list 拉人 / opt-in + confirm 双门控 / FEISHU_SESSION_SYSTEM_PROMPT 扩段教 LLM marker 等。Medium+,~1800 行 / 7 新 5 改 / 103 新单测 / 391 pass 0 fail / 0 R4 / 0 上游侵入(全 fork-only adapter-feishu-lark 包 + plugin.ts routing 扩)|
| [feishu-create-group-toggle-gui](./feishu-create-group-toggle-gui/) | done | 把 feishu-bridge-light 的 `enableAutoGroupCreate` opt-in flag 从 JSON 配置文件升级为 GUI 开关(2026-05-24)— **触发**:user 实测 2026-05-24 暴露 3 个问题 ① user 必须手编 `~/.opencode/feishu-config.json` 才能启用 ② LLM 通过 imbot edit 默认 allow 默改 JSON 用户不知道 ③ sidecar 无文件 watcher 改完不 hot-reload。**方案**:"编辑账号模型" dialog 升级为"编辑账号设置",加"模型 / 高级能力"两个分隔块,高级段含 "允许 AI 自动创建新群" checkbox + 副标说明。**API 设计**(Option A 拍板,user 选):扩 `updateAccountModel` → `updateAccountSettings` partial update,单 endpoint 服务多个 settings,未来加新 flag 0 endpoint 改动;旧 endpoint + Tauri 命令 + 前端 helper 保留 thin wrapper 向后兼容。**全链路改动**:account-store.ts 新加 `updateAccountSettings(id, patch)` 严格白名单 partial(其他 schema 字段 0 影响)+ server `POST /accounts/update-settings` endpoint 加 4 道闸校验(missing accountId / unknown_fields / empty_patch / invalid_field 类型)+ Tauri 命令 `feishu_update_account_settings` 用 `Option<Option<ModelRef>>` 表 model 三态(`undefined`不动 / `null`清 / `obj`设)+ 前端 `feishuUpdateAccountSettings` + dialog 加 checkbox + i18n 4 keys × 3 locale + `AccountSummary` 全链路加 `enable_auto_group_create` 字段。**hot-reload 走已有 `onAccountsChanged` 路径**,sidecar 重建 MessagePipeline,改完立即生效无需重启。Medium ~290 行 / 13 文件 / 10 新单测(account-store partial 7 + 兼容 3)/ 401 pass 0 fail / 16/16 typecheck / 0 R4 / 0 上游侵入(全 fork-only 文件 + Tauri 命令注册)。**测试取舍**:server endpoint HTTP 集成测试撤掉,因 Bun 缓存 `os.homedir()` 首次值,HOME env 改了不生效 → 测试会污染 user 真实 `~/.opencode/`(实施期间已发生过一次,立即清理 + 文档化教训);account-store 10 单测远超 R5 Medium ≥ 3 unit 达标 |
| [feishu-create-group-hard-block](./feishu-create-group-hard-block/) | done | Pipeline 层关键字硬拦截 — 接力 `feishu-create-group-toggle-gui` 修 provider-agnostic 漏洞(2026-05-24)。**触发**:user 实测 claude-code provider 下 soft constraint system prompt 完全不生效(plugin 跳过 `role=system` 消息,LLM 翻 fork 源码撞 imbot read permission 卡)。**修法**:`message-pipeline.ts handle()` 在 `/new` 早退之后加 3 道门控硬拦截 — `enableAutoGroupCreate=false` + `chatType==="p2p"` + `isGroupCreationIntent(cleaned)` 全命中则不调 LLM,直接 sendFeishuText GUI 引导(文案跟 system prompt soft constraint 一致 UX 统一)。**关键字识别**:中文走 regex `/(?:开\|建\|创建\|新建\|拉\|搞\|做)[^群]{0,20}群/`(动词 + 字符 + 群,精准识别"建一个项目讨论群"等真实表达;正确避开"群是怎么建的"动词在群后);英文走 lowercase substring(`create group` / `new group` / `make group` / `set up a group` 等 8 keyword)。helper extract 模式 `isGroupCreationIntent(text)` 纯函数独立可单测。Medium- ~190 行 / 4 文件 / 14 新 helper 单测 + 6 集成测 / 429 pass 0 fail / 16/16 typecheck / 0 R4 / 0 上游侵入(全 fork-only adapter-feishu-lark)。**3 道门控顺序保证不误拦**:flag=true 跳过让 LLM 走 marker / 群聊跳过避免误拦"群是怎么建的"学术问题 / 关键字命中才拦。**误拦权衡** documented:user 主动关 flag 时少量误拦(如"如何创建一个群")成本低,远好于漏拦让 LLM 撞权限卡 |
| [feishu-group-mention-policy](./feishu-group-mention-policy/) | done | 激活 dead 的 `requireMention` 配置 + GUI 暴露 + 顺手把 chat.create 默认 `chat_type` `"public"` → `"private"`(2026-05-24,主题"飞书桥接 privacy 默认值整治")。**触发**:user 研发场景"同 bot 多项目并行,每个项目一个群当 session 用,希望群里免 @"。**修法**:pipeline `handle()` 加 3 条件早退(`!p2p` + `requireMention=true` + bot 没被 @)→ 跳过 LLM;GUI dialog 高级能力分隔块加 checkbox "群里需要 @ 后再响应"(默认开,hint 含**前置条件警示**:user 须先在飞书开放平台改订阅模式为"全量群消息")。**Secondary deliverable**:`group-creator.ts` `chat_type` 改 `"private"` — DeskFox 创建群默认私密,企业内部成员搜不到,通过 share_link 邀请进。**复用** updateAccountSettings partial endpoint(自然扩 requireMention 字段)+ AccountSummary 全链路加 `require_mention`。Medium- ~220 行 / 11 文件 / 16 新单测(helper 7 + 集成 6 + account-store 3)/ 481 pass 0 fail / 16/16 typecheck / 0 R4 / 0 上游侵入(全 fork-only)。**前置条件**:user 必须先在飞书开放平台改订阅模式 + 老群可能需要 bot 退出重入 — 改 checkbox 单独不会自动生效 |
| [feishu-llm-strip-mention-placeholders](./feishu-llm-strip-mention-placeholders/) | done | 修自 `feishu-bridge-light` 起的潜在 bug — `message-pipeline.handle()` 传 raw text 进 LLM,**@bot 飞书占位符 `@_user_N` 没 strip** → LLM 误当另一个联系人,reply 含"我不是 @_user_1"类幻觉(2026-05-24 user 实测群 + p2p 各 1 case 复现)。**根因**:`cleaned = stripMentions(text, mentions)` 已算,`/new` 检测和 CREATE_GROUP intent 都用 cleaned,**但 `runOpencode(sessionID, text, ...)` 漏改 cleaned**。**修法**:1 行 `text` → `cleaned`(`message-pipeline.ts:483`)。Tiny ~4 行代码 + 2 bug-repro 单测 / 485 pass 0 fail / 16/16 typecheck / 0 R4 / 0 上游侵入。[bug-repro: LLM 收 raw text 含 @_user_N 占位符 — 应传 cleaned] |
| [feishu-attach-upload-robustness](./feishu-attach-upload-robustness/) | done | 修飞书 SDK 上传 socket disconnect 100% 失败(2026-05-24,user 实测 4.7KB notes.md 都断,关 Clash 仍发生)。**根因**:Bun runtime fetch 处理 Node `createReadStream` + multipart 编码不兼容 → P0 三件套合一 feat:① stream → readFileSync **Buffer**(绕兼容)② **retryUpload(3 次,指数退避 1s/3s)**仅对可恢复错(socket/ECONNRESET/EPIPE/timeout/5xx)重试,业务错误立即失败 ③ **withTimeout 30s 显式**(Promise.race 实现,Lark SDK 不接 AbortSignal)。3 个 helper 全 export 可独立单测(helper extract 模式)。Medium- ~200 行代码 + ~250 行 27 新单测 / 512 pass 0 fail / 16/16 typecheck / 0 R4 / 0 上游侵入 / fork-only file-uploader.ts。[bug-repro: socket disconnect on Bun fetch + Node createReadStream multipart] |
| [聊天选区-卡片化-换行](./聊天选区-卡片化-换行/) | done | **office-选中加聊天 后续 v1.5**(2026-05-25 单日 ship)— 解决 v1 ship 后两个遗留:① chat 区选区仍走老 markdown blockquote 塞 textarea(不一致)② 卡片溢出横向滚 user 看不到下一张。**方案 B `kind` 字段贯穿**:`FileContextItem.kind?: "chat" \| "file"` 数据维度统一,卡片 UI 按 kind 分流(chat 显 bubble icon + "聊天引用",file 仍 FileIcon + 文件名),`formatCommentNote` LLM 模板按 kind 分流(chat 走 "user is quoting earlier in this conversation" 让 LLM 明确引文来自同一对话历史继承上下文,file 走原 "regarding this file of {path}")。**Step 2 先 1 后**(user 拍板):纯 CSS `flex-wrap + max-h-[180px]` 先 ship → chat 卡片化大改稳扎稳打。3 commits / 12 文件 / +179 / -32 / 6 新单测(formatCommentNote 4 分支 + metadata round-trip + 非法 kind 兜底)/ 87 单测全过 / 0 R4 / 0 上游侵入。经验 L1-L3:Medium 起手建三文档 / kind 字段贯穿优于双 union 类型(字段共享率 90%+ 时)/ UI 与 LLM 模板同 kind 驱动避免分流逻辑分散 |
| [office-选中加聊天](./office-选中加聊天/) | done(v1)| **统一选区菜单架构** — 借 office 选中加聊天用户痛点,把"选中加聊天"这件事在多格式上做漂亮(2026-05-24)。**触发**:需求池 `DeskFox.Ai 需求池.md:31` 入池久未推进,user 远程开发模式拉出来重评估。**架构方案 B**(调研后采纳):`ContextMenuHost`(Solid 组件,根挂一次,document-level capture 监听 contextmenu)+ 可插拔 `SelectionProvider`(first-match-wins 路由),菜单 UI / 引用拼接 / focus / toast 永远只有一份代码;Provider 之间 IO 隔离不连坐。**v1 范围 A 方案**:chat + PDF/office 两处(MD viewer 留 v2 — `mdMenu` 有 编辑/导出 Word 等 MD-specific 动作,v1 只 2 个 use case 设计 `getMenuItems()` 接口形状容易拍偏,等 v2 跟 CodeMirror 一起做时 4 个真 use case 在手再扩接口才对,"三相似行优于过早抽象")。**Step 2 wrapper 替代**:原 plan 改 packages/ui pdf.tsx 加 data-slot 被 R4 黑名单拦,改在 file-tabs.tsx renderDefault 外层 conditional wrap `<div data-slot="pdf-viewer" class="contents">`,等价效果 0 R4 override 消耗。**跨页选区 v1 toast → inline hint**:菜单内联文字 + 按钮 title tooltip,比 toast 不打扰。**顶栏"用本机软件打开"按钮常驻**:soffice 转 PDF 图表/公式/艺术字光栅化选不到时永久兜底,UX 不让 user 反复掉坑。**透明迁移**:chat-selection-menu.tsx 258 → 30 薄壳,message-timeline.tsx 调用不动,行为 1:1 复刻。**1-spec 顺带固化"office WYSIWYG 编辑不重启" 决议**:本 feat 在产品语义上吃掉"office 编辑"需求 — 选中 → 加聊天 → AI 用 python-docx/openpyxl/python-pptx 改原文件 → 重走 office→PDF 管线预览,只是入口形态从 WYSIWYG 变成 AI agent。Medium ~600 行主交付 + Follow-up ~250 行 / 24 commits(主 9 + 第 1 轮 QA 9 + 第 2 轮 QA #1-#4 共 6)/ 72 新单测 / Logic 清单 ≥ 80% 达标 / **R4 2 笔满配额**(TextLayerBuilder + `--total-scale-factor`,packages/ui/pdf.tsx 当季锁死)/ 0 产品代码上游侵入(全 fork-only 新文件)/ View 清单 e2e 留 backlog(Phase ③ ready 后补)。**第 2 轮 QA #1-#4 关键修法**:`--total-scale-factor` 修行末漏字 + pointerdown snapshot 修行间右键 collapse + anchor/focus 修拖几行选中整页 + `commentOrigin: "quote"` 改卡片不附文件二进制(B 方案,LLM token 干净)。经验沉淀 T1-T7(R4 改前读完整 side effect 链 / DOM 顺序 ≠ 视觉顺序 / 事件链 default 时机 / 复用 UI vs 复用数据通路 / 引用 vs 全文 / CDP 验数据非验视觉 / 调研撞墙换层面)见 3-changelog § 经验沉淀 |
| [feishu-plugin-bundle-fs2](./feishu-plugin-bundle-fs2/) | done | 飞书插件 "failed to load plugin (fs.existsSync undefined)" 修复(2026-05-27,mac ship 验证发现的回归)。**根因**:opencode plugin loader `getLegacyPlugins` 遍历插件模块所有 export 当 plugin server 调 `fn(input,options)`;plugin.ts 为单测 export 了 `migrateLegacyWorkspace`/`applyStaleSessionsCleanup` 两个裸 helper → 被错参调 → 第 3 参 fs=undefined → 抛 4× "failed to load plugin"(feishu 功能其实正常,但 opencode 视插件加载失败)。imbot-workspace-rename 新加 export=回归点;media-gen 不 export 裸 helper 故同 sidecar 干净。曾误判打包 `--target`(red herring)。**修法**:helper 挪 `workspace-migrate.ts`,plugin.ts import 不 re-export → 入口只 export 真插件 + 守卫单测。真机 4→0、飞书 622 pass / typecheck 17/17。⚠️ Win prod `2026.5.27.1` 大概率同款需重发 | Medium,排查路径有复用价值 |
| [req-031-tray-icon-relaunch-show](./req-031-tray-icon-relaunch-show/) | done | REQ-031 关闭到托盘后点桌面/Dock/程序列表图标重开窗口(2026-05-28)— hide() 的窗口只 `set_focus + unminimize` 无效,必须先 `show()`。修法:`system_tray::show_main_window_impl` 改 pub,lib.rs single-instance 回调复用之,与 tray 菜单"打开 DeskFox"对齐。Tiny ~8 行 / 2 桌面 rs / cargo check 通过 / 0 R4 / 0 上游 TS / 真机 Win+Mac 抽查由 user 验。[bug-repro: 托盘常驻态下点图标重开窗口无反应] |
| [req-032-menu-clamp-viewport](./req-032-menu-clamp-viewport/) | done | REQ-032 选区菜单贴边沿被遮挡(2026-05-28)— 两处手写选区菜单(file-tabs.tsx mdMenu + host.tsx ContextMenuHost)无视口保护,user 实报 md 文件结尾右键按钮被裁。共享 helper `utils/menu-position.ts`(clampMenuToViewport 纯函数 + repositionMenu glue),两处接入 createEffect + queueMicrotask + ref + visibility:hidden 防闪。+9 helper 单测覆盖四边沿 + 角部 + flip + 自定义 margin + 极端值。Small ~190 行 / 4 文件 / typecheck 17/17 / app 738 pass / 0 R4。需求池审计:其余右键菜单全 Kobalte/相对定位 不受影响。长期 office v2 迁 Kobalte 后本 helper 可删。[bug-repro: 选区菜单贴边沿溢出视口] |
| [ui-toast-width-filetree-toggle](./ui-toast-width-filetree-toggle/) | done | 两个小 UI 修复(2026-06-04)— ① toast 弹窗宽度收窄 1/5(上游 400→320px,走 index.css override 不改上游)② 文件树点击 toggle:再次点击「正在查看 + 面板已开」的文件 → 收起整个查看面板(user 拍板语义)。`createOpenSessionFileTab` 加 3 个可选参 activeFileTab/isViewerOpen/closeViewer + toggle 守卫(pathFromTab 放最后保旧测试不变)。Tiny~Small ~25 行码 + 55 行测 / helpers 14 pass / typecheck 17/17 / 0 R4。真桌面响应性+视觉由 user 验。 |
| [stuck-working-status-reconcile](./stuck-working-status-reconcile/) | done | 主视图永久卡「思考中」+ 停止按钮关不掉修复(2026-06-12,Small)— user 报装新版 2026.7.1 后又卡死。**与 `stuck-working-indicator-fix` 不同路径**:那次修消息缺 `time.completed`(侧边栏旋转图标);这次卡**后端 `session_status` 停 busy 没回 idle**(主视图思考中)。**根因**:前端发消息乐观设 busy,回 idle 全靠后端 `session.status idle` 事件;进程被硬杀 / 中途断流 / sidecar 看门狗重启时事件不来,且 `session_status` 后端纯内存重启即清空不补发;前端唯一对账点 `bootstrap.ts:279`(重连/重 bootstrap 会走)用裸 `setStore`(merge)清不掉残留 busy key(对比隔壁 config 用了 reconcile)。停止按钮 `halt` 又 `.catch(()=>{})` 纯吞错 →「关不掉」。**修法**(全前端 fork-only):新增纯函数 `reconcileSessionStatus`(算权威表 + 列残留),`bootstrap.ts` 对账改 `reconcile(next, { merge: false })` 整体替换清残留 + `halt` abort 失败弹真实 toast 不再吞。装新版后重连/重启即自愈存量卡死。6 新单测(含真 solid store 根因守门) + app 845 pass + typecheck 16/16 / 上游改 2 文件均加 FORK marker。[bug-repro: 进程死/事件丢后前端 session_status 永不回 idle 致主视图永久思考中] | Small,3 文件(1 新 helper+test / bootstrap 1 处 / session.tsx halt)~80 行 / 0 R4 / 0 黑名单 / 回退 git revert |
| [stuck-working-indicator-fix](./stuck-working-indicator-fix/) | done | 会话列表"运行中"旋转图标永久卡死修复(2026-06-06,Medium)— user 报 DeskFox 任务点停止无效 + 杀重开仍转。**根因**:进程被硬杀(SIGKILL/崩溃/休眠)时正在生成的 assistant 消息漏盖 `time.completed`(`processor.ts:641` 的 `Effect.ensuring(cleanup)` finalizer 来不及跑),留下永久"未完成"残骸消息;前端 `isWorking()` 用 `findLast(任意缺 completed 的 assistant)` 判运行中 → 扫到埋历史里的残骸 → 永久转、杀重开仍在(残骸持久化 DB)、点停止无效(后端无活跃任务)。provider 无关(飞书桥接同样中招),全库审计 7 个残骸 session。**双层修复**(DeskFox 机制,不碰 claude-code 插件):① 后端 `messages` handler idle 时自愈补盖 `completed=created`(新 fork-only `heal-interrupted.ts`,busy 守卫不误伤在途)② 前端 `isWorking` 改纯函数 `deriveSessionWorking` 只看最后一条消息。14 新单测全过 + session/server 302+12 回归绿 + 两端 typecheck / 0 R4 / 上游改 2 文件(均加 FORK marker)。[bug-repro: 硬杀漏盖 time.completed 致残骸消息令"运行中"图标永久卡死] |
| [macos-monterey-no-launch](./macos-monterey-no-launch/) | done | macOS Monterey 12.0.1 装完点击无反应修复(2026-06-08,Tiny)— 根因:sidecar `opencode-cli`(Bun 1.3.14 编译)的 `minos`/`LC_BUILD_VERSION` 被标成 13.0,dyld 在 macOS 12 直接拒载后端进程(主程序 minos 11.0、Info.plist 10.13 能启动,卡点唯独 sidecar);Bun 自 1.2 弃 macOS 12,但据 Bun issue #15873 该标记是编译配置问题、实际代码可在 12 运行。**修法**(build-deskfox.sh 两处,全 fork-only):① sidecar 落位后 / Tauri 签名前用 `vtool -set-build-version macos 12.0` 把 minos 13.0→12.0 + `codesign --force --sign -` ad-hoc 重签(prod 后续 Developer ID 覆盖,签名不改 minos);② `--config` 注入 `minimumSystemVersion:12.0` 钉主二进制 + Info.plist floor(低于 12 的用户得明确弹窗而非死图标)。**验证**:本机 dev build 三点全 12.0(Info.plist / 主二进制 / sidecar)+ LO soffice/122 dylib 全 11.0 无第二卡点 + sidecar 健康监听;⚠️ **真 Monterey 机验证待定**(赌 Bun 1.3.14 不依赖 macOS 13 独有符号,issue 实测的是 1.1.40,失败则退回"让对方升级系统")。决策:Steam 调查 ≤12 仅 ~0.41%,但修复成本极低值得做。 | Tiny,build-deskfox.sh +38 行 / 0 改上游 / 0 R4 / 0 黑名单 / 回退 git revert |
| [coldstart-toast-race](./coldstart-toast-race/) | done | sidecar 看门狗重启窗口冗余红 toast 静默(2026-06-08,Small)— 用户启动新装包撞「加载文件失败 / 无法加载 Downloads 会话(`error sending request` 64796)/ 后台已恢复」三连弹。**根因**:`ConnectionGate`(app.tsx)已把主 UI gate 在健康检查后渲染(正常冷启动不会在 sidecar 未 ready 时发请求),但 sidecar healthy 放行后**随后假死**被 sidecar 看门狗(REQ-049 Layer③)判无响应、同 port 重启;**重启窗口**里 `global-sync`(会话列表)/`file`(文件加载 + 文件树列表)请求以连接级不可达(`error sending request for url`,Tauri/reqwest 后端不可达)失败 → 各弹一条红 toast。而看门狗本已统管「后台引擎重启中 / 后台已恢复 / 多次重启失败」提示,这些 per-request toast 是冗余噪音、看着像真故障。**修法**:新增纯函数 `isBackendUnreachableError(error)`(`server-errors.ts`,吃 Error/string,正则识别连接级不可达 `error sending request`/`failed to fetch`/`connection refused` 等,**不含 HTTP 4xx/5xx** 业务/服务故障)→ 3 个 toast 站点遇连接级不可达 `return` 不弹(仍 console 记录);后端可用性 UX 统一交看门狗。**设计取舍**:只 suppress 不扩重载 wiring(后端恢复靠既有 `server.tsx` 健康轮询 + `global-sync` `bootstrap.refetch` 自愈);suppress 安全(渲染态「不可达」≈ 看门狗重启窗口,真·从未起来被 ConnectionGate 挡在 ConnectionError 页;终态失败由看门狗「多次重启失败」toast 兜底);per-call-site mirror 既有 `isStaleSessionError` 模式,精准低风险。未动 `file-tabs.tsx:1499`(SVG 渲染错非连接错)。[bug-repro: 看门狗重启窗口前端各请求各弹冗余红 toast — 连接级不可达应静默] | Small,4 文件 ~70 行(1 helper + 2 context + 1 test)/ +6 单测(server-errors 15 pass)/ app 814 pass 0 fail / typecheck 16/16 / 0 改上游 / 0 R4 / 0 黑名单 / 回退 git revert |
| [feishu-workspace-picker-hang](./feishu-workspace-picker-hang/) | done | 飞书「选工作目录」原生文件夹选择器第一步卡死修复(2026-06-08,Tiny)— 绑定页对账号点编辑 → 工作目录块点「选择」→ 原生 picker 弹出但全程点不动(Cancel/Open 都失效),整窗被模态挡住像死机。**诊断**:`ps`+`sample` 实证 DeskFox 自身没崩没死循环(主进程主线程停在正常 tao 事件循环、WebContent 空闲、sidecar 健康 401、本次启动 0 次看门狗重启 → 跟 macos-monterey / coldstart-toast 两笔都无关);卡的是 macOS 系统文件面板(out-of-process `openAndSavePanelService`/ViewBridge)弹到塞满大图(2000×2000 等)+ 111 项子文件夹的 Downloads,图标视图触发系统缩略图服务卡顿 + 整机 CPU 占用(WindowServer 34%)饿死点击响应。**根因代码** `feishu_adapter.rs::feishu_pick_workspace_dir`(feishu-account-workspace 2026-06-07 引入):`#[tauri::command]` 同步 fn 直调 `blocking_pick_folder`、无 `set_parent`、注释假设"同步命令跑 worker 线程"(脆弱)。**修法三处**:① 命令 `fn`→`async fn` + `tauri::async_runtime::spawn_blocking` 保证离 main 线程(blocking_pick_folder 文档禁 main 线程调)② `.set_parent(主窗口)` 自由浮动 app-modal panel → document-modal sheet,事件由主窗口模态会话统一处理 ③ `.set_directory(home)` 默认开到主目录避开重图 Downloads。配套 `use crate::windows::MainWindow`,前端 0 改。cargo check 过(async+spawn_blocking+set_parent+set_directory 全编译)+ **真机点验通过**(user 实测不再卡)。原生对话框 GUI 无法单测,按"CDP 自测 ≠ 真桌面 QA"+R5 例外以真机 QA 验收。[bug-repro: 原生 picker 第一步卡死] | Tiny,feishu_adapter.rs 1 文件 ~25 行 fork-only / 0 改上游 / 0 R4 / 0 黑名单 / 回退 git revert |
| [coldstart-project-reload-toast](./coldstart-project-reload-toast/) | done | 冷启动重载上次项目时弹冗余「无法重新加载 <项目>」红 toast 静默(2026-06-09,Small)— 接力 [coldstart-toast-race](./coldstart-toast-race/),补它未覆盖的 `bootstrapDirectory` 重载路径。**根因**:启动自动重载上次项目(如 OPENCODE-PLAN)时 bootstrap 在 sidecar 后端 ready 前抢跑,providers 查询抛 tanstack `Missing queryFn`(sdk 未 ready → queryFn=skipToken,fetchQuery 直接抛)、agent 查询抛 `error sending request`(连接级不可达),各弹一条红 toast;两者都是 transient,ready 后重跑即恢复。**修法**:新增纯函数 `isTransientStartupError`(`server-errors.ts`,复用 `isBackendUnreachableError` + 加识别 `Missing queryFn`)→ bootstrap.ts 两处 guard:providers reload catch transient 则静默;slow 汇总处用 `find(真错)` 只在含真错时弹(描述也用真错)。连接级恢复 UX 仍交看门狗统管。**搭车** 一笔独立 Tiny UI fix:`index.css` toast region `bottom 48→112px` 上抬清过聊天输入框提交按钮([fix: toast-above-composer-submit])。server-errors 19 pass / app 830 pass 0 fail / typecheck 16/16 / 0 改上游 / 0 R4 / 0 黑名单 / 回退 git revert | Small,3 文件 ~50 行(主修复)+ 4 行 CSS(搭车)/ +4 组单测 / 全 fork-only(bootstrap.ts 两处 FORK marker)。[bug-repro: 冷启动重载项目 providers/agent 抢跑各弹冗余「无法重新加载」红 toast — transient 应静默] |
| [feishu-settings-workspace-above-advanced](./feishu-settings-workspace-above-advanced/) | done | 飞书「编辑账号设置」dialog 把「工作目录」整块上移到「高级能力」之上(2026-06-09,Tiny)— user 要工作目录更靠近常用模型设置、高级能力下沉。纯 JSX 顺序对调(`feishu-edit-account-dialog.tsx` 单文件):模型 → 工作目录 → 高级能力(/group + 免@);state/handler/保存逻辑/i18n key 全不动,顶部加 FORK marker。typecheck 16/16 / app 830 pass 0 fail(纯重排 0 回归)/ 0 改上游 / 0 R4 / 0 黑名单。纯视觉由 dev 包真机验。 | Tiny,1 文件净 +1 行 fork-only。 |
| [macos-dock-reopen-show-window](./macos-dock-reopen-show-window/) | done | macOS 主窗口关闭后点 Dock 图标主窗口不重开修复(2026-06-09,Tiny)— 接力 [req-031-tray-icon-relaunch-show](./req-031-tray-icon-relaunch-show/) 补 Mac 漏网路径。**现象**:主窗口关(实为 hide 到后台)后,点托盘图标能开、点 Dock 图标开不了。**根因**:Dock 图标点击(app 已运行)在 macOS 走 `RunEvent::Reopen`(专有事件),不走 single-instance(二次启动);req-031 只覆盖 tray 左键 + single-instance,`lib.rs` 的 RunEvent match 只处理 Exit/WindowEvent,无 Reopen arm → 落 `_ => {}` 被忽略。**修法**:加 `#[cfg(target_os="macos")] RunEvent::Reopen {..}` arm 调幂等 `show_main_window_impl`(跟 tray 左键同一恢复入口);cfg gate 必需(Reopen 是 macOS-only 变体,Win 编译该变体不存在)。cargo check 过 / Win 不受影响 / 真机 QA 验。[bug-repro: 主窗口关闭后点 Dock 图标不重开 — RunEvent::Reopen 未处理] | Tiny,lib.rs 1 文件 ~9 行 fork-only;0 改上游产品逻辑 / 0 R4 / 0 黑名单。 |
| [new-project-hide-file-viewer-default](./new-project-hide-file-viewer-default/) | done | 新项目打开默认收起右侧内容预览器,点文件才展开(2026-06-09,Tiny)— user 要打开/重开项目是干净的聊天 + 文件树,点文件才弹内容预览。调研:预览器 = `view().reviewPanel`(全局持久化 `panelOpened ?? true`),点文件展开链路(createOpenSessionFileTab→openReviewPanel)已现成,唯一问题是默认展开。修法:`session.tsx` 加 `createEffect(on(() => sdk.directory, () => view().reviewPanel.close()))` —— 监听项目目录变化主动收起;打开/切/重开项目都触发,同项目内切会话 directory 不变不触发(保持状态)。文件树保留。typecheck 16/16 / app 835 pass 0 fail / 0 改上游 / 0 R4。Tiny 豁免强制单测、真机验。 | Tiny,session.tsx 1 文件 ~6 行 fork-only。 |
| [file-tab-close-others](./file-tab-close-others/) | done | 文件 tab 右键加「关闭其他标签」(2026-06-09,Small)— user 要 tab 右键关掉除当前外所有已开 tab。调研:文件 tab(`session-sortable-tab.tsx`)原无右键菜单,terminal tab 已有现成 `DropdownMenu` 模式可照搬。修法:`helpers.ts` 加纯函数 `closeOtherTabs(tabs,keep,close)`(+5 单测)+ `session-sortable-tab.tsx` 加 `onContextMenu` 弹菜单「关闭其他标签」+ `session-side-panel.tsx` 传 `onCloseOthers` 回调 + i18n en/zh/zht 加 `common.closeOtherTabs`(余 14 locale fallback en)。typecheck 16/16 / app 835 pass 0 fail(+5 新单测)/ 0 改上游 / 0 R4。右键 GUI 真机验。 | Small,5 文件代码 + 1 test ~70 行 fork-only。 |
| [batch-port-edit-mdlink](./batch-port-edit-mdlink/) | in-progress | 三笔独立小修(2026-07-07,Medium 批)— REQ-029 飞书 plugin 重启换端口后 main 进程 `feishu.ts` `cached` 永不失效仍打旧端口(修法 mtime 失效)+ REQ-074 文件查看器右键「编辑」永久灰显(`file-tabs.tsx` `isTauri()` 检测 `__TAURI_INTERNALS__` 换基座回归,改 `isDesktopApp()` 检测 `window.deskfox`)+ REQ-075 聊天消息里相对路径 md 链接无点击拦截走原生导航把 SPA 导航空白(提取 `handleMdLinkClick` 共享 util + 聊天容器委托接 `onOpenTab` 按项目根解析 + main 进程 `will-navigate`/`setWindowOpenHandler` 兜底)。需求源 OPENCODE-PLAN/需求计划/2026-07-07.md。 | 进行中 |
| [filetree-hover-collapse-hint](./filetree-hover-collapse-hint/) | done | 给「再次点击收起预览」隐藏交互加发现性提示(2026-06-09,Tiny)— `filetree-toggle` 做的"再次点击正在查看的文件收起预览区"无发现性,user 要传递给用户又不干扰。三方案对比选最轻的**纯 hover tooltip**:正在查看的那一行 + 预览区已开时,悬停浮出「再次点击可收起预览」,无图标、零布局变化。文案「点击收起预览」,`placement="bottom-end"`(行下方右对齐)。修法:`file-tree.tsx` FileTreeNode 加 `viewerOpen` prop + 应用内 `Tooltip` 包裹行(`inactive={!(viewerOpen && node.path===active)}`,仅 active 行包 Trigger;条件与 toggle 的 isViewerOpen 一致)+ 主 FileTree 透传(含**递归子目录调用**)+ `session-side-panel` 给「所有文件」tab 传 `viewerOpen`(diff tab 不传)+ i18n×3。**踩坑两轮**:① 首版原生 HTML `title` 在 Tauri WKWebView 不渲染(嵌入式 WebView 通病)→ 改应用内 Tooltip;② 递归子目录 `<FileTree>` 漏传 viewerOpen → 子目录文件(png/mp4/json)不显示 → 补递归透传。typecheck 16/16 / app 835 pass 0 fail / 0 改上游 / 0 R4。真机三轮验。 | Tiny,file-tree.tsx ~17 行 + side-panel 1 prop + 3 i18n 行 fork-only。 |
| [auto-updater-install-silent-fail](./auto-updater-install-silent-fail/) | done | macOS「安装并重启」点击后静默无反应根因修复(2026-06-12,Small)— 已装 2026.7.0 用户检查更新弹 toast 显示 2026.7.1、点「安装并重启」等很久零反应、app 仍停旧版无报错。**根因(已复现实证)**:updater 升级包 tarball 被 macOS BSD `tar` 塞进 AppleDouble `._DeskFox.app` 等 3532 个 `._` 成员(`build-deskfox.sh` 2.6 段 LO 重签后裸 `tar -czf` 未禁 copyfile);Tauri `install_inner` 的 `path.skip(1)` 把首条 `._DeskFox.app` 折成空路径 → 把文件 unpack 到临时目录 → EPERM → 解压第 0 步即抛错(在动 /Applications 前,故旧 app 完好);客户端 `updateAndRestart` 又 `.catch(()=>false)+if(!installed)return` 吞掉错误 → 「点了没反应」。**复现**:最小 Rust 程序复刻 install_inner 解压循环跑真实 235MB tarball,原版 `UNPACK FAIL @ entry #0 ._DeskFox.app EPERM`、`COPYFILE_DISABLE=1` 重打 3532 entry 全过;python3 tarfile 看 raw 成员原版 7064(3532 真+3532 `._`)/ 修复版 3532·0 个 `._`(bsdtar `tar tzf` 自动隐藏 `._` 故肉眼/grep 看不到)。**修法**:① `build-deskfox.sh` 2.6 `tar -czf`→`COPYFILE_DISABLE=1 tar -czf` + python3 防回归断言(检到任何 `._` 成员即删毒产物+报错,后续签名/部署安全失败)② `index.tsx` 停止吞 check/download/install 错误如实抛出 ③ settings-general/layout 两 toast action 捕获弹真实错误 + 后台轮询 `.catch(()=>{})`(error.tsx 早已捕获不动)。typecheck 16/16 / 0 改上游 / 0 R4 / 0 黑名单 / 回退 git revert。**线上重发(2026-06-12 ✅ + 真机验收)**:staple 现有公证票(免苹果重审)→ COPYFILE_DISABLE 重打 → minisign 重签 → R9 三验 → 传 OSS + 部署 manifest。**踩 CDN 同名缓存陷阱**(Aliyun CDN 仍 `X-Cache HIT` 吐旧带毒字节 → 验签失配),`upload-asset-to-oss.sh` 无自动刷新 → 改文件名 cache-bust `DeskFox-2026.7.1-fix1-darwin.app.tar.gz`(`.sig` 对字节签名与名无关仍有效)解决;端到端下载线上字节 sha256==本地基准 / 0 AppleDouble / Rust 解压 3533 OK;user 2026.7.0 真机点升级成功(TC-3 首跑通)。[bug-repro: auto-updater 安装/检查静默失败] | Small,1 build 脚本 + 3 前端文件 ~47 行 fork-only。 |
| [electron-brand-cleanup](./electron-brand-cleanup/) | done | Electron 换基座 DeskFox 品牌残留接线补齐(2026-06-14,Small–Medium）— 桌面入口绕开 fork 品牌单一源致预览版露 OpenCode。**接回单一源**(不新建机制）:① 文本 desktop renderer i18n 出口套已有 `rebrandDict`（扩 `Go` 短语 + key 感知白名单 wsl.*/mcpFailed/freeModels.title）覆盖 B3–B7；② `Splash` import 换源 `@opencode-ai/branding/logo`（A1，theme.css 已由 app.tsx→index.css 注入，无需另 import）；③ `copy-icons` 叠加 branding 图标到 resources/icons（A2，喂 exe 内嵌 icon）。**独立修复**:B1 main 进程 `page-title-updated`+`preventDefault` 按 channel 锁窗口标题（真机 OS 标题实证 "DeskFox Dev"）；A5 通知图标外网 favicon→本地 DeskFox data URL（离线可显）；D1 `extraMetadata.author.name` 改 DeskFox（NSIS 发布者根源，`win.publisherName` 经实测被 eb26 schema 拒）；C1 默认 package script 指 deskfox config 防误发。**defer**:A6 favicon（symlink 占位+资产未就绪+桌面 tab 几不可见）。typecheck app/desktop 各 0 / rebrand 单测 10+2 pass / app 全量 419 pass（2 无关预存在 fail）/ electron-vite build + electron-builder --dir 出 DeskFox Dev.exe + boot smoke 6 进程无崩溃 / 改上游均 FORK marker / 0 R4 / 0 黑名单。[feat: electron-brand-cleanup] | Small–Medium,14 文件（~含 2 新测）/ 多为接线。真机视觉 QA 待 user。 |
| [viewer-selection-tray-style](./viewer-selection-tray-style/) | done | 文件查看器选区高亮统一蓝 + 托盘图标 mac template(2026-06-14,Medium)— user 报 3 项:① mac 托盘图标偏大+品牌蓝,要白色自适应+缩小 ② HTML 预览选中右键加聊天后底色变灰 ③ CSV 选中右键后底色消失。诉求各格式统一,user 拍板**统一标准蓝**。根因:选区 overlay 仅右键时画(原微软红),HTML 在 iframe 失焦变灰且父无 overlay 投影,CSV 故意不画 overlay(grid 矩形溢出)。修法:overlay 红→蓝 `rgba(56,139,253,0.4)`;CSV 右键画 overlay 蓝+裁到容器边界(`clampRectsToBounds`);HTML 桥接脚本传 iframe 选区 rects+注入蓝 ::selection,父 postMessage 投影(`projectIframeRects`)成 overlay 蓝;托盘 mac 改纯黑 template image(`setTemplateImage` 反色白+@2x 缩小),Win/Linux 留彩色蓝。selection-overlay 11 单测 + local-asset 5 回归 / typecheck 26/26 / user 真机逐格式验过。0 上游侵入 / 0 R4。 | Medium,全 fork(file-tabs+selection-overlay+local-asset+tray)+ 2 template png + 3 文档。 |
| [stale-path-hardening](./stale-path-hardening/) | in-progress | **项目入口 stale 路径全族根治(v2026.6.21)** — 改名/移动/默认路径不存在/大小写不一致致文件树 500、编辑保存静默失效、侧栏显示旧名、启动静默空白,在项目入口统一治理。**REQ-067** 大小写防御兜底(`ignore-path.ts` 归一大小写 + `safeIgnores` 防 `..` RangeError,Windows 不复现仅护 mac,平台无关单测);**REQ-068**(新做)启动 pre-check(desktop IPC `pathExists`/`fs-probe.ts` errno 分类 + `startup-precheck.ts` 决策 + layout.tsx autoselecting 前置校验 + 分模态引导 toast,missing 清 lastProject、unreachable 保留);**REQ-061** M5 三态修复(`project-rebind.ts` `isWorktreeConfirmedMissing`:仅 ENOENT 判 missing,检查出错保守不重绑,治网络盘/U盘 offline 误重绑);**REQ-064** update 自愈(NotFound 时 fromDirectory 重解析现行 id 重试)。搭车修 `build-media-gen-plugin.ps1` 缺时间戳跳过 + PS5.1 stderr 误判。Large,4 新 fork-only 文件(+4 test 20 测)+ 12 上游 FORK 注入;opencode/app/desktop typecheck 0 错。真机四模态 QA 待 user。 | Large。[feat: stale-path-hardening]
| [updater-deploy-stale-ip](./updater-deploy-stale-ip/) | done | updater 部署脚本 SSH host 硬编码旧 IP 修复(2026-07-08,Tiny)— 2026.8.4 发版后 user 报本地检测不到更新。根因:`deploy-electron-updater.sh`/`bridge-electron-updater.sh` SSH host 硬编码旧 IP `52.197.46.120`,服务器换 IP 到 `57.180.8.119`(旧 IP web 还活但 SSH 22 死)→ `scp` 静默超时 → OSS 产物成功但 manifest 未部署 → 两条 mac 升级源停在 Win 2026.8.3。修法:3 脚本 SERVER 改域名 `${DESKFOX_UPDATE_SSH:-ubuntu@updates.deskfox.ai}`(DNS 跟随+env override)。运维已手动补部署两条源到 2026.8.4。 | Tiny,3 fork-only 运维脚本 ~15 行;0 改上游/0 R4。 |
| [deskfox-data-namespace-isolation](./deskfox-data-namespace-isolation/) | done | DeskFox 运行期数据命名空间隔离(2026-07-12,Large,**1-spec 已审签**)— Intel 真机「任意模型连不上」真因:DeskFox 与同机另装的上游 OpenCode 共用 `~/.local/share/opencode/opencode.db`,异版本核心 schema 打架 `no such column` 必崩。**厘清**:跟紧上游收益靠 code merge、不靠共用 runtime DB。**方案(实施后微调)**:`index.ts` `whenReady` 后单源设 `XDG_DATA_HOME`+`XDG_CONFIG_HOME` 指向 deskfox 专属根(纯 fork env 注入、0 改上游 core,比 MiMo 直改 core `app` 常量省 merge 成本;撤销原 `sidecar.ts` passthrough 方案,避免覆盖失败回退)+ 首启非破坏 copy 迁移保住存量数据。审签:D1=a 共享 deskfox 命名空间 / D2=a 非破坏 copy / D3 本期不动飞书。参照 MiMo(`APP="mimocode"` 冷隔离无迁移)。**已实施**:`data-namespace.ts`(纯函数 `resolveDeskfoxXdg`/`planNamespaceMigration` + 编排 `applyDeskfoxDataNamespace`);TC-1~5 单测 + TC-7 加强 e2e 共 12 pass;TC-7 真机 local 版验证通过(真实 1.2G 非破坏迁移 + app 内数据可用);上游侵入 1 文件(index.ts +7)/ 0 改 core / 0 R4。TC-6(与上游版同机并存)标 backlog(需上游版复现)。[feat: deskfox-data-namespace-isolation] |
| [macos-intel-x64-build](./macos-intel-x64-build/) | done | macOS Intel x64 安装包构建支持(2026-07-11,Medium,REQ-081)— Apple Silicon 开发机**交叉编译**出 Intel x64 签名+公证包,与 arm64 共享版本号、独立文件名。**难点=node-pty 交叉打包两层坑**:① `@lydell/node-pty` 按平台拆独立预编译包,bun 在 arm64 主机只装 darwin-arm64 → x64 build 时磁盘无 x64 `pty.node`(修:`bun install --cpu='*' --no-save` 装齐 + 硬校验);② **真凶** `electron.vite.config.ts` 的 node-pty-narrower 插件用**构建机** `process.arch` 算子包名 → x64 bundle 被写死 `import "node-pty-darwin-arm64"` → Intel 启动崩 `Cannot find darwin-x64/pty.node`(修:改读 `DESKFOX_TARGET_ARCH` 目标 arch,build 脚本按 `--arch` 注入)。第一轮只修①、用「仓库源走主包分发」的 Rosetta 测试**误判通过**,真机仍崩;第二轮修②并**升级测法=字节 grep 交付 app.asar + app 自带 x64 Electron 在 Rosetta 实跑**。U1 LO 落独立 `macos-x64/`;U2 config `targetArch` + `mac.target` 显式 arch。**真 Intel Mac(2020 i5/15.7.7)安装启动成功**。arm64/prod 流程零变化。**含 1 笔 R4**(`electron.vite.config.ts` 黑名单,Q3 第 3 笔超基线)。[feat: macos-intel-x64-build] | Medium,4 文件 ~76 行(3 fork/豁免 + 1 上游 R4);updater 双 arch 合并 yml 留后续。 |
| [macos-install-restart-no-quit](./macos-install-restart-no-quit/) | done | macOS「安装并重启」只关窗口不升级修复(2026-06-22,Tiny)— 用户点更新提示「安装并重启」后只关桌面窗口、软件不真退、不升级,仅 macOS。**根因**:DeskFox「关闭到托盘」(`tray.ts` attachCloseToTray)在 `isQuittingFlag=false` 时把窗口 close 拦成 hide;而 `quitAndInstall()` 走 Squirrel.Mac 的 `before-quit-for-update`(非 `before-quit`)从不调 `setQuitting()` → 窗口被 hide、app 不退 → Squirrel.Mac 无法替换 bundle。Windows NSIS 独立进程接管不受影响。**修法**:抽纯函数 `withQuitIntent(backend, markQuitIntent)` 包 backend,`quitAndInstall` 前先 `setQuitting()`;`updater.ts` 用 `withQuitIntent(autoUpdater, setQuitting)`。新测 2 pass + desktop main 71 pass + typecheck 通过。真机端到端待下次发 2026.8.2 验。[bug-repro: macOS 安装并重启只关窗口不升级] | Tiny,2 新文件(helper+test)+ updater.ts 接线 3 行 fork-only。 |
| [renderer-snapshot-oom](./renderer-snapshot-oom/) | done | REQ-087 renderer 状态快照 OOM + 磁盘写入超限治理(2026-08-02,Large)— Crashpad 实证 V8 堆 3.9GB stringify OOM + 29s 连环崩 + macOS 磁盘写入超限。四件套:persist 桌面写盘 trailing-throttle(800ms)+ 16M chars 体积熔断;prompt-history 剥离图片 dataUrl(存量 migrate 首启缩容);主进程连环崩自愈(120s 窗口复崩 → 快照 .dat 隔离 .bak + reload)。draft 图片外置 deferred。app 537 pass(2 预存 fail)/ 新增单测 15 / typecheck 0 错。[feat: renderer-snapshot-oom] | Large,3 上游 FORK 标记 + 1 fork-only 新模块 + 3 测试文件。 |
| [sidecar-oom-brake](./sidecar-oom-brake/) | done | REQ-049 sidecar OOM L1 内存刹车 + L3 断连提示(2026-08-02,Medium)— utility.log respawn ×7 实证日抛级挂死。Electron utilityProcess 支持 execArgv → L1 硬帽 `--max-old-space-size=3072` 纯 fork 侧落地(免上游核心 override);软刹车 30s 采样 80% 阈值上报(120s 静默/70% 重武装);L3 renderer 首次真正消费看门狗事件(顺手修通道缺 deskfox: 前缀致历史广播全部收不到的 bug)→ restarting/恢复/放弃/内存压力 toast;停止键不可达不再静默吞错。desktop 150 pass / 新增单测 9 / typecheck 0 错。[feat: sidecar-oom-brake] | Medium,2 fork-only 新模块 + 4 上游 FORK 标记。 |
| [permission-filter-concurrency](./permission-filter-concurrency/) | done | REQ-078 方案D 权限过滤并发挂死修复 + 过滤下沉共享层(2026-08-02,Medium)— composer 私有 resource 布尔 source 只在 false→true 沿拉一次 permission.list,同 session 并发第二个权限被陈旧快照 fail-closed 藏死(turn 挂死,飞书 240s 超时)。改「候选 id 集签名」驱动 refetch(空签名不 fetch 保 e2e/离线 gate;乱序丢弃;失败 fail-open),过滤下沉 context/permission `canResolve` 共享层,composer/侧栏徽标/头像统一消费消灭幻影徽标。app 533 pass(2 预存 fail)/ 新增单测 5(含 bug-repro 主线)/ typecheck 0 错。[feat: permission-filter-concurrency] | Medium,1 fork-only 新模块 + 4 上游 FORK 标记。 |
| [popup-enter-passthrough](./popup-enter-passthrough/) | done | REQ-085 「加入聊天」浮层 Enter 穿透 toggle 文件预览区修复(2026-08-02,Tiny)— 根因:file-tree 快捷键 window 级 keydown B 路径;浮层提交同步卸载 textarea → activeElement 回落 body → 误判中性区放行,Enter 被当文件树导航键。修法:守卫改用事件原始 target(detach 后仍准),树外可编辑控件不接管。bug-repro 4 用例。[feat: popup-enter-passthrough] | Tiny,1 上游 FORK 标记 + 1 测试文件。 |
| [session-content-search](./session-content-search/) | done | REQ-095 会话记录内容搜索(2026-08-07,Large,1-spec 蓝本经 user 审签)— ⌘K 面板新增「会话内容」分组:消息正文全文检索(中文子串 trigram + 1–2 字 LIKE 回退),命中高亮片段,点击经 `#message-<anchor>` 锚点跳转定位(复用现有 hash 定位 + 自动 loadMore);默认当前项目、固定「在所有项目中搜索」切换行。数据层 external-content FTS5 挂 part 表触发器增量(delta 是 ephemeral 不落库,零打爆),**不走上游 migration**(fresh DB 不执行 migrations 的陷阱)而是首搜幂等 bootstrap + backfill + meta 版本重建;FTS 不可用全链路降级隐藏分组。真 server curl 抓到 workspace-routing 中间件把 `search` 当 sessionID 的 500(照 status 先例豁免 2 行)。sidecar 28 测 + snippet 6 测 + e2e 3 条全绿,万级消息 <1s。[feat: session-content-search] | Large,fork-only ~1126 行(含测试);上游 42 行 FORK 标记 + SDK regen;R4 override 1 笔(opencode/sdk 收口)。 |
| [session-list-ux](./session-list-ux/) | done | REQ-096 会话列表操作体验(2026-08-07,Medium)— ①标题编辑失焦保存(含 Chromium detach-blur 补发陷阱守卫,Esc 仍显式放弃)②会话行右键菜单(重命名/分享/归档/删除,Kobalte ContextMenu,i18n 跟随,原生英文 Copy Link 菜单就地消解)③行内 inline 重命名(Enter/blur 双路径)④归档 hover 图标移除 + 撤销 toast。**真机 DB 校验抓到隐藏 bug**:取消归档在投影层被 drizzle 跳过 undefined 列静默丢失(UI 恢复重启又消失),修 core projector `?? null` 1 行 + 真 projector 回归测试;HTTP 层补 archived:null=取消归档(2 行)。单测 24 + e2e 6 + 真机 CDP 两轮(含 DB NULL 断言)全绿。[feat: session-list-ux] | Medium,1 fork-only 组件 + 4 上游 FORK + 19 语言 3 键;R4 override 1 笔(opencode 2 行 + core 1 行 + 测试)。 |
| [in-session-find](./in-session-find/) | done | REQ-097 会话内查找 ⌘F(2026-08-07,Medium)— 会话页查找条:数据层按轮次计数「n/total」+ CSS Custom Highlight 双层高亮(零 DOM 改动,虚拟化滚动补染)+ Enter/⇧Enter 环形跳转(revealMessage + 轮内 Range 定位)+ Esc;⌘F capture 期接管(压过 composer CodeMirror 自带 Mod-F);焦点作用域:文件预览区 `data-deskfox-find-ignore` 不响应(将来预览区自建查找的注册口,user 拍板架构);**⌘K 内容命中点击 → 查找条带词自动展开并定位到命中轮次**(REQ-095 体验闭环)。单测 16 + e2e 3(全量 29/29)+ 真机 CDP(中文计数/跳转/高亮截图/联动/作用域)全绿。[feat: in-session-find] | Medium,4 fork-only 模块 + 4 上游 FORK(全 app 白名单)+ 19 语言 3 键;**0 R4 override**。 |
| [native-menu-i18n](./native-menu-i18n/) | done | 原生右键菜单 i18n(2026-08-07,Medium)— electron-context-menu 标签跟随 **app 内语言设置**(非 OS locale):fork-only 19 语言 × 12 标签 + locale 归一(zh-TW→zht/pt→br/nb→no/未知→en),`applyContextMenuLanguage` 按语言 dispose+重挂,启动 OS locale 兜底、renderer 经 `deskfox:set_context_menu_language` IPC 同步真实设置,切语言即时生效。真机 cliclick 实拍:composer 右键=「剪切/复制/粘贴」。补齐 REQ-096 时留下的全局原生菜单缺口(会话行已走应用层菜单)。[feat: native-menu-i18n] | Medium,3 fork-only + 3 处上游 FORK(全白名单)0 R4;desktop 单测 4 用例 229 断言。 |
| [pre-commit-forkfile-exempt](./pre-commit-forkfile-exempt/) | done | REQ-048 最小子集:pre-commit 黑名单对 fork 自建文件动态豁免(2026-08-10,Medium)— 黑名单 packages 目录前缀段一刀切误伤 fork 自建文件(实测已 **47 个**,非 doc 点名的 3 个;fork 累计 69 笔 override 多为此类)。改为动态「上游基准树」判定:新 tag **`upstream-base`**(= main 最近上游 merge 的上游侧 parent `be227503af` @1.17.4),文件在该树不存在 = fork 自建 → 豁免;存在 → 照旧拦。只对 packages 目录段生效(根 `*.config.*`/`.github/` 等段行为不变),tag 缺失 fail-closed;merge guide 新增 §5.8「sync 后移 tag」防闸变松。REQ-103 上游同步前置项(sync 中途不再被守门脚本反复打断)。全面评估其余四块不碰,REQ-048 留 backlog。[feat: pre-commit-forkfile-exempt] | Medium,hook + 2 治理文档,**0 R4 override**;R8 用例 T1-T8 实跑全过(fork 文件放行/上游文件仍拦/根文件段不豁免/fail-closed/§4.6 仍响)。 |
| [fork-param-merge-guard](./fork-param-merge-guard/) | done | REQ-104 fork 关键参数防上游 merge 丢失(2026-08-09,Tiny)— `packages/app/package.json` 的 `--conditions=browser`(fork 必需,缺了 solid-js 落 server 构建、`createEffect` 变 no-op)上三道锁:`.husky/pre-commit` **§4.6 无条件断言**(机器锁,唯一不依赖人)+ `_fork_notes` 顶层键(就地锁)+ `UPSTREAM-MERGE-GUIDE` §3.6/§5.7 前后 checklist(流程锁)。**施工中实测推翻立项时的"静默假绿"说法**:删该参数 = 604 pass / **2 fail**(非全绿),但报错只指向 `project-restore.test.ts` 业务语义、零线索指向 package.json ⇒ 真实风险是**误诊**(会跑去修业务代码),且信号只挂一个 fork-only 测试文件、占比 2/606 易被淹没。§5.7 据此加了**反向用法**:merge 后见该测试报 effect 没生效,先查参数。REQ-103 上游同步的前置项。[feat: fork-param-merge-guard] | Tiny,3 文件,**0 R4 override**(app/ 与仓根 docs/ 及 .husky/ 均不在黑名单;断言刻意不放黑名单的 scripts/);删→红 / 恢复→绿 对照实跑。 |
