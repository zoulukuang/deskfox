# Features 索引

> 规范 v2(2026-04-27)起,每个 feature 一个目录,含 1-spec / 2-plan / 3-changelog 三文档。
> 详情见 [`CLAUDE.md`](../../CLAUDE.md) 的"完整文档链路"段。
>
> **分支名注**:2026-05-21 起本仓主分支 `dev` → `main`(详 `rename-dev-to-main` feat)。以下 feat 描述中"dev 分支"字眼(historical entries)指向当前 `main`,描述作为历史快照不回填。

## 主分支命名空间(2026-05-21 起)

| 命名空间 | token | 含义 |
|---|---|---|
| Git 分支 | `main` | 本仓主分支(原 `dev`,2026-05-21 重命名)|
| Git 分支 | `feat/<name>` | feat 分支,合 main 即销毁 |
| Git 分支 | `upstream/dev` | 上游 anomalyco/opencode 主分支(他们仍叫 `dev`)|
| Installer channel | `prod` | 稳定版(发布物,无版本号后缀)|
| Installer channel | `beta` | 测试版(预发,保留 AppId 备用,日常不主动 ship)|
| Installer channel | `dev` | 预览版(可对外发,版本号 `-dev` 后缀)|

**两个命名空间不撞**:`main` 是分支维度,`dev`(channel)是 installer 维度。同一个 `main` 分支可以 build 出 prod / beta / dev 三种 installer。

| feat-id | 状态 | 简介 |
|---|---|---|
| [file-tree-test-client-env](./file-tree-test-client-env/) | done | file-tree.test.ts Windows 测试修复(Tiny,2026-05-27)— 远程同步代码后 app 单测在 Win 跑出 1 fail(`Client-only API called on the server side`)。根因:bun test 解析 solid-js **服务端构建**,`import ./file-tree` 连带加载 `@opencode-ai/ui/context-menu`(Kobalte)→ `@kobalte/core` 求值期调 client-only API 抛错(纯测试环境问题,真实 app 有浏览器正常)。测试已 mock 其余 UI 组件独缺 context-menu,补一条同款 mock 隔离。上游测试 #12468 加 FORK marker。app 711/0(原 708/1)。commit `b073c5fa5` |
| [feishu-merge-forward-image-400](./feishu-merge-forward-image-400/) | done | 合并转发图片 graceful 降级(2026-05-27)— 真机发现合并转发子图下载全 400(飞书错误码 234043:**官方不支持读取合并转发内子消息资源**,平台硬限制不可修;原机制单测 mock fetch 掩盖、真实从没成功)。改 graceful:flatten `maxImages=0`(不建下载列表+不渲染假的"已展开识别",加 `imageCount`)+ **停掉死下载**(省 5×400)+ prompt 告知 LLM 只看文字 + **回复头部诚实提示**「📷 合并转发里的 N 张图我读不了(飞书限制),需识别请直接转发图」。飞书 622 pass + 真机 UX user 验通。commit `aadbd245c` |
| [feishu-win-test-compat](./feishu-win-test-compat/) | done | 飞书单测 Windows 兼容修复(Tiny,2026-05-27)— 飞书 image-recognition/merge-forward 那批在 Mac 验"622 全过"、Win 跑 607/15 fail(media-gen 合并前体检发现,实证 origin/main 自带非 media-gen 引入)。① **真 Win 代码 bug**:`image-downloader.ts` 路径越界 assert `resolve(p)+"/"` 在 Win(反斜杠路径)前缀永不匹配 → 飞书图片下载全失败,改 `+path.sep`;② classifyAttachment 测试 `ROOT="/tmp/.."` Unix 路径 → 改 `resolve(...)`;③ defaultFilePath 断言写死 `/` → 改 `join(...)`;④ dedup 2 个 TTL 刷新测试时序裕度太窄(Win ~16ms 粒度)→ 放大 ttl/sleep。修后 622 pass / 0 fail(Win)。commit `b6e2f2f4a` |
| [media-gen-bundle](./media-gen-bundle/) | done | media-gen 创作插件打包进安装包(同飞书机制,2026-05-27)— 从"靠开发仓绝对路径加载"升级为软件内置部分:Tauri resources ship + DeskFox.iss [Files] 装到 `{app}\plugin\media-gen` + `feishu_plugin_install.rs` 通用化加 `ensure_media_gen_plugin_in_config` 开机注入。装完连 Alibaba (China) 填 key 即自动可用、零手动配。注入逻辑通用化(`inject_plugin` 从末两段自动推标识,签名不变测试零改)+ `media_gen_and_feishu_coexist` 单测。顺带修 build-deskfox 清理块 PS5.1 编码 bug(Get-Content/Set-Content 默认 ANSI → .NET UTF-8 无 BOM,否则写坏含中文 opencode.jsonc 致 setup hook 注入全废)。真机端到端验通(build→清理→注入→/healthz+8模型7能力→文生图落项目根)。commit `b977f4130` |
| [window-resizable](./window-resizable/) | done | 修窗口无法鼠标拖拽改大小(Tiny,2026-05-27)— 真因:`tauri-plugin-window-state` 持久化恢复 fullscreen/maximized,残留 `.window-state.json` 的 `fullscreen:true` 反复恢复 → 窗口钉死全屏抓不到边。修:`constants.rs` window_state_flags 减 MAXIMIZED+FULLSCREEN + `windows.rs` 去 `.maximized(true)` 改窗口态(1280×800 / min 800×600 / center)+ 清残留状态文件。~8 行 / 2 上游文件 FORK marker。native 拖拽 user 真桌面验通。commit `44180a261` |
| [media-creation-mode](./media-creation-mode/) | done | 创作模式(2026-05-26~27,REQ-030)— 否决"AI 自动调工具"改显式模式菜单:底部统一菜单(Chat + 文生图/图片编辑/文生视频/图生视频/语音合成/语音识别/专业翻译)+ 左侧随模式换模型/音色 + 输入即生成。结果融入聊天滚动流(轻量卡:提示词+输入缩略图+小结果+已保存路径+打开)+ 产出落当前项目根 `creations/`。fork-frontend(prompt-input/message-timeline 注入)+ 本地生成服务(Bun.serve 51737 R6 SSE)+ 模块级 store,引擎复用 media-gen-alibaba。CDP 真机端到端自测 + user 桌面 QA。14 commits。无模型引导 / 通用引擎留 backlog(见 OPENCODE-PLAN 后续路线 doc) |
| [media-gen-alibaba](./media-gen-alibaba/) | done | 多模态生成第一竖切(REQ-030)— 阿里(DashScope/通义万相)全能力适配。新建 `packages/media-gen` opencode plugin(**0 改上游**),6 工具:文生图 / 改图 / 文生视频 / 图生视频 / 翻译 / 语音合成 / 语音识别。8 模型实测(`wanx2.1-t2i-turbo/plus`、`qwen-image-edit`、`wanx2.1-t2v/i2v-turbo`、`qwen-mt-turbo`、`qwen-tts`、`paraformer-v2`),3 协议(异步 task / 同步 chat / 同步 multimodal)。**关键决策**:先竖切不抽象(通用层等第二家逼出)+ 0 Rust(边车进程调不到 Tauri 命令)+ 本地文件 OSS 上传(ASR)/base64(改图)+ 改图 `wanx2.1-imageedit`→`qwen-image-edit` 纠偏(下载结果图用 Read 工具亲眼验证)+ `tool-shim` 避 effect 进 bundle。Large ~1500 行全新 fork-only / 16 单测 / typecheck 净 / dist 417K 冒烟 6 工具 / 真厂商人眼验 / **0 R4 / 0 上游侵入** / 8 commits。已证伪 `wan2.6-t2i`(url error)`wanx2.1-imageedit`(换背景无效)。follow-up:拖拽附件改图、OSS 持久化、`/media` 兜底、转 minimax 抽通用层。完整规划见 REQ-030 |
| [feishu-merge-forward](./feishu-merge-forward/) | done | 飞书 user 合并转发消息识别(2026-05-26)— **最具鲁棒性方案落地**:D1=C 全图展开 MAX_IMAGE_COUNT=5(继承 S1-S5)/ D2=SDK `client.im.v1.message.get` + Promise.race 30s timeout / D3=MAX_SUB_MESSAGES=50 末尾省略 / D4=嵌套 1 层递归 + depth≥2 占位 / D5=群聊带 sender(open_id 前 6 位)+ p2p 不带 / D6=中文 user 视角占位 + 元信息(`[文件: x.docx 1.1MB]` / `[语音 12s]` / `[分享: 群 oc_xxx]`)。**R1-R4 鲁棒补强**:30s timeout / `sortByCreateTime` R2 / 0 子消息+拉取失败+深度超限 友好兜底 / vision 预检 textOnly 降级。**关键解耦**:`merge-forward-flatten.ts` 纯函数 helper(`flattenMergeForward` / `renderSubMessage` / `sortByCreateTime` / `hasAnyImage`,8 种 msg_type 占位)+ `merge-forward-fetcher.ts` SDK 调用层 + `message-pipeline.ts` `handleMergeForward` 独立路径 + `expandNestedMergeForward` 嵌套展开(`  ↳ ` 缩进 + 占位 replace);runOpencode `imageOpts.imageParts[]` 多 file part 注入。**5 经验沉淀** changelog:① global image index 必须独立计数器(初版 `images.length+1` 截断后停增)② 嵌套递归不能放纯函数(IO 不允许)③ SDK timeout 用 Promise.race 兜底(SDK 不支持 AbortController)④ 子消息资源访问用**子消息自己的 message_id**(API 二分跟 image-recognition 同款风险,提前规避)⑤ SDK get_message 普通 JSON 0 multipart 风险(只 image.create/file.create multipart 撞 axios+form-data 链)。**测试**:622/622 全过(基线 574 + M 37 + F 11)/ 16/16 typecheck / 0 R4 / 0 上游侵入。Medium 1.5d / 1891 行 / 10 文件。**留 backlog**:file/audio/video/sticker 多模态展开 / 嵌套≥3 层 / sender_id→用户名缓存 / 结构化 JSON pattern D |
| [feishu-image-recognition](./feishu-image-recognition/) | done | 飞书 user 发图给 bot 识别(2026-05-26)— **D 方案落地**:image 下载到 `~/.opencode/imbot-workspace/feishu-images/<chatId>/`,`FilePartInput.url=file://<absolutePath>` 由 opencode-cli `prompt.ts:1230` 自动 readFile + base64 inline 给 LLM(0 临时 server / 0 R6 / 0 改上游)。**最终落地范围**:text + image + post 三 messageType(post = 图+文字混合);**vision 预检**:收图前查 `model.capabilities.input.image`,不支持的 model 直接友好提示换模型,避免卡死"识别中..."(10min TTL cache)。**S1-S5 加固**:20MB 大小硬限 / 30s fetch timeout / mime 白名单(7 种 image)/ 路径越界 assert / error 不带 token。**测试**:I1-I5 + S1-S5 + U5 + U1 + U2-U4 共 57 新增 case(`parseMessageContent` / `extractVisionSupport` / `isVisionCacheFresh` 三个 helper extract);全包 574 pass / 0 fail / 16/16 typecheck / 0 R4 / 0 上游侵入。**5 个 fix follow-up 踩坑沉淀** changelog:① `/images/` 端点对 user 上传图返 400(必用 `/messages/{id}/resources/`)② post messageType 不是 image(图文混合默认走 post)③ vision capability ≠ attachment ④ plugin → pipeline → downloader 循环 import(downloader 自己重算 IMBOT_WORKSPACE 路径)⑤ sidecar binary 时间戳陷阱;Medium 1.5d / 1687 行 / 8 文件;留 backlog:merge_forward(合并转发) / file / audio / video / sticker / 多图聚合 |
| [md-editing-iter-3](./md-editing-iter-3/) | done | MD 编辑态语义样式增强(白领基线,2026-05-25,iter-1/iter-2 后第 3 迭代,14 commits,6 轮矫正)。**最终 copy 方案**:GitHub MD CSS heading 比例(h1=2em / h2=1.5em / h3=1.25em,公众号 / 知乎 / 简书 / Notion 训练 10 年)+ iA Writer opacity 0.7 标记符弱化 + GitHub Primer 单蓝 accent 仅给链接(`var(--text-interactive-base)`,跟 markdown.css 预览侧统一)+ inline code 无 chip(源模式靠反引号自识别)+ Strikethrough 划线 + heading 无下划线。**矫正链 6 轮**:② var(--primary) 未定义 bug + Option A 现代办公配色;③ 删 monospace chip 反 fenced code block token 灾难;④ 加 strikethrough + Prec.high 尝试;⑤ 去 default `fallback:true` + Prec.high 翻 CSS cascade 反 default heading/link underline;⑥ 扩 codeLanguages(Python/SQL/JSON/YAML/CSS)+ HTML 解决 user 反馈"代码块都该有高亮"割裂;⑦ Cmd+Shift+E keybind workaround Tauri WebView + SolidJS Portal 互操作限制(GUI 自测 Workaround)。**自测路径完整**:Playwright e2e 视觉断言 + 滚动 tour(12-22s)+ 真 .app + Python Quartz CGEvent 滚动 + screencapture 兜底(20 张视觉档案 `/tmp/iter3-final-shots/`)。Medium ~+800 行 / 9 文件 / 4 单测 H1-H4 / 16/16 typecheck / e2e 全过 / 真 .app 视觉验证 PASS / **2 R4**(@lezer/highlight + 6 个 @codemirror/lang-*)。8 语言完整高亮(JS/TS/Python/SQL/JSON/YAML/HTML/CSS);Bash/Diff/Mermaid 单色(无官方 lang-* 已知边界)。10 个深坑沉淀到 3-changelog 经验段(用户画像优先 / CSS 变量验证 / lezer-markdown tag 范围 / CM6 fallback 全或无语义 / CSS cascade 与 Prec.high / e2e 断言阈值 / mock vs prod 差异 / Tauri WebView Portal 限制 / vite minifier 反转 / cliclick t: vs kp:)|
| [imbot-workspace-rename-followup](./imbot-workspace-rename-followup/) | done | imbot-workspace-rename 后 follow-up:user 升级后老 chatSessionStore 里 session ID 还绑老 directory(`feishu-workspace`)→ LLM 用老 system prompt + 老 cwd → emit ATTACH 用老路径 ENOENT。修法:plugin 启动时 `applyStaleSessionsCleanup` 一次性清 chatSessionStore(marker 文件 `.imbot-workspace-rename-cleanup-applied` 幂等),让 plugin 重建 session 用新 directory。Trade:user 失去 multi-turn memory(one-time)。Medium- 净 +280 行 / 2 文件 / 6 单测 T1-T6(5 mock + 1 真 fs)/ 517 测试 0 fail / 16/16 typecheck / 0 R4 / 0 上游侵入 |
| [imbot-workspace-rename](./imbot-workspace-rename/) | done | home base 路径 `~/.opencode/feishu-workspace/` → `~/.opencode/imbot-workspace/` — 对齐 ADR "imbot 跨 IM 共享 home base" 语义,未来 telegram/钉钉 接入时不再叫"feishu-workspace" 困惑。**改名 only**(其他 feishu-*config/sessions/server/dedup 真 feishu-specific 保留)。`migrateLegacyWorkspace` helper 启动时自动 mv 老路径(原子 rename),T1-T6 6 case 全覆盖(5 mock + 1 真 fs)。Medium 净 +180 行 / 511 测试 0 fail / 16/16 typecheck / 0 R4 / 0 上游侵入。配套 ADR 同步路径(OPENCODE-PLAN 仓 separate commit)|
| [im-account-agent-workspace-binding](./im-account-agent-workspace-binding/) | **rejected-superseded** | 🛑 **已否决,不实施**(2026-05-25)。研究阶段做完代码审计(硬编码真 9 处而非 7,line 号漂移 ±40,ChatSessionStore 路径变了,enableAutoGroupCreate 已删),user 跟架构师角色讨论后**全盘推翻**原 per-account workspace 绑定方案。**取代架构**:所有 IM 共享 home base workspace + 唯一 `imbot` agent,定制能力放 `<home-base>/.opencode/agent/imbot.md` 单文件,**0 代码改动**。否决理由 6 个(抽象层错位 / 1:1 锁死 / user-facing primitive 错位 / systemPrompt 不可靠 / GUI 选 agent 是安全漏洞 / 解决未验证需求)。1-spec 保留作审计快照。详 [`OPENCODE-PLAN/架构决策/im桥接-imbot单一架构.md`](../../../OPENCODE-PLAN/架构决策/im桥接-imbot单一架构.md) + 配套 user 指南 [`docs/governance/imbot-定制指南.md`](../governance/imbot-定制指南.md) |
| [feishu-group-new-cmd-and-mention-rename](./feishu-group-new-cmd-and-mention-rename/) | done | 飞书桥接"高级能力"面板全面整顿(2026-05-25):① `/new` 群里启用条件 — 私聊永远允许,群里只在 `requireMention=false`(免@ 模式)时允许,清 session 含"影响所有成员"提示 ② GUI checkbox 语义反转 + 默认改关 — "群里需要 @ 后再响应"(默认勾)→ "允许 AI 免@ 读取群里所有信息"(默认不勾),i18n key 改名 `requireMention.*` → `allowReadAll.*` ③ **彻底删 `enableAutoGroupCreate` 死开关全栈**(`feishu-group-slash-command` 后已无引用):config-schema / store / server / Tauri AccountSummary / Wire types / GUI utils / dialog checkbox / i18n key 全删,换 1 行 info paragraph 说明 `/group` 用法。Medium 净 -2 行 / 16 文件 / 505 测试 0 fail / 16/16 typecheck / Rust cargo check 过 / 0 R4 / 0 上游侵入 |
| [feishu-group-slash-command](./feishu-group-slash-command/) | done | 把建群从自然语言改成 `/group <群名>` 显式命令 — 删 LLM marker 路径 + 缩窄自然语言 regex 为白名单短语(中文 14 + 英文 4)+ 查询后缀排除兜底 + 命中降级为引导文案 + 群聊禁用一致 `/new`;`/group` 路径 0 调 LLM 解耦 provider(claude-code / imbot / default 都一样能建群);删 `parseCreateGroupShortForm` 半结构化路径。Medium 净 -378 行 / 505 测试 0 fail / 16/16 typecheck / 0 R4 / 0 上游侵入 |
| [upstream-rename-sst-to-anomalyco](./upstream-rename-sst-to-anomalyco/) | done | forward-looking docs 统一改 `sst/opencode` → `anomalyco/opencode`(2026-05-24 user 拍板对齐:sst 已 transfer 到 anomalyco,GitHub 自动 redirect,以后讲"opencode 官方/上游"一律指 anomalyco)— 27 文件 / 净 +2 行(主要 1:1 文本替换 + CLAUDE.md 1 行历史注);历史 archive / 旧 feat changelog / `docs/STATUS.md` / `改动日志.md` / `docs/installer-versions.md` 保留原 sst 字眼作为历史快照不动;memory 加 `reference_upstream_opencode_owner.md` 防未来 agent 再犯"二级 fork"误判 |
| [e2e-pre-push-gate](./e2e-pre-push-gate/) | done | 测试纪律自动化最后一公里 — `.husky/pre-push` 在原 bun 版本 + typecheck 后追加 Phase 1 e2e gate 段:**只在 push 含 main 时触发**(feat 分支 push 仍只 typecheck,不付 ~24s e2e 成本);vite mock 必须已在 :3000(不自动 spawn,避开 Win Git Bash 跨平台 pid 管理坑,fail 时清楚指引);通过 `git push --no-verify` + `[override-pre-push: 理由]` tag 跳过;3 种场景手动验证全过(无 vite+main→fail / 无 vite+feat→skip e2e / vite+main→11 passed 24.1s)|
| [e2e-vite-warmup](./e2e-vite-warmup/) | done | R5 v4 flaky 48h 内修首次履约 — vite mock 冷启动 + Playwright 多 worker 并行 → 第 1 批 2-3 spec timeout 偶发 fail;双保险方案:① `vite/e2e-mock.js` server.warmup.clientFiles 8 个核心 SolidJS 组件(启动时主动预编)② 新建 `e2e/global-setup.ts` 跑测试前真 chromium 加载 `/` 一次让 module 编完(`playwright.config.ts` 接 globalSetup);4 次连跑(含真冷启动)全 11 passed / 0 flaky / 20.4-24.0s;Tiny+ 58 代码 + 80 文档;同时删 `OPENCODE-PLAN/需求池/e2e-vite-cold-start-flaky.md` backlog(闭环)|
| [e2e-bug-repro-3case](./e2e-bug-repro-3case/) | done | Phase 1 e2e 首批 bug-repro 示范用例落地 — A6 large-file-preview-guard(100MB+ txt → FileTooLarge 卡 + 2 按钮)/ A5 chat-drop-overlay-stuck-fix(dragover 激活 → drop+stopPropagation → window capture-phase 兜底清 overlay)/ A4 auto-save-debounce-flush(D1 降级路径:invoke write_text_file → memfs 同步 + mtime 自增 + 0 误 toast + mtime 冲突保护);实施中暴露 Phase 1 fixture infra 4 bug(mock timing / shape 转换 / memfs.list root prefix / Playwright glob query string)全数顺手修;~640 行(266 代码 + 24 fixture/memfs + 350 文档),Medium 4h 实施;全套 e2e 11 passed / 1 skipped / 14s,0 回归;R5 v4 bug-repro 提级到 Phase 1 e2e 的首批履约 |
| [e2e-phase1-mock-mode](./e2e-phase1-mock-mode/) | done | 自动化测试 Phase 1 基础设施落地 — Vite mock plugin(条件激活 `--mode e2e-mock`)+ 内存 fs MemFS(170 行,mtime 严格自增 + watcher event + cross-process via `window.__deskfoxE2eMemfs`)+ Tauri invoke dispatch 22 命令(fs 11 / 外部 app 3 / 飞书 8 stub + override 表)+ bootstrap mock(4 query:config / provider / path / project)+ workspace entry + 7 个 Playwright fixture helpers + `mock-foundation.spec.ts` 端到端 smoke 8/8 assertions;**治理同步升 R5 v3→v4**(Medium feat 强制 ≥ 1 Phase 1 e2e / bug-repro 提级到 reactive 层 / View 清单硬门槛即时生效);1280 行新代码 + 5.5 工作日实际(原估 15-21d,节省 10-15d);Stage ② 5 spec 全过 + 0 回归 + 10.5s 全套(<2 min 远超 A1)|
| [auto-save-debounce-flush](./auto-save-debounce-flush/) | done | 文件查看器编辑态 debounce auto-save + flush(REQ-001 dirty tab 修复,Obsidian 风混合方案 C)— 1s debounce 后自动落盘;`<Show keyed>` 切 tab unmount 时 `onCleanup` flush(snapshot 模式+`setStoredContent` 同步更新 store 避免 race);窗口关 emit `deskfox-flush-before-close` event 前端转 DOM custom event flush;`markSelfWriting` 500ms 窗口拦自写触发的"AI 修改了此文件"误 toast;9 单测 |
| [abandon-cloud-build-workflows](./abandon-cloud-build-workflows/) | done | 云端 build workflow 永久废止 — 3 个 release workflow yml(`release-deskfox.yml` / `release-mac-deskfox.yml` / `release-mirror-gitee-deskfox.yml`)从 "DISABLED 2026-05-11 待复活" 改 "OFFICIALLY ABANDONED 2026-05-21";所有 ship 走唯一本地路径(pack-installer → commit bump → tag → push → gh release create [--prerelease] → Gitee API → mirror-asset-to-gitee);治理 doc §四/§五/§六/§七 全面更新 |
| [ship-scripts-naming-fix](./ship-scripts-naming-fix/) | done | ship 脚本 4 个对齐新命名规则(installer-naming-cleanup 后续) — `pack-installer.{ps1,sh}` path echo / .dmg rename 用 NumericVer(strip env suffix);`mirror-asset-to-gitee.{ps1,sh}` tag regex 加 `dev` 识别(Tier 2 预览版),Mac 顺手补 asset 自动定位。本次首次跑 Tier 2 ship 时暴露的 4 个脚本 bug |
| [installer-naming-cleanup](./installer-naming-cleanup/) | done | installer 文件名去重 — `OutputBaseFilename` 模板用 `NumericAppVersion`(strip env suffix)替 `AppVersion`,Tier 2/beta 产物从 `DeskFox-Dev-2026.5.21.1-dev-setup.exe` 简化为 `DeskFox-Dev-2026.5.21.1-setup.exe`;env 只通过 OutputBase 前缀体现,版本号保持纯数字;Tier 1 prod 产物名不变(历史一致);治理 doc §3.4 新增命名规则段 |
| [3tier-versioning-governance](./3tier-versioning-governance/) | done | 3-tier 发布渠道 + 版本号体系正式立规 — Tier 1 稳定版(prod 无后缀,GitHub Release latest)/ Tier 2 预览版(dev `-dev` 后缀,GitHub Release prerelease,可对外发)/ Tier 3 本地测试版(`-NoBundle` raw exe,不发布)。新治理 doc `docs/governance/版本号与发布渠道规范.md`(9 段 centerpiece)+ 5 处 cross-link(README × 2 / 应用身份-命名规则 / CLAUDE.md / pack-installer.{ps1,sh} 头注)|
| [rename-dev-to-main](./rename-dev-to-main/) | done | 主分支 `dev` → `main` 重命名(2026-05-21,与 installer channel `dev` 命名空间解耦)— Phase A 文档全面更新 + Phase B 实际 git 操作(local rename / push origin main / GitHub default branch / 删 origin/dev / Gitee 同步)全部落地 |
| [installer-version-env-suffix](./installer-version-env-suffix/) | done | 安装包版本号按 env 加后缀 + N 序列独立(B2 口径)— bump 脚本加 `-Env dev\|beta\|prod` 参数,prod 无后缀 `2026.5.21.1` / beta `2026.5.21.1-beta` / dev `2026.5.21.1-dev`;N 计数器**平台 × env 双维度独立**;Inno Setup `VersionInfoVersion` 用 ISPP `Copy(...)` strip 后缀给 PE header(N.N.N.N 数字格式);双端 parity(ps1 + sh)|
| [large-file-preview-guard](./large-file-preview-guard/) | done | 大文件预览统一防护(REQ-025)— 4 层防御:L1 入口闸门(`get_file_size` 新 Tauri 命令 + load() pre-check)+ L2 媒体改 localasset 不进 JS 内存(顺手修原图片"不可预览"bug)+ L3 阈值兜底(text/markdown/html 10MB / office 200MB / media+binary ∞ / default 100MB)+ L4 UX 兜底组件(用本机软件打开 / 打开所在文件夹);office 50MB→200MB 决议(常见 PPT 含图含视频);canEdit() 加 tooLarge 守卫防数据丢失;19 单测 |
| [chat-drop-overlay-stuck-fix](./chat-drop-overlay-stuck-fix/) | done | 拖拽接收浮层卡死修复 — file-tree 行 onDrop 调 stopPropagation 杀掉 document bubble drop 致浮层 setDraggingType(null) 不执行;window 级 capture-phase drop + dragend 双兜底,ONLY 清状态不参与 drop 处理;R5 bug-repro 测试先行(DOM 事件流前置假设)|
| [chat-input-focus-follow](./chat-input-focus-follow/) | done | 聊天输入框焦点跟随 — 聊天对话区右键 + MD/HTML viewer 选区"加入聊天"后光标到末尾 + 输入框 focus,user 可立刻继续打字(模块级 singleton ref + rAF;Tiny+ 4 文件 53 行) |
| [chat-selection-menu](./chat-selection-menu/) | done | 聊天对话区右键选区菜单 — 替换 WebView2 原生为 DeskFox 自家两项(添加到聊天 / 复制),输入面板跟文件查看器一致 + 红色 overlay 选区高亮;helper extract 模式 + 12 单测 |
| [file-tree-multi-drag-to-chat](./file-tree-multi-drag-to-chat/) | done | 文件树多选(Shift/Ctrl)拖到聊天窗口接通 — attachments 加 `application/x-deskfox-paths` MIME 消费,abs→rel 转换走 helper extract 模式 + 10 单测;0 R4 |
| [file-tree-llm-write-refresh](./file-tree-llm-write-refresh/) | done | AI 创建新文件后右侧文件树自动浮现 — `file.edited` watcher 加父目录刷新分支,修"loaded:true + expanded:false 缓存目录里新文件需 F5 才出现"bug;R5 bug-repro 测试先行 |
| [html-viewer-ux-polish](./html-viewer-ux-polish/) | done | HTML 预览 UX 优化 — 去顶部 toolbar + iframe 内右键桥接(postMessage)弹 DeskFox 菜单 + 左键点 iframe 内菜单消失 + 阈值 2MB→10MB + CodeMirror HTML 编辑高亮;R4 第 6 笔本季(超配,user 授权)|
| [html-viewer-allow-scripts](./html-viewer-allow-scripts/) | done | HTML 预览 iframe 开 `allow-scripts` — 解决 PPT/Slides 翻页按钮等内嵌 JS 失效问题(反转 md-office-improvements spec A1.9,跨 origin 论证 MDN 警告不适用)|
| [file-tree-dnd](./file-tree-dnd/) | done | 文件树拖放移动 — 全功能(核心+多选+剪切粘贴+撤销+外部拖入) |
| [getbot-接入](./getbot-接入/) | done | GetBot 模型聚合 Provider 接入(热门首位 + 推荐标 + 内置 apiKey 流程) |
| [规范-v2](./规范-v2/) | done | 三文档分离 + diff 阈值放宽 + 注册型扩展点出黑名单 |
| [加聊天-preview-fix](./加聊天-preview-fix/) | done | 文件查看器右键加聊天后选中文字未传到模型 — synthetic text 里带 preview |
| [查看器-自动刷新](./查看器-自动刷新/) | done | 模型 Edit/Write 后查看器不更新 — client 加 file.edited 监听 + 编辑态 dirty 守卫 |
| [禁自动升级](./禁自动升级/) | done | 关闭 opencode 官方自动升级入口和通道,防止 DeskFox 被覆盖 |
| [installer-打包](./installer-打包/) | done | DeskFox 打成 Windows installer(不签名)— Inno Setup 路线;顺手修 icon pipeline bug 让 exe 嵌入多分辨率狐狸 |
| [icon-pipeline-deep-fix](./icon-pipeline-deep-fix/) | done | Tauri winres 实只读 icons/dev/icon.ico、无视 prod.json override — apply-icons 同步覆盖 dev/ + 新设计资源全量更新 |
| [macos-打包](./macos-打包/) | done | DeskFox 打 macOS .app + .dmg(arm64,不签名)— Phase 1 scaffolding + Phase 2 user Mac 实战打通 |
| [claude-code-loop-fix](./claude-code-loop-fix/) | done | 修 claude-code plugin 选 Claude 模型时 step loop 不 break、UI 卡死 — 兜底块用 step-finish part 替代 finish 顶层字段(R4 override,case 1) |
| [build-pipeline-sidecar-fix](./build-pipeline-sidecar-fix/) | done | Windows build-deskfox.ps1 加 sidecar 自动 build 步骤 — 时间戳判断 + non-baseline 优先(绕 clash 代理下 bun-baseline 下载失败) |
| [plugin-cwd-channel](./plugin-cwd-channel/) | done | opencode 加 _opencode providerOptions namespace 暴露 cwd 给 spawn-based plugin(R4 第 3 笔本季特批,不扣下季度配额) |
| [installer-versioning](./installer-versioning/) | done | installer 版本号规则 YYYY.M.D.N(年.月.日.当天第几版)+ bump 脚本 + pack-installer 一键脚本 + docs/installer-versions.md 版本日志 |
| [md-viewer-typography](./md-viewer-typography/) | done | 文件查看器看 .md 时排版升级 — 标题阶梯(字号+加粗双轨,h1/h2=700,h3-h6=600)/ 行内代码芯片 / 引用块底色 / HR 显形 / 表头底色,scoped 在 data-context="file-viewer" 下,聊天侧零影响 |
| [bump-script-encoding-fix](./bump-script-encoding-fix/) | done | bump-installer-version.ps1 加 UTF-8 BOM 修中文 placeholder 乱码(PS 5.1 源解析陷阱)|
| [macos-右键选区-修复](./macos-右键选区-修复/) | done | macOS WKWebView 文字上右键选区丢失 — 选区历史栈挑最长 + overlay div 替代 stale 的 CSS Custom Highlight + 关闭菜单时统一清原生/Pierre 选区 |
| [加聊天-option-enter](./加聊天-option-enter/) | done | 文件查看器"+ 添加到聊天窗口"对话框 macOS 加 Option+Enter 提交快捷键 + 底部提示文案平台化(Tiny) |
| [macos-pack-installer](./macos-pack-installer/) | done | macOS 一键打 .app/.dmg 脚本(pack-installer.sh + bump-installer-version.sh)+ icon.icns 加 .gitignore + 4 个 sh +x 权限(Tiny) |
| [office-installer-macos](./office-installer-macos/) | done | LibreOffice 自动安装 macOS 适配 — DMG 下载 + hdiutil 挂载 + cp -R 到 ~/Applications + soffice 检测路径(Tiny,fork-only,纯增量) |
| [prod-bundle-id-fix](./prod-bundle-id-fix/) | done | macOS prod / beta Bundle ID 独立 override(prod=ai.opencode.desktop,beta=...beta),修应用程序网格搜不到 .dev Bundle ID 应用的问题(Tiny) |
| [bundle-id-debrand](./bundle-id-debrand/) | done | 完整品牌切割 — Bundle ID 三档全去 `opencode` 字眼,改 `ai.deskfox.app` 系列(prod/.beta/.dev),与 anomalyco/opencode 上游 0 命名空间共享(Tiny) |
| [分支策略-v2](./分支策略-v2/) | done | dev 单一稳定主干 + 上游同步分离 + 三档环境正交分支 — feat → dev 一次性 merge 切换(`fae01d2a8`,187 文件 / +19721 行),废除 4.3 节"禁止直 push 到 dev",origin 双 push 临时改单推 gitee |
| [win-tri-env-appid](./win-tri-env-appid/) | done | Windows 三档 AppId 同机共存 — `DeskFox.iss` 加 `#if AppEnv` 切档(beta `{86413DCA-...}` + dev `{4C5D29F2-...}` 新生成,prod GUID 锁死不变);`pack-installer.ps1` 加 `-Env` 参数;Mac/Win 三档共存能力对齐 |
| [数据目录-deskfox-隔离](./数据目录-deskfox-隔离/) | spec | DeskFox 与上游 opencode 共用全部数据目录(auth/sessions/config/cache/install_id),两档实施:Phase 1 install_id 独立(fork-only,3 行)/ Phase 2 全量隔离(改上游 R3);从隐私协议 v0.5 待办挪入。**2026-05-04 评估暂搁(deferred)** — 当前主受众非工程并行使用场景,99% 用户 0 收益;触发条件(并发冲突 / schema 破坏 / 商业化 / 10w+ 用户)未出现前继续 backlog,详见 1-spec.md "评估决策" 段 |
| [post-sync-build-fix](./post-sync-build-fix/) | done | sync/upstream-2026-05-02 merge 后两个 latent build bug 浮出 — Windows symlink 落空(`.d.ts` 转真文件 + triple-slash reference,R4 override)+ Bun baseline runtime ~190MB 下载绕过(build-deskfox.ps1 直接 build --single 跳 predev.ts) |
| [dev-typecheck-fix](./dev-typecheck-fix/) | done | dev typecheck 555 错根因 — sync abort 后 node_modules symlinks 没 rollback 到 lock,同 codebase 看到两份 effect 类型;`bun install` 修(0 代码),并把 abort 后必跑 reconcile 写进 UPSTREAM-MERGE-GUIDE §5.0 / §7 / TL;DR |
| [zod-schema-bridge](./zod-schema-bridge/) | done | 上游 sync prep — office-pdf-ref 从 schema enum 抽离到 fork-only vendor MIME 协议(`application/x-deskfox-pdf-ref`),producer + consumer 都改走 `@opencode-ai/shared/office-pdf-protocol`;`Content` schema 上 fork 字段值依赖归零,以后上游改 Content 0 冲突;R4 override 第 2 笔本季 |
| [updater-disable-adapter](./updater-disable-adapter/) | done(rolled back) | 上游 sync prep — 禁自动升级走法从"条件 spread 藏 UI"改成"上游 sentinel + menu 灰显"风格;**翻车** UX 回归(立即检查按钮变可点击 + 假 toast),被 `updater-disable-adapter-rollback` 撤回;constants.rs 注释更新保留 |
| [updater-disable-adapter-rollback](./updater-disable-adapter-rollback/) | done | 撤回 `updater-disable-adapter` 的 createPlatform sentinel + menu 灰显改动,恢复 conditional spread 模式(method undefined when off → controls 自动 disable);constants.rs 注释保留;UPSTREAM-MERGE-GUIDE §4.4 表格 + 教训段沉淀:**改 method 暴露策略前先 grep `disabled={!platform.<method>}` 类多重信号 callsite** |
| [sync-2026-05-03-aborted](./sync-2026-05-03-aborted/) | done | 上游 sync 实战 abort 复盘 — 8 个 conflict 全 resolve 后撞 SDK 双路径互斥(httpapi vs hono 二选一);过程中踩 `bun.lock` 自由 resolve 坑(`poe-oauth: *` → mcp-oauth bug);全 lessons 沉淀到 UPSTREAM-MERGE-GUIDE §4.7(lock 处理方法学)+ §7(2 行新踩坑)+ TL;DR(2 条);决策走 office-routes-effect-httpapi(下笔 spec) |
| [office-routes-effect-httpapi](./office-routes-effect-httpapi/) | done | fork 4 个 office routes(`/file/office-pdf` + `/office-tooling/{status,install,progress}`)从 Hono 迁到 Effect HttpApi PublicApi,httpapi-mode SDK 含 fork office method;实施在 sync-2026-05-03-2 期间(新建 `file-office.ts` 集中 schema + groups/handlers FORK block);老 Hono routes 暂留(共存,follow-up 删) |
| [win-bun-install-fix](./win-bun-install-fix/) | done | Windows `bun install` 不再被 `tree-sitter-powershell` postinstall 阻断 — 上游 `trustedDependencies` 列了它但 fork+upstream 都只用 wasm(native binding 0 用),且 `tree-sitter-powershell@0.25.10` 不发 Win prebuilds → 必走 fallback 编译 → Windows 没 VS Build Tools 报错。fix:root `package.json` 删该 entry(R4 第 3 笔本季,超 1 笔配额,user 明确授权);typecheck 15/15 + DeskFox.exe build 通过;pre-push 安全网恢复 |
| [sync-2026-05-03-2](./sync-2026-05-03-2/) | done | 本季 sync 实战成功 — 462 commits / 1157 文件 / +58k/-53k merge 完成;早上 abort 的 SDK 双路径互斥被 office-routes-effect-httpapi 解锁;`bun.lock` 走新 §4.7 take theirs 绕开 mcp-oauth 坑;9 个 fork 文件现场调整(import path adapt + Instance→InstanceState.context 适配);typecheck 15/15 + DeskFox.exe build 通过;0 R4 |
| [release-mac-ci](./release-mac-ci/) | done | GitHub Actions 自动打 macOS .dmg(arm64,不签名)— 新增 `release-mac-deskfox.yml`,push `ship-mac-(prod\|beta)-*` tag 触发,与 Win workflow 独立;复用 mac 端现有 build-deskfox.sh + workflow 内自带 .dmg 重命名 |
| [repo-migration-deskfox](./repo-migration-deskfox/) | done | 主仓从 `yuesoue/opencode-for-office-deskfox`(无 fork 关系)迁到 `zoulukuang/deskfox`(真 fork from anomalyco/opencode;owner 名 2026-05-04 由 `yuesoue` 改名得来,详见 user-rename-zoulukuang);修品牌诚信 + 解锁上游 PR 工作流;dev + 全 tag push,4 处 docs URL 更新,老仓 dev 加 deprecation banner;Mac 端 origin 切换 / Gitee 镜像迁 / 老仓 archive 留 backlog |
| [user-rename-zoulukuang](./user-rename-zoulukuang/) | done | GitHub user 改名 `yuesoue` → `zoulukuang`(对齐 Gitee 用户名);本地 origin / origin-legacy URL 切到 zoulukuang;5 处活跃 docs URL 更新;老仓 deprecation banner URL 改名;**`yuesoue` 占位空号 2026-05-04 评估弃做**(项目小 / 老仓 0 stars/forks,squat 实际风险低,接受不防)(Tiny) |
| [gitee-release-mirror](./gitee-release-mirror/) | done | GitHub Release publish 后同步到 Gitee Release — **混合方案**:workflow `release-mirror-gitee-deskfox.yml` 镜像元数据(tag/name/body,30s)+ 本地脚本 `mirror-asset-to-gitee.ps1` 上传 .exe 附件(5.5s,76Mbps);3 次实测后发现 GitHub US runner 上行 Gitee CN 50MB 被 GFW 节流到 30min timeout,pivot 让本地 IP 干上传活;首次实测 `ship-prod-2026.5.3.1` 全链路通(workflow 创 release_id=669251 + 本机秒传 .exe);**2026-05-04 续笔**:补 Mac `mirror-asset-to-gitee.sh`(~230 行 bash,镜像 .ps1 逻辑,自动定位 .dmg + GitHub fallback 下载 + jq 解析,跨平台 stat,文件 mode 100755),Mac 协作者拉 dev 实测 `ship-mac-prod-2026.5.4.1`(GitHub draft 待 publish 后跑) |
| [readme-deskfox-自家版](./readme-deskfox-自家版/) | done | GitHub 主仓 README 从上游 OpenCode 完整重写为 DeskFox 自家版 — **英文主 `README.md` + 中文 `README.zh.md`**(2026-05-04 修订,因国外申请数字签名期间英文更合适;原计划中文主)— 匹配 deskfox.ai 站点定位 / 品牌 / URL;删 21 个上游 locale README;复制 1 个 logo SVG + 4 张产品截图(hero + PDF/PPT/视频 预览)到 `docs/assets/`;走 feat 分支迭代 + GitHub 上预览审稿(DeskFox 自家 markdown preview 不渲染本地图,backlog);merge dev 后删 feat 分支;后续修订英文化直接 dev |
| [actions-node24-bump](./actions-node24-bump/) | done | 两 workflow yml(release-deskfox + release-mac-deskfox)4 个 actions 升级 Node 20→24:`checkout@v4→v5` / `cache@v4→v5` / `upload-artifact@v4→v6` / `action-gh-release@v2→v3`;清掉 GitHub 6 月 2 日 force Node 24 前的 deprecation 警告;mac + win dispatch dev 双端实测全过(5m37s + 18m19s),Node 警告完全消失(Tiny chore,8 行 / 2 文件)|
| [filetree-ctrlc-textsel-fix](./filetree-ctrlc-textsel-fix/) | done | 修聊天气泡 / md 查看器选文本后 Ctrl+C 失效 — `useFileTreeShortcuts` 的 B 路径在中性区(activeElement=body)+ 文件树 selection 非空时误抢,`event.preventDefault()` 阻断原生 copy 但 `clipboard.setCopy` 又不写 OS 剪贴板,净效果"键失灵";加一道 `hasTextSelectionOutsideFileTree()` 闸,文本选区在文件树外时让原生走;A 路径与 B 路径设计意图(文件树焦点 / 文件树有选中无文本选区)均保留;Ctrl+X 一并修好(Tiny,fork-only,~25 行 / 1 文件,0 R4)|
| [file-tree-ux-polish](./file-tree-ux-polish/) | done | 文件树 UX 一揽子优化(5 项 / 5 笔 commit / Medium / 0 上游侵入)— ① 自动刷新(LLM busy→idle 边沿触发递归 refresh)② 节点右键菜单 4 组重整(删打印 + 加复制路径/刷新 + 文案去 .md 但默认值保留 markdown)③ 空白菜单 2 组重整 + 修刷新只刷根的递归 bug(新加 `refreshAllExpanded` 扫所有 expanded 子目录)④ 默认面板展开 + tab "all"(决议 D 不迁老用户)⑤ 键盘 ↑↓/Enter/F2/Delete/Backspace(macOS)— 复用现有 shouldTrigger gate;rebase 远端 ctrlc-textsel-fix 0 冲突 |
| [menu-i18n](./menu-i18n/) | done | 文件树菜单 / 对话框 / toast 接入 i18n 框架 — `fileTree.*` 命名空间 49 个 key(en/zh/zht 三本同步,其他 14 种自动 fallback en);file-tree.tsx + dialog-file-tree.tsx 全部硬编码中文 → `language.t()`;rebase 到 file-tree-ux-polish 后 audit 加减 key(删 print 加 copyPath/copyPathSuccess);Medium 324 行 / 6 文件 / 0 R4 / 0 上游侵入 |
| [viewer-ctrlc-fix](./viewer-ctrlc-fix/) | done | 文件查看器非 .md 内容(`@pierre/diffs` shadow DOM)Ctrl+C 失效修复 — 加 window-capture keydown 复用既有 `pickBestRecentSelection()` shadow-aware 选区机制 + `navigator.clipboard.writeText`;编辑态 / input / textarea / contenteditable 全让原生路径;Tiny ~36 行 / 1 文件 / 0 R4 / 0 上游侵入 |
| [imbot-permission-minimal](./imbot-permission-minimal/) | done | imbot v2 务实档(~30 条 ask)→ v3 极简档(8 条 ask)— read 只拦 .env/.ssh、bash 只拦 8 条真不可逆(rm -rf / git --force / aws s3 rb / aws ec2 terminate / dd/mkfs/fdisk / shutdown)、webfetch 撤回 allow,其他全沿用 build default;user 信任"飞书 IM 消息流可见"= 看得到 LLM 在做什么,把可逆操作信任度调高;Medium / ~60 行净 / 6 单测调整 + 2 新 / 0 R4 / 0 上游侵入 |
| [imbot-windows-delete-cmds](./imbot-windows-delete-cmds/) | done | v3.1 micro-patch — imbot v3 实测发现 Windows 漏洞:LLM 在默认 PowerShell shell 跑 `rm -rf` 时实际调 `Remove-Item -LiteralPath ...`(不是 unix rm),绕过 `bash["rm -rf *"]: ask` pattern,目录被真删。补 4 条 Win 风格 pattern(Remove-Item / rmdir / del / rd),覆盖跨 shell 调用;`rmdir *` 在 PowerShell 里是 `Remove-Item -Recurse` function alias 能删非空目录,从 v3 砍掉名单移回 ask;Tiny / 4 pattern + 3 测试调整 + 1 新测试 / 0 R4 / 0 上游侵入 |
| [md-office-improvements](./md-office-improvements/) | done | MD 查看器办公场景综合优化 — 一笔大 feat / 4 Phase / 8 项 scope:Phase 1 共建 Tauri protocol localasset:// + 本地图 + 音视频 + HTML 预览 / Phase 2 Frontmatter 隐藏 + Callout + 脚注 / Phase 3 Mermaid 动态加载(runtime 0 网络)/ Phase 4 MD 内链跳转 + 文件树焦点同步。吸收 6 个需求池条目;Large(~660 行净代码 / 11 文件 / 7 R4 override);新依赖 marked-alert + marked-footnote + mermaid;DeskFox.exe 32.27→34.79MB(+2.5MB);post-release 修 5 轮 user 反馈 + 4 项 polish 完成 |
| [md-editing-enhance](./md-editing-enhance/) | in-progress | .md 编辑体验增强(Tier B 全套)— 表 1+2 共 11 项编辑增强:列表续延(普通/编号/任务)+ Ctrl+B/I/K 格式化 + 拖图/截图自动 ![](path) + Ctrl+Enter 任务勾选 + 块引用续延 + Heading 折叠 + 智能 URL 粘贴 + 表格 Tab 跳格 + Ctrl+F/H 搜索替换(`@codemirror/search` 即插)+ 修保存后双提示框 bug(toast id 跟踪 + dismiss);Medium(~340 行净代码 / 5 文件 / **0 R4 override** — 全在 app 层);DeskFox.exe 34.79→34.79MB(+40KB);吸收需求池 obsidian-md编辑体验 + 保存后双提示框 + 保存后提示优化 三个条目 |
| [right-click-stale-selection-fix](./right-click-stale-selection-fix/) | done | .md 文件查看器右键残留旧选区修复 — `handleSelectionContextMenu` + `pickBestRecentSelection` 历史栈机制(2026-04-29 为 macOS WebKit Shadow DOM collapse 加的)被全局套到 light DOM 上,引入两个副作用:① 点空白后旧选区从栈里复活(`selectionchange` 不 fire "选空"事件)② 算法只比 length 不比 recency,选 A(长)→ 选 B(短)永远命中 A。fix:`renderMarkdown` 改挂新 `handleLightDomContextMenu`(直接读 `window.getSelection()`,无栈),`renderDefault`(Pierre Shadow DOM)路径一字未改 → Mac 零回归(Tiny ~30 行 / 1 文件 / 0 R4 / 0 上游侵入)|
| [setup-version-badge](./setup-version-badge/) | done | 设置面板左下角版本牌改 DeskFox 自家文案 + installer 版本号 — 原"OpenCode Desktop / v1.14.33"(走上游 i18n key + 上游 package.json `version` 字段)改为"DeskFox for Windows / v2026.5.6.2"(平台名根据 `platform.os` 选 Windows/macOS/Linux,版本号读新 fork-only `packages/branding/installer-versions.json`)。bump 脚本(`.ps1` + `.sh`)各加 step 3 — surgical regex 替换对应平台 key value(保留缩进/key 顺序),前端 `import` JSON 渲染。0 上游冲突(`package.json version` 字段不动 → 上游 sync 0 冲突);平台名 hardcode 不走 i18n(专有名词行业惯例不翻译);dead i18n key `app.name.desktop` 留 17 份字典里不删(上游 sync 兼容)。Mac bump.sh 待 Mac 端首次 bump 自然 trigger 验证(Tiny ~50 行 / 5 文件 / 1 新 JSON / 0 R4 / 0 上游侵入)|
| [md-export-pdf-word](./md-export-pdf-word/) | done | .md 导出 Word(.docx)— `@jinzhongjia/markdown-docx@1.0.4` + viewer 右键菜单 + 6 项极致优化(A1 代码块 / A2 Mermaid / A5 本地图 / B1 emoji / B2 错误友好 / C1 选区显菜单);v1 PDF 路线 drop(Tauri WKWebView `window.print()` silent issue #5330);v2 实测 4 笔根治(代码块切分 single-paragraph + 修 inline code 误并 / emoji 字体 fallback / Mermaid foreignObject patch + scale 3x + 5s 等待 / Replace 报错修);Medium(17 笔 commit / ~700 行净代码 / 1 R4 override — `bun.lock` 加 fflate dep)|
| [menu-always-show-with-disabled](./menu-always-show-with-disabled/) | done | .md 查看器右键菜单始终显示 4 项,选区相关项("添加到聊天窗口" / "复制")按 `mdMenu().text.trim()` 灰显 disabled,"编辑" / "导出 Word" 不依赖选区始终可用 — 替代之前的"选了文字 4 项 / 没选 1 项"两套行为;UX 一致性 / user 一眼看到全部能做的事;Tiny(1 笔 commit / 净 -12 行 / 0 R4 / 0 黑名单)|
| [md-context-menu-i18n](./md-context-menu-i18n/) | done | .md 右键菜单完整接入 i18n,跟随软件语言切换 — 之前只有"导出 Word"接 i18n,其他项硬编码中文(英文 locale 用户看到中英混杂)。加 5 个 `fileViewer.menu.*` key(addToChat / copy / input.placeholder / input.shortcutHint / input.submit)+ 复用 `common.edit` / `common.cancel`,zh/zht/en 三本字典同步,其他 14 种语言自动 fallback en;Tiny(1 笔 / 净 +29 行 / 4 文件 / 0 R4 / 0 黑名单)|
| [tests-md-to-word-integration](./tests-md-to-word-integration/) | done | **MD → Word 端到端集成测试**(基础设施实证 — 用 D 系列 + e2e 基础设施跑通 `exportMdAsDocx` 主入口完整流程,11 测试全 pass)。复用 D4 Tauri invoke mock + 加 saveDialog 控制 + showToast mock,真跑 markdown-docx / docx / fflate / 所有 fork helper。覆盖 11 场景:取消对话框 / 完整流程参数校验 / docx 是合法 zip(含 word/document.xml)/ defaultFileName 拼 .docx / i18n.title 传 dialog / invoke 失败友好错误(EACCES → 无写入权限 + [详细])/ **代码块 syntax 高亮 token 切分**(>const< 单 run)+ MdCode 段落样式 + `<w:br/>` 软换行 / **emoji `<w:rFonts>` Segoe UI Emoji 覆盖** / 无 mdFileDir 跳本地图 / 有 mdFileDir 走 read+write 双 invoke / mermaid 块无 viewerEl 不抛错。踩坑:syntax 高亮把代码切 token 不再连续字符串(改 assert `>token<`)。`md-export-docx.ts` 成为 Logic 清单**首个 helper + 集成双层覆盖**的范本。测试 531 → 543(+11 全 pass)| Medium ~230 行 / 1 文件 / 0 R4 / 0 上游侵入 |
| [e2e-mock-infrastructure](./e2e-mock-infrastructure/) | done | **e2e 基础设施 mock 路径建立 + 5 个真 e2e 跑通** — V2 双清单 View 清单门槛的前置依赖搞定。3 选 1 中选 Playwright `page.route` 拦截路径(不改前端 / 不依赖 sidecar / 跑得快)。新建 `e2e/fixtures.ts`(~50 行,`installServerMock` + `mockedPage` 扩展 fixture)拦截 4096 所有请求(/health 200 ok / GET 空数组 / POST 200 ok)。扩 `smoke.spec.ts`(baseline + mock 路径 2 测试,实测 body 渲染 345 字符,真 UI 出来)。新建 `i18n-sidebar.spec.ts`(3 测试):**实证 i18n drift 补全的真实 UI 渲染**(`No projects open` / `Open a project to get started` / `Getting started` / `free models`)+ 0 fatal console error 守门。踩坑 networkidle 永不 idle(改 domcontentloaded + 2s wait)+ strict mode 多 match(`.first()`)。**V2 双清单逻辑闭环完成** — Logic 严格守门 / View 基础设施已建门槛可生效。e2e 6 总 / 5 pass / 1 skip(上游 todo.fixme)/ 18.5s | Medium ~190 行 / 3 文件 / 0 R4 / 0 上游侵入 |
| [governance-test-dual-list-c](./governance-test-dual-list-c/) | done | 测试治理规范升级到**双清单**(选项 C)— D 系列实施完成后 user 选 C(B 仅记录数字 / A 修单清单语义 / **C 双清单**)。决策 2 单清单 → Logic 清单(行覆盖 80%)+ View 清单(e2e ≥ 1 happy path)。原 4 文件分配:Logic 4(`md-export-docx` / `markdown-editor-extensions` / `dialog-settings-version` / `file-tabs-helpers`)+ View 2(`dialog-settings.tsx` / `file-tabs.tsx`)。**helper extract 模式正式入规则**(组件抽出 helper 进 Logic / 原组件留 View)。**View 清单硬门槛延后**(等 e2e 基础设施 setup 完成,opencode sidecar 或前端 mock mode);承认基础设施未 ready 但清单已建,长期逼着 e2e 走通(否则 View 清单永远空话)。CLAUDE.md R5 v3.1 + governance v2 + 需求池长期规划 v1.2 同步修订(Tiny ~120 行 / 3 文件 / 0 R4 / 0 上游侵入)|
| [tests-codemirror-fixture-d3](./tests-codemirror-fixture-d3/) | done | **D 系列第 2 笔(D3)** — CodeMirror Mock View + 6 Command + handlePasteHook 测试。**关键洞察**:Command 只用 `view.state` + `view.dispatch`,不需真 EditorView(避开 happydom 兼容)。Mock view 工厂用 `EditorState.create + simplified dispatch`,~30 行 setup 解锁全部 Command 测试。export 6 个 Command + handlePasteHook,新建 `markdown-editor-extensions-commands.test.ts`(~310 行 / 36 测试):toggleBold/Italic 各 2 / insertLink 2 / toggleTaskCheck 8 含 Tier B 空 `[]` 放宽 + 缩进 + 多行光标定位 / continueList 9 含编号递增 + 空 bullet 退出 + 选区返 false / tableTab 5 / handlePasteHook 8 含 URL+preventDefault+stopPropagation 校验 + uri-list 多行 # 注释跳过 + clipboardData null 防御。`markdown-editor-extensions.ts` 估算覆盖率从 ~17% 升到 **~75%**(接近 80% 门槛,剩 3 个异步路径 readFileAsBase64/handleImageDrop/主入口)。测试 437 → 473(+36 全 pass)| Medium ~310 行 / 2 文件 / 0 R4 / 0 上游侵入 |
| [tests-tauri-invoke-mock-d4](./tests-tauri-invoke-mock-d4/) | done | **D 系列第 1 笔(D4)** — Tauri invoke mock 模式建立 + `md-export-docx.ts` 推到 100%。新建 `md-export-docx-inline.test.ts`(~180 行 / 22 测试,单独文件因 `mock.module` 污染)用 `mock.module("@tauri-apps/api/core")` + `invokeImpl` 可变状态 + `invokeCallLog` 断言模式;覆盖 `inlineLocalImages` 早返回(3:mdFileDir 缺/空 md/纯文字)+ 外链跳过(8:http/https/data:/blob:/file:/localasset:/协议相对/锚点)+ 本地替换(9:成功/title保留/不识别扩展/**invoke 失败保留原文**/**percent-encoded 解码**/多张并发/混合外链+本地/absolute 路径/invoke 参数 root="")+ alt 保留(2:空/中文)。`md-export-docx.ts` 8/8 helpers ≈ 100%(关键模块清单首个 100%)。mock 模式可复用到后续 D3 异步路径。测试 415 → 437(+22 全 pass)| Tiny ~180 行 / 2 文件 / 0 R4 / 0 上游侵入 |
| [tests-markdown-editor-extensions](./tests-markdown-editor-extensions/) | in-progress | `markdown-editor-extensions.ts` 易测部分覆盖(R5 决策 2 关键模块清单第 2 个文件)— **未达 80% 门槛**(实际 ~17% 行覆盖,**透明报告**)。给 7 个 helper 加 export(LIST_PATTERNS/TASK_PATTERN/URL_PATTERN/IMAGE_EXT_PATTERN/timestampName/depthOf/PHRASES)+ 写 42 测试(LIST_PATTERNS 4 子模式 12 / TASK_PATTERN 含 `[]` 空放宽 4 / URL_PATTERN 4 / IMAGE_EXT 3 / timestampName 5 / depthOf 8 含 Win 反斜杠 / PHRASES 4 含 zh-zht 简繁差异校验)。剩 11 项(6 个 CodeMirror Command 需 EditorView fixture / 3 个异步 IO 需 Tauri invoke mock / 主入口 markdownEditorExtensions)转独立 backlog,需 setup CodeMirror 测试基础设施 + Tauri mock。测试 373 → 415(+42 全 pass)| Medium ~280 行 / 2 文件 / 0 R4 / 0 上游侵入 |
| [tests-md-export-docx-coverage](./tests-md-export-docx-coverage/) | done | `md-export-docx.ts` 覆盖率推到 80% 门槛(R5 决策 2 关键模块清单**首个达标**)— 给 4 个 helper(`mergeCodeBlockParagraphs` / `base64ToBytes` / `bytesToBase64` / `splitRunsForEmoji` / `patchForeignObjects`)加 export 注解,扩展测试 +26(base64 5 / emoji 7 / mergeCode 7 含 inline code bug 防护 / foreignObject 7 用 happydom DOMParser SVG fixture)。覆盖 7/8 helpers ≈ 87.5%。剩 `inlineLocalImages` 转 backlog(需 Tauri invoke mock)。测试 347 → 373(+26 全 pass)。3 个关键模块(markdown-editor-extensions / dialog-settings / file-tabs)继续 backlog,单独 feat 推进 | Medium ~210 行 / 2 文件 / 0 R4 / 0 上游侵入 |
| [e2e-smoke-探路](./e2e-smoke-探路/) | done | Playwright e2e 架子探路 + smoke baseline 建立 — 链路验证(vite dev server + chromium headless),baseline 只测"链路通"(HTTP / HTML / URL),**故意不测 UI 内容**(web 版需要 opencode server 跑在 4096,无 sidecar 时 body 为空)。踩坑:首次跑报 chromium 没装(`bunx playwright install chromium` 修);body 内容为空(降级 baseline)。新增 `e2e/smoke.spec.ts`(~30 行 / 1 测试 pass,10.2s)+ `e2e/README.md`(~70 行,现状 / 跑法 / 后续 3 选 1 接入路径 A 启 opencode-server / B mock mode / C fixture replay)。后续接入路径转独立 backlog,推荐 B(mock mode)先做。0 上游侵入(不动 todo.spec.ts / playwright.config.ts)(Tiny ~100 行 / 2 新文件 / 0 R4)|
| [i18n-history-drift-补全](./i18n-history-drift-补全/) | done | zh/zht 历史 i18n 漂移补全(`tests-mac-recent-feats` feat 揭露)+ 守门升级到全字典 — 23 key 缺失:command.project.* / session.child.* / session.review.noBranch* / settings.general.section.advanced / settings.general.row.shell.* (4) / settings.general.row.show{FileTree,Navigation,Search,Status,Terminal}.* (10) / sidebar.empty.* (2)。zh/zht 各加 23 行翻译跟随已有风格(zh 用"会话/项目/默认"/ zht 用"工作階段/專案/預設"等),`i18n-completeness.test.ts` 守门范围从"关键 namespace(fileViewer/common)"升级到**全字典 100% 覆盖** + 加 dead key 检查;以后任何 sync / feat 加 en key 不补 zh/zht → CI 立即 fail。测试 346 → 347(+1 dead key 守门测试)(Medium ~100 行 / 3 文件 / 0 R4 / 0 上游侵入)|
| [tests-mac-recent-feats](./tests-mac-recent-feats/) | done | 为最近 3 个 Mac 端推的 feat 补 unit 测试(R5 测试纪律首笔实施 / 测试金字塔 70% unit 起步)— `md-export-pdf-word` + `menu-always-show-with-disabled` + `md-context-menu-i18n`。新建 `i18n-completeness.test.ts`(7 测试,关键 namespace `fileViewer.*` / `common.*` 守门 zh/zht 100% 覆盖 en + 11 个核心 key 显式断言 + shortcutHint 模板校验)+ `md-export-docx.test.ts`(19 测试,`mimeFromPath` 9 种扩展名/大小写/Win 反斜杠/URL 风格/边界 + `friendlyError` 9 类常见错误中文化)。`md-export-docx.ts` 加 2 处 export 提升可测性(0 行为变化)。**揭露真实问题**:zh / zht 缺 en 上游 ~20 个非关键 key(转 backlog `i18n-history-drift-补全`)。测试增量 320 → 346(+26 全 pass);关键模块清单 `md-export-docx.ts` 覆盖率 0% → ~25% ⬆(Medium ~220 行 / 3 文件 / 0 R4 / 0 上游侵入)|
| [automation-test-governance](./automation-test-governance/) | done | 自动化测试治理规范固化(**只定纪律不启动开发**)— user 拍板 1A 2B 3A 4A 5B 后:① 新 feat 必含至少 1 个测试(Tiny 例外)② 关键模块覆盖率 ≥ 80%(初版 4 文件:markdown-editor-extensions / dialog-settings / file-tabs / md-export-docx)③ 70/20/10 金字塔比例 ④ 修 bug 必先写复现测试 + 同 commit + `[bug-repro: ...]` tag ⑤ Claude 自审起步,后期接 mutation testing。新建 `docs/governance/自动化测试规范.md`(~280 行),CLAUDE.md 加 R5 测试纪律 + v3 修订记录;需求池长期规划保留 backlog 等 user 决定第 1 期启动时机(Medium ~300 行 / 2 文件 / 0 R4 / 0 上游侵入)|
| [build-script-skip-baseline](./build-script-skip-baseline/) | done | Mac `build-deskfox.sh` 跳 Bun baseline runtime 下载 — 跟 Win `build-deskfox.ps1`(2026-05-02 commit `696bbcc00`)同等修法补 Mac 侧。原本调 `predev.ts` 走 `build --single --baseline`,Bun.compile 拉 ~190MB `bun-darwin-arm64-baseline` 运行时,2026-05-07 GitHub Actions macos-latest runner build 卡 57 分钟触发 1h 超时,`ship-mac-prod-2026.5.7.1` 0 release 产出。改成 `cd packages/opencode && bun run build --single`(无 `--baseline`,复用本机 bun runtime 零下载)+ `RUST_TARGET → BUILD_DIR_NAME` 映射 + cp 到 sidecar 路径(commit `8f22c4a27`);Tiny(1 笔 / +27 / -6 / 1 文件 / 0 R4 / 0 上游侵入)|
| [claude-md-iron-rules](./claude-md-iron-rules/) | done | CLAUDE.md 加 dev 分支三条铁律(强约束,2026-05-08 立)— ① 永不直接 dev 开发(都开 feat 分支)② 所有 → dev 合并必经 user 同意 ③ 所有 dev → 远端 push 必经 user 同意。每层把关给 user 一次刹车机会。例外:开 feat 分支 / feat 分支内 commit / feat 分支 push origin agent 可自主。加在"默认仓库约定(分支策略 v2)"段开头作为顶层规则;Tiny(纯 doc / 1 文件 / +8 行 / 0 R4)|
| [md-export-word-iter-2](./md-export-word-iter-2/) | done | **MD → Word 导出第二轮迭代 — 全面保真度提升**:HTML 标签全转 Word 元素 / GFM Alerts 5 类彩色独立块 / blockquote 同段视觉一体 / 远程图片走 Tauri reqwest + sniff 字节头 + SVG 转 PNG / 表格全边框 + header 灰底 / mermaid 居中 / **数学公式走 LaTeX→MathML(KaTeX)→OMML(mml2omml)→Word 原生公式可编辑** / 字号行距 Word default / 目录 Ctrl+点击跳转(heading bookmark + #anchor 转 internal hyperlink + _rels 改) / `==高亮==` 黄底 / GFM emoji shortcode `:rocket:` 等 80+。viewer 侧也加 `<mark>` / emoji / heading anchor / 嵌套 link 渲染。Sentinel 占位 + 后处理 docx XML 双夹策略,15+ helper 全有单测(72 → 147)。后端加 `fetch_url_base64` Tauri 命令,加 katex + mathml2omml 2 个 npm 包(净 +1MB)。已知 deferred:`\int_0^∞` 等 nary 转 OMML 后 `<m:e>` 留空,Word 显占位框(库 bug,3 次修补失败,Backspace 可删)+ save dialog 默认按钮在"否"(Tauri-plugin-dialog 限制),详见需求池(Large ~2600 行 / 6 文件 / 2 npm 依赖 / 0 R4 / 0 上游侵入)|
| [md-editing-iter-2](./md-editing-iter-2/) | done | **.md 编辑体验第二轮基础体验补齐** — iter-1(`md-editing-enhance`)交付 Tier B 全套后 user 反馈 3 项基础空白:① 长行不软换行(出水平滚动条)② 选区底色不明显(--surface-raised-stronger 跟当前行色阶接近,看不出选中没)③ 写到第几行不知道。fix:加 `EditorView.lineWrapping` + `drawSelection()` extension(行尾空白也绘连贯)+ index.css 选区色换 GitHub 蓝半透明 `rgba(56,139,253,0.35)`;新组件 `cm-status-bar.tsx` 显示行/列/选中字符数,`code-mirror-view` 加 `onCursorChange` prop + mount 初始回调,`file-tabs.tsx` 编辑态挂状态栏;i18n 三语补 `fileViewer.editor.statusBar.{line,col,sel}`;7 单测覆盖光标计算(空文档/单行/多行/中文/选区/跨行/反向)。AI 改写选区(原 spec 项 4)D4=B 拆出独立后续 feat。0 npm 依赖(drawSelection 来自既有 `@codemirror/view`),跨链路保证不影响 viewer 预览 + Word 导出(Tiny ~150 行 / 8 文件 / 0 R4 / 0 上游侵入)|
| [feishu-bridge](./feishu-bridge/) | done | DeskFox 飞书 / Lark 桥接 — system tray 常驻 + OAuth Device Flow 扫码绑定 + WSS 长连接收消息 + opencode plugin 跑 LLM → 回写飞书。X1 plugin 自带 server 架构(单 IM 简洁,N 个 IM 时再演进 channel registry);per-account model + bot 名后台刷新 + openId 脱敏 + 消息 ack reaction;37 笔 commit / 189 单测 / Phase 2 闭环 user 实测通过 |
| [macos-docx-viewer-fix](./macos-docx-viewer-fix/) | done | 修 Mac 端 .docx/.xlsx/.pptx 在文件查看器打不开伪 regression — 根因 `build-deskfox.sh` 只在 sidecar 不存在时 build,导致 packages/opencode/src 改动 3 周不进 sidecar binary;补上跟 Win 端 b9581b76e 同等的时间戳判断(Tiny,1 文件,0 R4,0 上游侵入)|
| [feishu-bridge-ship-packaging](./feishu-bridge-ship-packaging/) | done | 让飞书桥接真正进 installer — 之前 Mac/Win 都跑不了 fresh install(开发者手动改 user config 才能跑);bun build adapter-feishu-lark plugin → 单 3.4MB .js → tauri.conf.json bundle.resources 打进 .app/.exe → DeskFox setup hook 注入 user opencode 配置;双端 build script 同步;Medium,5 新文件 + 4 改动 / 0 R4 / 0 上游侵入 |
| [feishu-plugin-install-win-path](./feishu-plugin-install-win-path/) | done | 补完 `feishu-bridge-ship-packaging` Win 端实际仍跑不通的两个 bug — Win user config 路径选错(`dirs::config_dir()` 返 `%APPDATA%\Roaming\` 跟 sidecar 实读 `~/.config/` 不重叠;Mac 那笔 follow-up 基于"xdg-basedir 在 Win 用 APPDATA"的错误假设)+ plugin URL 反斜杠 + UNC `\\?\` 前缀(Node import / opencode plugin loader 不接受);抽 `to_file_url` helper + 4 单测;Win user 实测扫码绑定通(Tiny ~53 行 / 1 文件 / 0 R4 / 0 上游侵入) |
| [feishu-bridge-newuser-onboarding](./feishu-bridge-newuser-onboarding/) | done | 全新用户拿 .dmg 装完即用 happy path 加固 — A1 plugin 路径失效自愈(user 在 .dmg 挂载点双击后拖 Applications 不再卡死,5 unit test)+ A4 default model 缺失检测(前端 warning 卡片 + 后端 friendlyErrorReply 关键字识别 → 中文可操作指引,7 unit test)+ A3 .dmg 拖拽引导背景图(Swift CoreGraphics 脚本生成 660x400 PNG,显式 dmg layout);Medium,3 笔代码 + 1 笔 docs / 0 R4 / 0 上游侵入 |
| [feishu-bridge-empty-reply-ghost](./feishu-bridge-empty-reply-ghost/) | done | 修飞书桥接 5 条丢失 reply 两个独立 bug:① opencode agent loop 在工具调用 / 多步回复尾部追加 0-token 空 placeholder ghost,plugin 倒序找 last assistant 取到 ghost → 空 reply(占 4/5 case);② `promptTimeoutMs` 默认 5min 对长 agent 任务不够,7m18s 长回复触发硬超时 → plugin 返空(占 1/5 case)。修:抽 `findLastUsefulAssistant` helper 跳 ghost + timeout 5min → 30min;13 单测覆盖 7 场景;Medium ~25 行核心代码 / 1 测试文件 / 0 R4 / 0 上游侵入。Layer 2 message.updated 完成信号重写留独立 backlog |
| [feishu-installer-bundle-plugin](./feishu-installer-bundle-plugin/) | done | 补完 `feishu-bridge-ship-packaging` Win 端最后一个 ship-blocker — Inno Setup `.iss` `[Files]` 段没列 plugin/feishu-bridge/ 目录,prod installer 装完没 plugin/ → DeskFox 启动 resolve_plugin_dir 返 None → 飞书桥接 UI 显示"未启动"。加 2 行 Source 把 `plugin/feishu-bridge/{package.json,dist/plugin.js}` 打入 install dir;副带 bump 2026.5.10.1。教训沉淀:Tauri NSIS(默认/Mac)和 Inno Setup(本仓 Win 自定义)两套配置,资源声明要同步动(Tiny ~5 行 / 1 文件 / 0 R4 / 0 上游侵入)|
| [feishu-server-loopback-bind](./feishu-server-loopback-bind/) | done | 修飞书桥接 plugin server 默认绑 `0.0.0.0` 暴露 LAN 端口的安全 + UX 双 bug — `Bun.serve()` 没传 hostname 默认绑所有网卡,触发 Win Firewall 弹"Bun 是否允许公网访问"对话框 + 同 WiFi 任何人能扫到端口。1 行 fix 加 `hostname: "127.0.0.1"`(Tiny 5 行 / 1 文件 / 0 R4 / 0 上游侵入)|
| [network-bind-safety-guard](./network-bind-safety-guard/) | done | `feishu-server-loopback-bind` 教训沉淀 — CLAUDE.md 加 R6 网络监听安全规则 + `.husky/pre-commit` §4.5 闸,任何新增 `Bun.serve` / `*.listen()` 不显式 loopback 直接 commit block,例外走 `[network-bind-public]` tag override;测试文件豁免;实测假 `Bun.serve` 被 block + 安全版通过(Tiny 46 行 / 2 文件 / 0 R4 / 0 上游侵入)|
| [feishu-bridge-completion-signal-rewire](./feishu-bridge-completion-signal-rewire/) | reverted | Layer 2 重构尝试(dispatcher 切到 `message.updated` + `time.completed` 强信号)→ 实测发现根本误判 opencode 行为(一次 user prompt 产生**多条** assistant message,锁定 first 只发开场白)→ revert 回 Layer 1。沉淀教训 + Layer 2.1 设计方向。Medium 实施 ~270 行新 dispatcher + 17 单测 + 简化 pipeline,然后整体 revert(0 R4 / 0 上游侵入 / 净改动 0)|
| [feishu-bridge-system-prompt-disable-question](./feishu-bridge-system-prompt-disable-question/) | done | 修 LLM 调 `question` 工具反问用户后死锁 bug(飞书 user 看不到 GUI 对话框,agent loop 永远等)— promptAsync body 加 `system: <禁用 question 工具>` 让 LLM 不用反问类工具,信息不足直接答或写"请补充"。临时止血,真互动版(form 卡片 + synthetic message 注入)是 OpenClaw 对齐 roadmap #5,Large 后续做(Tiny 23 行 / 1 文件 / 0 R4 / 0 上游侵入)|
| [office-installer-mirror-cascade](./office-installer-mirror-cascade/) | done | 修 LibreOffice 自动安装"download failed: HTTP 403"用户机直接死的 bug — `pickFastestMirror` 只返 winner 一个,挂了无 fallback。改 `rankMirrors` 返完整排序列表 + `startInstall` 加 cascade 循环(任一 mirror 失败自动切下一个,5 个全失败才报错聚合);UI 切换瞬间显示"镜像 X 失败,改用 Y…"。User 反馈源自飞书桥接外另一台 Windows 机正式 installer 用户。Tiny 64/-32 / 1 文件 / R4 第 5 笔本季(超 3 笔配额,延续 office 引擎 fork-only override 链)/ 0 上游侵入 |
| [feishu-bridge-permission-card](./feishu-bridge-permission-card/) | done | 修飞书 user 任务被 opencode 权限 GUI 对话框卡死的死锁 — 拦截 `permission.asked` Bus event,渲染飞书 CardKit 交互卡片,user 在飞书显式选[允许一次/始终允许/拒绝],plugin 调 `client.postSessionIdPermissionsPermissionId` SDK 回写 opencode 解锁。**保持 user 显式批准 trust 边界**,非 auto-allow。点击后 delete+send 替换为已确认状态卡片(header 绿/红/灰)。findLastUsefulAssistant 加 parentID 约束防 reject 后回放前轮答案。Medium 7 文件 / +1198 行 / 51 单测 / 5 笔 commit(踩 4 坑陆续修)/ 0 R4 / 0 上游侵入 |
| [pack-installer-rebuild-step](./pack-installer-rebuild-step/) | done | 修 `pack-installer.ps1` 顺序错位 SOP bug — bump 跟 ISCC 之间没 rebuild exe,导致 installer 文件名(`.11.1`)跟 exe UI 显示版本号(`.10.1`)mismatch(`installer-versions.json` 经 vite **烧进 bundle**,bump 后不重 build 不传导)。加 step 1.5 自动调 build-deskfox.ps1 + 新 `-SkipBuild` flag(CI 场景手动跳)。Tiny ~25 行 / 1 文件 / 0 R4 / 0 上游侵入 |
| [win-ship-local-pack-switch](./win-ship-local-pack-switch/) | done | 停掉所有 GitHub Actions 自动出安装包 + Gitee 镜像 workflow(Win build / Mac build / Gitee mirror 三条),改"本地打包 + 手动上传 GitHub Release + API 创 Gitee release + 跑 mirror-asset-to-gitee.ps1"。决策起源 ship 2026.5.11.1 时 Win build 撞 sidecar build deadlock(50min stdout 静默 + 60min job timeout cancel)+ 顺手发现 Gitee mirror 长期 silent fail。Mac 端 user 拍板一起停(维持 CI 需持续投精力)。复活方式注释段内置(Tiny 85/-32 / 3 文件 / 0 R4 / 0 上游侵入)|
| [feishu-bridge-imbot-agent](./feishu-bridge-imbot-agent/) | done | 飞书桥接默认 agent `build` → `imbot` — 收紧 unattended 远程触发场景危险工具默认权限。新加 `imbot` agent(setup hook 自动注入到 user opencode.jsonc,idempotent)同 build 能力同 system prompt 但 bash/edit/write/apply_patch/webfetch + 敏感目录 read 默认 ask;saveAccount 新绑账号默认走 imbot,主 GUI 仍走 build 0 影响。安全闭环:LLM 经 webfetch 拉到 prompt injection 网页被诱导用 bash exfil 时,user 在飞书看到权限卡能即时拒绝。Medium ~120 行 / 5 文件 / 7 单测 / 0 R4 / 0 上游侵入(per-agent 走上游 schema 原生支持)|
| [mac-pack-installer-and-gitee-rename](./mac-pack-installer-and-gitee-rename/) | done | 修补 [macOS] 2026.5.11.1 .dmg 文件名(`DeskFox_1.14.33_aarch64.dmg` → `DeskFox-2026.5.11.1_aarch64.dmg`,对齐 Win 命名)+ Gitee 镜像仓 2026-05-11 改名 `opencode-for-office-deskfox` → `deskfox`(对齐 GitHub)。GitHub release asset 重传 + Gitee 老仓 release 删除 + 新仓重建。`pack-installer.sh` 已存在(rename 逻辑早就有,5.11.1 误绕过)— 本笔文档化为 Mac ship 唯一入口 SOP。legal docs(zh/en)+ memory(reference_push_remotes + MEMORY.md)Gitee URL 同步改名,历史 changelog 保留不改 |
| [win-ship-imbot-5.11.2](./win-ship-imbot-5.11.2/) | **撤回** | Win 端补 ship 5.11.2 把 `feishu-bridge-imbot-agent` 安全 agent 推给 Win 用户 — **装出来撞 castError 启动崩,GitHub draft + Gitee release 已删,被 5.11.4 替代**。原意:5.11.1 ship 时序(10:24)撞 imbot merge(13:14)之前,Mac 5.11.1(13:53)含 imbot 而 Win 5.11.1 不含,**安全 regression(相对 Mac)**。崩因:vite chunking 非确定性,详见 5.11.4 changelog |
| [win-ship-prod-5.11.4](./win-ship-prod-5.11.4/) | done | 5.11.1/2/3 三次 ship 全撞 **vite chunking 非确定性 bug**(同源码不同时刻 build 行为不同,启动期 `castError({})` 撞死)。**5.11.4 workaround** = 用 known-working `target/release/DeskFox.exe`(b268ce694 dev tip 那次 build 实测 OK)直接 ISCC 重打 installer,**不重新 vite build** 避免再撞非确定性。接受 mismatch:Windows 控制面板 + installer 文件名 = 5.11.4 / DeskFox UI 左下角 = v2026.5.11.2(bundle 没动)。Tiny ship chore,根因未真修留 backlog(vite manualChunks 显式分块 / SDK falsy-error fallback 改 Error 而非 {}) |
| [imbot-permission-pragmatic](./imbot-permission-pragmatic/) | done | imbot v1 → v2 务实档 — v1 把 bash/edit/write/apply_patch/webfetch 全 ask 实测 "寸步难行"(opencode bash always 按 arity prefix 颗粒度,user 点 5+ 次 always 仍要点)。v2 重设计:webfetch 全 ask + read 敏感目录 ask 不变(prompt injection 攻击瓶颈),bash 默认 allow + 30+ 删除/不可逆 pattern ask(rm/git --force/git reset --hard/*delete*/*uninstall*/docker rm/aws s3 rm/shutdown/dd/mkfs 等),edit/write/apply_patch 不设走 build defaults(bash echo >> file 等价能力)。permission-card timeout 5→30min 对齐 promptTimeoutMs。攻击面防御等价 v1(攻击瓶颈在 read 不在 bash),user 体验大幅改善。cargo 16/16 + adapter feishu 262/262 + typecheck 16/16。user 升级 v1→v2:删 ~/.config/opencode/opencode.jsonc 的 .agent.imbot 块,重启 DeskFox。Medium ~30 行 spec + 2 单测 / 2 核心文件 / 0 R4 / 0 上游侵入(per-pattern bash 走上游 Wildcard.match 原生)|
| [wss-text-dedup](./wss-text-dedup/) | done | wss-client.ts 加同 text 短期 dedup(同 chatId + 同 trimmedText 10s 内重复 skip)— 防飞书 IM 连击/retry 推**不同 message_id 但内容一致**的多条消息触发 LLM 重复执行 + 弹多张权限卡。复用现有 DedupCache(ttl=10s,maxEntries=200),仅 text 类型生效。imbot v2 实测中 user 发看 /Users/openclaw/.ssh/known_hosts一次但 sidecar 收 2 条独立 message(messageId 不同 SDK dedup 拦不住)导致弹 2 张卡,本笔解决。Tiny,1 文件 +27/-1 行 / 0 新单测(复用 DedupCache 19 测试)/ 0 R4 / 0 上游侵入 |
| [sdk-falsy-error-fallback-fix](./sdk-falsy-error-fallback-fix/) | done | 兜底 SDK `client.gen.ts:102/220` 的 `finalError \|\| ({} as unknown)` falsy fallback 抛空 `{}` → SolidJS `castError` 转 "Unknown error" 空错误页的真凶 — 5.11.x ship 翻车真因 surface fix。在 fork-only `createSdkForServer`(`packages/app/src/utils/server.ts`)的 fetch 边界包一层把 falsy reject 转有效 Error,SDK 看到的 error 永远 truthy → 那个 fallback 永远不触发。同时 console.error 留 `[FETCH-FALSY-REJECTION]` URL + originalError 证据链。**不直接改 SDK**(在 pre-commit 黑名单 + auto-generated 文件下次 regen 被覆盖)。Tiny,1 文件 +44/-3 行,R2 FORK marker / 0 R4 / 0 上游侵入。根因(vite chunking 非确定性 / fetch 为何抛 falsy)未真修,留 backlog 等下次撞时 console 抓到具体证据再查 |
| [dedup-cache-persist](./dedup-cache-persist/) | done | DedupCache 加 persistPath 持久化(JSON 落盘 + 100ms debounce flush + 原子 rename + corrupt 不 crash)— sidecar 重启后 load 上次 mark 过的 message_id,防飞书 WSS 重连后 server 重推老 message 时 in-memory cache 失忆把老 message 当新 message 重复 process。实测起源:user 飞书发 2 条"你是谁" 但 sidecar 收到一条 user 看不到的"看 ~/.ssh/known_hosts"触发 LLM + 弹卡。P5 verify(user 飞书 IM 端没显示这条 user message)确认服务端重推。wss-client 第一层 dedup 配 ~/.opencode/feishu-wss-dedup.json;textDedup 不持久化(10s TTL 落盘无意义)。dedup 测试 19 → 30(+11);adapter-feishu-lark 278 → 289;typecheck 16/16。P2-P4(plugin 多 instance)留 backlog。Medium ~218 行 / 3 文件 / 11 新单测 / 0 R4 / 0 上游侵入 |
| [feishu-plugin-dedup-decision](./feishu-plugin-dedup-decision/) | done | 把"opencode.jsonc 累积多个 feishu-bridge plugin entry → multi-instance → 双推 user message"的根因诊断 + **不做产品层防御**的决策**显式写下**。`imbot-permission-pragmatic v2` + `wss-text-dedup` + `dedup-cache-persist` 后 user 仍撞双推,深查定位:开发机三档来回切换累积 4 个 feishu-bridge entry(空格/raw target/prod/`%20`),opencode loader 并发 import → 3 个独立 plugin instance(3 plugin server port + 3 WSSClient 同 appId connect 飞书),server 给不同 connection 推同 user message 分配不同 message_id → SDK dedup 拦不住 → bot 弹两张卡。**决策不做产品层防御**(普通用户单装永不撞 + L1/L2/L3 三层修法都 over-engineering)。改动:① `feishu_plugin_install.rs` 头加 15 行决策注释列出 L1/L2/L3 候选方案防未来 agent 重复实施 ② `build-deskfox.{sh,ps1}` 加 post-build 清开发机 jsonc 多余 entry(下次 DeskFox 启动 setup hook 自动 inject 当前 .app)③ 本三文档。前一个 verdict 误判"飞书 server 行为,接受现状"被 user 拒绝事后拦截哲学,深查后才定位真凶。Tiny,3 代码文件 +55/-0 + 3 docs / 0 R4 / 0 上游侵入 / 测试豁免(Tiny + bash/ps1 函数 + Rust 注释 = 0 业务逻辑改动)|
| [build-script-json-fallback](./build-script-json-fallback/) | done | `feishu-plugin-dedup-decision` 立的 post-build jsonc 清理只查 `.jsonc`,user 用 `.json`(无 c)的开发者享受不到 — 补 fallback,Win + Mac 双端 build-deskfox 脚本对称跑两种文件名 cleanup;**只影响开发者**(普通用户不跑 build-deskfox 脚本,setup hook 逻辑不动);Tiny / ps1 + sh 各 +6 行 / 单 changelog / 0 R4 / 0 上游侵入 / 测试豁免(对齐 dedup-decision 决策)|
| [mac-ship-prod-5.12.1](./mac-ship-prod-5.12.1/) | done | Mac 端跟 Win 5.12.1 同步出 prod .dmg + 双平台分发(GitHub + Gitee,**反转 2026-05-06 立的"Mac 不跑 Gitee"memory**)。包内容完全等于 Win 5.12.1。Build:bump macos 5.11.1→5.12.1 + `pack-installer.sh --env prod --no-bump` + 手动 rename(--no-bump 时 NEW_VERSION 空跳过 rename — 需求池后续修)+ cp 兜底 .app/MacOS/DeskFox(shasum 验证一致)。产物 `DeskFox-2026.5.12.1_aarch64.dmg` 64.6MB。Tag `ship-mac-prod-2026.5.12.1`(加 mac 前缀避免跟 Win `ship-prod-2026.5.12.1` 撞)。需求池新加 `prod-首次启动-sidecar-idle.md` + `imbot-agent-自动升级.md` + pack-installer-no-bump-rename。Tiny ship chore / 0 R4 / 0 上游侵入 / 测试豁免 |
| [sdk-falsy-empty-body-fix](./sdk-falsy-empty-body-fix/) | done | 补 [sdk-falsy-error-fallback-fix](./sdk-falsy-error-fallback-fix/)(2026-05-12 surface fix)未盖的路径 ② — fetch 成功但 response 4xx/5xx + body 空字符串时 SDK `client.gen.ts:220` 触发 `finalError \|\| ({} as string)` fallback,throw `{}` → SolidJS castError → "出了点问题 / 原因: {}"。5.21.1-dev Mac 首次 Tier 2 ship 翻车真凶。修法:`wrapFetchWithFalsyGuard`(extract export 函数为单测铺路)在 baseFetch 成功后加 layer 2 检查,`!response.ok` + `clone().text() === ""` 时主动抛 Error 截断。顺手修 5-12 layer 1 的 `Object.keys` 错判 Error 为 empty 的 latent bug。Medium-,3 文件 / ~155 行 / 13 单测全过 / 0 R4 / 0 上游侵入 |
| [frontend-stale-session-fallback](./frontend-stale-session-fallback/) | done | 接力 [sdk-falsy-empty-body-fix](./sdk-falsy-empty-body-fix/) 把 5.21.x ship 翻车修到产品级闭环 — `directory-layout.tsx` 启动 createResource 调 sync.session.sync(id) 时如果 sidecar 401/404(stale session id 来自累积的 workspace state),navigate 去掉 stale id 降级到 directory 根("未选择项目" + 最近项目列表),不让 ErrorBoundary 兜底。修法只在启动恢复路径兜底,不动 sync.session.sync 内部(其他 caller 期望错误 surface)。新 helper `isStaleSessionError`(`packages/app/src/utils/stale-session-error.ts`)识别 wrapFetchWithFalsyGuard 抛的 401/404 格式,5xx/403/network/AbortError 不视为 stale。Tiny+,3 新 1 改 文件 / ~115 净行 / 10 单测全过 / 0 R4 / 0 上游侵入 |
| [feishu-pipeline-401-fix](./feishu-pipeline-401-fix/) | done | 修飞书桥接 reply 全静默 bug — 用户视角:发飞书消息没回复(实测 3 条全 empty reply)。**Mac 端复现 / Win 端不复现**的真因:sidecar build 时 `git branch --show-current` fallback baked CHANNEL=`dev`(2026-05-21 改名前主分支名)→ 命中 `HTTPAPI_DEFAULT_ON_CHANNELS` → 走上游不成熟 effect-httpapi 路径,撞**两个 sub-bug**:① Authorization 多 security `{basic, authToken}` 实现成 AND 而非 OR → SDK 只带 basic 就 401 ② `StepFinishPart.reason` schema 必填但 writer 写入 undefined → 编码 400。修法走方案 A:build script 锁 `OPENCODE_CHANNEL=prod` 让 sidecar 走 Hono legacy stack(`c.json` 直出不做 Schema encode + basic auth 单 mechanism 内置 OR fallback)双 bug 全规避,**0 上游 override**;副作用 = DB 文件改名(`opencode-dev.db` → `opencode.db`),配 `lib.rs:initialize()` 头部加 `migrate_pre_prod_db()` 一次性幂等 hook(cp `.db`/`.db-wal`/`.db-shm` 3 文件,源保留作回退)。Medium ~157 行 / 3 文件 / 6 unit + curl e2e / 0 R4 / 0 上游侵入 |
| [mac-ship-dev-2026.5.21.1-dev](./mac-ship-dev-2026.5.21.1-dev/) | done | **Mac 端首次 Tier 2 预览版**(对应 Win 端 `win-ship-dev-2026.5.21.1-dev` 同期发车)— bump commit `b6916bd90`(2026-05-22,installer-versions.json macos `5.12.1` → `5.21.1-dev`)直到 2026-05-23 才补完对外发布(因 bump 后撞 `sdk-falsy-empty-body-fix` + `frontend-stale-session-fallback` 翻车修复 + `feishu-pipeline-401-fix` 核心修复决议一起 ship)。**ship tag on HEAD `f1bd32503`**(含 post-bump feishu fix,user 实测过的 build),走完整 4 步 SOP:`gh release create --prerelease` GitHub Release(prerelease)+ `curl POST /releases` Gitee API 创建 release id=689661 + `mirror-asset-to-gitee.sh` 上传 .dmg(82 Mbps / 6s)。产物 `DeskFox-Dev-2026.5.21.1_aarch64.dmg` 64.5MB / Bundle ID `ai.deskfox.app.dev`。Tiny ship chore / 0 R4 / 0 上游侵入 / 测试豁免(用户三轮验证 stale state + 干净启动 + 二次幂等 + 飞书 reply 回归全过)|
| [feishu-bridge-light](./feishu-bridge-light/) | done | 飞书桥接增强三件套(2026-05-23 落地,opt-in 自动建群默认关)— **(1) `/new` 私聊清话题**(群聊禁用避免共享 chatId 误清全员;stripMentions 处理 @bot 形态;清三处:disk + chatToSession + sessionToChat)**(2) `[ATTACH:path]` 文件回传**(AI 在 reply 嵌 marker → 路径白名单 `~/.opencode/feishu-workspace/` 子树内防 LLM 把 `/etc/passwd` 发出去;按扩展名分流 `image.create`(10MB)/ `file.create`(30MB,file_type 受限枚举,docx 等走 stream 兜底);单 marker 失败不阻断其它,warning append 不漏 marker)**(3) `[CREATE_GROUP:name]` 自动建群**(opt-in `enableAutoGroupCreate` 默认 false + permission-card 二次确认双门控防 prompt injection;仅私聊触发;`chat.create + user_id_list` 拉人 + `chat.link` 拿 share_link 团队群失败降级 chat_id;system prompt 按 opt-in 动态拼避免"哑回复")。**架构决策**:confirm-card.ts 独立成模块而非内嵌 permission-card(语义/类型分离 + 复用 controller 架构模式);system prompt 改成 `getSystemPrompt()` 动态拼。**v1 草稿 15 处假设错配修订**:chatSessionStore.delete 已存在不需新增 / 图片走 image.create 非 file.create / chat.create({data,params}) 真实签名 / chat.link 单独调拿邀请链接 / user_id_list 拉人 / opt-in + confirm 双门控 / FEISHU_SESSION_SYSTEM_PROMPT 扩段教 LLM marker 等。Medium+,~1800 行 / 7 新 5 改 / 103 新单测 / 391 pass 0 fail / 0 R4 / 0 上游侵入(全 fork-only adapter-feishu-lark 包 + plugin.ts routing 扩)|
| [feishu-create-group-toggle-gui](./feishu-create-group-toggle-gui/) | done | 把 feishu-bridge-light 的 `enableAutoGroupCreate` opt-in flag 从 JSON 配置文件升级为 GUI 开关(2026-05-24)— **触发**:user 实测 2026-05-24 暴露 3 个问题 ① user 必须手编 `~/.opencode/feishu-config.json` 才能启用 ② LLM 通过 imbot edit 默认 allow 默改 JSON 用户不知道 ③ sidecar 无文件 watcher 改完不 hot-reload。**方案**:"编辑账号模型" dialog 升级为"编辑账号设置",加"模型 / 高级能力"两个分隔块,高级段含 "允许 AI 自动创建新群" checkbox + 副标说明。**API 设计**(Option A 拍板,user 选):扩 `updateAccountModel` → `updateAccountSettings` partial update,单 endpoint 服务多个 settings,未来加新 flag 0 endpoint 改动;旧 endpoint + Tauri 命令 + 前端 helper 保留 thin wrapper 向后兼容。**全链路改动**:account-store.ts 新加 `updateAccountSettings(id, patch)` 严格白名单 partial(其他 schema 字段 0 影响)+ server `POST /accounts/update-settings` endpoint 加 4 道闸校验(missing accountId / unknown_fields / empty_patch / invalid_field 类型)+ Tauri 命令 `feishu_update_account_settings` 用 `Option<Option<ModelRef>>` 表 model 三态(`undefined`不动 / `null`清 / `obj`设)+ 前端 `feishuUpdateAccountSettings` + dialog 加 checkbox + i18n 4 keys × 3 locale + `AccountSummary` 全链路加 `enable_auto_group_create` 字段。**hot-reload 走已有 `onAccountsChanged` 路径**,sidecar 重建 MessagePipeline,改完立即生效无需重启。Medium ~290 行 / 13 文件 / 10 新单测(account-store partial 7 + 兼容 3)/ 401 pass 0 fail / 16/16 typecheck / 0 R4 / 0 上游侵入(全 fork-only 文件 + Tauri 命令注册)。**测试取舍**:server endpoint HTTP 集成测试撤掉,因 Bun 缓存 `os.homedir()` 首次值,HOME env 改了不生效 → 测试会污染 user 真实 `~/.opencode/`(实施期间已发生过一次,立即清理 + 文档化教训);account-store 10 单测远超 R5 Medium ≥ 3 unit 达标 |
| [feishu-create-group-hard-block](./feishu-create-group-hard-block/) | done | Pipeline 层关键字硬拦截 — 接力 `feishu-create-group-toggle-gui` 修 provider-agnostic 漏洞(2026-05-24)。**触发**:user 实测 claude-code provider 下 soft constraint system prompt 完全不生效(plugin 跳过 `role=system` 消息,LLM 翻 fork 源码撞 imbot read permission 卡)。**修法**:`message-pipeline.ts handle()` 在 `/new` 早退之后加 3 道门控硬拦截 — `enableAutoGroupCreate=false` + `chatType==="p2p"` + `isGroupCreationIntent(cleaned)` 全命中则不调 LLM,直接 sendFeishuText GUI 引导(文案跟 system prompt soft constraint 一致 UX 统一)。**关键字识别**:中文走 regex `/(?:开\|建\|创建\|新建\|拉\|搞\|做)[^群]{0,20}群/`(动词 + 字符 + 群,精准识别"建一个项目讨论群"等真实表达;正确避开"群是怎么建的"动词在群后);英文走 lowercase substring(`create group` / `new group` / `make group` / `set up a group` 等 8 keyword)。helper extract 模式 `isGroupCreationIntent(text)` 纯函数独立可单测。Medium- ~190 行 / 4 文件 / 14 新 helper 单测 + 6 集成测 / 429 pass 0 fail / 16/16 typecheck / 0 R4 / 0 上游侵入(全 fork-only adapter-feishu-lark)。**3 道门控顺序保证不误拦**:flag=true 跳过让 LLM 走 marker / 群聊跳过避免误拦"群是怎么建的"学术问题 / 关键字命中才拦。**误拦权衡** documented:user 主动关 flag 时少量误拦(如"如何创建一个群")成本低,远好于漏拦让 LLM 撞权限卡 |
| [feishu-group-mention-policy](./feishu-group-mention-policy/) | done | 激活 dead 的 `requireMention` 配置 + GUI 暴露 + 顺手把 chat.create 默认 `chat_type` `"public"` → `"private"`(2026-05-24,主题"飞书桥接 privacy 默认值整治")。**触发**:user 研发场景"同 bot 多项目并行,每个项目一个群当 session 用,希望群里免 @"。**修法**:pipeline `handle()` 加 3 条件早退(`!p2p` + `requireMention=true` + bot 没被 @)→ 跳过 LLM;GUI dialog 高级能力分隔块加 checkbox "群里需要 @ 后再响应"(默认开,hint 含**前置条件警示**:user 须先在飞书开放平台改订阅模式为"全量群消息")。**Secondary deliverable**:`group-creator.ts` `chat_type` 改 `"private"` — DeskFox 创建群默认私密,企业内部成员搜不到,通过 share_link 邀请进。**复用** updateAccountSettings partial endpoint(自然扩 requireMention 字段)+ AccountSummary 全链路加 `require_mention`。Medium- ~220 行 / 11 文件 / 16 新单测(helper 7 + 集成 6 + account-store 3)/ 481 pass 0 fail / 16/16 typecheck / 0 R4 / 0 上游侵入(全 fork-only)。**前置条件**:user 必须先在飞书开放平台改订阅模式 + 老群可能需要 bot 退出重入 — 改 checkbox 单独不会自动生效 |
| [feishu-llm-strip-mention-placeholders](./feishu-llm-strip-mention-placeholders/) | done | 修自 `feishu-bridge-light` 起的潜在 bug — `message-pipeline.handle()` 传 raw text 进 LLM,**@bot 飞书占位符 `@_user_N` 没 strip** → LLM 误当另一个联系人,reply 含"我不是 @_user_1"类幻觉(2026-05-24 user 实测群 + p2p 各 1 case 复现)。**根因**:`cleaned = stripMentions(text, mentions)` 已算,`/new` 检测和 CREATE_GROUP intent 都用 cleaned,**但 `runOpencode(sessionID, text, ...)` 漏改 cleaned**。**修法**:1 行 `text` → `cleaned`(`message-pipeline.ts:483`)。Tiny ~4 行代码 + 2 bug-repro 单测 / 485 pass 0 fail / 16/16 typecheck / 0 R4 / 0 上游侵入。[bug-repro: LLM 收 raw text 含 @_user_N 占位符 — 应传 cleaned] |
| [feishu-attach-upload-robustness](./feishu-attach-upload-robustness/) | done | 修飞书 SDK 上传 socket disconnect 100% 失败(2026-05-24,user 实测 4.7KB notes.md 都断,关 Clash 仍发生)。**根因**:Bun runtime fetch 处理 Node `createReadStream` + multipart 编码不兼容 → P0 三件套合一 feat:① stream → readFileSync **Buffer**(绕兼容)② **retryUpload(3 次,指数退避 1s/3s)**仅对可恢复错(socket/ECONNRESET/EPIPE/timeout/5xx)重试,业务错误立即失败 ③ **withTimeout 30s 显式**(Promise.race 实现,Lark SDK 不接 AbortSignal)。3 个 helper 全 export 可独立单测(helper extract 模式)。Medium- ~200 行代码 + ~250 行 27 新单测 / 512 pass 0 fail / 16/16 typecheck / 0 R4 / 0 上游侵入 / fork-only file-uploader.ts。[bug-repro: socket disconnect on Bun fetch + Node createReadStream multipart] |
| [聊天选区-卡片化-换行](./聊天选区-卡片化-换行/) | done | **office-选中加聊天 后续 v1.5**(2026-05-25 单日 ship)— 解决 v1 ship 后两个遗留:① chat 区选区仍走老 markdown blockquote 塞 textarea(不一致)② 卡片溢出横向滚 user 看不到下一张。**方案 B `kind` 字段贯穿**:`FileContextItem.kind?: "chat" \| "file"` 数据维度统一,卡片 UI 按 kind 分流(chat 显 bubble icon + "聊天引用",file 仍 FileIcon + 文件名),`formatCommentNote` LLM 模板按 kind 分流(chat 走 "user is quoting earlier in this conversation" 让 LLM 明确引文来自同一对话历史继承上下文,file 走原 "regarding this file of {path}")。**Step 2 先 1 后**(user 拍板):纯 CSS `flex-wrap + max-h-[180px]` 先 ship → chat 卡片化大改稳扎稳打。3 commits / 12 文件 / +179 / -32 / 6 新单测(formatCommentNote 4 分支 + metadata round-trip + 非法 kind 兜底)/ 87 单测全过 / 0 R4 / 0 上游侵入。经验 L1-L3:Medium 起手建三文档 / kind 字段贯穿优于双 union 类型(字段共享率 90%+ 时)/ UI 与 LLM 模板同 kind 驱动避免分流逻辑分散 |
| [office-选中加聊天](./office-选中加聊天/) | done(v1)| **统一选区菜单架构** — 借 office 选中加聊天用户痛点,把"选中加聊天"这件事在多格式上做漂亮(2026-05-24)。**触发**:需求池 `DeskFox.Ai 需求池.md:31` 入池久未推进,user 远程开发模式拉出来重评估。**架构方案 B**(调研后采纳):`ContextMenuHost`(Solid 组件,根挂一次,document-level capture 监听 contextmenu)+ 可插拔 `SelectionProvider`(first-match-wins 路由),菜单 UI / 引用拼接 / focus / toast 永远只有一份代码;Provider 之间 IO 隔离不连坐。**v1 范围 A 方案**:chat + PDF/office 两处(MD viewer 留 v2 — `mdMenu` 有 编辑/导出 Word 等 MD-specific 动作,v1 只 2 个 use case 设计 `getMenuItems()` 接口形状容易拍偏,等 v2 跟 CodeMirror 一起做时 4 个真 use case 在手再扩接口才对,"三相似行优于过早抽象")。**Step 2 wrapper 替代**:原 plan 改 packages/ui pdf.tsx 加 data-slot 被 R4 黑名单拦,改在 file-tabs.tsx renderDefault 外层 conditional wrap `<div data-slot="pdf-viewer" class="contents">`,等价效果 0 R4 override 消耗。**跨页选区 v1 toast → inline hint**:菜单内联文字 + 按钮 title tooltip,比 toast 不打扰。**顶栏"用本机软件打开"按钮常驻**:soffice 转 PDF 图表/公式/艺术字光栅化选不到时永久兜底,UX 不让 user 反复掉坑。**透明迁移**:chat-selection-menu.tsx 258 → 30 薄壳,message-timeline.tsx 调用不动,行为 1:1 复刻。**1-spec 顺带固化"office WYSIWYG 编辑不重启" 决议**:本 feat 在产品语义上吃掉"office 编辑"需求 — 选中 → 加聊天 → AI 用 python-docx/openpyxl/python-pptx 改原文件 → 重走 office→PDF 管线预览,只是入口形态从 WYSIWYG 变成 AI agent。Medium ~600 行主交付 + Follow-up ~250 行 / 24 commits(主 9 + 第 1 轮 QA 9 + 第 2 轮 QA #1-#4 共 6)/ 72 新单测 / Logic 清单 ≥ 80% 达标 / **R4 2 笔满配额**(TextLayerBuilder + `--total-scale-factor`,packages/ui/pdf.tsx 当季锁死)/ 0 产品代码上游侵入(全 fork-only 新文件)/ View 清单 e2e 留 backlog(Phase ③ ready 后补)。**第 2 轮 QA #1-#4 关键修法**:`--total-scale-factor` 修行末漏字 + pointerdown snapshot 修行间右键 collapse + anchor/focus 修拖几行选中整页 + `commentOrigin: "quote"` 改卡片不附文件二进制(B 方案,LLM token 干净)。经验沉淀 T1-T7(R4 改前读完整 side effect 链 / DOM 顺序 ≠ 视觉顺序 / 事件链 default 时机 / 复用 UI vs 复用数据通路 / 引用 vs 全文 / CDP 验数据非验视觉 / 调研撞墙换层面)见 3-changelog § 经验沉淀 |
| [feishu-plugin-bundle-fs2](./feishu-plugin-bundle-fs2/) | done | 飞书插件 "failed to load plugin (fs.existsSync undefined)" 修复(2026-05-27,mac ship 验证发现的回归)。**根因**:opencode plugin loader `getLegacyPlugins` 遍历插件模块所有 export 当 plugin server 调 `fn(input,options)`;plugin.ts 为单测 export 了 `migrateLegacyWorkspace`/`applyStaleSessionsCleanup` 两个裸 helper → 被错参调 → 第 3 参 fs=undefined → 抛 4× "failed to load plugin"(feishu 功能其实正常,但 opencode 视插件加载失败)。imbot-workspace-rename 新加 export=回归点;media-gen 不 export 裸 helper 故同 sidecar 干净。曾误判打包 `--target`(red herring)。**修法**:helper 挪 `workspace-migrate.ts`,plugin.ts import 不 re-export → 入口只 export 真插件 + 守卫单测。真机 4→0、飞书 622 pass / typecheck 17/17。⚠️ Win prod `2026.5.27.1` 大概率同款需重发 | Medium,排查路径有复用价值 |
