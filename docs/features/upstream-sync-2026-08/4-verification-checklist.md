feat-id: upstream-sync-2026-08
status: in-progress
related: ./1-spec.md ./2-plan.md ./3-changelog.md

# 全量比对验收清单(基准版 vs 当前分支)

> **立此清单的原因**:2026-08-12/13 连续出现「修了又复发」「修 A 坏 B」「读了 CSS 就宣布修好、其实压根没渲染」
> 三类问题,user 明确指出方法论有问题。本清单是矫正后的产物 —— 先定方法,再按方法执行。

## 一、方法论(先定方法,方法决定结果)

### 1.1 清单怎么保证「全」

| 原则 | 含义 |
|---|---|
| **可枚举优先于可回忆** | 凡机器能列的一律机器列(命令注册表 / 快捷键 / 菜单树 / 设置页 / DOM aria-label / FORK feat-id),不靠人想 |
| **多源交叉,互相补漏** | 单一来源必有系统盲区,取并集:**入口源**(能点到什么)+ **定制源**(改过什么)+ **风险源**(修过什么)+ **能力源**(产品命脉) |
| **以「用户能做的动作」为单元** | 不以代码模块为单元 —— 验收方式是亲手操作。「session-side-panel.tsx」不是条目,「tab 右键→关闭其他标签」才是 |
| **每条必带可判定对照** | 固定格式「动作 → 基准版表现 / 当前版表现 / 是否一致」。没有基准列的条目不算数,那等于退回拍脑袋 |

**自检环节(必做)**:清单初稿完成后反问「哪些来源没跑?哪个盲区没覆盖?」
—— 本清单初稿 28 项,自检后补到 **63 项**,翻了一倍多,证明自检不是形式。

### 1.2 验证怎么保证「真」

三条硬规矩,都是踩坑换来的:

1. **真实输入,不用合成事件**。`element.click()` / `new KeyboardEvent()` 触发不了 SolidJS handler,
   必须用 CDP `Input.dispatchMouseEvent` / `dispatchKeyEvent`。
   (2026-08-13 踩坑:用 JS click 验证侧栏 toggle,得出「仍然失效」的错误结论,差点二次误判。)
2. **可靠指标,不用文案判定**。`innerText.includes("新建会话")` 这类判定会被其他区域的同名文案污染,
   要读驱动该行为的**变量本身**(如 `--main-right`)或做**像素级确认**。
3. **视觉改动必须像素级验收**。读 computed style 只能证明「样式挂上了」,不能证明「用户看得见」。
   (2026-08-13 踩坑:分隔线 computed style 显示 1px solid,实际被子元素覆盖、放大 8 倍截图后是纯白。)

### 1.3 修改怎么保证「不反复」

1. **发现有害 pattern → 全仓 grep 同类,逐个评估**,不能只改复现路径那一处。
   (row-reverse 修了又复发,就是只改了触发点。)
2. **新增功能必须回归它影响的既有交互**。
   (自动收起劫持了侧栏手动 toggle;8 条单测全绿却没抓到,因为测的是纯函数、bug 在接线时序。)
3. **渲染条件依赖桌面环境的约束,e2e 抓不到,必须上源码级静态守卫**。
   (`SessionSidePanel` 外层 `Show when={isDesktop()}`,web e2e 整棵子树不渲染,断言必然空过。)
4. 守卫要做**反向验证** —— 故意注入违规,确认它会红。不做反向验证的守卫可能是装饰品。

## 二、基准版

**基准 = `e77443750e`**(合上游之前的最后一个 main commit,2026-08-10 Win prod 2026.9.1 收口批),
即 user 所说「我们原来的 fork 项目」。用 `git worktree` 隔离在 `/Volumes/ExtSSD/deskfox-baseline`,
同样打 local 档包,与当前分支包**交替运行**比对(同 appId 有单例锁,不能并存)。

**执行策略**:当前版全量走一遍 63 项并记录 → 异常/存疑项再去基准版跑同一条 → 判定「回归」还是「本来如此」。
这比两版逐条并排高效,严谨度不打折。

## 三、清单(63 项)

图例:`[ ]` 未验 / `[x]` 一致 / `[!]` 有差异待处理 / `[-]` 不适用

### 第 1 组 桌面外壳(Mac 专属,Win 端验不了)

- [ ] 1. 原生菜单栏逐项展开(应用/文件/编辑/视图/转到/窗口/帮助),文案 + 快捷键
- [ ] 2. 托盘图标:左键行为、右键菜单逐项
- [ ] 3. 关窗到托盘 → Dock 点击重现主窗口
- [ ] 4. 防休眠开关(托盘勾选,重启后保持)
- [ ] 5. 窗口尺寸/位置记忆(首启默认 + 用户调整后保持)
- [ ] 6. 多窗口:新建窗口、窗口间独立性
- [ ] 7. 文件关联:双击 .md 用 DeskFox 打开
- [ ] 8. 深链 `opencode://`
- [ ] 9. 拖拽文件进窗口
- [ ] 10. 首启引导(REQ-083)
- [ ] 11. 更新器 UI(检查更新对话框)
- [ ] 12. 崩溃/无响应恢复对话框

### 第 2 组 主界面骨架

- [ ] 13. 标题栏工具组三按钮(状态/文件树/审查)开→关→再开
- [ ] 14. ⌘B 切换会话侧栏(点击 + 快捷键,各连续 3 次)
- [ ] 15. 文件树 tab「所有文件 / N 更改」互切
- [ ] 16. 面板开关矩阵(树/审查/预览/终端 各组合)下不遮挡、分隔线在
- [ ] 17. 拖拽调整各面板宽度,松手后保持
- [ ] 18. rail 项目图标切换(project.next/previous)
- [ ] 19. 打开项目(project.open)
- [ ] 20. 前进/返回导航
- [ ] 21. 通知面板(alt+T)
- [ ] 22. 切换终端 / 新建终端(多终端)

### 第 3 组 会话与聊天

- [ ] 23. 会话列表:新建 / 切换 / 重命名 / 归档+撤销 / 删除确认 / 分享 / 取消分享
- [ ] 24. 会话内查找 ⌘F:计数、回车循环、Esc 关闭
- [ ] 25. 全局搜索 ⌘K:文件 / 命令 / 会话内容三类结果
- [ ] 26. 聊天引用(选中文字加聊天)→ 卡片 → 点击不开空白页
- [ ] 27. md 内链点击拦截
- [ ] 28. **agent 切换**(composer 上的 Build/Plan 下拉,`agent.cycle`)
- [ ] 29. **Shell 模式切换**(`prompt.mode.shell`)
- [ ] 30. **会话撤销/重做**(`session.undo/redo`)+「撤销此消息」
- [ ] 31. 会话压缩 / 分叉(`session.compact` / `fork`)
- [ ] 32. 会话间导航(next/previous/**unseen**)
- [ ] 33. 消息间导航(`message.next/previous`)
- [ ] 34. 复制消息 / 复制回复
- [ ] 35. 查看上下文用量
- [ ] 36. 跳转到最新
- [ ] 37. 步骤展开/收起(`steps.toggle`)
- [ ] 38. 导出会话 / 导出日志
- [ ] 39. 中断生成(取消)

### 第 4 组 文件与预览

- [ ] 40. 文件树展开/收起/点开文件/当前文件高亮
- [ ] 41. 预览 .md
- [ ] 42. 预览 .docx(内置 LibreOffice)
- [ ] 43. 预览 .pdf
- [ ] 44. 预览 .xlsx
- [ ] 45. 预览 图片
- [ ] 46. 大文件预览守卫(large-file-preview-guard)
- [ ] 47. 预览区右键 → 加入聊天 / 导出
- [ ] 48. tab 右键:关闭 / 关闭其他 / 重开已关闭(`tab.reopenClosed`)
- [ ] 49. **附加文件**(composer 附件上传)

### 第 5 组 创作与供应商

- [ ] 50. 创作模式入口 → 切换 → 生成一次
- [ ] 51. 供应商页:GetBot 首位 + 推荐标、连接弹窗逐个开
- [ ] 52. 模型选择器
- [ ] 53. 模型变体切换(composer 上的「默认」,`model.variant.cycle`)

### 第 6 组 飞书桥接(定制最密,160+ 处 FORK)

- [ ] 54. 设置→飞书桥接页各项开关
- [ ] 55. 账号 / 工作区绑定流程
- [ ] 56. 群消息 @ 策略、重试反馈等设置项

### 第 7 组 设置与全局

- [ ] 57. 六个设置页逐页开、改一项、重启后保持
- [ ] 58. 主题切换(含 Fox Blue)+ 深浅色(`theme.scheme.*`)
- [ ] 59. 语言切换 → 界面 + 原生菜单同步
- [ ] 60. **权限自动接受开关**(`permissions.autoaccept.*`,安全相关)
- [ ] 61. MCP 开关(`mcp.toggle`)
- [ ] 62. server 切换(`server.switch`)
- [ ] 63. workspace 切换(`workspace.toggle`)

## 四、执行记录

(逐条填写:动作 / 基准版表现 / 当前版表现 / 判定)
