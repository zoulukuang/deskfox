feat-id: upstream-sync-2026-08
status: in-progress
related: ./1-spec.md ./2-plan.md ./3-changelog.md

# 全量比对验收清单(基准版 vs 当前分支)

> **立此清单的原因**:2026-08-12/13 连续出现「修了又复发」「修 A 坏 B」「读了 CSS 就宣布修好、其实压根没渲染」
> 三类问题,user 明确指出方法论有问题。本清单是矫正后的产物 —— 先定方法,再按方法执行。

## 一、方法论(先定方法,方法决定结果)

### 1.1 清单怎么保证「全」

| 原则 | 含义 |
|---|---|
| **可枚举优先于可回忆** | 凡机器能列的一律机器列(命令注册表 / 快捷键 / 菜单树 / 设置页 / DOM aria-label / FORK feat-id),不靠人想 |
| **多源交叉,互相补漏** | 单一来源必有系统盲区,取并集:**入口源**(能点到什么)+ **定制源**(改过什么)+ **风险源**(修过什么)+ **能力源**(产品命脉) |
| **以「用户能做的动作」为单元** | 不以代码模块为单元 —— 验收方式是亲手操作。「session-side-panel.tsx」不是条目,「tab 右键→关闭其他标签」才是 |
| **每条必带可判定对照** | 固定格式「动作 → 基准版表现 / 当前版表现 / 是否一致」。没有基准列的条目不算数,那等于退回拍脑袋 |

**自检环节(必做)**:清单初稿完成后反问「哪些来源没跑?哪个盲区没覆盖?」
—— 本清单初稿 28 项,自检后补到 **63 项**,翻了一倍多,证明自检不是形式。

### 1.2 验证怎么保证「真」

三条硬规矩,都是踩坑换来的:

1. **真实输入,不用合成事件**。`element.click()` / `new KeyboardEvent()` 触发不了 SolidJS handler,
   必须用 CDP `Input.dispatchMouseEvent` / `dispatchKeyEvent`。
   (2026-08-13 踩坑:用 JS click 验证侧栏 toggle,得出「仍然失效」的错误结论,差点二次误判。)
2. **可靠指标,不用文案判定**。`innerText.includes("新建会话")` 这类判定会被其他区域的同名文案污染,
   要读驱动该行为的**变量本身**(如 `--main-right`)或做**像素级确认**。
3. **视觉改动必须像素级验收**。读 computed style 只能证明「样式挂上了」,不能证明「用户看得见」。
   (2026-08-13 踩坑:分隔线 computed style 显示 1px solid,实际被子元素覆盖、放大 8 倍截图后是纯白。)

### 1.3 修改怎么保证「不反复」

1. **发现有害 pattern → 全仓 grep 同类,逐个评估**,不能只改复现路径那一处。
   (row-reverse 修了又复发,就是只改了触发点。)
2. **新增功能必须回归它影响的既有交互**。
   (自动收起劫持了侧栏手动 toggle;8 条单测全绿却没抓到,因为测的是纯函数、bug 在接线时序。)
3. **渲染条件依赖桌面环境的约束,e2e 抓不到,必须上源码级静态守卫**。
   (`SessionSidePanel` 外层 `Show when={isDesktop()}`,web e2e 整棵子树不渲染,断言必然空过。)
4. 守卫要做**反向验证** —— 故意注入违规,确认它会红。不做反向验证的守卫可能是装饰品。

## 二、基准版

**基准 = `e77443750e`**(合上游之前的最后一个 main commit,2026-08-10 Win prod 2026.9.1 收口批),
即 user 所说「我们原来的 fork 项目」。用 `git worktree` 隔离在 `/Volumes/ExtSSD/deskfox-baseline`,
同样打 local 档包,与当前分支包**交替运行**比对(同 appId 有单例锁,不能并存)。

**执行策略**:当前版全量走一遍 63 项并记录 → 异常/存疑项再去基准版跑同一条 → 判定「回归」还是「本来如此」。
这比两版逐条并排高效,严谨度不打折。

## 三、清单正文已迁出(改为长期复用资产)

> **2026-08-13 迁移**:user 指出这份清单不该只服务本次 sync,应进测试工具模块长期复用。
> 清单正文(63 项 + 枚举方法 + 每项的「动作/预期/工具」)已移至:
>
> **[`packages/branding/smoke/CHECKLIST.md`](../../../packages/branding/smoke/CHECKLIST.md)**
>
> 与之配套:
> - [`packages/branding/smoke/UIPROBE.md`](../../../packages/branding/smoke/UIPROBE.md) —— 界面交互测试工具包(怎么测才算数)
> - [`packages/branding/smoke/README.md`](../../../packages/branding/smoke/README.md) —— 广度冒烟(有没有炸)
>
> **本文件只保留本次 sync 的执行记录**,清单本身请以上面那份为准 —— 避免两份不同步。

## 四、本次 sync 的执行记录

(逐条填写:动作 / 基准版表现 / 当前版表现 / 判定)

### 已完成

| 组 | 项 | 结果 |
|---|---|---|
| 1 | 原生菜单 7 顶层 + 全部子菜单 | ⚠️ [窗口] 菜单 Minimize / Zoom / Bring All to Front 仍为英文(同类 role 未翻译,待补) |
| 1 | 托盘菜单 | ✅ 全中文(打开/状态:就绪/保持电脑不休眠/退出) |
| 1 | 关窗到托盘 → Dock 重现 | ✅ 1→0→1,进程存活 |
| 1 | 多窗口 | ✅ |
| 1 | 窗口尺寸记忆 | ✅ 首启 1440×900,调整后保持 1600×950 |
| 2 | 四个面板开关各 ×2 | ✅ 全部正常翻转 |
| 2 | ⌘B 快捷键 | ✅ |
| 2 | 文件树 x 坐标 | ✅ 全程 65,不被 rail 遮挡 |
| 3 | agent 切换 | ✅ Build / Imbot / Plan |
| 4 | 文件树点击后焦点 | ✅ 落入文件树(修复后) |
| 4 | 选中交互一致性 | ✅ txt 拖选无评论框、右键出「加入聊天」 |
| 2 | #15 文件树 tab 互切 | ✅ 顺序对(所有文件在左);全量 14 项 → 更改 3 项(tab 标「2 更改」与 2 个改动文件吻合)→ 切回恢复全量 |
| 2 | #17 主区域无横向溢出 | ✅ overflowX=0,无越界子元素 |
| 2 | #18 rail 项目图标 | ✅ 四个项目均在 |
| 2 | #20 前进/返回 | ✅ 均在视口内 |
| 2 | #21 toast 通知区 | ✅ 就位(**条目原文有误,已更正**,见下) |
| 2 | #19 打开项目 | ✅ 原生面板 → Cmd+Shift+G → 目录加载;双向往返各一次,已脚本化 |

### 本轮的两处「工具/清单自身有错」

同一类问题:**有重名对象时,读到的和操作的不是同一个**,而工具没守住,于是结论失真。

1. **uiprobe 连错窗口**(commit `7f383877e4`)。两个 page target 的 title 都是 `OpenCode`、
   `attached` 都是 `None`,原本按 title 排序等于没排,稳定连到被节流的隐藏窗口 ——
   上个 session 的「CDP 超时」绕十几轮就是它。改为**实测** `visibilityState`,
   并把选取逻辑抽成纯函数以便离线回归(自检 14 → 18 项,含两条反向验证)。
   同时发现多窗口下 AppleScript 的「window 1」未必是 CDP 连着的那个(实测 1500×900 vs 视口 1600×950),
   已加 `window_untrusted` 标注 —— 否则「窗口尺寸记忆」这类判定必然错。

2. **NSOpenPanel 同名面板**。上一轮失败留下的面板没关,再跑一次就有两个「打开项目」窗口,
   `first window whose name is ...` 读的是其中一个而按键去了前台那个,表现为「sheet 一直不消失」,
   差点被判成「路径无效」。已加开工前的面板残留断言。
   附带修正两处「想当然」:面板是**独立窗口**不是 sheet;确认路径要按**两次**回车
   (自动补全展开时第一次只是接受补全)—— 故改为「按一次、等状态、没变再按」,不写死次数。

3. **清单条目本身写错**:#21「通知面板(alt+T)」实为第三方库 `solid-sonner` 的 Toaster
   无障碍标签,没有「面板」可开。已更正条目并在 CHECKLIST 新增 §三之二 记录这一类错误的识别方法。

### 第 3 组(会话与聊天)—— `run_group3.py`,16 项:通过 13 / 跳过 2 / 待处理 2

| 项 | 结果 |
|---|---|
| #23 会话列表 新建·切换·重命名·归档+撤销·删除确认 | ✅ 全通;归档 toast 实测文案「已归档 可在搜索中找到并恢复 撤销」 |
| #23b 分享 → 取消分享 | ✅ 已验(user 一次性授权,样本数据):生成 `https://opncd.ai/share/…`,取消后 **curl 实测失效** |
| #24 ⌘F 计数/循环/关闭键可点 | ✅ 计数 1/12→2/12;关闭键在视口内、未被遮挡、点击可关(本次修过的回归点) |
| #25 ⌘K 文件/命令/会话内容 | ✅ 三类均有结果 |
| #28 agent 切换 | ✅ build / imbot / plan |
| #29 Shell 模式 | ✅ mod+shift+x 进、**mod+shift+e 出**(不是同一个键) |
| #30 撤销/重做 | ✅ 消息数 13→12→13 |
| #31 压缩(入口)/ 分叉 | ⏸ 压缩命令在;分叉对话框正常弹出,需打开非空会话才有可分叉消息 |
| #32 会话间导航 | ✅ next/previous/unseen 三命令齐 |
| #33 消息间导航 | ✅ scrollTop 真的跳 |
| #34 复制消息/回复 | ⚠️ 复制按钮点得到,但系统剪贴板未变(疑 `clipboard.writeText` 因页面失焦被静默拒绝),**待定** |
| #35 上下文用量 | ✅ 面板出 token 统计(它是**中栏标签页**,不是弹层) |
| #36 跳转到最新 | ✅ 距底 0px |
| #37 步骤展开/收起 | ⚠️ 展开正常(32→199),**收起未生效**,待定 |
| #38 导出会话 | ✅ 成功 toast「会话已导出 已保存到 xxx.json」(走下载,不开原生面板) |
| #39 中断生成 | ✅ 停止控件出现→消失,哨兵文件未生成(证明后续命令真没执行) |

### 本轮暴露的**流程性**问题(比单个功能缺陷更值得记)

1. **产生对外状态的测试,清理必须验证**。分享测试两次报「已收回」而链接实际还活着,
   都是人工 curl 才发现;审计发现累计 3 条会话处于分享态。
   判据已改成只认站外实测。这是本轮最严重的一次失误。
2. **前提塌了会伪装成一片功能失效**。`session.*` 命令全部 `disabled: !params.id`,
   某一步把会话关掉后,后面五六项一起报「命令不存在」。
3. **CDP 合成 cmd 组合键会漏进 macOS 原生菜单层**:已坐实 `⌘A` → 弹「关于」(3/3)且不全选;
   主窗口反复进出全屏(一轮 68 次)也是同源,user 直接看到「窗口不停放大缩小」。
   真实系统按键送不进副屏窗口,故改为 `heal_window()` 自愈。**均为测试工具假象,非产品缺陷。**

### 待办

- 第 1 组 7~12(文件关联 / 深链 / 拖拽 / 首启引导 / 更新器 / 崩溃恢复)
- 第 3 组 #34 / #37 两项待定
- 第 4 组 40~50
- 第 5~7 组 全部
