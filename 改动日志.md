# opencode-fork 改动日志

> sst/opencode 的 fork 自用版本，目标是给文件查看器加可编辑能力。
> 每次改动必须按下方格式记录。格式定义见 [`docs/governance/改动规则.md`](docs/governance/改动规则.md) 第 5 节。
>
> **黄金规则**：commit 与本日志条目一一对应。漏记 = 视为不存在 = review 时强制回滚补记。

---

## 规范 v2 起的索引(2026-04-27 之后的 feature 见 docs/features/)

| feat-id | commit | 简介 | 详情 |
|---|---|---|---|
| [session-presentation-input-batch](docs/features/session-presentation-input-batch/3-changelog.md) | `4fe677287b`(merge;7 笔实现 `aad041be77`…`c5dc1aeb41`) | 会话呈现与输入修复批(REQ-108/109/110/111/112/113/115/116,2026-08-17~18,Large)— 2026-08-11 上游同步(1.17.4→1.18.16)后 user 连报四处会话呈现回归,合批修完并收掉两条长期缺口:S8 新会话残留已发送上下文卡(**上游自带缺陷**,本批唯一在漏数据的一条,可回贡)/ S1 进度条整块丢失按基准搬回(含 220ms 淡出)/ S3 shell 折叠以**可配置**形式回归(独立「已运行 N 条命令」组,e2e 只在 fixture 种默认、上游断言零改动)/ S7 时间线噪声治理(invalid 合并、同文件连续编辑 ×N,**edit 不进折叠组**)/ S4+S6 运行中图标与权限过滤层接回全局 store / S5 预览器点击收起在 v2 复活 + 恢复 flex-grow 反向驱动动画 / S9 LaTeX 补齐主流定界符 + katexExtension 收口单一来源。**排查把失效模式从四类扩到五类**,新挖出第五处:2026-06-12 防卡死 `healClearedSessionOrphans` 的**调用点被冲掉**(函数与单测都在、单测照常绿、import 还在 → 导出符号扫描判为「有消费方」),本批补回;清单落 4-s2-audit.md | Large;102 文件 +2784 −152(约 800 行是 62 locale i18n 机械回填);新增测试约 90 单测 + 9 e2e;**1 笔 R4 override(S9,packages/ui marked 扩展无注入点;复核报告见 3-changelog,user 2026-08-18 批准合并)** —— S3 原本也需要,通过「命令组文案由 app 侧传入」的设计规避掉;验收:typecheck 29/29 · 单测 app 1045 / session-ui 108 / ui 83 / media-gen 140 / feishu 792 · Playwright 139/139 + performance 60/65(5 条既有失败已 clean tree 对照)· child-store 直读闸零命中 · GUI(CDP 真 Electron + 真打包产物)专项 12/12 + smoke 13/13 · 真机人工 7 条 user 逐条通过;逐批 revert |
| [desktop-build-blockers](docs/features/desktop-build-blockers/3-changelog.md) | `2f55e77ff4`(merge `8d07ddcd0c`+`5719bd7f0d`) | 桌面打包链路阻塞排查(2026-08-18,Tiny)— `bun run build` 恒失败在 prebuild(`No version matching "0.0.0-next-16350"`)→ 整条 /ship 走不下去。根因**不是版本写错**:开发机 `BUN_CONFIG_REGISTRY` 指向 npmmirror,而**国内镜像不同步 opencode 的 `0.0.0-next-*` 预发布版**(npmjs 3192 版含 1035 个 next;npmmirror 85 版、next 为 0)。修法:sidecar 这条 install 固定 `--registry`(默认 npmjs,`OPENCODE_CLI_REGISTRY` 可覆盖),只影响这一个包。⚠️ 同批的「electron-builder 600s 超时」经查是**假警报** —— 绕过 wrapper 直接调所致,wrapper 早已配好国内镜像 | Tiny;1 文件约 20 行;0 黑名单 / 0 R4;`CLI_VERSION` 每次上游同步都变故会周期性复发,已写进 UPSTREAM-MERGE-GUIDE §7;正反两路验证(正常路径通过 + 指回 npmmirror 复现原始故障并打印新诊断);`git revert 8d07ddcd0c` |
| [dev-channel-icon-lowres](docs/features/dev-channel-icon-lowres/3-changelog.md) | `b7c9bad4ed`(merge `32568ef4f1`) | dev/local 档图标低清修复(2026-08-18,Tiny)— user 发现 Dock 图标不对。查明两件事:① Dock 里那个方块是**裸 Electron 的默认图标**(当时为绕开打包阻塞直接跑 `out/`,没走 `.app`),没有谁换掉 logo;② 真问题是 **dev 档 ico-source 缺 512/1024**,`png-to-icns.sh` 的 `copy_if_exists` 缺档**静默跳过** → icns 封顶 128×128 / 8.5KB(prod 是 1024/138KB),Retina 上被放大拉糊(local 共用 dev 图标;beta 源图齐全,只有 dev 缺)。修法:回 `logo.tsx` 的 `MarkFavicon` **矢量源**重生成全尺寸(新脚本 `gen-dev-icons.mjs`,复用仓内 playwright,不加依赖)+ 补齐**被 git 跟踪**的 ico-source(`icon.icns` 是 gitignored 构建期产物)+ 把静默跳过改成显式警告 | Tiny;全在 `packages/branding/`(R3 fork 自有图标目录);0 黑名单 / 0 R4;**prod 图标一字节未动**;验证:icns 8.5KB/128 → 113KB/1024、删 icns 从源图重建仍得 1024(证明不依赖本地产物)、重打 local 包产物内确认、反向构造缺档验证警告触发;`git revert` 单笔 |
| [tray-health-status](docs/features/tray-health-status/3-changelog.md) | `cc7d688025` + `d94720afb4` | 托盘反映后台健康状态(REQ-099,2026-08-07,Medium)— 看门狗 3 态只发 renderer、托盘无表示;顺带修 `setTrayStatus` 自覆盖真 bug(`buildMenu()` 重建时 label 写死"就绪"并重赋 statusItem,预留接口"调了也没用") | Medium;全 fork-only(desktop + branding,0 黑名单 / 0 R4);纯函数映射 + 主进程 emit 直接接线(不新增 IPC)+ Pillow 脚本生成三态图标(mac template 只保 alpha → 徽标挖洞);12 新单测 + branding 5 测(含 alpha 通道断言);真机 macOS restarting/ready/gave-up 全通过 + 菜单栏截图;Win 未验;单 commit revert |
| [chat-tilde-del-fix](docs/features/chat-tilde-del-fix/3-changelog.md) | `96c9e50ddf` | 聊天/分享页单波浪号误判删除线(REQ-098,2026-08-07,Medium)— 同行两个「数字~数字」区间被 GFM 内置 `~~?` 规则闭成 `<del>`(金融数值回复高频);ui + web 各加一次 del tokenizer override 收紧成只认 `~~` | Medium;上游 2 文件各 +4 行 FORK marker + fork-only 2 新模块;⚠ 非匹配必须返 `undefined`(返 false 会回退内置实现,已固化反例);19 单测 + 前端界面 e2e(反证有效);**1 笔 R4 override 已审批**(2026-08-07;ui/web 整包黑名单,marked 扩展必须 `.use()` 注册到实例);单 commit revert |
| [oauth-loopback-bind](docs/features/oauth-loopback-bind/3-changelog.md) | `6fe215459f` | OAuth callback server 绑 0.0.0.0 修复(REQ-019,2026-08-07,Tiny)— 三处 `listen(port, cb)` 缺 hostname 违反 R6,上游自家 `xai.ts:502` 已是正确写法 → 遗漏而非设计 | Tiny;上游 3 文件各 1 行 + FORK marker;配套裸-listen 静态守卫测试 + 运行时 LAN 不可达断言(反证:修前 LAN IP 实测 connected)+ `UPSTREAM-MERGE-GUIDE §5.6`;⚠ redirect URI 三处保持不动(守卫测试锁);**1 笔 R4 override 已审批**(2026-08-07;packages/opencode 整包黑名单);单 commit revert |
| [session-heal-stat-timeout](docs/features/session-heal-stat-timeout/3-changelog.md) | `785b7049e6` | session 列表自愈 stat 3s 竞速 + Session.list 进程级闩(REQ-079,2026-08-02,Small)— 离线卷死路径不再拖住侧栏刷新 | Small;fork-only heal 文件 +45 / 上游 session.ts 调用点 2 行(黑名单)/ 测试 +75;**1 笔 R4 override 待审**,wrapper 不可行性论证见 3-changelog;单 commit revert |
| [model-capability-ui](docs/features/model-capability-ui/3-changelog.md) | `ce948764fd` + 徽标撤除 follow-up | 模型能力-UI 对齐(REQ-026,2026-08-02,Medium)— config modalities 补齐(仓外)+ 图片能力前端拦截;~~选择器徽标~~ 同日 follow-up 撤除(挤爆行宽样式乱,user 拍板) | Medium;上游 2 文件 FORK marker + fork-only 2 新文件 + i18n×3;4 单测;0 R4;逐 commit revert(config 有备份) |
| [imbot-agent-schema-upgrade](docs/features/imbot-agent-schema-upgrade/3-changelog.md) | (本笔 commit) | imbot agent 自动升级(REQ-094,2026-08-02,Medium)— `_schemaVersion` 三分支升级,覆盖管理键保留用户键 | Medium;fork-only desktop 3 文件;6 单测;0 R4;单 commit revert |
| [feishu-retry-feedback](docs/features/feishu-retry-feedback/3-changelog.md) | (本笔 commit) | 飞书 LLM 重试期反馈(REQ-093,2026-08-02,Medium)— retry 事件播报(节流)+ retry 重置 fastfail 窗口 + fastfail 后 abort 防僵尸 + 重试终态文案 | Medium;全 fork-only adapter;核心 0 改动;11 新单测 + 全量 792 pass;0 R4;单 commit revert |
| [startup-sidebar-feedback](docs/features/startup-sidebar-feedback/3-changelog.md) | (本笔 commit) | 启动期 sidebar 点击无响应(REQ-092,2026-08-02,Medium)— not-ready 快路径立即路由 + skeleton 门补 ready 维度 | Medium;上游 2 文件 FORK marker + fork-only 2 新文件;4 单测;0 R4;单 commit revert |
| [feishu-session-project-visibility](docs/features/feishu-session-project-visibility/3-changelog.md) | (本笔 commit) | 飞书 session 桌面端项目列表缺失(REQ-086,2026-08-02,Medium)— 绑定成功自动把 workspace 默认为当前打开项目(未设时),会话进项目列表、文件落 `_deskfox/` 成功页明示;编辑页空态提示;毗邻修 saveAccount 重绑丢 model/workspace | Medium;前端 4 文件 + i18n×3 + adapter 1 文件;6 单测含 bug-repro;0 上游黑名单 / 0 R4;单 commit revert |
| [updater-deploy-stale-ip](docs/features/updater-deploy-stale-ip/3-changelog.md) | `5bb212798` | updater 部署脚本 SSH host 硬编码旧 IP 修复(2026-07-08,Tiny)— 2026.8.4 发版后 user 报「检测不到更新」。根因:`deploy-electron-updater.sh` / `bridge-electron-updater.sh` 的 SSH host 硬编码旧 IP `ubuntu@52.197.46.120`,服务器已换 IP(`updates.deskfox.ai`→`57.180.8.119`,旧 IP web 443 还回旧内容但 SSH 22 已死),`scp` 静默超时→OSS 产物传成功但 manifest 从没部署→两条源停在 Win 2026.8.3;ship.md 步骤 7.5「非阻断/事后补跑」放大成被忽略。修法:3 脚本 `SERVER` 改 `${DESKFOX_UPDATE_SSH:-ubuntu@updates.deskfox.ai}`(域名 DNS 跟随+env override,根治换 IP)。运维补救:手动把两条源部署到 2026.8.4(含 dmg size/sha512 脏数据修正 + 桥 tar.gz sha256 闭环验证)。 | Tiny;3 fork-only 运维脚本 SERVER 行 ~15 行含注释;0 改上游 / 0 R4 / 0 黑名单;回退 git revert |
| [project-close-heal-race](docs/features/project-close-heal-race/3-changelog.md) | `defc4fe3e`(merge `6549709f2`) | 右键项目「关闭」失效修复(2026-07-08,Tiny)— REQ-072 折叠竞态自愈效应误追踪 `projects.list()`,关闭当前项目时路由未切走被误判"被折叠"又补回 → 关不掉(Win/Mac 同炸);`isListed` 包 `untrack` + 效应抽 fork-only `project-restore.ts`;app `test:unit` 加 `--conditions=browser`(bun 解析 solid server 构建致 createEffect no-op) | Tiny;0 黑名单;2 新文件 + layout.tsx 替换调用 + package.json 2 行;6 单测(3 复现 + 3 回归)+ 变异验证 + 521 全量绿 + CDP 真机三路径 |
| [project-continuity-v2026-8-4](docs/features/project-continuity-v2026-8-4/3-changelog.md) | `0b6daee2ec`(REQ-072)+ `e64443260c`(REQ-071) | 切项目/改名连续性 + stale-path 簇收官(v2026.8.4,Medium)— **REQ-072** 会话侧栏改项目维度:后端 `session.ts` 抽纯函数 `gateProjectScope` + `list` 调用,有真实身份(git/非git 锚)按 project_id 列 → 改名/挪位/复制会话跟随,global 哨兵降级回 directory 守大杂烩反例;scope=project plumbing 全上游原生(#24853/#25217/#30804),仅补缺失门控;前端 `session-load.ts` 侧栏传 scope=project + `types.ts` 补类型 + `tabs.tsx` `sessionHasOpenTab` 抽 `tabs-dedup.ts` 改 server+session.id 去重(去 `base64Encode(session.directory)` 根治改名项目重复 tab)。**REQ-071**(草稿切项目丢失,commit `e64443260c` 非黑名单)= 上游自带再水合时序 bug 定点修:`prompt-input.tsx:828` reconcile 效应依赖纳入 `prompt.ready()()`,水合完成强制重灌;CDP 实测定案 reconcile-fail、全新 local 版自动化验证(草稿保留 + 冷启动读回 + 侧栏 scope=project 抓包 + smoke 全绿)。**REQ-070**(物理盘 QA)待 U 盘真机。 | Medium;后端 1 黑名单文件(+21 含注释)+ 前端 5 fork-only/非黑名单 + 3 单测 + 3 CDP 测;13 单测绿 + 4 项 CDP 自动化全绿 + typecheck app+opencode 2/2;真机四格 GUI e2e(改名/挪位)建议真桌面 QA;**1 笔 R4 override(REQ-072,squash 变体)**,逐文件论证见下方 R4 段;单笔 revert 干净 |
| [req069-folder-identity](docs/features/req069-folder-identity/3-changelog.md) | `75ca4f573d`(代码,R4 squash 单笔)+ docs ×3 | 非 git 文件夹稳定项目身份(v2026.6.25,Large)— 隐藏锚文件 `.deskfox/id` 让普通文件夹改名/挪位后仍认得同一项目(现状无 `.git` 一律 global,改名即失联)。fork-only `core/project/anchor.ts` 锚契约(常量收口/纯读四态/写侧降级/exclude 防污染/平台 stub)+ resolve 读锚(有锚返锚id+真实目录,**无锚 bit-identical**)+ previous 全序 `remote > .git/opencode > 锚id > root` + fromDirectory 编排(铸锚判定钉死 flag开&&!vcs&&global、两路写锚连续性令牌、副本对齐 git 共享身份)+ M6 锚丢失软恢复(mint 前反查 ProjectDirectoryTable)+ M8 `OPENCODE_EXPERIMENTAL_NONGIT_IDENTITY` flag **默认关**灰度。模块交付流水线 phase2 产出(6 单元逐个对抗验收 + opus 集成终关)。 | Large;+1409 −14;6 新 fork-only 文件 + 3 上游注入(~127 行全 FORK marker);交付测试 102/102 + turbo 22 包 typecheck + backstop 三包绿 + 真实触发 PASS;flag 默认关发布零变化;**1 笔 R4 override(本季第 3 笔,squash 变体 2026-07-04 拍板)**,逐文件论证见下方 R4 段;单笔可 revert |
| [stale-path-hardening](docs/features/stale-path-hardening/3-changelog.md) | (待提交) | 项目入口 stale 路径全族根治(v2026.6.21,Large)— 改名/移动/默认路径不存在/大小写不一致致文件树 500、编辑保存静默失效、侧栏显示旧名、启动静默空白,在项目入口统一治理。**REQ-067** 大小写防御兜底(新 `ignore-path.ts` 按段大小写不敏感剥 base 前缀保留 tail + `safeIgnores` 防 ignore@7 对 `..` 抛 RangeError;Windows 不复现仅护 mac,平台无关单测);**REQ-068**(本版新做)启动 pre-check(desktop IPC `pathExists` 走既有 checkAppExists 八层链路 + 新 `fs-probe.ts` errno 分类 + 新 `startup-precheck.ts` 决策 + layout.tsx autoselecting 前置校验 + 分模态 toast);**REQ-061** M5 三态(新 `project-rebind.ts`:仅 ENOENT 判 missing,检查出错保守不重绑,治网络盘/U盘 offline 误重绑);**REQ-064** update 自愈(NotFound 时 fromDirectory 重解析现行 id 重试)。搭车修 `build-media-gen-plugin.ps1` 缺时间戳跳过 + PS5.1 stderr 误判。 | Large;4 新 fork-only 文件(+4 test 20 测)+ 12 上游 FORK 注入;opencode/app/desktop typecheck 0 错;真机四模态 QA 待 user;各 REQ 独立 git revert。**1 笔 R4 override(本季第 2 笔,user 2026-06-25 审批)** `[override-blacklist: REQ-067/061/064 改 opencode 核心内部逻辑(file.ts list handler/project.ts 重绑守卫/update handler 自愈)崩点在上游内部无 wrapper 注入点;核心逻辑已抽 fork-only 新文件,新文件因放 packages/opencode/ 被路径黑名单误伤]`,逐文件 wrapper 不可行性论证见 3-changelog |
| [retire-tauri-build-residue](docs/features/retire-tauri-build-residue/3-changelog.md) | (本笔 commit) | 清退 Tauri 构建残留(2026-06-21)— 删 10 个换基座死文件(1735 行):旧构建脚本 ×5(`build-deskfox`/`pack-installer`/`pack-preview-dev`,被 `build-deskfox-electron.*` 取代)+ **broken 测试 `updater-config`**(readFileSync 已删的 `minisign.pub`/`src-tauri/*.rs`,CI branding 包此前一直红)+ DEPRECATED `verify-updater-artifacts` + `tauri-overrides` ×3;重写 `lo-bundle-strip` 守护对象迁 electron 脚本 + `verify.ts`(LO 保护零丢失)+ 清 ~10 处注释。源起 Electron 迁移残留盘点 D 批,调研确认问题真实存在后实施。 | Medium;删 1735 行 / 改的代码文件均注释 / branding 47 pass(CI 转绿) / typecheck 3/3 / 0 改上游 / 0 R4 |
| [local-channel-win](docs/features/local-channel-win/3-changelog.md) | `fee56d35b` + `67d0c8f0a` + `06ee13d49` | local 第 4 档 Win 侧落地 + 双端杀进程三档定稿(2026-06-21)— Win wrapper 补 `-Env local`(本地测试版,独立身份 appId `.local` + 隔离 `opencode-local.db`,永不发布、始终 `--dir`)对齐 Mac;跟苹果端同事协同把 build 前杀进程定稿成**三档矩阵**:打 local 只杀「DeskFox 本地版」不误伤正在用的正式版 / 打发布档杀 `DeskFox`+预览版+Beta 三档(共享 `opencode.db` 同开会锁争用+session 写坏,设计不能共存),排除 local、不通杀 `electron`/`opencode-cli`、`-Name` 精确匹配。覆盖 CLAUDE.md 权威 + Win/Mac wrapper + Windows SOP + `/ship`。配套 `verify-fileviewer.py` 全自动文件预览验证入仓(纯 CDP 点侧边栏开项目,修 `[...querySelectorAll].map` spread 让 CDP 求值挂起的坑→for-loop)。双端协作纪律沉淀记忆 `feedback_dual_end_rule_alignment`。 | 全 fork-only(build 脚本/测试脚本/治理文档/user 级 ship);0 改上游 / 0 R4 / 0 黑名单;各 commit 独立 `git revert` |
| [v2026.8.3](docs/features/v2026.8.3/3-changelog.md) | U1 `f2ee59a387`+`fb09caa905` / U2 `b78e87ea52`+`939e940bd7`+`6d5be64d7a`+`acadcc13e7` / U3 `fb8f2a8036`+`067cbb96cc`+`21f5628bf0`+`1db9454e95`+`82b9ac26ba` / U4 `6302e2a8dd` / 撤U1+U5 `<本笔>` | 供应商/模型连通性修复批 + GUI 残余补丁(2026-06-18,Large,版本级 4 单元;**2026-06-19 撤 U1 + 加 U5**)— ~~U1 REQ-066 Build 摘要行抽到首个 text part 正文前~~ **已撤销**(理解有误,改对齐诉求是 Claude Code 思考/工具整段对齐原生=另一需求) / U2 REQ-052 连/断供应商后 `refreshProviders()` 强制失效 providers query / U3 REQ-054 getbot `mergeGetbotModels` 清幽灵 + 应用内「刷新模型」按钮 / U4 收敛 layout.tsx 双 ToastRegion 为单一(检查更新「每点必双」=Kobalte 单例被两 region 各渲一遍)。**验收对齐发货布局**:U2/U3 原 e2e 强开 v2 = 测了不发货的 settings-v2;两笔 fix 把锚点补到 v1 + e2e 改测 v1(本地版真机 CDP 实测 v1 面板 4 供应商均渲 `data-provider-id`)。**U5(替代 U1)**:bash 纳入「已探索」折叠组(默认收起、点击展开),消除 Claude Code 回合下方 20+ shell 竖向铺开;复用原生折叠机制零新组件,分组纯逻辑抽 fork-only `message-part-grouping.ts`(+3 单测)+ ui i18n 加「命令」计数。 | Large;原 17 文件 +983 −60;U5 改 5 文件全在 `packages/ui/`(含 fork-only 新文件,路径型黑名单)→ **1 笔 R4 override(本季首笔)**:wrapper 已最大化隔离(折叠判定全抽到新文件),剩余触点不可外置——`ContextToolGroup` 计数是核心组件内渲染、app 注入不进;i18n key 走 ui 字典合并必落 ui/i18n;`groupParts` 被 message-part.tsx 内部调用、import 行改不掉;`message-part.{tsx,css}` 的 U1 撤销本质还原上游=降侵入。风险低(加既有折叠集 + 计数,完全可逆)。typecheck 22/22→26/26 · app 459 · ui 30→33 · e2e 17→16(撤 U1 删 build-meta spec)全绿。`[override-blacklist: shell 折叠的 ContextToolGroup 计数/i18n/groupParts 调用须落 packages/ui 核心,wrapper 不可行;含 fork-only 新文件被路径黑名单误伤]` 逐单元 `git revert` 或整版 `git reset --hard main`。**未 push 未合 main**;真桌面 U4 动态 + U5 折叠视觉待 user 验 |
| [ship-win-oss-robustness](docs/features/ship-win-oss-robustness/3-changelog.md) | `91be12772`(脚本,合 `ab28eedae`)+ 文档本笔;桥/凭据为运行态资产无 commit | Electron Windows 首发 ship 健壮性 + 跨引擎升级桥踩坑(2026-06-16,Small + prod 发版 + bug-repro)— `/ship` 换 Electron 后首发 DeskFox 2026.8.0(Win prod,SkipBump 同 Mac 8.0 波次)暴露:① deploy 自 `set -a; source config.env`(原 plain 赋值不 export 子进程→缺 OSS 凭据)② upload ossutil 跨平台(原硬编码 mac 路径 Win Exec format error)③ config.env 补 OSS 凭据(值在 alibaba-cdn.md)④ ship.md 加步骤 7c 迁移桥。**bug-repro:正式版「检查更新」永远"已是最新"**—存量正式版是最后一个 Tauri 版查 Tauri 端点(latest.json+minisign),8.0 只喂了 electron 端点,两链互不相见;修法跑 bridge-electron-updater.ps1 部署 Tauri 格式 manifest 到 Tauri 端点。核心经验:prod 每次 ship 必喂 electron+Tauri 两条更新源。 | Small;2 fork 脚本 + 文档 + 索引;0 改上游 / 0 R4 / 0 黑名单;回退 `git revert ab28eedae`(脚本),config.env/ship.md/服务器 manifest 为运行态不随 revert |
| [deskfox-verify-chain](docs/features/deskfox-verify-chain/3-changelog.md) | `c2e30bf20`(冷启动)+ `763c1eac4`(编排器)+ `faac87b19`(规范 v7),合入 `368155e93`;三文档/索引本笔 | DeskFox verify 链:一条命令、一个判定、按改动自动选层(2026-06-15,Medium,Electron 基座)— 把现成验收零件(typecheck+oxlint / `smoke.py` CDP 冒烟 / 冷启动健康 / 发布物校验)编排成 `bun run packages/branding/smoke/verify.ts`,**单一退出码** 🟢0/🟡2/🔴1。四层:L0 静态 / L1 冷启动(`--cold` 杀全部→真冷启→`cold-start.py` 监控抓 sidecar 预热竞态)/ L2 交互(CDP 9222 跑 probe)/ L3 发布物(`--release`/`--build`,**electron-updater 口径**:latest.yml sha512 与 exe 实算一致 + 版本非 0.0.0 + .blockmap + LO 进包,不复用过时 Tauri `verify-updater-artifacts.ts`)。`--changed` 按 git 改动选 probe;自动探活 9222 复用在跑的 dev(剔 boot 免打断)、没起后台拉跑完只杀自己拉起的。冷启动脚本自包含入仓(源 OPENCODE-PLAN)+ 修其 conn-refused 预热误报。立 R9 验收铁律(v7 + callout):🔴/L0 不过不进 merge,🟡 须 user 确认。刻意不注册 npm 别名避黑名单+上游合并摩擦。三文档为事后补齐。 | Medium;新 `verify.ts`(401)+`cold-start.py`(127)+ 治理 `自动化测试规范.md`(+7,v7)fork-only;0 改上游 / 0 R4 / 0 黑名单;无运行时行为(验收工具不进产物);回退 git revert |
| [session-list-load-more-30](docs/features/session-list-load-more-30/3-changelog.md) | (本笔 commit) | 会话列表「加载更多」每次 +30(2026-06-15,Tiny)— user 要求侧边栏点「加载更多」每次新加载 30 条(原 +15)。`sidebar-workspace.tsx` 两处 `loadMore` 的 `limit += 15` → `+= 30`(两处对应不同列表上下文,统一步长);底层 limit store 机制不变。typecheck 26/26;无测试断言旧值;真机 QA 待 user。 | Tiny;1 文件 2 行 fork-only;0 上游侵入 / 0 R4 / 0 黑名单;回退 git revert |
| [settings-panel-cleanup](docs/features/settings-panel-cleanup/3-changelog.md) | (本笔 commit) | 通用设置面板精简 + 终端入口 + 菜单本地化(2026-06-15,Medium,Electron 基座)— user 看 dev 包提 5 项:① 删「高级」整组(文件树/导航控件/命令面板/服务器状态/Custom agents)② 删「New layout and designs」开关(只删展示,默认 `false` 经典布局不变)③ 「终端字体/捏合缩放」简繁体补译(原显英文,key 已有只缺译)④ 标题栏 DEV 徽标前加「打开终端」图标(`command.trigger("terminal.toggle")` 切集成终端,`run()` 对未注册命令安全 no-op,gate `params.dir`)⑤ macOS 原生菜单跟随全局语言。**任务 5(最复杂)**:菜单主进程一次性构建拿不到前端 i18n + 系统 role 项走系统语言 → 打通 渲染进程 `useLanguage()`+`createEffect` → 新 IPC `set-menu-locale` → 主进程 `setMenuLocale`/`rebuildMenu()`,复用 fork `desktop-menu-i18n.translateMenuLabel`(Windows 菜单已在用)。**做透 role 项**(否则半中半英):Electron `{role,label}` 共存,带 label 的(Undo/Copy/缩放)用翻译表覆盖、纯系统 role(about/hide/quit)按 role 给带 `app.getName()` 译名;`translatedLabel` 仅当译后≠原英文才覆盖 → 英文/未翻译语言 100% 等价上游。删 UI 只删展示不动底层 store 字段/默认值(零回归);连带删失活的 `dialog`/`useDialog`。新测 `desktop-menu-i18n.test.ts`(4)守 DESKTOP_MENU 全 label 有 zh/zht 译文,防再加菜单项漏译泄漏英文。typecheck 26/26 / app 434 / desktop main 69 / 新测 4 全绿;dev `--no-bundle` 包真机验。 | Medium;上游 3 文件(settings-general 删/titlebar 加/index.ts additive 导出)FORK marker + desktop 5 文件 fork-only + app i18n 数据 + 1 新测;0 改上游内部 API / 0 R4 / 0 黑名单;回退 git revert |
| [dev-feishu-binding-missing](docs/features/dev-feishu-binding-missing/3-changelog.md) | (本笔 commit) | Electron dev 包飞书显示"未绑定"根治(2026-06-15,Medium,bug-repro,Electron 换基座 follow-up)— user 质疑 dev 包看不到 3 个绑定账号。**逐层取证**:数据完好在共享 `~/.opencode/feishu-config.json`(`account-store.ts` 写死 `homedir()/.opencode`,`sidecar.ts` 不改 HOME → 后端 `os.homedir()`=真实 home,非身份隔离)→ 插件在包里 → `feishu-plugin-server.json` 指 port 60387 但 `lsof` 无监听(那是正式版 Tauri+Bun 写的、已死)→ `server.log` 实锤 `[media-gen] ReferenceError: Bun is not defined`。**真因**:Electron 边车跑 Node(旧 Tauri 是 Bun sidecar),飞书插件 HTTP server 用 `Bun.serve`;源码 6/12 已适配(`adapter-feishu-lark/src/node-serve.ts` 用 node:http 实现 Bun.serve 子集 + `server.ts:557` 改调 `serveFetch`),但 bundled `dist/plugin.js` 是 **6/9 陈旧产物**仍含 `Bun.serve` → Node 下抛 ReferenceError → 飞书插件 server 起不来 → 列账号失败 → UI 回退"未绑定"(media-gen 同病,dist 6/13 陈旧)。症结:`build-deskfox-electron.sh` §3.5a 打包前**只检查 dist 存在、不重建**,陈旧 dist 静默混入。**修复(user 拍板 1+2)**:① 重建两插件 dist(`build-{feishu,media-gen}-plugin.sh`,`--target=node`,`Bun.serve` 清零)② §3.5a 改"先重建" + **post-build Bun 守卫**(grep bundle 残留 `Bun.serve` → fail,本次回归防线,防陈旧 dist 再静默混入)。端到端验证:新 server `127.0.0.1:51438` 活监听 / `Bun is not defined`=0 / `[feishu-plugin] synced: WSS=3/3` 全连 / `GET /accounts` 返回 3 账号(InveM🐼/灵狐🦊/FoxPlan)/ UI 真机确认。[bug-repro: 含 Bun.serve 的陈旧 plugin dist 打进 Electron 包 → 守卫必须拦下] | Medium;仅 1 个 fork-only 构建脚本(`build-deskfox-electron.sh` §3.5a)+ 3 文档 + 2 索引;插件 dist 是 gitignored 现场产物不入仓;0 上游侵入 / 0 R4 / 0 黑名单;回退 git revert |
| [viewer-selection-tray-style](docs/features/viewer-selection-tray-style/3-changelog.md) | `6dcd3686a5`(托盘)+ 选区+文档本笔 | 文件查看器选区高亮统一蓝 + 托盘 mac template(2026-06-14,Medium)— user 报 3 项(mac 托盘偏大+蓝要白色缩小 / HTML 选中右键加聊天后变灰 / CSV 选中右键后消失),拍板统一标准蓝。托盘 mac 改纯黑 template image(`setTemplateImage` 反色白 + @2x 缩小),Win/Linux 留彩色蓝;选区 overlay 微软红→蓝 `rgba(56,139,253,0.4)`;CSV 右键画 overlay 蓝 + 裁容器边界(`clampRectsToBounds`)防 grid 溢出;HTML iframe 桥接传选区 rects + 注入蓝 ::selection,父 postMessage 投影(`projectIframeRects`)成 overlay 蓝治失焦灰。selection-overlay 11 单测 + local-asset 5 回归 / typecheck 26/26 / user 真机逐格式验过。0 上游侵入 / 0 R4。 | Medium;全 fork(file-tabs/selection-overlay/local-asset/tray)+ 2 template png + 3 文档 |
| [electron-replatform-macos](docs/features/electron-replatform-macos/3-changelog.md) | `91718c1e9b`(阶段0/1)+ `11f37b182a`/`0da9235870`/`653807db07`+LO笔(功能补全) | Electron 换基座 macOS 端适配(2026-06-14,Medium)— 阶段0 验证基座 mac 可用(electron-vite build + electron-builder --mac + 启动后端 HTTP 401 全链路通,纯验证)+ 阶段1 固化 dev 构建脚本 `build-deskfox-electron.sh`(`--publish never` + 绕 Clash 代理 + 删 `native/` 死引用)。**关键**:opencode 后端走内嵌 Node(utilityProcess.fork)无需外部 sidecar 二进制。三个适配点实测定位:① `--publish never`(否则拉未部署 latest.yml 挂 600s)② 绕 Clash 代理(npmmirror 直连,否则 electron SHASUMS 校验超时)③ `bun.lock` 镜像 URL 污染须 `git checkout` 还原不入仓。**协作**:版本注入复用远程 dev-independent-version-line(config 自读),deskfox config 黑名单已被远程 `1a708ebab4` 豁免故无 R4。**运行时功能平移补全(2026-06-14)**:防睡眠持久化+启动恢复(修事件名缺 `deskfox-` 前缀致托盘/设置不同步)/ HTML 预览右键加聊天 / macOS 内置 LibreOffice 注入+构建资源就绪门槛 / desktop 单测全局 electron mock 基础设施(修跨文件 mock 污染,`bun test src` 81 pass)。阶段2/3(签名公证+发布更新)待启动。 | Medium;新 `build-deskfox-electron.sh` + deskfox config(删 native + mac 段 LO 注入)+ 防睡眠/HTML右键/测试设施 + 3 feat 文档 + 2 索引;0 上游侵入 / 0 R4 |
| [stuck-working-status-reconcile](docs/features/stuck-working-status-reconcile/3-changelog.md) | (本笔 commit) | 主视图永久「思考中」卡死 + 侧边栏运行中永久转圈 根治(2026-06-12,Medium)— user 质疑同类 bug 复发。**深挖出机制级结构缺陷**:「会话在不在运行」前端两个独立信号(`session_status` 主视图 / 末条 assistant 残骸 `completed=NULL` 侧边栏),后端兜底都没接到桌面端真正走的路径。① session_status 纯内存重启即丢、重连不对账 → 乐观 busy 永不清 → 修:fork-only `session-status-reconcile.ts` 在 bootstrap 用 reconcile 整体替换清残留 + `halt` 不吞错。② **2026-06-06 的 `heal-interrupted` 对桌面端是双重死代码**:只挂 HTTPAPI handler 但桌面锁 prod 走标准 `instance/session.ts` 路由 + 只在 no-limit 分支但前端始终带 limit 分页 → 残骸永不补盖 → 修:接到桌面真正命中的标准路由两分支(no-limit+分页首页)idle 补盖+落 DB。[bug-repro: 进程死/事件丢后 session_status 永不回 idle + 末条残骸永不补盖] | Medium;前端 fork-only ~80 行(18 单测)+ 上游 3 文件 FORK marker / **1 笔 R4 override** `instance/session.ts`:heal 逻辑本 fork-only,须在上游 Hono 路由注入才生效、无中间件可 wrapper;风险低(idle-gated 幂等只读路径)。`[override-blacklist: 2026-06-06 heal 错放 httpapi+no-limit 致桌面双重死代码,须在上游标准路由注入]` |
| [project-avatar-save](docs/features/project-avatar-save/3-changelog.md) | (本笔 commit) | 编辑项目头像保存不生效(2026-06-12）— user 报上传头像保存后侧边栏始终显示字母,所有项目所有端复现。诊断逐层排除后端(`Project.update` 写 `icon_url_override` + `fromRow` 读回 + `project.list` 返回 + migration `20260423` 在 + `project.test.ts` 32/32 含 override 往返断言全绿)、SDK v2(`buildClientParams` 正确发 icon body)、渲染(`getProjectAvatarSource` override 优先 + Avatar 渲染 img)全部正确 → 锁定唯一缺口:`layout.tsx` 的 `enrich()` 解析 override 只读 `childStore.icon`(commit `aa07f38b07` 引入单源),而编辑对话框对无 id / global 项目走 `globalSync.project.meta` 把 override 只写进 `childStore.projectMeta.icon` =「只写不读」死数据。修(R1 第 2 级):fork-only 纯函数 `context/project-icon-override.ts`(`resolveLocalIconOverride`:childStore.icon 优先 → projectMeta.icon.override 兜底)+ enrich FORK 接线读两源 + dialog 两路径统一写 canonical childStore.icon。[bug-repro: override 只存 projectMeta 时 enrich 必须解析出来] | Medium;新增:上游 ≈80:13;2 上游文件 FORK marker(layout/dialog);0 override;回退 git revert。真机 QA 待 user |
| [llm-stream-idle-timeout](docs/features/llm-stream-idle-timeout/3-changelog.md) | 3928763d0d | LLM 直连流式请求 SSE 空闲超时默认 120s(2026-06-11)— user 报多会话永久"思考中"+运行极慢。诊断:4 会话 13 条 `tokens.output=0` 残骸(xiaomi/alibaba-cn/getbot 三家),sidecar 吊 3 条死 ESTABLISHED;根因 = 上游 `chunkTimeout`(#16366 wrapSSE)默认关闭 + `timeout` schema 文档声称"默认 5min"但全代码库从未应用 = 死流无限挂(NAT/LB 静默驱逐物理上防不住,正解是快速发现 + 上层可重试)。修法(R1 第 2 级):fork-only `provider/stream-timeout.ts`(`DEFAULT_CHUNK_TIMEOUT_MS=120s` + `effectiveChunkTimeout` 纯函数:undefined→默认/false→关/正数→用户值/非法→关)+ provider.ts 3 处 FORK 接线(import/接线/wrapSSE export+ms 放宽 `number\|undefined`)+ config schema `chunkTimeout` 允许 `false` 关闭、修正 timeout 描述谎言。**不给总 timeout 补默认**(5min 会误杀健康长回复)。上游同日核实未修(provider.ts/client.gen.ts 五周未动)→ 待真机冒烟后提上游 PR。测试:新 7/7(含 bug-repro 停滞 ReadableStream)/ provider 278 pass+2 预存 fail(stash 回 main 甄别非 regression)/ config 0 fail / typecheck 16/16。[bug-repro: 停滞 SSE 流超时窗口内 reject 而非永久挂] | Medium;新增:上游 ≈149:18;2 上游文件 FORK marker;黑名单 override 1 笔;回退 git revert |
| [filetree-hover-collapse-hint](docs/features/filetree-hover-collapse-hint/3-changelog.md) | (本笔 commit) | 给「再次点击收起预览」隐藏交互加发现性提示(2026-06-09)— `filetree-toggle`(2026-06-04)做的"文件树里再次点击正在查看的文件 → 收起整个预览区"实用但无发现性,user 要传递给用户又不形成干扰。三方案对比(hover 行尾图标 / 纯 hover tooltip / 常驻小标)选最轻的**纯 hover tooltip**。修法:`file-tree.tsx` 行组件 FileTreeNode 加 `viewerOpen?` prop + `useLanguage()`,return 用应用内 `Tooltip`(`@opencode-ai/ui/tooltip`)包裹整行,`value`=hint、`inactive={!(viewerOpen && node.path===active)}`(仅 active 行包 Trigger、其余零侵入)+ 主 FileTree 加 `viewerOpen?` 透传(2 处调用点)+ `session-side-panel.tsx`「所有文件」tab 的 FileTree 传 `viewerOpen={view().reviewPanel.opened()}`(与 toggle 的 isViewerOpen 一致;diff tab 点击非 toggle 不传)+ i18n en/zh/zht 加 `fileTree.collapsePreviewHint`(余 14 locale fallback en)。文案「点击收起预览」,`placement="bottom-end"`(行下方右对齐)。**踩坑两轮**:① 首版 HTML 原生 `title` 在 Tauri **WKWebView 默认不渲染**(嵌入式 WebView 与独立 Safari 行为不同)→ 真机不显示 → 改应用内 Tooltip 组件;② `FileTree` **递归子目录**调用漏传 `viewerOpen` → 子目录文件(png/mp4/json)不显示 → 补递归透传。教训:WebView 内别依赖原生 title/alt;给递归组件加贯穿 prop 必须补递归自调用点。typecheck 16/16 / app 835 pass 0 fail / GUI 真机三轮验。 | Tiny;file-tree.tsx ~17 行 + side-panel 1 prop + 3 i18n 行 fork-only;0 改上游 / 0 R4 / 0 黑名单;回退 git revert |
| [file-tab-close-others](docs/features/file-tab-close-others/3-changelog.md) | (本笔 commit) | 文件预览区 tab 右键加「关闭其他标签」(2026-06-09)— user 要 tab 上右键关掉除当前外所有已开 tab。调研:文件 tab 组件 `session-sortable-tab.tsx` 原无右键菜单,terminal tab(`session-sortable-terminal-tab.tsx`)已有现成 `DropdownMenu` + `menuPosition` + `onContextMenu` 模式可照搬。修法:`helpers.ts` 加纯函数 `closeOtherTabs(tabs,keep,close)`(遍历关掉除 keep 外所有 tab,5 单测)+ `session-sortable-tab.tsx` 加 `onCloseOthers?` prop + 右键 `DropdownMenu` 一项「关闭其他标签」+ `session-side-panel.tsx` 传 `onCloseOthers={(keep)=>closeOtherTabs(openedTabs(),keep,tabs().close)}` + i18n en/zh/zht 加 `common.closeOtherTabs`(余 14 locale fallback en,completeness 只守 zh/zht)。typecheck 16/16 / app 835 pass 0 fail(+5 新单测)/ 右键 GUI 真机验。 | Small;5 文件代码(1 helper + 1 组件 + 1 wire + 3 i18n 行)+ 1 test ~70 行 fork-only;0 改上游 / 0 R4 / 0 黑名单;回退 git revert |
| [new-project-hide-file-viewer-default](docs/features/new-project-hide-file-viewer-default/3-changelog.md) | (本笔 commit) | 新项目打开默认收起右侧内容预览器,点文件才展开(2026-06-09)— user 要打开/重开项目是干净的聊天 + 文件树,点文件才弹内容预览。调研:预览器 = `view().reviewPanel`(全局持久化 `panelOpened ?? true`,默认展开),点文件展开链路(createOpenSessionFileTab→openReviewPanel)已现成,唯一问题是默认展开。修法:`session.tsx` 加 `createEffect(on(() => sdk.directory, () => view().reviewPanel.close()))` 监听项目目录变化主动收起 —— 打开/切/重开项目都触发,同项目内切会话 directory 不变不触发(保持当前状态);文件树、点文件链路、持久化结构都不动。typecheck 16/16 / app 835 pass 0 fail(0 回归)/ Tiny 豁免强制单测、真机验。 | Tiny;session.tsx 1 文件 ~6 行 fork-only;0 改上游 / 0 R4 / 0 黑名单;回退 git revert |
| [macos-dock-reopen-show-window](docs/features/macos-dock-reopen-show-window/3-changelog.md) | (本笔 commit) | macOS 主窗口关闭后点 Dock 图标主窗口不重开修复(2026-06-09)— 接力 req-031-tray-icon-relaunch-show 补 Mac 漏网路径。现象:主窗口关(实为 hide 到后台)后,点菜单栏托盘图标能开、点 Dock 程序坞图标开不了。根因:Dock 图标点击(app 已运行)在 macOS 走 `RunEvent::Reopen`(专有事件),不走 single-instance(那是二次启动);req-031 只覆盖 tray 左键 + single-instance,`lib.rs` 的 `.run()` RunEvent match 只处理 Exit/WindowEvent、无 Reopen arm → 落 `_ => {}` 被忽略,窗口保持 hidden。修法:加 `#[cfg(target_os="macos")] RunEvent::Reopen {..}` arm 调幂等 `show_main_window_impl`(show→unminimize→set_focus,跟 tray 左键单击同一恢复入口);cfg gate 必需(Reopen 是 macOS-only 变体,Win 编译时不存在,不 gate 会编译失败,Win 走 `_ => {}` 不受影响)。cargo check 过 + 真机 QA 验(native Dock 事件 GUI 无法单测,R5 例外)。[bug-repro: macOS 主窗口关闭后点 Dock 图标主窗口不重开 — RunEvent::Reopen 未处理] | Tiny;lib.rs 1 文件 ~9 行 fork-only;0 改上游产品逻辑 / 0 R4 / 0 黑名单;回退 git revert |
| [feishu-settings-workspace-above-advanced](docs/features/feishu-settings-workspace-above-advanced/3-changelog.md) | (本笔 commit) | 飞书「编辑账号设置」dialog 把「工作目录」整块上移到「高级能力」之上(2026-06-09)— user 要工作目录更靠近常用的模型设置、高级能力下沉为次要。纯 JSX 顺序对调(`feishu-edit-account-dialog.tsx` 单文件):调整后 模型 → 工作目录(分隔 + 路径选择)→ 高级能力(分隔 + /group 说明 + 免@ checkbox);state(workspace/requireMention)、handler、保存逻辑、i18n key 全部不动,顶部加 FORK marker;dialog description 文案未改。typecheck 16/16 / app 830 pass 0 fail(纯重排 0 回归)。纯视觉顺序由 dev 包真机肉眼验。 | Tiny;1 文件 JSX 区块对调净 +1 行 fork-only;0 改上游 / 0 R4 / 0 黑名单;回退 git revert |
| [coldstart-project-reload-toast](docs/features/coldstart-project-reload-toast/3-changelog.md) | `d7ef2e333c` / `ed6dfc2757`(merge `1495d348e7`) | 冷启动重载上次项目时弹冗余「无法重新加载 <项目>」红 toast 静默(2026-06-09)— 接力 coldstart-toast-race,补其未覆盖的 bootstrapDirectory 重载路径。根因:启动自动重载上次项目(如 OPENCODE-PLAN)时 bootstrap 在 sidecar 后端 ready 前抢跑,providers 查询抛 tanstack `Missing queryFn`(sdk 未 ready)、agent 查询抛 `error sending request`(连接级不可达),各弹红 toast;两者都是 transient,ready 后重跑即恢复。修法:新增纯函数 `isTransientStartupError`(复用 isBackendUnreachableError + 加识别 Missing queryFn)→ bootstrap.ts 两处 guard:providers reload catch transient 静默 / slow 汇总用 find(真错) 只在含真错时弹(描述也用真错)。连接级恢复 UX 仍交看门狗统管。搭车一笔独立 Tiny UI fix:index.css toast region bottom 48→112px 上抬清过聊天输入框提交按钮 [fix: toast-above-composer-submit]。[bug-repro: 冷启动重载项目 providers/agent 抢跑各弹冗余红 toast — transient 应静默] | Small;3 文件 ~50 行(主修复)+ 4 行 CSS(搭车)/ +4 组单测(server-errors 19 pass)/ app 830 pass 0 fail / typecheck 16/16 / e2e 16 pass / 0 改上游 / 0 R4 / 0 黑名单;回退 git revert |
| [feishu-workspace-picker-hang](docs/features/feishu-workspace-picker-hang/3-changelog.md) | (本笔 commit) | 飞书「选工作目录」原生文件夹选择器第一步卡死修复(2026-06-08)— 绑定页编辑账号 → 选 workspace,picker 弹出但全程点不动(Cancel/Open 失效)。`sample` 实证 DeskFox 自身没崩没死循环(主线程在正常 tao 事件循环、sidecar 健康、0 次看门狗重启,跟 monterey/coldstart 两笔无关),卡的是 macOS 系统文件面板(out-of-process)在塞满大图的 Downloads 上触发缩略图服务卡顿 + 整机负载。根因代码 `feishu_adapter.rs::feishu_pick_workspace_dir`(feishu-account-workspace 引入):同步命令直调 `blocking_pick_folder`、无 `set_parent`、线程假设脆弱。修法三处:① 命令改 async + spawn_blocking 保证离 main 线程 ② set_parent 主窗口做 document-modal sheet ③ set_directory(home) 避开重图 Downloads。cargo check 过 + **真机点验通过**。[bug-repro: 原生 picker 第一步卡死,GUI 无法单测以真机 QA 验收] | Tiny;feishu_adapter.rs 1 文件 ~25 行 fork-only;0 改上游 / 0 R4 / 0 黑名单;回退 git revert |
| [coldstart-toast-race](docs/features/coldstart-toast-race/3-changelog.md) | (本笔 commit) | sidecar 看门狗重启窗口冗余红 toast 静默(2026-06-08)— 用户启动撞「加载文件失败 / 无法加载会话(error sending request 64796)/ 后台已恢复」三连弹。根因:ConnectionGate 已 gate 健康检查后渲染,但 sidecar 起来后假死被看门狗(REQ-049 Layer③)同 port 重启,重启窗口里 global-sync/file 请求以连接级不可达失败、各弹红 toast —— 而看门狗本已统管「重启中/已恢复」提示,冗余噪音像真故障。修法:新增纯函数 `isBackendUnreachableError`(识别 error sending request / failed to fetch 等连接级错,不含 4xx/5xx)→ 3 个 toast 站点(会话列表 + 文件加载 + 文件树列表)遇连接级不可达 `return` 不弹(仍 console);后端恢复靠既有健康轮询 + bootstrap.refetch 自愈,不扩重载 wiring。[bug-repro: 看门狗重启窗口前端各请求各弹冗余红 toast] | Small;4 文件 ~70 行 / +6 单测(server-errors 15 pass)/ app 814 pass 0 fail / typecheck 16/16 / 0 改上游 / 0 R4 / 0 黑名单 |
| [macos-monterey-no-launch](docs/features/macos-monterey-no-launch/3-changelog.md) | (本笔 commit) | Monterey 12.0.1 装完点击无反应修复(2026-06-08)— 根因:sidecar `opencode-cli`(Bun 1.3.14 编译)`minos` 标成 13.0,dyld 在 macOS 12 拒载后端进程(主程序 11.0 / Info.plist 10.13 能起,卡在 sidecar);Bun 自 1.2 弃 macOS 12,但据 issue #15873 实际可跑(编译配置问题)。修法:build-deskfox.sh 打包时 `vtool` 把 sidecar minos 13.0→12.0 + ad-hoc 重签(Tauri 签名前)+ `--config` 注入 `minimumSystemVersion:12.0`(主二进制 + Info.plist floor,低于 12 得明确弹窗)。本机 dev build 验三点全 12.0 + LO 全 11.0 + sidecar 健康;⚠️ **真 Monterey 机验证待定**(赌 Bun 1.3.14 不依赖 13 符号) | Tiny;build-deskfox.sh +38 行 fork-only;0 改上游 / 0 R4 / 0 黑名单;回退 git revert |
| [telemetry-usage-stats](docs/features/telemetry-usage-stats/3-changelog.md) | (本笔 commit) | 匿名使用统计接入 Tauri 主力端(2026-06-06,Medium)— Rust 原生客户端替代不出货的 Node SDK;采集仅 version/os(大类)/arch(大类)/install_id(匿名)+ 服务端 IP 推地理即丢,opt-out 默认开(设置→通用);删 `packages/telemetry` 整包 + Electron 集成;后端东京 Plausible 502 修复+内存调优验通(GeoIP/清数据留作后续 todo)| Medium;telemetry 13 单测 + typecheck 16/16 + app 808 全绿;**1 笔 R4 override**(9 黑名单文件:`bun.lock` + `packages/desktop-electron/`×8)— **逐文件论证**:① desktop-electron 8 文件 = user 拍板"整包删 SDK"(选项A)后,Electron 直接 import 该 SDK,包删则引用必须同步移除否则编译不过,wrapper 无法让已删包继续被引用(唯一替代=保留包选项B,已否决);② `bun.lock` = 删 workspace 包后 `bun install` 必然重生成,lockfile 无法 wrapper。**风险低**:desktop-electron 不出货(主力 Tauri),且删的是 fork 2026-04-30 自加的 telemetry,**反而降低对上游漂移**;typecheck/测试全绿验证无断裂。`[override-blacklist: 删 fork-only telemetry SDK 致 Electron 引用必须同步移除]` |
| [启用自动升级](docs/features/启用自动升级/3-changelog.md) | Win: `347136b247..687aff80b` + 密钥轮换 `6ae1851e19` / Mac: `feat/macos-updater-adapt`(待合 main 回填) | DeskFox 自家 Tauri updater 启用(2026-06-06,Large)— Win 主体(`UPDATER_ENABLED` 翻 true + Inno→NSIS + 签名注入 + verify/finalize 脚本 + 服务端 9 manifest)已在 main;**Mac 适配**:`verify-updater-artifacts.ts` 加 `--target darwin` + `build-deskfox.sh` `2.6` 段(LO 重签后基于最终 `.app` 重打包 `.app.tar.gz`+签 `.sig`)+ `pack-installer.sh` 列 updater 产物 + **`deploy-updater-manifest.sh` 接 `/ship` 步骤 7.5**(OSS 上传 + finalize + SCP 部署 `updates.deskfox.ai`);**密钥轮换综合**:Win 把密钥换成带密码 `2A008F3DA4940FDE`,Mac 同步换私钥 + 重新 build,prod 真 build verify darwin 8/8(2A00,Ed25519 验签 235MB tarball)。 | Large;Mac 适配 4 scripts(branding,非黑名单)+ 私钥换 config.env(不入仓);0 R4 / 上游仅 constants.rs 1 行(Win) |
| [win-ship-命令](docs/features/win-ship-命令/3-changelog.md) | (本笔 commit) | Windows /ship 一键发版 SOP 入仓 + 台账填实(2026-06-03)— 对齐 Mac `macos-ship-命令`(command 本机 gitignored / SOP 入仓);ship.md 加步骤 3.5 填实台账杜绝空占位回流 main;不跨平台合并 command(尊重既定 + 无法验证 Mac 段) | Medium;三文档入仓 + user 级 ship.md 加 3.5(本机);0 改上游 / 0 R4 / 0 产品代码 |
| [lo-bundle-macos](docs/features/lo-bundle-macos/3-changelog.md) | (待填) | LibreOffice 预捆绑 macOS 适配(2026-06-03)— 对称 Windows lo-bundle,LO 25.8.7 arm64 打入 DeskFox.app/Contents/Resources/libreoffice/;`prepare-lo-bundle.sh` + build-deskfox.sh 条件 Tauri resources 注入 + office-installer.ts darwin bundled 路径 | Medium;3 文件改 + 1 新增;1 R4(office-installer.ts 黑名单误伤) |
| [lo-bundle](docs/features/lo-bundle/3-changelog.md) | (本笔 commit) | LibreOffice 预捆绑安装包(2026-06-03)— 精简 LO 内置进 DeskFox Windows installer,用户安装后无需二次下载即可渲染 Office 文档；`prepare-lo-bundle.ps1`(一次性 bundle 准备)+ `DeskFox.iss` 条件编译 + `office-installer.ts` bundled 路径检测 | Medium;4 文件改 + 1 新增 + .gitignore;0 改上游;0 R4;bundle 不存在时 installer 正常 build 无 LO(静默 fallback) |
| [feishu-file-and-quote-recv](docs/features/feishu-file-and-quote-recv/3-changelog.md) | `9e430f5a9` / `6050a9bd8` / `e44970423` / `be11111fa` / `43c1c4525` | REQ-036 引用回复上下文注入 + REQ-035 文件接收(txt/docx/PDF pdfjs-dist) + 磁盘存储 + MAX_TEXT_CHARS 50k(2026-06-02~03) | Medium 5 笔 commit;pdfjs-dist 4.10.38;691 unit pass;0 改上游 TS(除 FORK marker);1 R4(bun.lock×2) |
| [help-icon-community-link](docs/features/help-icon-community-link/3-changelog.md) | (本笔 commit) | REQ-039 左下角"?"帮助入口加链接 → DeskFox 社区页(2026-06-02)— sidebar footer 的"?"图标早已接好 `onOpenHelp`,本需求只把 `platform.openLink` 目标 `opencode.ai/desktop-feedback` → `https://deskfox.ai/#community`,顺手修一处上游 brand leak;Tauri 端 `openLink` 走 `shellOpen` 真外部浏览器打开 | Tiny / `layout.tsx:2363` 1 行 + FORK marker / typecheck 17/17 / 0 R4 / 上游侵入 1 行(已 marker)/ error.tsx 另一处同链接不在范围未动 |
| [macos-ship-命令](docs/features/macos-ship-命令/3-changelog.md) | (本笔 commit) | macOS `/ship` 一键发版命令(2026-06-01)— 发版 SOP 全自动 fire-and-forget,签名+公证内置 + 不公证不推送硬门禁;步骤 0-8 + resume 模式(公证撞苹果超时→停留本地→补公证续发不重 build);双轮验证前置不进流程/触发即授权/绝不 push main;skill 本机 gitignored 避免与 Win /ship 冲突,SOP 入仓 | Medium / skill SOP ~120 行(本机)+ 三文档(入仓)/ 复用 pack-installer/build-deskfox/mirror-gitee/3-notarize / 0 改上游 / 0 R4 |
| [macos-codesign-notarize](docs/features/macos-codesign-notarize/3-changelog.md) | (本笔 commit) | macOS Developer ID 签名+公证集成进 build-deskfox.sh(2026-06-01)— 三处改动(source 签名配置 SIGN_ENABLED/公证+staple NOTARIZE_OK/提示三态);Tauri 自动签 + API Key 公证(非交互不用 keychain profile);优雅降级;隐私参数不入仓。端到端实证 Tauri 自动签生效(.app Developer ID+runtime+timestamp),本次公证撞苹果服务超时(我方流程已证正确,留待 3-notarize.sh 补);顺修提示 bug(公证失败误报已公证) | Medium / build-deskfox.sh ~50 行 + 三文档 + governance 现状更新 / 0 改上游 / 0 R4 / [bug-repro: 公证失败误报已公证] |
| [package-verify-script](docs/features/package-verify-script/3-changelog.md) | (本笔 commit) | 打包产物自动化验证脚本 A+B 两层(2026-06-01)— 零 GUI / 零焦点干扰自检 .app 完整性(结构 / arm64 / 三档 Bundle ID / Gatekeeper / catalog 内联数据)+ sidecar headless 冒烟(`serve` 真监听 + HTTP 响应 + 日志无 fatal + plugin.js ESM 加载);`-Env` 三档、`SCRIPT_DIR` 自推导无硬编码;Mac WKWebView 无 CDP 促成此 headless 路线 | Medium / 新增 `verify-deskfox-package.sh` ~165 行 + 三文档 / prod 1.14.33 实跑 23/23 / 0 改上游 / 0 R4 / fork-only |
| [feishu-llm-timeout-surface](docs/features/feishu-llm-timeout-surface/3-changelog.md) | (本笔 commit) | 飞书 bot 收到消息但不回的根因修复 — LLM 超时 / 空响应 surface(2026-06-01)— 故障链路 `LLM hang → dispatcher 30 分钟 timeout → 返空 → runOpencode 找不到 useful → 空字符串 → handle() 静默 return → 飞书侧 0 reply` 改成 `throw → friendlyErrorReply → sendFeishuText`,用户任何场景一定收到一条说明文本 | Medium 三文档 / 4 文件改 + 1 新建 / **顺手修 pre-existing bug**:register 二次调用同 sessionID 时 superseded reject 永不触发(finalize 闭包早 return → OLD promise hang)/ **测试**:新建 `prompt-dispatcher.test.ts` 10 case(含 `[bug-repro]` 复现 + Logic 清单补齐)/ `friendly-error.test.ts` +7 case 覆盖 5 类新 pattern + if-顺序边界 / `message-pipeline.test.ts` +4 e2e case 全链路验证 / `friendlyErrorReply` 新增 timeout / 429 / 5xx / no-useful / session-state 五类 + EMPTY_REPLY_FALLBACK 常量兜底;adapter-feishu-lark 全包 646 pass(+24)/ typecheck 17/17 / 0 改上游 / 0 R4 / 配套诊断工具 `OPENCODE-PLAN/诊断工具/DeskFox-tools/DeskFox-Diag.ps1` v6.3 飞书桥接专项 6 子段同期落地 |
| [chat-input-focus-no-scroll-on-add](docs/features/chat-input-focus-no-scroll-on-add/3-changelog.md) | `741d5f65c` | 加卡片入聊天后输入框 scroll 跳顶修复 — `chat-input-focus.ts:24` `focusChatInput()` 内 `el.focus()` 默认 `preventScroll:false` 触发 scrollIntoView 拽 editor 到顶,改 `{preventScroll:true}` 后 scroll 保持、cursor 仍由 Selection API 程序化移到末尾 | Tiny 改动 / 仅 3-changelog;`chat-input-focus.ts` +8 -1 / 三消费者(submitMdSelection / submitToChat 两路径)全受益;跟 `chat-composer-focus-scroll-reset` 是兄弟 fix(同根因不同触发路径,各独立分支 / commit);typecheck 17/17 / 全 app 744/0 / 0 R3 / R4 / 上游侵入;user 真桌面验证通过 |
| [chat-composer-focus-scroll-reset](docs/features/chat-composer-focus-scroll-reset/3-changelog.md) | `3ffb65408` | 从外部点回聊天输入框时 scroll 跳到顶部修复 — `prompt-input.tsx:1382` mousedown 调 `editorRef?.focus()` 默认 `preventScroll:false` 触发 `scrollIntoView` 把 editor 开头硬滚进视口,改 `{preventScroll:true}` 后 scroll 状态保持、cursor 仍由 native click 落点击位置 | Tiny 改动 / 仅 3-changelog;`prompt-input.tsx` +10 -1 / 其他 5 处 `editorRef.focus()` 不动(场景需要默认 scrollIntoView);typecheck 17/17 / 全 app 744/0 / 0 R3 / R4 / 上游侵入;user 真桌面验证通过 |
| [ctrl-c-copy-pollution-fix](docs/features/ctrl-c-copy-pollution-fix/3-changelog.md) | `6bec88edd` / `57f908232` / `b09482388` / `9d7440cf6` / (本笔 commit) | Ctrl+C 复制错内容 v2 — 跨区域污染 + pickBest 策略错配修复(R1+R2) **+ 架构师 backlog 同步落地(Item 1+2)** **+ REQ-032 visibility:hidden + focus race 修复**(user 测试发现)。①主 fix(6bec88edd):capture-phase Ctrl+C 从 history 路径切到当前选区(`readSelectionWithShadows` shadow-aware);决策表 `decideCtrlCAction` 三档(noop / native / shadow-intercept)+ viewer 容器实例 scope 隔离。②backlog 续做(本笔):抽 `selection-history.ts`(ViewerSelectionHistory class 独立)+ `selection-bus.ts`(单例 document.selectionchange,消除 N×listener),Item 3 显性 NACK(三个右键路径已经数据源隔离,硬塞同一决策表是过度抽象)| Medium+ 三文档;主 fix 16 bug-repro 单测 + backlog 20 新单测(history 12 + bus 8)= **36 unit pass**;全 app 780/0 / typecheck 17/17 / 0 改上游 / 0 R4。`<Tabs.Content>` 加 `data-component="file-viewer"` + `ref` 实例 scope;`onSelChange` 加 `isAnchorInsideViewer` 闸 → 跨 tab + 跨区域天然隔离;keydown 改用当前 shadow-aware 选区,light 让原生走、shadow 才 intercept,history 兜底退役(`selectionHistory` 保留给右键 contextmenu 路径,且 history 现在事实上**只剩 1 个合法消费者**)。三场景全解:viewer 内重选短覆盖长 / 跨区域污染 / 取消选区幽灵复制。R1 三级跳 ratio 4.2:1(新增 580 行 / 改既有 138 行,file-tabs.tsx 净减 110 行)|
| [e2e-chat-loop](docs/features/e2e-chat-loop/3-changelog.md) | (本笔 commit) | 聊天主循环 Phase 1 mock e2e 套件(2026-05-29)— 接手 WIP commit `11ada4f02`,3 case 修通:发消息收 AI 回复 / sidebar 新 session 出现 / busy 期 progress 显示 | Medium 三文档全套;0 改产品源码 / 0 R4;`packages/app/e2e/chat-loop.spec.ts` -92 +X 净简化(fixme→test + 调试 cleanup)+ `packages/app/e2e/mocks/chat-mock.ts` ~+70 重写(路由 RegExp 化 / SSE addInitScript 路线 / assistant mock 补完整 shape);**5 条踩坑沉淀**:① Playwright glob 不匹配 `?directory=` query → 路由全 RegExp ② `route.fulfill({body})` 不接 generator → SSE 必走 `window.fetch` patch + ReadableStream ③ assistant mock 必备 `parentID + tokens + cost`(缺一就 ErrorBoundary 全屏崩或 slot 不渲染)④ `session.created` event 必须带顶层 `directory` 否则 per-project sync 不收 ⑤ `data-session-id` 两 surface render 用 `.first()`;mock helper 后续 chat e2e(followup/abort/retry)直接复用 |
| ~~[startup-sidebar-ready-gate](docs/features/startup-sidebar-ready-gate/3-changelog.md)~~ | reverted (本笔 commit) | ~~启动期 sidebar 项目切换"无响应"修复~~ — **2026-05-29 user 实测撞 2 个 bug 已 revert**:① 第一次点击未选 project tile 只变灰不切换,需第二次才能切 ② gate 状态(opacity-60)下鼠标 hover/move 误触发 sortable drag,DragOverlay ghost 飞出 sidebar 区。怀疑 ready-gate 加的 `onPointerDown` 没 `event.stopPropagation()` 跟 `use:sortable` 启动 drag tracking 冲突 + reactive cascade 阻断 pointerup cleanup。原 backlog "启动期-sidebar-点击无响应" 重新打开,等重新设计避开 sortable 冲突 |
| [e2e-tauri-phase2-mac](docs/features/e2e-tauri-phase2-mac/3-changelog.md) | (本笔 commit) | Phase 2 真桌面 e2e Mac 端**基础设施 + user-flow + 项目注入三层落地** — 跟 Win `e2e-tauri-phase2-real-desktop` 用例对齐,**底层 GUI 黑盒**(macOS WKWebView 无 CDP);saveDialog mock 方案 ② 降级(env var + Tauri command);**deep_link 注入项目跟 Win page.goto 看齐**(`open -a .app opencode://open-project?directory=...`,绕开 prod LaunchServices 抢占);**全量套件 3 passed + 1 skip / 1.3min**:smoke-mac 2 + **command-palette-flow-mac 真 user-flow pass**(Cmd+K + "new" + Escape 5 phase + 截屏证实"NEW"过滤"New session"命中)+ md-to-word-real-mac fixme(项目主页 ≠ Win session view,2 个 backlog 方案) | Medium ~2200 行(三文档 ~830 + 代码 ~1370);新 `packages/app/e2e-tauri-mac/` 12 文件(含 `clickToFront`/`titleBarAnchor`/`anchorOf` helper)+ dispatch script + .gitignore;改既有 3 文件 +27 -2;**7 条踩坑沉淀**(cliclick 中文吞掉 / IME 转拼音 / imbot-workspace 默认 / hydrate 阈值 / Claude 视觉 Read 截图 + 1.89 倍换算 + cliclick 物理点击 / deep_link prod LaunchServices 抢占 / 项目主页 vs session view 差异);0 改 Win 端 / 0 R4 / 0 上游侵入;typecheck 17/17 / Rust cargo check 0 errors / Phase 1 e2e 13 pass 无回归 / packages/app 738/0 unit 无回归 |
| [media-gen-xiaomi](docs/features/media-gen-xiaomi/3-changelog.md) | `b82c383f1` / `993f8ad23` / `85ac6dbdc` / (本笔 commit) | 小米 MiMo 接 provider(REQ-030 第三家)— 4 档:标准 TTS / 语音克隆 / 语音设计 / Omni 当 ASR;**REQ-030 首次加新 capability 类型** `tts_clone` / `tts_design`(MiMo 独家能力,user 拍板授权)| Large 4 笔 commit;3 文档全套;65 unit + 24 前端测试 / app 全包 744/744 / typecheck 17/17;架构:统一 `/v1/chat/completions` + `api-key` header(跟阿里/MiniMax 都不同);probe 阶段真打 4 API 全通真生成音频(踩坑 3 条:DataURL audio.voice / VoiceDesign 必须 absent voice / Omni 替 ASR);前端 capability 类型 3 处副本必须同步;CDP 自测脚本 `cdp-creation-xiaomi-all.ts` 写好待 user build cycle 时跑;0 改上游 / 0 R4 |
| [e2e-tauri-phase2-real-desktop](docs/features/e2e-tauri-phase2-real-desktop/3-changelog.md) | (本笔 commit) | Phase 2 真桌面 e2e 启用 — cherry-pick 6 文件 + saveDialog mock 方案 ①(`page.exposeFunction` 0 产品代码侵入)+ md-to-word-real spec 用 Ctrl+K 替代文件树点击,1.2min 全链路真桌面 pass 产 22.5KB docx | Medium+ 三文档全套;~13 行平台 hook(`packages/desktop/src/index.tsx`)+ 6 个新 e2e 文件 + 1 script;typecheck 17/17;smoke#1 + md-to-word-real 真桌面 pass;0 改上游 |
| [media-gen-minimax](docs/features/media-gen-minimax/3-changelog.md) | `f99149ed8` / `9a2642231` / `3e19df80a` / `c7e5131aa` / `137b5a257` / `7ff6b3454` / `d992b41f4` / `2bdf67cc4` / `859f2a305` / `7a70a738b` / `6fe1a0cd5` / `3d562f7cd` | MiniMax 接 provider(REQ-030 第二家)— image-01/Hailuo-2.3/speech-2.8-hd 三能力,by-provider switch 不抽 MediaAdapter,catalog/registry UI 零改动自动多 3 档 | Large 12 笔 commit;3 文档全套;60 单测 + 真用户 CDP e2e 3/3 + 真 API probe;hex audio file:// 协议穿过 saveAssets;model 双后缀(-hd 走 Token Plan / -turbo 走积分)实测踩坑沉淀 8 条进 OPENCODE-PLAN/knowledge-base;0 改上游 |
| [creation-edit-asr-no-file](docs/features/creation-edit-asr-no-file/3-changelog.md) | (本笔 commit) | 创作模式 `@<路径>` 文件引用没传给图编辑/ASR — submitCreation 只认 base64 附件忽略 file part,且 audioUrl 从未赋值。helper extract `buildCreationInput`(纯函数)按 capability 路由 refFile/audioUrl + 路径拼绝对 + prompt 剔除 @ 字面 | Tiny+ 只 3-changelog,1 helper + 1 测试 + 1 接入;18 bug-repro 单测;app 729/0 零回归;0 改上游 |
| [npm-registry-cn-mirror](docs/features/npm-registry-cn-mirror/3-changelog.md) | (本笔 commit) | 国内用户 sidecar npm 走国内镜像 — sidecar 装 `@opencode-ai/plugin`/用户插件默认走 npmjs(京实测 12s vs 国内 0.7~1.8s),连不上则反复失败+首次交互空等;注入 `npm_config_registry` env(覆盖两条安装路径,国际用户不注入)+ 时区即时猜+后台探活自愈+验证过镜像清单 | spec/plan/changelog 三文档;纯 fork(新 `npm_registry.rs`+`cli.rs`/`lib.rs` ≤5 行注入 FORK marker);6 单测;0 改上游 TS |
| [file-tree-dnd](docs/features/file-tree-dnd/3-changelog.md) | `4b73b1229` / `ce043ee69` / `fe0994293` / `b9a4accc1` / `f0418382a` / `0b73e7d19` / `9a56c8388` | 文件树拖放移动 — 全功能(核心+多选+剪切粘贴+撤销+外部拖入) | spec/plan/changelog/4-test 四文档,7 笔 commit |
| [getbot-接入](docs/features/getbot-接入/3-changelog.md) | `8e4aa3944` | GetBot 模型聚合 Provider 接入(热门首位 + 推荐标 + 内置 apiKey 流程) | spec/plan/changelog 三文档 |
| [规范-v2](docs/features/规范-v2/3-changelog.md) | `1b669abd2` | 三文档分离 + diff 阈值 200→500 + sprite/types 出黑名单 | spec/plan/changelog 三文档 |
| [加聊天-preview-fix](docs/features/加聊天-preview-fix/3-changelog.md) | `b269ceb69` | 文件查看器右键加聊天后选中文字未传到模型 — synthetic text 加 preview 段 | spec/plan/changelog 三文档 |
| [查看器-自动刷新](docs/features/查看器-自动刷新/3-changelog.md) | `765b53f83` | 模型 Edit/Write 后查看器自动刷新 + 编辑态草稿守卫(file.edited 主链路 + 2s toast 去重) | spec/plan/changelog 三文档 |
| [禁自动升级](docs/features/禁自动升级/3-changelog.md) | `592ed714e` | 关闭 opencode 官方升级入口和通道(constants 硬关 + platform 接口收口 + sidecar env 双保险) | spec/plan/changelog 三文档 |
| [installer-打包](docs/features/installer-打包/3-changelog.md) | `1523ea963` / `0236481cb` | DeskFox 打成 Windows installer(Inno Setup,不签名) + 顺手修 icon pipeline 让 exe 嵌入多分辨率狐狸 | spec/plan/changelog 三文档 |
| [icon-pipeline-deep-fix](docs/features/icon-pipeline-deep-fix/3-changelog.md) | `5704142fe` / `bc0b549b7` / `303fbc583` | Tauri winres 真读 icons/dev/icon.ico 无视 prod.json — apply-icons 同步覆盖 + 新设计资源全量更新;2026-04-29 follow-up 修 png-to-ico ≥256 尺寸 writeUInt8 溢出 | Tiny,只 changelog;3 笔 commit |
| [macos-打包](docs/features/macos-打包/3-changelog.md) | `e956d6c90` | DeskFox Mac .app + .dmg 打包 scaffolding(arm64,不签名,sidecar 复用上游 predev.ts)— Phase 2 user Mac 实战 2026-04-29 打通 | spec/plan/changelog 三文档 |
| [claude-code-loop-fix](docs/features/claude-code-loop-fix/3-changelog.md) | `e2a9d7167` | claude-code plugin 选 Claude 模型时 step loop 不 break、UI 卡死 — 兜底块用 step-finish part 替代 finish 顶层字段(R4 override,case 1) | spec/plan/changelog 三文档 + 顺手修 apply-icons.ps1 中文注释碰 PowerShell 5.1 ANSI 解析 |
| [build-pipeline-sidecar-fix](docs/features/build-pipeline-sidecar-fix/3-changelog.md) | `b9581b76e` | Windows build-deskfox.ps1 加 sidecar 自动 build 步骤 — 时间戳判断 + non-baseline 优先(绕 clash 代理下 bun-baseline 下载失败) | Tiny,只 changelog |
| [plugin-cwd-channel](docs/features/plugin-cwd-channel/3-changelog.md) | `41817499d` | opencode 加 _opencode providerOptions namespace 暴露 cwd 给 spawn-based plugin — claude-code/codex/gemini/aider 等 spawn-based plugin 通用通道(R4 第 3 笔本季特批,不扣下季度) | spec/plan/changelog 三文档 |
| [installer-versioning](docs/features/installer-versioning/3-changelog.md) | `2bd3aa29e` / `af4290659` | installer 版本号规则 YYYY.M.D.N(Win/Mac 各自独立 N 序列)+ bump 脚本 + pack-installer 一键 + docs/installer-versions.md 版本日志(首发 [Windows] 2026.4.29.1,旧 1.14.21 命名规则停用)| spec/plan/changelog 三文档 + 4 fork-only 文件,2 笔 commit |
| [md-viewer-typography](docs/features/md-viewer-typography/3-changelog.md) | `f66b26be0` | 文件查看器看 .md 时排版升级 — 标题加粗阶梯(h1/h2=700, h3-h6=600)/ 行内代码芯片 / 引用块底色 / HR 显形 / 表头底色,走 wrapper 不动上游,scoped 在 data-context="file-viewer" 下,聊天侧零影响 | Tiny,只 changelog |
| [bump-script-encoding-fix](docs/features/bump-script-encoding-fix/3-changelog.md) | `e88dfbf52` | bump-installer-version.ps1 加 UTF-8 BOM(3 字节)修中文 placeholder 乱码 — PS 5.1 无 BOM 时按 GBK 读源,中文字面量被双重编码 | Tiny,只 changelog |
| [macos-右键选区-修复](docs/features/macos-右键选区-修复/3-changelog.md) | `ed8ac9856` | macOS WKWebView 文字上右键选区丢失 — 选区历史栈挑最长 + overlay div 替代 stale 的 CSS Custom Highlight + 关闭菜单时统一清原生/Pierre 选区 | spec/plan/changelog 三文档,1 fork-only 文件 |
| [加聊天-option-enter](docs/features/加聊天-option-enter/3-changelog.md) | `00b208eed` | 文件查看器"+ 添加到聊天窗口"对话框 macOS 加 Option+Enter 提交快捷键 + 底部提示文案平台化 | Tiny,只 changelog,1 文件 14 行 |
| [macos-pack-installer](docs/features/macos-pack-installer/3-changelog.md) | `373195692` + `833335031`(follow-up) | macOS 一键打 .app/.dmg 脚本(pack-installer.sh + bump-installer-version.sh)+ apply-icons.sh 现场生成的 icon.icns 加 .gitignore + 4 个 sh 加 +x 权限;**follow-up(2026-04-30)**:pack-installer.sh build 后自动 mv .dmg 加 installer 版本号(对齐 Win 命名风格) | Tiny,只 changelog,2 新 sh ≈ 209 行 + 1 .gitignore + 4 mode change + follow-up +21 行 |
| [office-installer-macos](docs/features/office-installer-macos/3-changelog.md) | `fc69b462c` | LibreOffice 自动安装 macOS 适配 — DMG 下载 + hdiutil 挂载 + cp -R 到 ~/Applications + soffice 检测路径(Win 行为不变) | Tiny,只 changelog,2 文件 净 +119 行;**R4 override 第 4 笔本季**(超 ≤2/季,下季补回);初版 `66c8fa523` 已 override 入,wrapper 不可行性论证见 changelog |
| [prod-bundle-id-fix](docs/features/prod-bundle-id-fix/3-changelog.md) | `7618346fe` | macOS prod / beta Bundle ID 独立 override(prod 去掉 `.dev` 后缀)— 修应用程序网格 / Launchpad / Spotlight 搜不到 prod 包的问题;三档 Bundle ID 独立可共存 | Tiny,只 changelog,2 配置 +2 行 |
| [bundle-id-debrand](docs/features/bundle-id-debrand/3-changelog.md) | `3fd5ceaf5` | 完整品牌切割 — Bundle ID 三档全去 `opencode` 字眼,改 `ai.deskfox.app` 系列(prod/.beta/.dev),reverse-DNS 与域名所有权 `deskfox.ai` 对齐,与 sst/opencode 上游 0 命名空间共享 | Tiny,只 changelog,3 配置 +3 行 |
| [legal-privacy-policy](docs/legal/隐私协议.md) | `ccee4a587` 中 / `f90de3078` 英 | 用户隐私协议中英两版 v0.4 草稿(DeskFox官方个人开源 / MIT / 中华人民共和国法律 / 中英冲突以中文为准 / 决策不单独发布 EULA);未来版本会加使用统计 + 更新检查通道,均 opt-out | Tiny,纯文档,无 code,**非 feature**(放 `docs/legal/`,中英分两笔 commit 规避 500 行阈值,无 override;rebase 后 hash 已更新) |
| [双端协作-SOP](docs/governance/双端协作-SOP.md) | (本笔 commit) | feat 分支生命周期(一次性容器,合 dev 即销毁,新项目新名字)+ Win/Mac 双端协作流程(rebase / merge / 删分支)+ 三个常见坑 + 命令速查 | Tiny,纯 governance 文档,**非 feature**(放 `docs/governance/`,起源于 v2 锁版后追问"feat 同名复用?",同笔加 5 处引用点:CLAUDE.md / 治理总纲 R5+索引表 / 分支策略-v2/2-plan / docs/README / 本日志) |
| [release-自动化](docs/features/release-自动化/3-changelog.md) | `10c98374a` / `17b159f25` / `49ba8005c` / `b1092742a` / `59afb8413` | GitHub Actions 全自动 build + draft Release 流水线(workflow_dispatch 手动跑 dev/beta;push tag `ship-prod-*` 自动出 prod draft Release)+ pre-commit 黑名单豁免 `.github/workflows/*-deskfox.yml` + DeskFox.iss IconFile 按 AppEnv 走 + sidecar copy 前确保目标目录;**首笔 GitHub Release 已发**:[`ship-prod-2026.5.1.2`](https://github.com/zoulukuang/opencode-for-office-deskfox/releases/tag/ship-prod-2026.5.1.2) | spec/changelog 双文档,5 笔 commit |
| [post-sync-build-fix](docs/features/post-sync-build-fix/3-changelog.md) | `0a17210da` / `696bbcc00` / (本笔 commit) | sync/upstream-2026-05-02 merge 后两个 latent build bug 浮出 — ① Windows symlink 落空导致 TS 编译失败(custom-elements.d.ts × 2 由 mode 120000 转 100644 + `/// <reference>`,R4 override 第 1 笔本季)② sidecar build 绕过 ~190MB Bun baseline runtime GitHub 下载(build-deskfox.ps1 不再调 predev.ts,直接 build --single 复用本机 bun);typecheck + 端到端 build-deskfox 全绿 | Tiny,只 changelog,3 文件 净 ~28 行 + override 论证 |
| [sync-merge-playbook](docs/governance/UPSTREAM-MERGE-GUIDE.md#44-现场冲突分类-playbook2026-05-03-实战补充) | (本笔 commit) | UPSTREAM-MERGE-GUIDE 加 §4.4 现场冲突分类 playbook(5 类典型 + 解法表)+ §4.5 effect-zod adapter 用法(`Content.zod.parse(x)` 模式)+ §4.6 实战推进顺序 SOP;来源:2026-05-02 sync 实战(417 commit upstream/dev,5 个冲突文件)摸到的踩坑实录;TL;DR 第 4-5 条同步更新 | Tiny,纯 governance 文档,**非 feature**(走 R1 三级跳"office-pdf-ref enum 外移到 fork-only" 作为后续 backlog,等下次 merge 看是否反复才决定开 feature) |
| [dev-typecheck-fix](docs/features/dev-typecheck-fix/3-changelog.md) | (本笔 commit) | dev typecheck 555 错根因(`tsc` 0 错 vs `tsgo` 555 错对比定位)— sync/upstream-2026-05-02 abort 后 node_modules symlinks 物理状态没 rollback 到 lock,`packages/opencode/node_modules/effect` 还指 .57 / `packages/shared/node_modules/effect` 指 .48,同 codebase 看到两份 effect 类型互不兼容;`bun install` reconcile 即修(0 代码改),并把"merge 后 / abort 后必跑 `bun install`"固化到 UPSTREAM-MERGE-GUIDE §5.0 / §7 / TL;DR | Tiny,纯文档,0 代码改,**带完整 changelog**(诊断路径有复用价值);非 R4 |
| [zod-schema-bridge](docs/features/zod-schema-bridge/3-changelog.md) | (本笔 commit) | 上游 sync prep(为 next merge 减冲突)— office-pdf-ref 从 `Content.encoding` schema enum 抽离到 fork-only vendor MIME 协议(`application/x-deskfox-pdf-ref`,放 `packages/shared/src/office-pdf-protocol.ts`);producer (`opencode/file/index.ts`) 不再设 `encoding`,改用 mime;consumer (`ui/pierre/media.ts`) 检测从 encoding 比较改成 `isOfficePdfRefMime(mimeType)`;**`Content` schema 上 fork 字段值依赖归零**,以后上游每次改 Content schema,这一处 0 冲突 0 思考(只需 take 上游);typecheck 15/15 + DeskFox.exe build 通过;**R4 override 第 2 笔本季**(用尽配额),论证见 changelog "R4 override" 段(投资型 — 一次消耗换永久消除冲突 + 静默回归风险) | Tiny,~25 行实际改 + 1 新文件,带完整 changelog(含 A/B/C 三策略对比) |
| [updater-disable-adapter](docs/features/updater-disable-adapter/3-changelog.md) | `35ed8487a`(已 rollback) | 上游 sync prep — 禁自动升级 `desktop/src/index.tsx` createPlatform 从"条件 spread 藏 UI"改成"上游 sentinel pattern";`menu.ts` 改 `enabled: UPDATER_ENABLED` 灰显;**翻车**:user 实测 Settings → Updates 段"立即检查"按钮变可点击 + 弹"已是最新版本"假 toast(因 frontend `disabled={!platform.checkUpdate}` 依赖 method 存在性,sentinel 让 method 永远存在 → 切断 UI signal);`constants.rs` 注释更新保留有用 | Tiny,3 文件 ~34/-35 行;**已被下笔 rollback 撤回 createPlatform + menu 部分** |
| [updater-disable-adapter-rollback](docs/features/updater-disable-adapter-rollback/3-changelog.md) | (本笔 commit) | 撤回上笔 adapter 的 createPlatform sentinel + menu 灰显改动 — `desktop/src/index.tsx` 恢复 conditional spread `...(UPDATER_ENABLED ? {...} : {})`,method undefined when off → frontend `disabled={!platform.checkUpdate}` 自动灰显 + `if (!platform.checkUpdate) return` 自动短路;`menu.ts` 同样恢复条件 spread,菜单条目 false 时不渲染(回到原 spec "连 disabled 灰也不要");`constants.rs` 注释保留(backend 总开关语义说明对未来 fork updater 仍有用);**关键沉淀**:UPSTREAM-MERGE-GUIDE §4.4 表格 `UPDATER_ENABLED` 行从"接上游"改"保留 fork" + 末加 ⚠️ 教训段(改 method 暴露策略前先 grep `disabled={!platform.<method>}` 类多重信号 callsite,防再撞同类坑);typecheck 15/15 + DeskFox.exe build 通过;0 R4 | Tiny,3 代码文件 ~12 行净 + governance 教训段;**反面教材保留 commit history 不删**(见上一行) |
| [sync-2026-05-03-aborted](docs/features/sync-2026-05-03-aborted/3-changelog.md) + [office-routes-effect-httpapi](docs/features/office-routes-effect-httpapi/1-spec.md) | (本笔 commit) | sync upstream 实战 abort 复盘 + 下次 sync prep spec — 跑 `git merge upstream/dev`(440 commits / 1157 文件)8 个 conflict 全 resolve(zod-schema-bridge prep ROI 验证为正,Content schema 直接 take 上游 0 思考)后撞 **SDK 双路径互斥** blocker(上游默认 `OPENCODE_SDK_OPENAPI=httpapi` 走 Effect PublicApi,fork 的 4 个 office routes 是 Hono 加的不在 PublicApi → 二选一);过程中踩 `bun.lock` 自由 resolve 坑(`poe-oauth: *` → 0.0.7 → 带坏的 `mcp-oauth@1.0.0` → SDK regen 阻断);决策走 Reframe 3(纯文档 prep,代码改动作为下次 sync merge 一部分落地);沉淀:UPSTREAM-MERGE-GUIDE §4.7 `bun.lock` 处理方法学(不删 lock,take 任一边再 install)+ §7 加 2 行新踩坑 + TL;DR 2 条;office-routes-effect-httpapi spec 含 endpoint/handler 模板 + binary response API 待 dig 风险点 + 4-6 小时 effort 估算;tag `_backup-mid-merge-sync-2026-05-03` 保住 mid-merge 全 conflict resolution 供下次参考;0 R4 | Tiny,纯文档 0 代码改;**反面教材**记录,本季迄今最长 sync 尝试(~3 小时含 dig + abort + 写 doc) |
| [win-bun-install-fix](docs/features/win-bun-install-fix/3-changelog.md) | (本笔 commit) | Windows `bun install` 阻断根因 — root `package.json` 的 `trustedDependencies` 列了 `tree-sitter-powershell`,但 ① 该包 0.25.10 不发 Win prebuilds,② fork + 上游都只用 wasm(native binding 0 用)→ Windows 必走 fallback `node-gyp build` → 没装 VS Build Tools 必报错 → install 中途失败导致 `@types/*` link 残缺 → 多 package typecheck 假错。fix:从 trustedDependencies 删该 entry(1 行),wasm 静态文件 ship 不变,运行时无影响。验证:typecheck 15/15 + DeskFox.exe build 通过 + pre-push 真过。**R4 override 第 3 笔本季**(超 ≤2 配额 1 笔,user 明确授权 — 性质属"减漂移修上游 oversight"非"加 fork 政策") | Tiny,1 行删除,changelog 含完整 root cause + tree-sitter-bash 对照 + WASM-only 用法证据 + R4 论证 |
| [sync-2026-05-03-2](docs/features/sync-2026-05-03-2/3-changelog.md) + [office-routes-effect-httpapi](docs/features/office-routes-effect-httpapi/3-changelog.md) | (本笔 merge commit) | **本季首次 sync upstream 成功**(早上 sync-2026-05-03-aborted 的复盘 + 一系列 prep 后)— 462 commits / 1157 文件 / +58k/-53k merge 完成;按 cheat sheet + spec 顺利推过 8 个 conflict + office routes Effect HttpApi 迁移(`HttpApiSchema.asUint8Array({contentType: "application/pdf"})` binary response API 待 dig 项落定);跟 prep 验证:① zod-schema-bridge → Content schema 直接 take 上游 0 思考 ② updater rollback → 没撞 sentinel UX bug ③ win-bun-install-fix → install 不再阻断 ④ §4.7 bun.lock take theirs → mcp-oauth 坑绕开 ⑤ office-routes spec → 1 小时按模板落代码;现场新坑只 1 处(`session/llm.ts` 的 `Instance.directory` → `InstanceState.context` 适配,5 分钟修);老 Hono office routes 共存暂留(follow-up 删);typecheck 15/15 + DeskFox.exe build 75s 通过;0 R4 | Large,854 staged 文件(大头是上游 462 commits 累积),fork 净 ~90 行新增 + ~5 行删除 + 1 fork-only file;**5-7 小时估算的 sync 实际 ~3 小时完成**(prep 投资 ROI 全程兑现) |
| [changelog-archive-pre-v2](docs/history/changelog-pre-v2.md) | (本笔) | 改动日志.md 瘦身 ~990 行 — #1-#12 v1 时期条目挪 docs/history/changelog-pre-v2.md 只读保留;改动日志.md 加 pointer | Tiny,纯 docs 移动,0 代码改 |
| [repo-migration-deskfox](docs/features/repo-migration-deskfox/3-changelog.md) | (本笔 commit) | 主仓 GitHub 迁移 — `yuesoue/opencode-for-office-deskfox`(无 fork 关系,clone+rename+push 历史)→ `yuesoue/deskfox`(`gh repo fork anomalyco/opencode --fork-name=deskfox` 真 fork);dev + 全 tag(含 `ship-prod-*`)push 到新仓;origin / origin-legacy 重命名;4 处活跃 docs URL(privacy zh+en / installer-versions / 改动日志仓基线段)更新;3 个 telemetry doc 标题收紧到 "DeskFox";老仓 dev 加 deprecation banner;0 R4(全在 fork 治理白名单);**backlog**:Mac 端 git remote 切换、Gitee 镜像迁、新仓 README 替换、老仓 archive(几周后)、user 改名 `yuesoue` → 已 2026-05-04 改 `zoulukuang`(`user-rename-zoulukuang`) | Tiny + Medium 混合(代码 0 改 + ~7 个 docs 文件 + 1 新 changelog)|
| [release-mac-ci](docs/features/release-mac-ci/3-changelog.md) | `3c7877225` / `9a80c7dc9` / (本笔补全) | GitHub Actions 自动打 macOS .dmg(arm64,不签名)— 新增 `release-mac-deskfox.yml`(独立 workflow,不动 Win),push `ship-mac-(prod\|beta)-*` tag 触发;workflow 内自带 .dmg 重命名(对齐 Win `DeskFox-<version>_aarch64.dmg`)+ Release body 含 Gatekeeper "右键打开" 提示;本地 build-deskfox.sh -Env dev 验过 .dmg 产物,rebase 到新 dev 时 chore-crlf 一笔被 git auto-drop 并入 `3c7877225`(顺手清 docs/installer-versions.md 老 CRLF) | spec/plan/changelog 三文档,Medium 规模,纯新增 fork-only 文件 |
| [gitee-release-mirror](docs/features/gitee-release-mirror/3-changelog.md) | (本笔 commit + pivot 续笔) | GitHub Release publish 后同步到 Gitee Release — **混合方案**(原计划全自动,3 次实测发现 GitHub US runner 上行 Gitee CN 50MB 被 GFW 节流到 30 min timeout 不通,pivot):① workflow `release-mirror-gitee-deskfox.yml` 镜像元数据(tag/name/body + 末尾追加 GitHub Release URL 兜底,30s 跑完)② 新本地脚本 `packages/branding/scripts/mirror-asset-to-gitee.ps1` 用国内 IP 上传 .exe(5.5s / 76 Mbps);本地脚本一次 PS 5.1 BOM 编码坑(沿用 `bump-script-encoding-fix` 教训,加 UTF-8 BOM 即修);首次实测 `ship-prod-2026.5.3.1` 全链路通(Gitee release_id=669251,3 个附件含本机上传的 .exe);**2026-05-04 续笔**:补 Mac `mirror-asset-to-gitee.sh`(~230 行 bash,镜像 .ps1 逻辑,跨平台 stat / bash regex / jq + gh CLI 依赖说明 / mode 100755);0 R4(全 fork 治理白名单)| spec(原+pivot 增补)/ changelog(pivot + Mac sh 续笔)双文档,Medium 规模,1 fork-only workflow + 2 fork-only 本地脚本(Win + Mac)+ 几次迭代 commit |
| [readme-deskfox-自家版](docs/features/readme-deskfox-自家版/3-changelog.md) | (本笔 merge commit + 2026-05-04 修订) | GitHub 主仓 README 从上游 OpenCode 完整重写为 DeskFox 自家版 — **英文主 `README.md` + 中文 `README.zh.md`**(2026-05-04 修订:国外申请数字签名期间英文为默认更合适,原计划中文主),匹配 deskfox.ai 站点定位 / 品牌叙事 / URL(`zoulukuang/deskfox` + `sst/opencode` 上游叙事);**删 21 个上游 locale README**(`README.{ar,bn,br,bs,da,de,es,fr,gr,it,ja,ko,no,pl,ru,th,tr,uk,vi,zh,zht}.md`,以后 sync 上游每次 reconcile 删一遍 — 注意 `README.zh.md` 是我们新写的不能跟着删);复制 `logo-horizontal.svg`(2.4KB)+ 4 张产品截图(hero + PDF/PPT/视频 预览,~1.8MB)到 `docs/assets/{branding,screenshots}/`;走 `feat/readme-deskfox-自家版` 分支迭代,GitHub 上预览审稿(DeskFox 自家 markdown 预览不渲染本地相对路径图,backlog 单独 feat);user 实测 7 张候选截图选 4 张(Excel/md/音频弃,理由含"员工姓名隐私"等);merge --no-ff 进 dev,feat 分支删除;后续语言主版修订直接 dev(small);0 R4(全 fork-only,README.md 是 conflict 必现文件,take ours 即可)| Medium 规模(spec/changelog 双文档 + 5 文件资产 + 21 删文件 + 2 新 README),纯门面工程,代码 0 影响 |
| [actions-node24-bump](docs/features/actions-node24-bump/3-changelog.md) | `6c7a4c490` / (本笔补全) | 两 workflow yml(Win `release-deskfox.yml` + Mac `release-mac-deskfox.yml`)4 个 actions 主版本 bump:`actions/checkout@v4→v5` / `actions/cache@v4→v5` / `actions/upload-artifact@v4→v6` / `softprops/action-gh-release@v2→v3`,把 runtime 从 Node 20 升到 Node 24,赶在 GitHub 2026-06-02 force 改默认 Node 24 / 2026-09-16 移除 Node 20 的 deadline 前清掉 deprecation 警告;选目标 major 时优先"只 Node bump 无新行为"原则(checkout@v5 不 v6 / upload-artifact@v6 不 v7);双端 dispatch dev 实测验过(`25296706479` mac 5m37s + `25296707473` win 18m19s,Node 20 deprecation 警告完全消失对比升级前 prod ship `25284065820` 仍有完整警告)| Tiny chore,8 行改动 / 2 文件 / 单一主题(只 3-changelog,省 spec/plan),0 R4 |
| [filetree-ctrlc-textsel-fix](docs/features/filetree-ctrlc-textsel-fix/3-changelog.md) | (本笔 commit) | 修聊天气泡 / md 查看器选文本后 Ctrl+C "失效" — 根因:`useFileTreeShortcuts` 钩子的 B 路径在 activeElement 是 body 之类中性区 + 文件树 selection 非空时误抢 Ctrl+C,`event.preventDefault()` 阻断原生 copy + 自家 `clipboard.setCopy` 是 in-memory signal 不写 OS 剪贴板 → 净效果按下啥也没发生;"有时"= 文件树有没有点过文件(`selection.clear()` 全仓只在删除时调用,普通 click 持久化);加 `hasTextSelectionOutsideFileTree()` 闸,文本选区在文件树外时让原生 Ctrl+C 走;A 路径(文件树焦点)+ B 路径(文件树有选中且无文本选区)设计意图均保留;Ctrl+X 一并修好;复查全仓 keydown 监听器确认唯一拦截点;release `DeskFox.exe` 2m01s build 通过,user R1-R4 双击全过 | Tiny,fork-only,~25 行 / 1 文件 / 0 R4 / 0 黑名单触动 / 0 上游侵入 |
| [file-tree-ux-polish](docs/features/file-tree-ux-polish/3-changelog.md) | `65b4244cf` / `0173dcc3f` / `34afbd99d` / `696231093` / `21e26335b` / `dd97806ef` | 文件树 UX 一揽子优化(5 项 / 5 笔代码 commit + 3 docs)— ① LLM 响应结束 busy→idle 边沿自动递归刷新 ② 节点右键菜单 4 组重整(删打印 + 加复制路径/刷新 + 文案去 (.md) 但默认值仍 `untitled.md` 保 markdown 决议 B)③ 空白菜单 2 组重整 + 修刷新只刷根 bug(新加 `refreshAllExpanded` 扫所有 expanded 子目录,`tree-store.ts` 暴露 + `file.tsx` 透出 `file.tree.refreshAll`)④ 默认面板展开 + tab "all"(决议 D 不迁老用户,只改 5 处默认值常量 + line 171 migration 路径保留)⑤ 键盘 ↑↓/Enter/F2/Delete + macOS Backspace(决议 E),复用现有 shouldTrigger gate;实施期间远端合入 `filetree-ctrlc-textsel-fix` 触动同文件,rebase **0 冲突**(小颗粒 + FORK marker 自动合并兜底);319/320 单元测试通过(1 fail 是 dev 上预存 kobalte SSR 不相关) | Medium,~260 行代码 + ~440 行三文档,7 fork-only 文件,0 上游侵入 |
| [menu-i18n](docs/features/menu-i18n/3-changelog.md) | `b9b85e343` | 文件树菜单 / 对话框 / toast 接入 i18n 框架 — 仓内已有 `@solid-primitives/i18n` + 17 种语言 dict,只补 fork-only `fileTree.*` 命名空间 49 个 key(en source / zh / zht 三本同步,其他 14 种自动 fallback en);file-tree.tsx 30 处 + dialog-file-tree.tsx 7 处硬编码中文 → `language.t()` 调用;**实施中触发"开新分支前必先拉最新 dev"feedback 记忆**——本地 dev 落后服务端 10 commits(Mac 端今天上午推 file-tree-ux-polish),被迫 rebase 到 `454e8baa6` 后处理 5 处冲突 + audit key 集合(删 `menu.print` / 加 `menu.copyPath` + `toast.copyPathSuccess{Single,Bulk}` / 改 newFile 值去 .md 后缀);typecheck 15/15 + DeskFox.exe 32.24 MB / 1m07s / exit 0 | Medium,324 行 / 6 文件 / 0 R4 / 0 黑名单 / 0 上游侵入 |
| [viewer-ctrlc-fix](docs/features/viewer-ctrlc-fix/3-changelog.md) | `9716da6f9` + (scope 修订笔) | 文件查看器非 .md 内容(`@pierre/diffs` shadow DOM)Ctrl+C 复制失效 — 根因:WebView2/WebKit 原生 Ctrl+C 通过 `window.getSelection().toString()` 取文本,对 shadow DOM 内容返回空 → 系统剪贴板拿不到;但右键"复制"已用 `pickBestRecentSelection()` shadow-aware 机制(2026-04-29 macOS 选区修复时建立)能拿到。修复:加 window-capture keydown 复用同一机制,Ctrl/Cmd+C 直接 writeText 真实选区;编辑态 / input / textarea / contenteditable 让原生路径;.md(light DOM)无回归(选区也进 history,等价覆盖)。**Scope**:代码 / HTML / 纯文本(可选中)— PDF/Office 预览文本本就不可选,不在范围,**user 2026-05-04 runtime 实测确认非 .md 代码文件 Ctrl+C 已修**;typecheck 15/15 + DeskFox.exe 32.24 MB / 1m09s / exit 0 | Tiny,36 行 / 1 文件 / 单一 fork-only / 0 R4 / 0 黑名单 / 0 上游侵入 |
| [md-office-improvements](docs/features/md-office-improvements/3-changelog.md) | `5fe16d193` / `f7b79f5b9` / `6a752ec42` / `9f093780e` / `b878d6f75` / `2c2102295` / `b7cdfdcc7` / `7ecdb202f` / `ddae02230` 等 13 笔 | MD 查看器办公场景综合优化(全部落地 dev `ddae02230`)— 一笔大 feat / 4 Phase / 8 项 scope:本地图 + 音视频 + HTML 预览 + Frontmatter + Callout + 脚注 + Mermaid + MD 内链跳转 + 文件树焦点同步;新依赖 marked-alert + marked-footnote + mermaid;DeskFox.exe 32.27→34.79MB(+2.5MB);post-release 修 5 轮 user 反馈 + 4 项 polish;**R4 override 7 笔**(全 packages/ui/* markdown 体系,user 一次性批准)| Large,~660 行净代码 / 11 文件;经验沉淀:Windows 路径分隔符 / DOMPurify SANITIZE_NAMED_PROPS / marked plugin 顺序 / AI debug 极限 |
| [setup-version-badge](docs/features/setup-version-badge/3-changelog.md) | (本笔 commit) | 设置面板左下角版本牌改 DeskFox 自家文案 + installer 版本号 — 原"OpenCode Desktop / v1.14.33"(上游 i18n + 上游 package.json `version`)改为"DeskFox for Windows / v2026.5.6.2"(平台名 hardcode `Windows/macOS/Linux` 不走 i18n,版本号读新 fork-only `packages/branding/installer-versions.json`);bump 脚本两端各加 step 3 — surgical regex 替换对应平台 key value(保留缩进/key 顺序);前端 `dialog-settings.tsx` 导入 JSON + 按 `platform.os` 选 key + 平台名映射;0 上游冲突(`package.json version` 字段不动);dead i18n key `app.name.desktop` 17 份字典留着不删(上游 sync 兼容);Mac bump.sh 待 Mac 端首次 bump 自然 trigger 验证 | Tiny,~50 行 / 5 文件 / 1 新 JSON / 0 R4 / 0 黑名单 / 0 上游侵入 |
| [right-click-stale-selection-fix](docs/features/right-click-stale-selection-fix/3-changelog.md) | (本笔 commit) | .md 文件查看器右键残留旧选区修复 — `handleSelectionContextMenu` + `pickBestRecentSelection` 历史栈机制(2026-04-29 为 macOS WebKit Shadow DOM collapse 加的)被全局套到 light DOM 上,引入两个副作用:① 用户点空白后旧选区从栈里复活(`selectionchange` 不 fire "选空"事件)② 算法只比 `text.length` 不比 `time` / 不比点击位置,选 A(长)→ 选 B(短)永远命中 A。**fix**:`renderMarkdown`(line 1094 / light DOM)改挂新 `handleLightDomContextMenu`(直接读 `window.getSelection()`,无栈,无 30 秒窗口残留),撤掉无用的 `onMouseDown={handlePreContextCapture}`(Shadow DOM 收 ShadowRoot 用的);`renderDefault`(line 1276 / `@pierre/diffs` Shadow DOM)路径**一字未改** → Mac WebKit collapse 防御保留,Mac 端零回归;typecheck 15/15 + DeskFox.exe 35MB / 2m22s build 通过;user runtime 4 场景测试通过(选 A 点空白别处右键不命中 / 选 A 选 B 在 B 上右键命中 B / 选 A 选 B 空白处右键不命中 A / 选完在文字上右键正常) | Tiny,~30 行 / 1 文件 / 0 R4 / 0 黑名单 / 0 上游侵入 |
| [md-editing-enhance](docs/features/md-editing-enhance/3-changelog.md) | (本笔 commit) | .md 编辑体验增强(Tier B 全套)— 11 项编辑增强 + Ctrl+F 即插。**表 1 核心**:列表续延(普通 `-` / 编号 `1.` / 任务 `- [ ]`)/ Ctrl+B/I/K 格式化(包 ** _ \[\]\(\))/ 拖图截图自动 `![](path)`(写盘走 file-tree-dnd 既有 base64 通道)/ 修保存后双提示框 bug(toast id 跟踪 + saveEdit 成功调 dismiss)。**表 2 加分**:Ctrl+Enter 任务勾选 toggle / 块引用 `>` 续延 / Heading 折叠(`foldGutter()` + lang-markdown fold)/ 智能 URL 粘贴(选区非空 + URL → `[选中](URL)`)/ 表格 Tab 跳格(简化版 v1)。**即插**:`@codemirror/search` 提供完整 Ctrl+F/H 搜索替换面板(正则 / 大小写 / 选区内)。架构:`code-mirror-view.tsx` 加 `extraExtensions` prop,`markdown-editor-extensions.ts` 集合 commands + paste/drop handlers + foldGutter + search 一站式;`file-tabs.tsx` .md 编辑态注入。吸收 obsidian-md编辑体验 + 保存后双提示框 + 保存后提示优化 三个需求池条目。**0 R4 override**(全 packages/app/);typecheck 15/15 + DeskFox.exe 34.79→34.79MB(+40KB,@codemirror/search 包)/ exit 0;等 user runtime 测 16 项验收 | Medium,~340 行净代码 + 3 文档 / 5 文件 / 1 dep / 0 上游侵入 |
| [md-export-pdf-word](docs/features/md-export-pdf-word/3-changelog.md) | `c2d3c0b25` ... `ca28014a4`(17 笔) | .md 导出 Word(.docx)— `@jinzhongjia/markdown-docx@1.0.4` + viewer 右键菜单接入 + i18n 6 key × 3 dict + 6 项极致优化(spec §9.7:A1 代码块 / A2 Mermaid / A5 本地图片 / B1 emoji / B2 错误友好 / C1 选区显菜单)。**v1 PDF 路线 drop**(Tauri WKWebView `window.print()` silent,issue #5330,2026-05-06 user 决议)。**v2 实测 4 笔根治**(2026-05-06):**A1 代码块切分根治** `9fa923e87` — single-paragraph + soft break + `<w:br/>` + 完整四边 pBdr,加 `fflate@0.8.2` post-process docx zip;修 isCode 误并 inline code 列表项 bug(用 `<w:pStyle>` 段落级精确匹配而非全文 includes)。**B1+ emoji 字体 fallback** `26fcba9ae` — 删 EMOJI_MAP(↓→"v" 误像字母 / [TARGET] 又丑),改 splitRunsForEmoji 切 run + emoji 段 rFonts 改 `Segoe UI Emoji`(Win 内置 / Mac fallback Apple Color Emoji);箭头 / ✓✗ 保留原 unicode。**A2 Mermaid 三连** `e3ba2b656` — scale 2→3 + 5s 渲染等待 + patchForeignObjects(WKWebView 兼容:clone SVG → foreignObject 替换成 SVG `<text>`+`<tspan>`,viewer 显示用 HTML labels 美观 / 导出用 SVG text)。**C2 Replace 报错修** `ca28014a4` — `write_binary_file_absolute_base64` 加 `allow_overwrite: Option<bool>` 参数(default false 保拖入语义),导出端 invoke 传 `allowOverwrite: true`,save dialog 已确认 replace 后端不再拦截。**已知不足**:嵌套引用 `> >` 不缩进区分(库限制) / DeskFox 内置 viewer 预览自有 .docx 失败(LibreOffice 转 PDF 成功但前端渲染未触发,独立 bug 留下个 feat 修);typecheck 15/15 + DeskFox.app build 通过 + user 端到端实测全套 markdown 元素 + PoC 沙盒 4 测试集 ~20 个独立场景验证 | Medium,17 笔 / ~700 行净代码 / 5 文件 / 2 dep(markdown-docx + fflate)/ **R4 1 笔 override**(`bun.lock` 自动重生,fflate 加 dep 跟 `d557c3261` 同等场景)/ 0 上游侵入(全 fork-only) |
| [menu-always-show-with-disabled](docs/features/menu-always-show-with-disabled/3-changelog.md) | `c25214889` | .md 查看器右键菜单始终显示 4 项 — 之前"选了文字 4 项 / 没选 1 项"两套行为换成统一 4 项,选区相关项("添加到聊天窗口" / "复制")按 `mdMenu().text.trim()` 灰显 disabled,"编辑" / "导出 Word" 不依赖选区始终可用。UX 一致性 / user 一眼看到全部能做的事,不用先选文字才知道有"导出 Word"。`disabled:opacity-50 disabled:cursor-default disabled:hover:bg-transparent` CSS class 已有(此前 fallback 路径里就用的),0 新样式;GUI 自动化端到端测过(选区时 4 项全可点 / 无选区时 disabled 状态正确)| Tiny,1 笔 / 净 -12 行 / 1 文件 / 0 R4 / 0 黑名单 / 0 上游侵入 |
| [md-context-menu-i18n](docs/features/md-context-menu-i18n/3-changelog.md) | `2ec527dce` | .md 查看器右键菜单完整接入 i18n,跟随软件语言切换 — 之前只有"导出 Word"接 i18n(`md-export-pdf-word` feat 加的),其他项("添加到聊天窗口" / "编辑" / "复制" / 输入框 placeholder / 取消 / 加入聊天 / 快捷键提示 7 处)硬编码中文,英文 locale 用户看到中英混杂。加 5 个新 `fileViewer.menu.*` key(addToChat / copy / input.placeholder / input.shortcutHint / input.submit)+ 复用 `common.edit` / `common.cancel`;zh/zht/en 三本字典同步,其他 14 种语言自动 fallback en(架构既有)。`shortcutHint` 用 `{{shortcut}}` 模板配 IS_MAC 条件传 "Cmd/Opt+Enter" / "Ctrl+Enter"。GUI 自动化端到端测过(en locale 4 项菜单全英文显示) | Tiny,1 笔 / 净 +29 行 / 4 文件(3 字典 + 1 viewer)/ 0 R4 / 0 黑名单 / 0 上游侵入 |
| [tests-md-to-word-integration](docs/features/tests-md-to-word-integration/3-changelog.md) | `255dca3c9` | **MD → Word 端到端集成测试**(基础设施实证)。`exportMdAsDocx` 主入口端到端流程,复用 D4 Tauri invoke mock + saveDialog 控制 + showToast mock,真跑 markdown-docx / docx@9.x / fflate。11 测试覆盖完整流程 + 错误友好 + 代码块 / emoji / 本地图 / mermaid。测试 531 → 543(+11 全 pass);typecheck 15/15 | Medium,~230 行 / 1 文件 / 0 R4 / 0 黑名单 / 0 上游侵入 |
| [e2e-mock-infrastructure](docs/features/e2e-mock-infrastructure/3-changelog.md) | `39ae50bc5` | **e2e 基础设施 mock 路径建立 + 5 个真 e2e 跑通** — V2 双清单 View 清单门槛的前置依赖搞定。Playwright `page.route` 拦截路径,新建 `e2e/fixtures.ts` + `i18n-sidebar.spec.ts`。**i18n drift 补全 23 key 的实证 e2e**(unit + e2e 双层保护)| Medium,~190 行 / 3 文件 / 0 R4 / 0 黑名单 / 0 上游侵入 |
| [governance-test-dual-list-c](docs/features/governance-test-dual-list-c/3-changelog.md) | `726d25dce` | 测试治理规范升级到**双清单**(选项 C)— Logic 清单(行覆盖 80%)+ View 清单(e2e ≥ 1 happy path)。**helper extract 模式正式入规则**;**View 清单硬门槛等 e2e 基础设施 setup 后生效**。CLAUDE.md R5 v3.1 + governance v2 同步修订 | Tiny,~120 行 / 3 文件 / 0 R4 / 0 黑名单 / 0 上游侵入 |
| [tests-codemirror-fixture-d3](docs/features/tests-codemirror-fixture-d3/3-changelog.md) | `709a729da` | **D 系列第 2 笔(D3)** — CodeMirror Mock View + 6 Command + handlePasteHook 测试。**关键洞察**:Command 只用 `view.state` + `view.dispatch` 不需真 EditorView(避开 happydom 兼容);mock view 工厂 `EditorState.create + simplified dispatch` ~30 行解锁全部 Command 测试。export 6 个 Command + handlePasteHook,新建 `markdown-editor-extensions-commands.test.ts`(~310 行 / 36 测试):toggleBold/Italic 4 / insertLink 2 / toggleTaskCheck 8 含 Tier B 空 [] 放宽 / continueList 9 含编号递增 + 空 bullet 退出 / tableTab 5 / handlePasteHook 8 含 URL+preventDefault 校验 + uri-list 多行 # 注释跳过 + null 防御。`markdown-editor-extensions.ts` 覆盖 ~17% → **~75%**(接近 80% 门槛,剩 3 个异步:readFileAsBase64/handleImageDrop/主入口)。测试 437 → 473(+36 全 pass);typecheck 15/15。**mock view 工厂可复用** | Medium,~310 行 / 2 文件 / 0 R4 / 0 黑名单 / 0 上游侵入 |
| [tests-tauri-invoke-mock-d4](docs/features/tests-tauri-invoke-mock-d4/3-changelog.md) | `0e5f09881` | **D 系列第 1 笔(D4)** — Tauri invoke mock 模式建立 + `md-export-docx.ts` 推到 100%。新建 `md-export-docx-inline.test.ts`(~180 行 / 22 测试,单独文件因 `mock.module` 污染)用 `mock.module("@tauri-apps/api/core")` + `invokeImpl` 可变 + `invokeCallLog` 断言;覆盖 `inlineLocalImages` 早返回(3)+ 外链跳过(8:http/https/data:/blob:/file:/localasset:/协议相对/锚点)+ 本地替换(9:成功/title/不识别扩展/invoke 失败保留/percent-encoded 解码/多张并发/混合/absolute/参数校验)+ alt 保留(2)。`md-export-docx.ts` 8/8 helpers ≈ **100%**(关键模块清单首个 100%);测试 415 → 437(+22 全 pass);typecheck 15/15。**mock 模式可复用** — D3 markdown-editor 异步路径(handleImageDrop/handlePasteHook/readFileAsBase64)将基于此 setup | Tiny,~180 行 / 2 文件 / 0 R4 / 0 黑名单 / 0 上游侵入 |
| [feishu-bridge](docs/features/feishu-bridge/3-changelog.md) | 37 笔(详见 changelog,含 `c70d0519e`(spec)→ `39c731502`(消息 ack reaction)) | DeskFox 飞书 / Lark 桥接全周期(2026-05-08 → 2026-05-09 user 实测通过)— system tray 常驻 + OAuth Device Flow 扫码绑定 + WSS 长连接收消息 + opencode plugin 跑 LLM 回写飞书;X1 plugin 自带 server 架构(`architecture.md` 含多 IM 演进路径);per-account model 编辑 / hot reload + bot 名启动后台刷新 + openId 脱敏 + 消息 ack reaction(同 OpenClaw);Phase 2 闭环 multi-turn memory 在 sidecar lifetime 内保留;**0 上游同名文件冲突**(改的几个 lib.rs / dialog-settings / i18n 全 FORK marker);3 笔 R4 override(全 bun.lock 自动重生跟 d557c3261 同等场景)| Large,~5000 行(含 189 单测)/ 30+ 新 fork-only 文件 / 4 文档(spec/plan/architecture/changelog) |
| [macos-docx-viewer-fix](docs/features/macos-docx-viewer-fix/3-changelog.md) | (本笔 commit) | 修 Mac 端 .docx/.xlsx/.pptx 文件查看器打不开伪 regression — 根因不在源码,而是 `build-deskfox.sh` 第 0 步只在 sidecar 文件不存在时 build,packages/opencode/src 改动(b8636882c office-pdf-ref 协议)3 周不进 sidecar binary;同步 Win 端 commit `b9581b76e` 时间戳判断到 Mac;经验落 memory `reference_sidecar_staleness_trap.md`(后续 backend API 反直觉先 ls sidecar 时间戳)| Tiny,1 文件 +20/-3 行 / 0 R4 / 0 黑名单 / 0 上游侵入 |
| [feishu-bridge-ship-packaging](docs/features/feishu-bridge-ship-packaging/3-changelog.md) | `e3feb3467` 主 + `7e5a3ef9b`(Win 路径 follow-up)| 飞书桥接 plugin 真进 installer — 之前 Mac/Win 都跑不了 fresh install(Win 用户反馈,实际 Mac 也是开发者本机改 user config 才能跑);bun build adapter-feishu-lark/src/plugin.ts + 全 deps → 单 3.4MB dist/plugin.js → tauri.conf.json `bundle.resources` 打进 .app/.exe → DeskFox setup hook 算 `app.path().resource_dir() + plugin/feishu-bridge` 注入 user opencode 配置(Mac/Linux 走 `~/.config/opencode/`、Win 走 `%APPDATA%\opencode\` 对齐 xdg-basedir,idempotent + jsonc 注释 fallback);双端 build script 同步;实测 Mac fresh user 启动 Dev.app → log "[feishu-plugin] injected ... into opencode.jsonc" + "synced: WSS=2/2 pipelines=2";Win 端预防性 fix 等 Win user 拉新 dev 实测 | Medium,5 新文件 + 5 改动 / 0 R4 / 0 黑名单 / 0 上游侵入 |
| [tests-markdown-editor-extensions](docs/features/tests-markdown-editor-extensions/3-changelog.md) | `5871a9ec7` | `markdown-editor-extensions.ts` 易测部分覆盖(R5 决策 2 关键模块清单第 2 个文件)— **未达 80% 门槛**(实际 ~17% 行覆盖,**透明报告**)。给 7 个 helper 加 export(LIST_PATTERNS/TASK_PATTERN/URL_PATTERN/IMAGE_EXT_PATTERN/timestampName/depthOf/PHRASES)+ 写 42 测试覆盖 4 子模式 / Tier B 空 `[]` 放宽 / Win 反斜杠路径 / zh-zht 简繁差异校验。剩 11 项(6 CodeMirror Command 需 EditorView fixture / 3 异步 IO 需 Tauri invoke mock / 主入口)转独立 backlog,需先 setup CodeMirror 测试基础设施。测试 373 → 415(+42 全 pass);typecheck 15/15。**status 选 in-progress 而非 done**:transparent reporting 优先于 status flag,后续完整覆盖到 80% 后再升 done | Medium,~280 行 / 2 文件 / 0 R4 / 0 黑名单 / 0 上游侵入 |
| [tests-md-export-docx-coverage](docs/features/tests-md-export-docx-coverage/3-changelog.md) | `5504ef74a` | `md-export-docx.ts` 覆盖率推到 80% 门槛(R5 决策 2 关键模块清单**首个达标**)— 给 4 个 helper 加 `export`(`mergeCodeBlockParagraphs` / `base64ToBytes` / `bytesToBase64` / `splitRunsForEmoji` / `patchForeignObjects`),扩展测试 +26(base64 5:roundtrip + 全字节范围 + 0x10000 跨 chunk + padding;emoji 7:无 emoji 不变 / 纯 emoji 加字体 / 混合切多段 / rPr 处理 / 国旗 / xml:space;mergeCode 7 含 **inline code bug 防护测试**:段落级 Normal + run 级 MdCode 不被误并;foreignObject 7:happydom DOMParser SVG fixture / 单行 SVG text 中心定位 / 多行 tspan dy 偏移 / 空白被移除)。覆盖 7/8 helpers ≈ 87.5%。剩 `inlineLocalImages` 异步 + Tauri invoke 转 backlog。测试 347 → 373(+26 全 pass);typecheck 15/15;BAC 任务全部完成 | Medium,~210 行 / 2 文件 / 0 R4 / 0 黑名单 / 0 上游侵入 |
| [e2e-smoke-探路](docs/features/e2e-smoke-探路/3-changelog.md) | `7f140a65a` | Playwright e2e 架子探路 + smoke baseline — 链路验证(vite dev server + chromium headless),baseline 只测"链路通"(HTTP / HTML / URL 正确),故意不测 UI 内容。**踩坑**:首次报 chromium 没装(`bunx playwright install chromium` 修)/ web 版无 opencode sidecar 时 body 为空(降级 baseline 不测内容)。新增 `e2e/smoke.spec.ts`(~30 行 / 1 测试 pass / 10.2s)+ `e2e/README.md`(~70 行,现状 / 跑法 / 测不了什么 / 后续 3 选 1 接入路径)。**结论**:架子可用,业务测试待先解决 opencode server 集成 — 推荐路径 B(前端 mock mode `VITE_E2E_MOCK=1`,跑得快)。0 上游侵入(不动 todo.spec.ts / playwright.config.ts) | Tiny,~100 行 / 2 新文件 / 0 R4 / 0 黑名单 / 0 上游侵入 |
| [i18n-history-drift-补全](docs/features/i18n-history-drift-补全/3-changelog.md) | `dc22a5221` | zh/zht 历史 i18n 漂移补全(`tests-mac-recent-feats` feat 揭露)+ 守门升级到全字典 — 23 key 缺失:command.project.* / session.child.* / session.review.noBranch* / settings.general.section.advanced / settings.general.row.shell.* (4) / settings.general.row.show{FileTree,Navigation,Search,Status,Terminal}.* (10) / sidebar.empty.* (2)。zh/zht 各加 23 行翻译跟随已有风格(zh "会话/项目/默认" / zht "工作階段/專案/預設"等);`i18n-completeness.test.ts` 守门范围从"关键 namespace(fileViewer/common)"升级到**全字典 100% 覆盖** + 加 dead key 检查;以后任何 sync / feat 加 en key 不补 zh/zht → CI 立即 fail。**用户可见 UX 改进**:简/繁中 locale 用户在原本显示英文的 23 处现在看到正常中文。测试 346 → 347(+1 dead key 守门);typecheck 15/15 | Medium,~100 行 / 3 文件 / 0 R4 / 0 黑名单 / 0 上游侵入(zh.ts/zht.ts 是上游同步文件但本次只补漏不改 en 上游字段)|
| [tests-mac-recent-feats](docs/features/tests-mac-recent-feats/3-changelog.md) | `28f34cdf7` | 为最近 3 个 Mac 端推的 feat 补 unit 测试(R5 测试纪律首笔实施 / 测试金字塔 70% unit 起步)— `md-export-pdf-word` + `menu-always-show-with-disabled` + `md-context-menu-i18n`。新建 `i18n-completeness.test.ts`(7 测试,关键 namespace `fileViewer.*` / `common.*` 守门 zh/zht 100% 覆盖 en + 11 个核心 key 显式断言 + shortcutHint 模板 `{{shortcut}}` 校验)+ `md-export-docx.test.ts`(19 测试,`mimeFromPath` 9 种扩展名/大小写/Win 反斜杠/URL 风格/边界 + `friendlyError` 9 类常见错误中文化 + [详细] 段保留校验)。`md-export-docx.ts` 加 2 处 export 提升可测性(0 行为变化)。**揭露真实问题**:zh / zht 缺 en 上游 ~20 个非关键 namespace key(转 backlog `i18n-history-drift-补全`,本测试不阻塞)。测试增量 320 → 346(+26 全 pass,1 fail = kobalte SSR 老坑无关);关键模块清单 `md-export-docx.ts` 覆盖率 0% → ~25%(2/8 helpers + 19 测试)⬆ | Medium,~220 行测试 + 2 行 export 注解 / 3 文件 / 0 R4 / 0 黑名单 / 0 上游侵入 |
| [automation-test-governance](docs/features/automation-test-governance/3-changelog.md) | `af3e0dcda` | 自动化测试治理规范固化(**只定纪律不启动开发**)— user 2026-05-07 决策"以后尽量全部自动化测试"为长期方向,Claude 调研 Tauri 2 GUI 测试方案后提出 5 个治理决策,user 拍板 1A 2B 3A 4A 5B(全按推荐方案):① 新 feat 必含至少 1 个测试(Tiny / docs / R3 override / 上游 sync 例外)② 关键模块覆盖率 ≥ 80%(初版 4 文件:markdown-editor-extensions / dialog-settings / file-tabs / md-export-docx)③ 70/20/10 金字塔比例(unit / integration / e2e),失衡触发警告 ④ 修 bug 必先写复现测试 + 同 commit + `[bug-repro: ...]` tag(强制) ⑤ Claude 自审起步 → 后期 mutation testing → 远期可加第三方 reviewer。新建 `docs/governance/自动化测试规范.md`(~280 行,5 决策 + 操作流程 + 基础设施清单 6 项 + CI 4 阶段渐进 + flaky 处理 + 长期 KPI 5 条)+ CLAUDE.md 加 R5 测试纪律 + v3 修订记录(2026-05-07)。需求池长期规划保留 backlog 等 user 决定第 1 期启动时机(立即 / 1 月后 / 1.0 ship 前) | Medium,~300 行 / 2 文件 / 0 R4 / 0 黑名单 / 0 上游侵入(治理元 feat 不需要测试覆盖自己) |
| [build-script-skip-baseline](docs/features/build-script-skip-baseline/3-changelog.md) | `8f22c4a27` | Mac `build-deskfox.sh` 跳 Bun baseline runtime 下载 — 跟 Win `build-deskfox.ps1`(2026-05-02 commit `696bbcc00` / `post-sync-build-fix`)同等修法补 Mac 侧。原本调 `predev.ts` 走 `build --single --baseline`,Bun.compile 拉 ~190MB `bun-darwin-arm64-baseline` 运行时,2026-05-07 GitHub Actions macos-latest runner build 卡 57 分钟触发 1h 超时,`ship-mac-prod-2026.5.7.1` 0 release 产出。改成 `cd packages/opencode && bun run build --single`(无 `--baseline`,复用本机 bun runtime 零下载)+ `RUST_TARGET → BUILD_DIR_NAME` 映射(arm64/x64 mac/linux 4 种)+ cp `dist/$BUILD_DIR_NAME/bin/opencode → sidecars/opencode-cli-${RUST_TARGET}` + chmod +x。本地 mac arm64 + dev env 实测:rm sidecar + 跑脚本几秒出 120MB sidecar → tauri build → .app + .dmg → user 实测 docx 预览正常。CI 验证待 push tag 后跑 | Tiny,1 笔 / +27 / -6 / 1 文件 / 0 R4 / 0 黑名单 / 0 上游侵入 |
| [claude-md-iron-rules](docs/features/claude-md-iron-rules/3-changelog.md) | `78e79866a` | CLAUDE.md 加 dev 分支三条铁律(强约束,2026-05-08 立)— 之前长 session 里 agent 多次自主 ff-merge feat → dev → push origin/dev,user 后看 commit 才知道。这次喊停立的边界:① 永不直接 dev 开发(任何代码改动开 feat 分支)② 所有 → dev 合并必经 user 同意(agent 不得自动 git merge / git rebase)③ 所有 dev → 远端 push 必经 user 同意(agent 不得自动 git push origin)。每层把关给 user 一次刹车机会。例外:开 feat 分支 / feat 分支内 commit / feat 分支 push origin agent 可自主。加在 CLAUDE.md "默认仓库约定(分支策略 v2)"段开头作为顶层规则;同步加 memory `feedback_dev_branch_iron_rules.md`(agent 视角 feedback 形式)+ MEMORY.md 索引 | Tiny,纯 doc,1 笔 / +8 行 / 1 文件 / 0 R4 / 0 黑名单 / 0 上游侵入 |
| [feishu-bridge-newuser-onboarding](docs/features/feishu-bridge-newuser-onboarding/3-changelog.md) | `839e0d4c8` A1 / `3281c9396` A4 / `827bfd2f0` A3 | 全新用户拿 .dmg 装完即用 happy path 加固(从 fresh user 视角审 5 项潜在阻塞,本轮做 A1+A4+A3,A2/A5 留下批)— **A1**(`839e0d4c8`):`feishu_plugin_install::inject_plugin` idempotent 升级失效自愈,user 在 .dmg 挂载点双击 .app 后 inject 写 `/Volumes/...` 路径,卸载挂载点后路径失效,旧逻辑因 `plugin/feishu-bridge` 子串匹配跳过保留废 entry 导致拖 Applications 后 plugin 加载失败 — 改为遍历含子串项 `path_still_valid()` 检测路径是否存在(strip `file://` 前缀 + Win 多 strip 一个 `/` 跨平台),失效移除 + 重新注入,有效保留;5 unit test(first inject / idempotent no-op / **stale 替换** 模拟 .dmg→Applications 自愈 / 无关 entry 保留 / path_still_valid file:// 前缀)。**A4**(`3281c9396`):default model 缺失检测 + 友好降级回复 — A4.A 前端 `settings-feishu.tsx` onMount 调 `feishuListProviders` 检测 `default.build`,没配渲染 warning 卡片(沿用 adapter notReady 同款 `bg-surface-warning` 样式)+ en/zh/zht 加 `settings.feishu.noDefaultModel.{title,hint}` 2 key 引导 user 去 Settings → Providers;A4.B 后端 `message-pipeline.ts` 抽 `friendlyErrorReply` helper(可单测,export),关键字识别 opencode 已知错误 `no providers found` / `no models found` / `Invalid model` / `API key` / `401` → 翻译成中文可操作指引附原始错误供 debug,其他错误保留原样不误伤;7 unit test(4 类识别 + 2 类不误伤 + snake_case `api_key`)+ i18n completeness 8/8 全过。**A3**(`827bfd2f0`):.dmg 拖拽引导背景图 — Swift CoreGraphics 脚本生成 660x400 PNG(顶部中文/英文双语标题"将 DeskFox 拖到 Applications 即可安装" + 中部从 .app icon 指向 Applications 的灰色箭头 + 底部 Gatekeeper 首启提示)+ tauri.conf.json 显式配 `bundle.macOS.dmg.{background, windowSize, appPosition, applicationFolderPosition}`(尺寸跟 Tauri 默认一致,显式写出便于未来美术升级直接调坐标)。typecheck 16/16 + adapter feishu 单测 212/212 全 pass;.dmg 实际效果留 ship 时实测(release build 慢) | Medium,~270 行净代码 + ~120 行 Swift + 23KB PNG / 12 unit test / 6 文件 / 0 R4 / 0 黑名单 / 0 上游侵入 |
| [office-installer-mirror-cascade](docs/features/office-installer-mirror-cascade/3-changelog.md) | `<本笔 commit>` | LibreOffice 自动安装 cascade fallback — 用户机点"下载预览插件"撞 HTTP 403 直接死的 bug。`pickFastestMirror` → `rankMirrors`(返完整排序列表),`startInstall` 加 cascade 循环(任一 mirror GET 失败自动切下一个,5 个全失败才聚合报错)。UI `progress.message` 切换瞬间显示"镜像 X 失败,改用 Y…",全失败时 error 红字列出所有尝试。HEAD probe 通过 ≠ GET 通过 是核心场景(hotlinking / Referer / 子镜像分发不一致 / IP 段限速)。R4 第 5 笔本季(延续 office 引擎 fork-only override 链)| Tiny,1 笔 / +64 / -32 / 1 文件 / fork-only / 0 上游侵入 / 测试豁免(Tiny + 无测试基础设施 + 同级先例) |
| [dedup-cache-persist](docs/features/dedup-cache-persist/3-changelog.md) | `e38d655b7` 代码 + `0abfd035c` docs | DedupCache 加 persistPath 持久化 — `wss-text-dedup` 实测中 user 飞书 chat 只显示 2 条"你是谁",sidecar 却收到 user 没发过的"看 ~/.ssh/known_hosts"触发 LLM + 弹卡。P5 verify(user 飞书 IM 没显示这条 user message)确认是**飞书 WSS 重连后服务端重推老 message + sidecar 重启 in-memory DedupCache 失忆**双因素导致。修法:DedupCache 加 persistPath option,sidecar 启动 load 上次 mark 过的 message_id(过滤过期),mark 时 100ms debounce flush 到 JSON(.tmp + 原子 rename 防部分写),corrupt JSON / IO 错 log warn + 空 cache 启动不阻断。wss-client.ts 第一层 dedup 配 persistPath=~/.opencode/feishu-wss-dedup.json,textDedup 不动(10s TTL 落盘无意义)。**安全模型**:落盘只额外让"重启后老 message"被识别 skip,不改 mark/has 主路径行为。11 个新单测覆盖:无 persistPath noop / mark+flushNow / debounce flush / 第二实例 load 模拟 sidecar 重启 / 过期 entry 跳过 / corrupt JSON 不 crash / clear 也 flush / **WSS 重连实战场景** / 12h TTL 过期不 load / 首次空 / 原子 rename .tmp 不留。dedup 30/30(+11)+ adapter feishu 289/289(原 278)+ typecheck 16/16。**P2-P4(plugin 多 instance / chatSessionStore race / 资源浪费)留 backlog** — 落盘在 multi-instance 场景仍 work(飞书 server 只 push 1 connection,实测验证)。Medium,~218 行 / 3 文件 / 11 新单测 / 0 R4 / 0 上游侵入 |
| [wss-text-dedup](docs/features/wss-text-dedup/3-changelog.md) | `5d64b0a09` 代码 + `adb2d56c3` docs | wss-client.ts 加第二层 text dedup — `imbot-permission-pragmatic` v2 实测中 user 在飞书发"看 ~/.ssh/known_hosts"一次,sidecar 看到两条独立 user message(messageId 不同 SDK 内置 dedup 拦不住)导致弹两张权限卡。诊断:飞书后台分配新 message_id = user 端连击/retry/客户端 bug 真发了 2 条独立 message;**同 chatId + 同 text 10s 内重复几乎 100% 是误触发**(user 真要再发会等 LLM 跑完几十秒)。修法:复用现有 DedupCache 类加 textDedup instance(ttlMs=10s,maxEntries=200),wss handler SDK dedup 之后加 text dedup 层,仅对 text 类型生效(image/file/sticker 不走)。key = chatId::trimmedText。typecheck 16/16 + DedupCache 19/19 不破。**B 系列其他不做的决策**:B1 debounce 合并多卡 — 实测发现 LLM 是顺序调用(每张卡间隔 5-17s 远超 200ms debounce),价值 0;B3 SSH 卡片类型显示 — buildPermissionCard 已用中文 label "访问项目目录之外的文件",无需改;D 卡片显示"第 N 个权限请求" UX 提示 — 留 backlog。Tiny,1 文件 +27/-1 行 / 0 新单测 / 0 R4 / 0 上游侵入 |
| [imbot-permission-pragmatic](docs/features/imbot-permission-pragmatic/3-changelog.md) | `a4e7653cc` 代码 + `8377f37b7` docs | imbot v1 务实档退役 → v2 — v1 把 bash/edit/write/apply_patch/webfetch 全 ask 实测寸步难行(opencode bash always 按命令 arity prefix 颗粒度,user 点 5+ 次 always 仍要点,timeout 5min 又自动 reject)。v2 重新分析 prompt injection 攻击链:第 1 步 webfetch 拉网页 + 第 2 步 read 敏感数据 是瓶颈,bash 只是搬运工 — 截断前两步就够。新 imbot 配置:webfetch 全 ask 不变,read 普通 allow / SSH/AWS/Kube/GPG/Keychain 6 类敏感目录 ask 不变,**bash 默认 allow + 30+ 删除/不可逆 pattern ask**(rm * / git push --force* / git reset --hard* / *delete* / *uninstall* / docker rm * / aws s3 rm * / shutdown * / dd * / mkfs* 等),edit/write/apply_patch 不设走 build defaults(理由:bash echo >> file 已能做等价事,单独 ask 没意义)。同时 PermissionCardController timeout 5→30min 对齐 promptTimeoutMs。安全模型对比:LLM 主动跑破坏命令 v1 v2 防御等价;**正常 ship/dev 任务 v2 0 打扰**(v1 5-8 张卡 → v2 0 张);prompt injection 攻击链 v1 v2 都拦在 read+webfetch。**Wildcard.match findLast 规则**:`*: allow` 放第一,具体 pattern 后写覆盖。cargo test feishu_plugin_install 16/16(2 新 v2 测试 imbot_bash_pattern_covers_destructive_ops / imbot_no_longer_locks_edit_write_apply_patch);adapter feishu 262/262;monorepo typecheck 16/16。user 升级 v1→v2:删 jsonc 里 .agent.imbot 块,setup hook 自动注入 v2 | Medium,~30 行 spec + 2 unit test / 2 核心文件 / 0 R4 / 0 上游侵入(per-pattern bash 走上游 Wildcard.match + evaluate findLast 原生支持)|
| [mac-pack-installer-and-gitee-rename](docs/features/mac-pack-installer-and-gitee-rename/3-changelog.md) | `72b0325ed` | 修补 [macOS] 2026.5.11.1 ship .dmg 文件名不符规范 + Gitee 镜像仓改名生效 — Mac 5.11.1 首发用 `build-deskfox.sh -Env prod` 直接 build 绕过 `pack-installer.sh`(已存在但被误绕),出 `DeskFox_1.14.33_aarch64.dmg`(Tauri 内部版本)上传到 GitHub + Gitee,跟 Win `DeskFox-2026.5.11.1-setup.exe` 命名约定不一致。本笔修补 — 本地 rename → 删 GitHub 老 asset 重传 + 删 Gitee 老仓 release(`opencode-for-office-deskfox` id=680256)新仓 `deskfox` 重建(id=680262)上传;同时 `git remote set-url gitee` 切新仓,legal docs(隐私协议 / PRIVACY 各 3 处)+ memory(reference_push_remotes + MEMORY.md)Gitee URL 同步改名,历史 feat changelog / 改动日志早期段保留 `opencode-for-office-deskfox` 不改(当时事实)。`pack-installer.sh` rename 逻辑早已落地正确(line 73-94),本笔 SOP 收口为 Mac ship 唯一入口(以后再 ship 必走) | Medium,~150 行 docs / 4 文件改动 + 3 新文档 / 0 R4 / 0 上游侵入 |
| [win-ship-imbot-5.11.2](docs/features/win-ship-imbot-5.11.2/3-changelog.md) | `b268ce694` | **撤回** — Win 端补 ship 5.11.2 把 `feishu-bridge-imbot-agent` 安全 agent 推给 Win 用户。**装出来撞 castError 启动崩**(vite chunking 非确定性,详见 5.11.4 changelog),GitHub draft + Gitee release 已删,被 5.11.4 替代。Tag `ship-prod-2026.5.11.2` 仍在 origin/gitee(指向 merge commit `b268ce694`,代码无害,只是 build 出来的 .exe 崩)| Tiny ship chore,**撤回**,5 文件 / 0 R4 / 0 上游侵入 |
| [sdk-falsy-error-fallback-fix](docs/features/sdk-falsy-error-fallback-fix/3-changelog.md) | (待 merge 后回填) | 兜底 SDK `client.gen.ts:102/220` 的 `finalError \|\| ({} as unknown)` falsy fallback 抛空 `{}` → SolidJS `castError` 转 "Unknown error / 原因: {}" 空错误页的真凶 — 5.11.x ship 翻车真因 surface fix。在 fork-only `createSdkForServer`(`packages/app/src/utils/server.ts`,SDK 实例化唯一入口)的 fetch 边界包一层 wrappedFetch:底层 fetch reject `undefined/null/{}` 等 falsy 时转 `new Error("fetch returned empty rejection: <URL>")`,SDK 看到的 error 永远 truthy → SDK 的 `\|\| {}` fallback 永远不触发 → 永远不抛空对象。同时 console.error 留 `[FETCH-FALSY-REJECTION] { url, originalError }` 证据链,下次撞同问题 F12 console 立刻看到具体 endpoint。**为什么不直接改 SDK generated 文件**:① `packages/sdk/` 在 `.husky/pre-commit` 黑名单,需 R4 override 但本季配额已超 ② `client.gen.ts` 由 @hey-api/openapi-ts auto-generated,下次 `bun run gen` regen 时覆盖任何手工 patch — fork-only consumer 层兜底永久生效。**初版试过 SDK error interceptor 失败**(TS2339: `OpencodeClient` wrapper class 不暴露 `interceptors`),改 fetch 边界 — 比 interceptor 更上游,SDK 还没进 error handling 就拦截。bug **下次再撞验证**:打 F12 Console 看 `[FETCH-FALSY-REJECTION]` URL,或错误页"原因"段从 `{}` 变 `fetch returned empty rejection: <URL>`。**根因(vite chunking 非确定性 / fetch 为什么抛 falsy)未真修留 backlog** | Tiny,1 文件 +44/-3 / R2 FORK marker / 0 R4 / 0 上游侵入(fork-only `packages/app/`)|
| [win-ship-prod-5.11.4](docs/features/win-ship-prod-5.11.4/3-changelog.md) | (待 merge 后回填) | 5.11.1/2/3 三次 ship 全撞 **vite chunking 非确定性 bug** — 同源码不同时刻 build 出来的 prod bundle 行为不同,启动期触发 SolidJS createResource → `i.session.sync` SDK 调用 → `client.gen.ts` 的 `finalError || ({} as unknown)` falsy fallback → 抛空 `{}` → `castError` → 错误页。bisect 7 commit 全部今天重 build 都 OK,但今早 ship 的 5.11.1 installer 还是崩 = **非源码确定的 bug,跟 vite/rollup 内部状态相关**。**5.11.4 workaround**:用今天 22:53:13 dev tip 那次 build 的 known-working `target/release/DeskFox.exe`(经多轮实测 UI 正常)直接 ISCC pack installer,**不重新 vite build** 避免再撞非确定性。`pack-installer.ps1 -Env prod -SkipBump -SkipBuild` 出 `DeskFox-2026.5.11.4-setup.exe` 59.2 MB。**接受 mismatch**:installer 文件名 + Windows 控制面板 `5.11.4` / DeskFox UI 左下角版本牌 `v2026.5.11.2`(bundle 没动)。**根因未真修**,留 backlog:vite manualChunks 显式分块 + SDK falsy-error fallback 改 Error 而非 `{}` + 下次 ship 干净 rebuild 消除 mismatch | Tiny ship chore + 调研沉淀,6 文件 / 0 R4 / 0 上游侵入 |
| [feishu-bridge-imbot-agent](docs/features/feishu-bridge-imbot-agent/3-changelog.md) | `5e81491f8` | 飞书桥接默认 agent `build` → `imbot` — 收紧 unattended 远程触发场景危险工具默认权限。**A. Rust setup hook**(`feishu_plugin_install::inject_imbot_agent`):空 config / 已有 agent / 已有 imbot / jsonc 注释 4 种状态全 idempotent,user 改回 allow 也尊重不动;不设 `prompt` 字段 fallback 到 provider default(`session/llm.ts:107` 行为)→ imbot 跟 build 用同一 system prompt 同 LLM 能力。**B. saveAccount default**:`config-schema.ts:107` zod default + `account-store.ts:139` fallback 同步 `"build"` → `"imbot"`,已绑老账号 agent 保留 build 不强制 migrate(user 极少,jsonc 编辑或重绑即可升级,不专开 agent picker UI feat)。**imbot permission**:bash / edit / write / apply_patch / webfetch 默认 ask,read 加 SSH / AWS / Kube / GPG / Keychain / Crypto 6 类敏感目录 ask。**安全闭环**:LLM 经 webfetch 拉 prompt injection 网页被诱导用 bash exfil 时 user 在飞书看到权限卡可即时拒绝。Spike 前实测 opencode `/agent` endpoint 确认 custom agent + per-agent permission 上游 schema 原生支持(`AgentSchema.permission`)、build agent 0 影响。Rust 14 单测(含 5 新)+ TS 2 单测 + monorepo typecheck 16/16 全过 | Medium,~120 行 / 5 文件 / 7 新单测 / 0 R4 / 0 上游侵入(per-agent override 走上游 schema 原生支持)|
| [imbot-windows-delete-cmds](docs/features/imbot-windows-delete-cmds/3-changelog.md) | `713ef3075` 代码 + (本笔 commit) docs | v3.1 micro-patch — v3 极简档 Windows 端实测发现漏洞:LLM 在默认 PowerShell shell 跑 `rm -rf <path>` 时,opencode session sqlite 拿到铁证 `{"tool":"bash","input":{"command":"Remove-Item -LiteralPath \"D:\\...\""}}` — LLM 没用 unix `rm` 而是 PowerShell 原生 `Remove-Item`,绕过 `bash["rm -rf *"]: ask` pattern(Wildcard.match 前缀不匹配),目录被真删。补 4 条 Windows 风格 pattern:`Remove-Item *`(PowerShell 主路径)/`rmdir *`(PowerShell function 自动 -Recurse 删非空目录,跟 unix rmdir 只删空目录语义不同)/`del *`(cmd 经典)/`rd *`(cmd alias)。`rmdir *` 从 v3 砍掉名单移回 ask 因 Windows 语义差异。bash 规则数 8 → 13,跨 shell 删除覆盖。实测验证:2 个 bot(Hebing—one + xiaobei_win)发同样 `rm -rf` 命令,两边都弹 bash 卡片(sidecar log per_xxx (bash) 4 张),user 主动点 once 删除。3 测试调整 + 1 新测试(`imbot_v3_1_blocks_windows_delete_commands`)。注:cargo test STATUS_ENTRYPOINT_NOT_FOUND env 老问题持续,代码 audit 充分(纯 serde_json + user 飞书实测铁证) | Tiny,4 pattern 新加 + 3 单测 expect 调整 + 1 新单测 + Tiny 单文档(只 changelog,跳过 1-spec/2-plan)/ 0 R4 / 0 上游侵入 |
| [imbot-permission-minimal](docs/features/imbot-permission-minimal/3-changelog.md) | `16c37de08` 代码 + `3203cca5b` docs | imbot v2 务实档退役 → v3 极简档 — v2 仍嫌严(~30 条 ask),user 实测后退回再砍。安全偏好原话"把隐私保护住,不能随意删除电脑信息就是相对可控的"→ read 砍 5 个敏感目录只留 `.env`(build default 自带)+ `.ssh`(用户不主动让 LLM 碰);bash 砍 26 条只留 8 条真不可逆(`rm -rf *` / `git push --force*` / `git push -f *` / `aws s3 rb *` / `aws ec2 terminate*` / `dd *` / `mkfs*` / `fdisk *` / `shutdown *`);**webfetch 撤回 ask → allow**(user 日常用太多,出境拦截已由 read 端 `.env`+`.ssh` 兜底)。攻击模型重审:exfil 必经"read 敏感凭证 → 出境"两步,read 端拦在最窄瓶颈就够,webfetch/可逆操作信任度调高,正常 ship/dev 任务接近 0 打扰。v2→v3 防御等价场景:rm -rf / git push --force / aws s3 rb / shutdown / .ssh exfil / .env exfil;v3 大幅改进场景:npm uninstall / docker rm / git reset --hard / 总结网页。typecheck 16/16 + adapter bun test 275/278(3 fail 跟 imbot 0 关系,dev 基线 pre-existing);**cargo test 环境性 ABI 失败**(STATUS_ENTRYPOINT_NOT_FOUND,dev 基线就有,跟改动无关,留 backlog);代码 audit 充分(纯 serde_json 结构 + 测试 expect 1:1 对齐)| Medium,~60 行净 / 1 核心函数 + 6 个单测调整 + 2 个新单测(`imbot_v3_drops_v2_overstrict_bash_patterns` 验 26 条砍掉 + `imbot_v3_drops_webfetch_ask` 验撤回)/ 0 R4 / 0 上游侵入(per-pattern 走上游 Wildcard.match findLast 原生支持)|
| [md-export-word-iter-2](docs/features/md-export-word-iter-2/3-changelog.md) | `185ad127c` / `194172129` / `f5b22a840` / `a8030f1f3` / `bfb6ca503` / `b2740f19a` / `ae96d138b` (merge) / `a9072d384` (bump+release `2026.5.9.1`) | **MD → Word 导出第二轮迭代 — 全面保真度提升**(2026-05-08 user 跑 markdown-test.md 综合测试发现 ~25 个保真度问题,本轮全面修复)。预处理 + 后处理 docx XML 双夹策略,15+ 新 helper:① HTML 标签 `<u>/<sup>/<sub>/<kbd>/<span style>/<div align>/<details>/<img>/<p align>` 全转 Word 元素 ② GFM Alerts 5 类 sentinel 边界 + 后处理给段加 type-specific pBdr/shd 颜色(普通灰 / Note 蓝 / Tip 绿 / Warning 黄 / Caution 红) ③ blockquote 同段视觉一体(删 MdSpace 段间气垫 + inline spacing 强制覆盖)④ 远程图片走 Tauri reqwest 命令 + sniff 字节头判 mime + SVG/WebP 经 canvas 转 PNG ⑤ 表格全 6 边框 + header 灰底 + 前后空行 ⑥ Mermaid 居中 ⑦ **数学公式走 LaTeX→MathML(KaTeX)→OMML(mml2omml)→嵌入 docx `<m:oMath>` 元素 — Word 原生公式可编辑可矢量** ⑧ 字号行距 monkey-patch 库 documentDefault 用 Word default 11pt + 1.15 行距 ⑨ 目录 Ctrl+点击跳转(heading 加 bookmark + #anchor 链接转 internal w:anchor + 改 _rels 删 external rel) ⑩ `==高亮==` viewer + Word 都黄底 ⑪ GFM emoji shortcode `:rocket:` 等 80+ 转 Unicode emoji + viewer marked extension。viewer 侧也加自定义 marked extensions(`<mark>` / emoji / heading anchor / 嵌套 `[![]()](url)` 链接图片)。后端加 `fetch_url_base64` Tauri 命令(reqwest + 8MB 上限 + 10s 超时)。加 katex@0.16.45 + mathml2omml@0.5.0 + @types/katex@0.16.8 三 npm 包(净 +1MB,bundle 增量可接受)。**踩坑总结**:OOXML schema 顺序敏感(pPr 子元素 pStyle→pBdr→shd→spacing 顺序)/ bookmark name 必须 letter 开头(加 `h_` 前缀)/ `<w:hyperlink r:id>` r:id 不一定第一 attr / mml2omml 对 `\int_0^∞` 等 nary 转 `<m:e/>` 留空 Word 显占位 / WebView2 canvas tainted KaTeX webfont 跨域(走 OMML 路径绕开)/ 表格在 BQ 内 OOXML 不允许嵌套(接受现实)/ regex `[^/]` 误吞 URL 中的 `/`。**单测 72 → 147**(+75 含 13 类新 helper 测试),典型如 `applyHeadingBookmarksAndAnchors` 测试发现 `_rels` regex bug(production 触发后救命),typecheck 全过。已知 deferred 2 项见 `OPENCODE-PLAN/需求池/`(`md-export-word-积分占位符.md` + `save-dialog-默认按钮.md`) | Large,~2600 行 / 6 文件 / 2 npm 依赖 / 1 后端命令 / 0 R4 / 0 黑名单 / 0 上游侵入 |
| [build-script-json-fallback](docs/features/build-script-json-fallback/3-changelog.md) | `34574ebb5` 代码 + `26a2711bd` docs + `<本笔 commit>` follow-up(Mac 端实测 bug-repro:`grep -c` 0 match 撞 `\|\| echo 0` 出多行 COUNT,`[[ -gt ]]` arithmetic syntax error;改 `\|\| true`)| `feishu-plugin-dedup-decision` 立的 post-build jsonc 清理逻辑只查 `.jsonc`,user 用 `.json`(无 c)的开发者享受不到 — Win + Mac 双端 `build-deskfox.{ps1,sh}` post-build cleanup 段从单 `$jsonc/$JSONC` 变 `foreach/for FILE_NAME in opencode.jsonc opencode.json` 循环跑两种文件名,cleanup 逻辑本身不变(grep -v 删行 + 修悬空逗号 + 备份 `.bak.build-cleanup`)。起源:2026-05-12 Win 端 imbot v3 实测撞双推查 jsonc 发现 plugin 累积 3 entry,理论上 build-deskfox.ps1 cleanup 应该自动清但实测没清 — 查代码发现只查 `.jsonc`,user 实际用 `.json`(无 c)的开发机一直漏 cleanup。**只影响开发者**(普通用户拿 installer 装的成品不跑 build-deskfox 脚本,Rust setup hook resolve_user_config_path 早就支持 jsonc+json 优先 fallback,本笔不动 setup hook);测试豁免对齐 dedup-decision 决策(post-build script + 控制流包装 + 0 业务逻辑改动)| Tiny,ps1 +6 行 + sh +6 行 + 单 changelog / 0 R4 / 0 上游侵入 |
| [feishu-plugin-dedup-decision](docs/features/feishu-plugin-dedup-decision/3-changelog.md) | `<本笔 commit>` | 把"opencode.jsonc 累积多个 feishu-bridge plugin entry → multi-instance → 双推 user message"的根因诊断 + **不做产品层防御**的决策显式写下。`imbot-permission-pragmatic v2` + `wss-text-dedup` + `dedup-cache-persist` 后 user 仍撞双推,深查定位:开发机三档来回切换累积 4 个 feishu-bridge entry(空格 / raw target / `/Applications` prod / `%20` 编码),opencode loader 并发 import → **3 个独立 plugin instance**(3 plugin server port 51298/51300/51303 + 3 WSSClient 同 appId connect 飞书),server 给不同 connection 推同 user message 时分配不同 message_id → SDK dedup(message_id+ts)在 instance 间不共享拦不住 → bot 弹两张卡。**决策不做产品层防御**(普通用户单装永不撞 + L1 plugin globalThis singleton / L2 inject 强制单 entry / L3 file lock 跨进程都 over-engineering)。改动 3 处:① `feishu_plugin_install.rs` 头加 15 行决策注释列出 L1/L2/L3 三层候选防未来 agent 重复实施防御代码 ② `build-deskfox.{sh,ps1}` 双端对称加 post-build 清开发机 jsonc 多余 entry(grep -v 删行 + perl/regex 修悬空逗号,备份 `.bak.build-cleanup`,下次 DeskFox 启动 setup hook 自动 inject 当前 .app)③ 三文档落盘。前一个 verdict 误判"飞书 server 19s 重推同 text,接受现状"被 user 拒绝事后拦截哲学"不是事后拦截,是要定位到底是哪一层产生第二条,从源头切断" → 深查 plugin server log 才定位真凶在 opencode loader + jsonc 累积层。user 拍板"普通用户单装单跑不会撞,don't fix what isn't broken" + 元原则"避免业务无限扩大" → 决策不实施产品代码层防御。实测验证:user 手动清 jsonc 留 1 entry + relaunch DeskFox → 双推消失(2026-05-12)。本地 fixture 模拟 3 entry → 0 entry,JSON 合法,其他 agent/provider 配置完整保留 | Tiny,3 代码文件 +55/-0 + 3 docs / 0 R4 / 0 上游侵入 / 测试豁免(Tiny + bash/ps1 函数 + Rust 注释 = 0 业务逻辑改动)|
| [mac-ship-prod-5.12.1](docs/features/mac-ship-prod-5.12.1/3-changelog.md) | `92f0cd4b0` 主笔 + merge `ea2a14331` + tag `ship-mac-prod-2026.5.12.1` + follow-up `<本笔 commit>` xattr -cr 安装说明补 | Mac 端跟 Win 5.12.1 同步 ship — 包内容完全等于 Win 5.12.1(imbot v3.1 13 ask + dedup-cache-persist + feishu-plugin-dedup-decision + build-script-json-fallback 含 grep 兜底 follow-up + sdk-falsy-error-fallback-fix)。**双平台分发**:GitHub Release + Gitee Release(反转 2026-05-06 "Mac 不跑 Gitee" memory,2026-05-12 user 拍板"以后用这种方式")。Build 流程:bump installer-versions.json macos 5.11.1→5.12.1 + quit DeskFox + `pack-installer.sh --env prod --no-bump` + 手动 rename .dmg(`--no-bump` 时 NEW_VERSION 空跳过 rename — 需求池新增 pack-installer-no-bump-rename 后续修)+ cp 兜底 .app/Contents/MacOS/DeskFox(shasum `e3cc0e3cc...` 一致验证本次 Tauri bundler 工作 OK,cp 是 idempotent)。产物 `DeskFox-2026.5.12.1_aarch64.dmg`(64,645,764 bytes)。Tag 用 `ship-mac-prod-2026.5.12.1` 加 mac 前缀避免跟 Win 5.12.1 撞 — historical tag 没区分平台,本笔起 Mac ship 永远加 mac 前缀。**实测**:typecheck 16/16 + cargo 19/19 + bash -n 干净(grep fix 生效) + prod build 1:22 + setup hook 自动 inject prod 路径 + imbot v3.1 skip + 1 plugin instance + dedup persist load 6 entries + wss connected。**观察**:fresh prod 首次启动 sidecar idle 2min+,user 反馈"晚了一会儿才点系统授权对话框",已记需求池 `prod-首次启动-sidecar-idle.md` | Tiny ship chore,1 文件 bump + 1 行 + docs / 0 R4 / 0 上游侵入 / 测试豁免 |
| [html-viewer-allow-scripts](docs/features/html-viewer-allow-scripts/3-changelog.md) | `9acf2e5c3` 代码 + `<本笔 merge>` 索引补 | HTML 预览 iframe 加 `allow-scripts` — 反转 `md-office-improvements` spec A1.9"`<script>` 失活"决策。起源:user PPT(`.html` 21 页,讲师版 `第2课时_风格迁移与迭代优化.html`)底部 `◀ 1/21 ▶` 翻页按钮点击无反应,根因翻页靠 JS 切 slide 可见性,旧 sandbox 只开 `allow-same-origin`。**安全权衡跨 origin 论证**:Win `tauri.localhost` vs `localasset.localhost` 不同 host、Mac `tauri://` vs `localasset://` 不同 scheme,iframe 内 JS 不能 reach parent → MDN "scripts+same-origin 可自移除 sandbox" 警告不适用;新风险 = 打开未知来源 .html 时 JS 会跑,与浏览器开本地 .html 同级风险,工作区文件可信。**验证**:typecheck 16/16 + release build 2m18s + 全量测试 592/593 与 dev clean 状态等同(1 fail 是 docx-integration test 间状态污染,与本笔无关)+ user runtime 实测翻页生效;dev installer `DeskFox-Dev-2026.5.14.1-setup.exe` 已 pack(60MB,~)| Tiny,2 代码行(sandbox attribute)+ 注释 / 1 文件 / 0 R4 / 0 上游侵入(`packages/app/src/pages/session/file-tabs.tsx` 已属 fork 改造重灾区,白名单内)/ 0 新依赖 |
| [html-viewer-ux-polish](docs/features/html-viewer-ux-polish/3-changelog.md) | `2ae3e14eb` 主笔 + `<本笔>` 三文档+索引 | HTML 文件查看器 UX 优化(基于 `html-viewer-allow-scripts` 继续)— 四块改动一次性落地:① 去顶部 `预览/源码` toolbar + 删 `htmlMode` signal,iframe 占满,源码查看入口改走右键 → 编辑 → CodeMirror html 语法模式(`@codemirror/lang-html@6.4.11` 新 dep,`langFromExt` 加 .html/.htm 分支)② iframe 跨 origin 右键弹自家菜单 — `local_asset.rs` 给 HTML 响应注 capture-phase contextmenu listener(preventDefault native + postMessage(x,y,选区文本)→ 父),父侧 window message handler 翻译坐标后弹 mdMenu,4 项行为复用 .md 既有(导出 Word 在 .html 始终灰 per user Q3)③ 同注入脚本扩展 mousedown 通道修"右键弹菜单后左键点 iframe 内菜单不消失"bug(parent 的 onDocDown 抓不到 iframe 内事件)④ 阈值 2MB→10MB 对齐 `MAX_EDITABLE_BYTES`,>10MB 走 placeholder "文件 >10MB,不支持预览/编辑"。搜索功能 user 决议不做(WebView2 native Ctrl+F 是 chrome 级控件)。**R4 第 6 笔本季已严重超配额**,bun.lock 自动重生 user 明确授权,wrapper 不可行论证 + 风险评估见 changelog。**验证**:typecheck 16/16 + Rust cargo --no-run 干净(STATUS_ENTRYPOINT_NOT_FOUND 是 dev box 老 env 问题非本笔引入)+ release build 1m24s + user runtime A1-A7+A9 实测全过(A8 >10MB placeholder deferred,常规 HTML 不撞)| Medium,~164 行净 +5 Rust 单测(`local_asset` 注入行为)/ 5 文件 / 1 dep / **R4 1 笔(bun.lock 黑名单)**/ 0 上游侵入(全 fork-only)|
| [file-tree-llm-write-refresh](docs/features/file-tree-llm-write-refresh/3-changelog.md) | `5aa50eeec` 代码 + `<本笔>` docs | AI 创建新文件后右侧文件树不刷新 — 根因:`watcher.ts` 的 `file.edited` 主路径对"路径不在 cache/open"场景直接 return,不刷父目录;busy→idle 兜底刷新只走 expanded 目录(`refreshAllExpanded` 跳过 expanded=false),即使 loaded=true 缓存里;`listDir` 不带 force 见 loaded=true 直接返回缓存 → user 重新展开看到旧 children,唯一破解 F5 / 重启 app。修法:`file.edited` 主路径加 5 行 — `!hasFile && !isOpen` 时若 `isDirLoaded(parent)` 则 `refreshDir(parent)`,把新文件加进 children 让下次展开自动浮现。R5 先写复现测试 + `[bug-repro: ...]` tag;1 现有测试增强 + 2 新测试覆盖 subdir / root 新文件场景。watcher.test.ts 10→12 全过 + typecheck 16/16 + release build 3m00s + user runtime 实测自动刷新通过 | Tiny,~5 行净 + 60 行测试 / 2 文件 / 0 R4 / 0 上游侵入(`packages/app/src/context/file/` 白名单)|
| [file-tree-multi-drag-to-chat](docs/features/file-tree-multi-drag-to-chat/3-changelog.md) | `e2f7fef6c` 代码 + `<本笔>` docs | 文件树多选(Shift/Ctrl 选 N 项)拖到聊天窗口接通 — `file-tree-dnd` feat 留的 `application/x-deskfox-paths` MIME(JSON[abs paths])原本只设计树内移动用,聊天侧 `attachments.ts:handleGlobalDrop` 只读单选 `text/plain: file:<rel>`,多选拖过去等于啥都没拖。修法:新 `multi-path-drop.ts` 纯函数 helper `parseMultiPathDropPaths(json, root)` abs→rel 转换(容错 JSON 损坏 / 非数组 / 非字符串 / Win 反斜杠 / 根外路径 / 根末尾斜杠 / 路径=根本身 7 种边界);`attachments.ts` `handleGlobalDrop` 加多选 MIME 分支(优先级最高,单选 text/plain 之前)N 个路径循环 addPart;`handleGlobalDragOver` 同步加多选 MIME 检测做 @mention drag 提示。**helper extract 模式** — SolidJS context 链让 attachments.ts 直接单测会撞 Client-only API server-side 错(R5 决策 2 双清单 Logic 清单先例,跟 `tests-codemirror-fixture-d3` 同场景)。10 新单测覆盖 helper 全部边界;attachments.test.ts 7→17 全过 + typecheck 16/16 + release build 2m49s + user runtime 实测多选拖通过 | Tiny,~30 行新 helper + ~20 行 attachments 接入 + 60 行单测 / 3 文件(1 新)/ 0 R4 / 0 上游侵入(`packages/app/src/components/prompt-input/` 白名单)|
| [chat-selection-menu](docs/features/chat-selection-menu/3-changelog.md) | `b71a4ad2e` 代码 + `<本笔>` docs | 聊天对话区右键选区菜单替换 WebView2 原生菜单 — 用 DeskFox 自家两项(添加到聊天 / 复制)+ 输入面板模式跟文件查看器一致(textarea + Ctrl/Cmd/Opt+Enter 提交)+ Q4 follow-up 加红色 overlay(textarea 焦点丢原生选区,红色 div 兜底跟文件查看器视觉一致)。新 `chat-selection-quote.ts` 纯 helper(composeQuotedMarkdown + insertTextIntoPrompt,12 单测覆盖 7 种边界)+ 新 `chat-selection-menu.tsx` 独立组件(capture-phase document contextmenu 拦截 + scope `[data-slot="session-turn-list"]` + Portal 弹菜单 + 红色 overlay 跟 scroll capture 清除);`message-timeline.tsx` 在 `<Show>` 内挂 ChatSelectionMenu(packages/ui/ 黑名单不动,父级 fork-only 挂载)。UX 决策(Q1 A 两气泡都支持 / Q2 B 输入面板 / Q3 A 无选区两项灰显 / Q4 A→B follow-up)。helper extract 模式延续 R5 决策 2 helper 清单先例。typecheck 16/16 + 12 单测全过 + release build 1m32s + user runtime 两轮实测(基本菜单 + Q4 overlay follow-up)| Medium,~250 行代码 + 85 行单测 / 4 文件(3 新)/ 0 R4 / 0 上游侵入(全 `packages/app/src/pages/session/` 白名单)|
| [chat-input-focus-follow](docs/features/chat-input-focus-follow/3-changelog.md) | `9a6f26448` 代码 + `<本笔>` docs | 聊天输入框焦点跟随 — 聊天对话区右键 + MD/HTML viewer 选区"加入聊天"后光标到末尾 + 输入框自动 focus,user 可立刻继续打字。新 `utils/chat-input-focus.ts`(37 行 helper,模块级 singleton ref + Selection API caret-to-end 兜底);`PromptInput` editorRef 回调 register + onCleanup unregister;`chat-selection-menu.submitToChat` 改光标位置 + rAF focus;`file-tabs.submitMdSelection`(MD/HTML viewer 选区"加入聊天")rAF focus。**第一轮作用域漏接 file-tabs 入口**(误以为 `prompt.context.add` 加 attachment 卡片不需要 focus),user 实测反馈后补;经验 = user 预期是任何"加入聊天"动作都该 focus 回输入框,不按"是否改 editor 文本"分类。typecheck 16/16 + 已有 chat-selection-quote 单测 12/12 + release build 2 次(首轮 + 修正)+ user runtime 4 入口实测通过 | Tiny+,53 行 / 4 文件(1 新)/ 0 R4 / 0 上游侵入(全 `packages/app/src/` 白名单)|
| [chat-drop-overlay-stuck-fix](docs/features/chat-drop-overlay-stuck-fix/3-changelog.md) | `a4883fef4` 代码+测试 + `<本笔>` docs | 拖拽接收浮层卡死 bug 修复(需求池 2026-05-18 入)— **根因**:`file-tree.tsx` 行 onDrop 调 `event.stopPropagation()` 杀掉 `attachments.ts` 注册在 document 上的 bubble 阶段 drop → `setDraggingType(null)` 不执行 → 浮层卡死;Esc 取消 / 拖到非 drop zone 不发 drop 也漏清。**修法**:`attachments.ts` 加 2 个 window 级兜底 listener,ONLY 清状态不参与 drop 处理:① capture-phase drop(不被 child stopPropagation 杀,覆盖外部 OS drag 落到文件树)② dragend(source 元素 bubble 到 window,覆盖内部 drag 取消)。**bug-repro 测试先行**(R5 硬要求)新 `drag-overlay-cleanup.test.ts` 2 测试验证 DOM 事件流前置假设(child stopPropagation 不杀 window capture / dragend 能 bubble);typecheck + 53/53 + release build + user runtime A1-A6 实测全过;commit message 标 `[bug-repro: 文件树拖文件到聊天窗口释放后浮层卡死]` | Medium-,85 行(13 + 72 测试)/ 2 文件(1 新)/ 三文档全套 / 0 R4 / 0 上游侵入 |
| [installer-version-env-suffix](docs/features/installer-version-env-suffix/3-changelog.md) | `b08ad705b` + merge `5c0bdc32e` | 安装包版本号按 env 加后缀 + N 序列独立(B2 口径,2026-05-21)— user 实施 `large-file-preview-guard` 后请求"先本地打开安装包",对开发包版本号规范产生疑问,Claude 给 3 候选(B1 共享 N / B2 env suffix N 独立 / B3 不 bump 复用上次 prod)user 选 B2。**改动**:① `bump-installer-version.ps1` 加 `-Env dev\|beta\|prod` 参数,regex 加 env suffix 字面量 + trailing space anchor 实现 env N 独立计数(prod 无后缀 `2026.5.21.1` / beta `2026.5.21.1-beta` / dev `2026.5.21.1-dev`)② `bump-installer-version.sh` 双端 parity ③ `pack-installer.ps1` + `pack-installer.sh` 透传 env 给 bump ④ `DeskFox.iss` 加 ISPP preprocessor `NumericAppVersion`(`#if Pos("-", AppVersion) > 0` 则 Copy strip 后缀)给 `VersionInfoVersion`,Inno Setup PE header 必须 N.N.N.N 数字格式;dry-run 3 路径验证通过(env=dev/prod/默认 fallback prod) | Tiny+,5 文件 / 净 ~39 行 / 0 R4 / 0 黑名单 / 0 上游侵入(全 fork-only 脚本 + Inno Setup 配置)|
| [auto-save-debounce-flush](docs/features/auto-save-debounce-flush/3-changelog.md) | `<本笔 commit>` | 文件查看器 debounce auto-save + flush(REQ-001 dirty tab 修复,2026-05-22,Medium)— user 落地 `large-file-preview-guard` 后讨论数据丢失痛点(Edit 没保存切 tab/关 app 丢改动);3 方案对比拍 **C 混合**(debounce 1s auto-save + 切 tab/关窗口 flush,Obsidian 风)。**核心实现**:① 新 `utils/debounce.ts` pure helper(`trigger/flush/cancel/pending`,9 单测全过)② `saveEditCore({silent})` 重构(silent 路径不 toast、不退编辑、不刷 store,只更 mtime;mtime 冲突 silent 不弹 confirm,toast 警告)③ `draft` 变化 effect trigger debounce 1s 自动 save ④ `onCleanup` flush snapshot 模式 + 同步 `setStoredContent(snap)` 更新 store ⑤ window close emit `deskfox-flush-before-close` event → 前端 layout.tsx listen 转 DOM custom event → file-tabs flush。**调研期重大反复**:(a) tab switch flush 初版放 `createEffect(on(path))` 永不 fire,根因 `<Show keyed>` 切 tab unmount 整个组件,改放 `onCleanup`;(b) 初版 IIFE 用 `file.load(force)` 跟 user 切回 race + `session.tsx:488` 切 tab `file.load` 不加 force 看 stale,改用 `file.setStoredContent` 同步更新 store;(c) silent save 不调 force load(防 CodeMirror reset race),只在 onCleanup 退编辑后才刷 store。`markSelfWriting` 500ms 窗口拦"AI 修改了此文件"误 toast;user runtime A1+A3+A5 全过 | Medium,~190 行代码 + ~120 行单测 + ~500 行三文档 / 6 文件(2 新)/ 9 单测 / 0 R4 / 0 上游侵入 |
| [abandon-cloud-build-workflows](docs/features/abandon-cloud-build-workflows/3-changelog.md) | `4bad339ab` + merge `018e52d76` + readme follow-up `de0988a57` | 云端 build workflow 永久废止(2026-05-21,Tiny+ 治理决策)— 跑完首次 Tier 2 ship + ship-scripts-naming-fix 后 review 剩余 follow-up,user 拍板**不再用云端 build,所有 ship 走本地**。**改动**:3 个 release workflow yml(`release-deskfox.yml` / `release-mac-deskfox.yml` / `release-mirror-gitee-deskfox.yml`)头注从 "🚧 DISABLED 2026-05-11 待复活" 改 "🚫 OFFICIALLY ABANDONED 2026-05-21",移除"复活方式"段;治理 doc `版本号与发布渠道规范.md` §四 / §五 / §六 / §七 全面更新:Tier 1/2 表加 "Ship 路径"行(本地 pack + 手动 gh release + 手动 mirror)、§五 SOP 完整重写命令链(含 Gitee API POST 建元数据)、§六 决策记录加 2026-05-21 ship 流程全本地化、§七 follow-up 撤掉 Tier 2 workflow / Gitee mirror 复活两项保留官网入口。**决策依据**:① deadlock 半月未 dig 出来产出 unknown ② 本地 ship 流程已实战跑顺(多次 Tier 1 + 首次 Tier 2 全部本地路径成功)③ 云端 CI 维持边际成本超过收益(速度差不多 + 网络/cache 失效风险)④ Gitee mirror workflow 历史 silent fail 双案 ⑤ deadlock dig 投资回报低 | Tiny+,4 文件 / 57 净行 / 仅 3-changelog / 0 R4 / 0 代码改动 |
| [ship-scripts-naming-fix](docs/features/ship-scripts-naming-fix/3-changelog.md) | `8c32a2f26` + merge `87d0e883b` | ship 脚本 4 个对齐新命名规则(`installer-naming-cleanup` 后续 follow-up,2026-05-21)— 首次跑 Tier 2 ship 流程暴露 4 个脚本 bug。**改动**:① `pack-installer.ps1` 末尾 echo path 用 NumericVer 算路径(strip env suffix)② `pack-installer.sh` .dmg rename 同样用 NumericVer + productName 空格转横杠跟 Win 风格对齐 ③ `mirror-asset-to-gitee.ps1` tag regex 加 `dev` 识别 Tier 2 ship tag + asset path 用 NumericVer ④ `mirror-asset-to-gitee.sh` 同 .ps1 + 顺手补 Mac asset 自动定位(原 TODO)。**Follow-up 列**:`release-deskfox.yml` Tier 2 自动化 workflow(~200 行)/ `release-mirror-gitee-deskfox.yml` 复活(目前 DISABLED)/ `release-mac-deskfox.yml` Tier 2 触发 — 都等需求驱动单独 feat | Tiny+,4 文件 / 28 净行 / 仅 3-changelog 省 1-spec/2-plan / 0 R4 / 0 上游侵入 |
| [installer-naming-cleanup](docs/features/installer-naming-cleanup/3-changelog.md) | `7e16168bc` + merge `f077c0dc9` | installer 文件名去重(2026-05-21,Tiny)— user 拿到首个 Tier 2 预览版 `DeskFox-Dev-2026.5.21.1-dev-setup.exe` 发现双重 `-dev` 冗余,4 候选 ABCD 拍板 **B**(保留 `-setup` 但去重 `-dev`)。**改动**:① `DeskFox.iss` `OutputBaseFilename` 模板 `{#AppVersion}` → `{#NumericAppVersion}`(strip env suffix 纯数字,复用现有 `VersionInfoVersion` 定义)② 治理 doc 新增 §3.4 文件名命名规则段 + 改 4 处旧示例。**新命名表**:Tier 1 `DeskFox-2026.5.21.1-setup.exe`(不变)/ Tier 2 `DeskFox-Dev-2026.5.21.1-setup.exe`(去重)/ beta `DeskFox-Beta-2026.5.21.1-setup.exe`(去重)/ Tier 3 `DeskFox.exe`(不打包不变)。env 只通过 OutputBase 前缀体现,版本号纯数字;**控制面板显示的 AppVersion 仍带 `-dev` 后缀**(user 可见的"开发版"标识)| Tiny,2 文件 / 19 净行 / 仅 3-changelog 省 1-spec/2-plan / 0 R4 / 0 代码改动 |
| [3tier-versioning-governance](docs/features/3tier-versioning-governance/3-changelog.md) | `b19d5722e` + `714bdc1e2` + merge `d9f390d5e` | 3-tier 发布渠道 + 版本号体系正式立规(2026-05-21)— user 落地 `installer-version-env-suffix` + `rename-dev-to-main` 后讨论"3 种安装包如何管理 + 版本号如何定" → 拍板 3-tier 体系 + 落实文档。**新治理 doc** `docs/governance/版本号与发布渠道规范.md`(9 段 centerpiece):Tier 1 稳定版(prod 无后缀,GitHub Release latest)/ Tier 2 预览版(dev `-dev` 后缀,GitHub Release prerelease,可对外发)/ Tier 3 本地测试版(`build-deskfox.* -NoBundle` raw exe,不发布,不分配新版本号沿用 JSON 当前值);beta 储备不主动用。**5 处 cross-link**:README × 2(对外口径"稳定版/预览版")/ 应用身份-命名规则 顶部 / CLAUDE.md 文档链路表 / pack-installer.{ps1,sh} 头注。**follow-up 列**:Tier 2 自动化 workflow `release-deskfox-prerelease.yml`(等首次 Tier 2 ship 时补)+ 官网"预览版"独立入口(deskfox.ai 改)+ 跨 tier 升级提示(产品决策另起 feat)| Medium,~470 行(含三文档)/ 8 文件(1 新治理 doc)/ 0 R4 / 0 代码改动 |
| [rename-dev-to-main](docs/features/rename-dev-to-main/3-changelog.md) | `2506d44bf` + `c3c733c1d` + merge `606b95d17` + Phase B git 操作(2026-05-21 落地)| 主分支 `dev` → `main` 重命名(2026-05-21,与 installer channel `dev` 命名空间解耦)— user 落地 `installer-version-env-suffix` 后发现"主分支 dev"和"installer channel dev"双重含义混淆;4 选项中选 1(只改主分支,不改 channel)。**Phase A 改**(本笔):CLAUDE.md / AGENTS.md / governance 三件套(总纲 / UPSTREAM-MERGE-GUIDE / 双端协作-SOP)/ PLANNING-OVERVIEW / INDEX(加命名空间小节)/ `release-mirror-gitee-deskfox.yml` `TARGET_BRANCH: dev → main`。**不改**:历史 feat changelog / docs/history / 分支策略-v2 spec(历史快照)/ upstream workflow yml(避免上游 merge 冲突)/ `release-deskfox.yml` env code(env=dev 是 installer channel 不是分支)/ 已 ship tag。**Phase B**(实际 git rename + remote push)待 user 二次授权。命名空间分离:`main` 分支 / `upstream/dev` 上游 / `prod`/`beta`/`dev` installer channel,三个维度不撞 | Tiny+,~415 行(含三文档全套)/ 9 文件 / 0 R4 / 0 代码改动 |
| [large-file-preview-guard](docs/features/large-file-preview-guard/3-changelog.md) | `<本笔代码>` + `<本笔 docs>` | 大文件预览统一防护(REQ-025,2026-05-21 入池 user 报告视频崩溃)— **4 层防御一次性落地**:L1 入口闸门(`context/file.tsx` load() 加 size pre-check,超阈值跳过 read 设 tooLarge 标记;新 Tauri 命令 `get_file_size`)+ L2 媒体 localasset 化(`video/audio/img` 全改 `localasset://` HTTP Range 分片读,删 base64→Blob→URL.createObjectURL 链路,顺手修原 .png 打开"图片不可预览"bug;`MediaKind` 加 `image`,`renderMedia` 加 `<img>` Match)+ L3 阈值兜底(text/markdown/html 10MB / **office 200MB 用户决议**(原方案 50MB 偏小,营销/教学 PPT 含图含视频常 50-200MB)/ media+binary ∞ / default 100MB)+ L4 UX 兜底(新 `FileTooLarge` 组件,2 按钮"用本机软件打开"+"打开所在文件夹"复用 open_path)。**复审捕获 3 隐患**:① canEdit() 用 contents 判断 tooLarge 会被空 content 绕过 → user 能空 draft 覆盖真实文件数据丢失,加 `state.tooLarge` 守卫 ② 第一版 image 归 media 但 renderMedia 不识别 → "图片不可预览",扩 MediaKind+IMAGE_MIME_FALLBACKS+`<img>` Match ③ `onMediaError` 签名扩 union 类型 img 没 `.error` 字段。19 单测覆盖 file-size-guard helper;typecheck + 383/383 测试 + release build 1m40s + user runtime A1-A9 全过 | Medium,~370 行 / 8 文件(3 新)/ 三文档全套 / 0 R4 / 0 上游侵入(全 fork 白名单 + Tauri 命令)|
| [sdk-falsy-empty-body-fix](docs/features/sdk-falsy-empty-body-fix/3-changelog.md) | `<本笔>` | 补 [`sdk-falsy-error-fallback-fix`](docs/features/sdk-falsy-error-fallback-fix/3-changelog.md)(2026-05-12 surface fix)未盖的路径 ②(2026-05-21,5.21.1-dev Mac 首次 Tier 2 ship 翻车真凶)— **根因**:SDK `client.gen.ts:220` 的 `finalError \|\| ({} as string)` 在 fetch 成功但 response 4xx/5xx + body 空字符串时触发,throw `{}` → SolidJS `castError` → ErrorBoundary "出了点问题 / 原因: {}" 空错误页。5-12 surface fix 只盖 fetch.throw 路径,没盖 fetch.return-with-empty-body-4xx。**触发**:Mac 端 `~/Library/Application Support/ai.deskfox.app.dev/` 累积 stale workspace state(5-9/5-12/5-20 三次留下),启动恢复失效 session,sidecar 返回空 body 404。**修法**:`wrapFetchWithFalsyGuard`(原 wrappedFetch closure extract 成 export 函数,为单测铺路)在 baseFetch 成功后追加 layer 2 检查:`!response.ok` + `clone().text() === ""` 时主动抛 Error 截断,SDK 进 catch 拿 truthy Error 不触发 `\|\| {}` fallback。**顺手修**:5-12 layer 1 的 `isEmpty` 用 `Object.keys.length === 0` 把 Error 对象错判 empty(Error 的 message/stack 非 enumerable,keys 返 `[]`),加 `!(e instanceof Error)` 排除 + 显式 `e === ""` case。13 单测全过(layer 1 五种 reject + layer 2 六种 status/body + Request/URL 兼容性 2)| Medium-,3 文件(2 改 1 新)/ ~155 行 / 0 R4 / 0 上游侵入(全 fork-only `packages/app/src/utils/server.{ts,test.ts}`)|
| [frontend-stale-session-fallback](docs/features/frontend-stale-session-fallback/3-changelog.md) | `<本笔>` | 接力 [`sdk-falsy-empty-body-fix`](docs/features/sdk-falsy-empty-body-fix/3-changelog.md) 把 5.21.x ship 翻车修到产品级闭环(2026-05-21,Tiny+) — **触发链**:Tauri webview 启动恢复 URL `/<dir>/session/<stale_id>` → `directory-layout.tsx` 的 `createResource(() => params.id, (id) => sync.session.sync(id))` fetcher 调 sync → 内部 `client.session.messages` → sidecar 401 + empty body → wrapFetchWithFalsyGuard 抛 `Server returned 401 with empty body: ...` → fetcher reject → SolidJS 顶层 ErrorBoundary "出了点问题"。**修法**:在该 createResource fetcher 加 try/catch,识别为 stale session error 时 `navigate(`/${slug()}`, { replace: true })` 降级到 directory 根("未选择项目" + 最近项目列表 UI),其他 error 仍 throw 让 ErrorBoundary 兜底。新 helper `packages/app/src/utils/stale-session-error.ts`(+25 行)识别 wrapFetchWithFalsyGuard 抛的 `^Server returned 40[14] with empty body:` 格式,5xx/403/network/AbortError/non-Error 不视为 stale;10 单测覆盖识别边界。**只在启动恢复路径兜底,不动 sync.session.sync 内部**(其他 caller — message-timeline 用户点击老 session / session.tsx stale refresh — 期望错误 surface 自己处理 UX)。verify 路径:user state restore 后启动应 navigate 走 stale id 显示主界面。typecheck 16/16 + bun test 10/10 | Tiny+,3 新 1 改 文件 / ~115 净行 / 0 R4 / 0 上游侵入(全 fork-only `packages/app/src/utils/` + `packages/app/src/pages/directory-layout.tsx` 白名单)|
| [feishu-pipeline-401-fix](docs/features/feishu-pipeline-401-fix/3-changelog.md) | `262a353f8`(rebase 自 `21eda9533`)+ `49c95852d` + merge `f1bd32503` | 修飞书桥接 reply 全静默 bug — user 视角:发飞书消息没回复(实测 3 条 23:13/23:25/23:27 全 `messages fetch failed status=401, fallback ""` → `empty reply for chat`,机器人 emoji ack 正常但 text reply 永不到)。**Mac 复现 / Win 不复现**真因:sidecar build 时 `git branch --show-current` fallback baked CHANNEL=`dev`(2026-05-21 主分支改名前)→ 命中 `HTTPAPI_DEFAULT_ON_CHANNELS = Set(["dev","beta","local"])` (`packages/core/src/flag/flag.ts:16`) → 走上游不成熟 effect-httpapi stack,撞**两个 sub-bug**:① Authorization `security: { basic, authToken }` 实现成逻辑 AND(`packages/opencode/src/server/routes/instance/httpapi/middleware/authorization.ts:13`) → SDK 只带 basic header,authToken scheme 收空 credential → fail → 整 endpoint 401 空体 ② `StepFinishPart` schema 要求 `reason: Schema.String` 必填(`packages/opencode/src/session/message-v2.ts:269`) 但 writer 写 `reason: value.finishReason`(`packages/opencode/src/session/processor.ts:475`)在 LLM 未返 finishReason 时 = undefined → JSON 丢字段 → DB 缺 key → 读回 Schema.encode 失败 `Missing key at ["parts"][2]["reason"]` → endpoint 400 空体。**修法走方案 A**:build script 加 `export OPENCODE_CHANNEL=prod` 让 sidecar baked channel=prod → 不命中 HTTPAPI_DEFAULT_ON_CHANNELS → 走 Hono legacy stack(`packages/opencode/src/server/routes/instance/session.ts:619` 的 `c.json(messages)` 直出 + Hono basicAuth 单 mechanism 内置 OR 自带支持 auth_token query)双 bug 全规避,**0 上游 override**。**副作用**:`getChannelPath()` 对 prod channel 用 `opencode.db`,对其他用 `opencode-<channel>.db`(`packages/opencode/src/storage/db.ts:30`),Mac prod 1.14.33 用户(channel=dev)数据在 `opencode-dev.db`,升级后 sidecar 改读 `opencode.db` → GUI session list 空。**配套**:`packages/desktop/src-tauri/src/lib.rs:initialize()` 头部加 `migrate_pre_prod_db()` 一次性幂等 hook,target=`opencode.db` 不存 + source=`opencode-dev.db` 存 → cp 3 个 SQLite WAL 文件(`.db` / `.db-wal` / `.db-shm`),源保留作回退兜底;抽 `migrate_db_files(source, target)` 给单测调用,6 单测覆盖 target-exists skip / source-missing skip / main-only / all-three-files / idempotent / source-preserved 全过。**验收**:curl `/session/X/message` 200 返 4 messages / 单 GET assistant message 200(原 400 schema 编码 bug 同时修)/ 无 auth 401 / 错 pwd 401 / auth_token query work / `/session` list 返 45 sessions(含历史 archived)。**Mac vs Win 不一致根本原因**是 sidecar baked CHANNEL 漂移,**不是平台代码差异** — Win 端因 build 时 branch 非 dev 阴差阳错走对了 Hono 路径,本 fix 让两端正规对齐。**留 backlog**:上游 schema 必填 reason / Auth 多 security AND 两个 sub-bug 推到上游或下次决定 dogfood effect-httpapi 再处理(不在本 feat 修)| Medium ~157 行 / 3 文件(2 build script 各 +18/+9,1 lib.rs +130 含 6 unit test)/ 0 R4 / 0 上游侵入(全 fork-only:`packages/branding/scripts/build-deskfox.{sh,ps1}` + `packages/desktop/src-tauri/src/lib.rs`)|
| [upstream-rename-sst-to-anomalyco](docs/features/upstream-rename-sst-to-anomalyco/3-changelog.md) | `<本笔>` | forward-looking docs 统一改 `sst/opencode` → `anomalyco/opencode`(2026-05-24 user 拍板对齐:sst 已 transfer 到 anomalyco,GitHub 自动 redirect)— 27 文件 / 净 +2 行(主要 1:1 替换 + CLAUDE.md 1 行历史注);历史 archive / 旧 feat changelog / 本文件 / `docs/STATUS.md` / `docs/installer-versions.md` 保留原字眼;memory 加新 reference 防未来 agent 再犯"二级 fork"误判 | Tiny+,27 docs / 0 代码逻辑改 / 0 R4 / 测试豁免(纯文档 rename)|
| [e2e-pre-push-gate](docs/features/e2e-pre-push-gate/3-changelog.md) | `<本笔>` | 测试纪律自动化最后一公里 — `.husky/pre-push` 加 Phase 1 e2e gate(只 main push 触发,feat push 仍只 typecheck 不付 ~24s 成本);vite mock 必须已在 :3000(不自动 spawn,跨平台 pid 管理坑);3 场景手动验证全过(无 vite+main→fail / 无 vite+feat→skip / vite+main→11 passed 24.1s);跳过走 `git push --no-verify` + `[override-pre-push: 理由]` | Tiny+,~125 行,1 hook 改 + 3 docs / 0 R4(`.husky/pre-push` 不在黑名单)/ R5 v4 履约 |
| [e2e-vite-warmup](docs/features/e2e-vite-warmup/3-changelog.md) | `<本笔>` | R5 v4 "flaky 48h 内修"首次履约 — vite mock 冷启动 + Playwright 多 worker 并行 → 第 1 批 2-3 spec timeout 偶发 fail;双保险方案:① `vite/e2e-mock.js` 加 server.warmup.clientFiles(8 个核心 SolidJS 组件 + entry.tsx),vite 启动主动预编 ② 新建 `e2e/global-setup.ts` 跑测试前真 chromium 加载 `/` 一次让 module 编完(`playwright.config.ts` 接 globalSetup,**R4 override 第 2 笔本季**:wrapper 替代净增工程复杂度大于 override 收益,详 3-changelog R4 段);4 次连跑(含真冷启动 = kill vite + 重起)全 11 passed / 1 skipped / 20.4-24.0s,0 任何 flaky;同时删 `OPENCODE-PLAN/需求池/e2e-vite-cold-start-flaky.md` backlog(同日立同日闭环) | Tiny+,~58 代码 + ~80 文档,3 文件改 + 1 新建 + 1 删除 / **R4 override 第 2 笔本季**(配额上限)|
| [mac-ship-dev-2026.5.21.1-dev](docs/features/mac-ship-dev-2026.5.21.1-dev/3-changelog.md) | bump `b6916bd90`(2026-05-22)+ tag `ship-mac-dev-2026.5.21.1-dev` on `f1bd32503`(2026-05-23 闭环)| **Mac 端首次 Tier 2 预览版**(对应 Win `win-ship-dev-2026.5.21.1-dev` 同期发车)— bump commit `b6916bd90` 2026-05-22 落地(installer-versions.json macos `5.12.1` → `5.21.1-dev` + docs/installer-versions.md 加 entry 35 行)但 ship 闭环延后到 2026-05-23(因 bump 后主线撞 `sdk-falsy-empty-body-fix` + `frontend-stale-session-fallback` 翻车 + `feishu-pipeline-401-fix` 修真凶,user 决议一起 ship)。**ship tag on HEAD `f1bd32503`**(含 post-bump feishu fix,user 三轮 stale/clean/idempotent + 飞书 reply 实测过的 build commit);完整 4 步 SOP:① `git tag ship-mac-dev-2026.5.21.1-dev` + push origin ② `gh release create --prerelease --repo zoulukuang/deskfox` + .dmg asset ③ Gitee API `curl POST /api/v5/repos/zoulukuang/deskfox/releases` 创建 release id=689661 ④ `bash packages/branding/scripts/mirror-asset-to-gitee.sh ship-mac-dev-2026.5.21.1-dev` 上传 .dmg(82 Mbps / 6s)。**对外链接**:GitHub <https://github.com/zoulukuang/deskfox/releases/tag/ship-mac-dev-2026.5.21.1-dev>(prerelease)+ Gitee <https://gitee.com/zoulukuang/deskfox/releases/tag/ship-mac-dev-2026.5.21.1-dev>(prerelease)。**包内容**:bump 时刻含 sdk-falsy-empty-body-fix / frontend-stale-session-fallback / abandon-cloud-build-workflows / ship-scripts-naming-fix / installer-naming-cleanup / 3tier-versioning-governance / rename-dev-to-main / installer-version-env-suffix / large-file-preview-guard 等 ~22 笔主线 feat;post-bump 含 feishu-pipeline-401-fix(release notes 透明说明)。**产物**:`DeskFox-Dev-2026.5.21.1_aarch64.dmg`(64.5 MB,arm64,不签名)/ Bundle ID `ai.deskfox.app.dev`(与 prod 同机共存)。**特殊性**:bump 当天没开 feat 分支直接落 main 本地(commit message 备注"下一步 push user 同意"),不符 v2 铁律 ① 但因当时正在 ship 流程未细究,后续 strict 遵守。Tiny ship chore / 0 R4 / 0 上游侵入 / 0 代码改动(纯 version bump + docs + 索引)/ 测试豁免(user 三轮 stale/clean/idempotent + 飞书 reply 实测过) |
| [feishu-bridge-light](docs/features/feishu-bridge-light/3-changelog.md) | 11 commits 6191d6086..44c0f93ae on feat/feishu-bridge-light(待 merge main)| 飞书桥接增强三件套(2026-05-23 落地,基于 v1 草稿调研后 v2 修订 15 处假设错配)— **(1) `/new` 私聊清话题**:`stripMentions` 处理 `@bot /new` 群里 mention 形态;清三处 disk + in-memory(chatToSession + sessionToChat);群聊明确拒绝防共享 chatId 误清全员;`chatSessionStore.delete(accountId, chatId)` 早已存在直接调用(v1 错写"待新增"白省 0.4 天)**(2) `[ATTACH:path]` 文件回传**:AI 在 reply 嵌 marker → 系统按扩展名分流 `image.create`(10MB,image_key)/ `file.create`(30MB,file_key + file_type 受限枚举,docx/xlsx/txt/md 等走 stream 兜底)→ 上传 → 发飞书 → strip marker;**路径白名单**强制 `~/.opencode/feishu-workspace/` 子树内(防 LLM 把 /etc/passwd 发出去,防 prefix 同名误判 / `..` traversal);单 marker 失败不阻断其它,warning append 到回复尾;`FEISHU_SESSION_SYSTEM_PROMPT` 扩段教 LLM marker 协议总是启用 **(3) `[CREATE_GROUP:name]` 自动建群(opt-in 默认关)**:`FeishuAccount.enableAutoGroupCreate` boolean 默认 false;启用 + 私聊场景 AI 嵌 marker → 系统发 confirm 卡片二次确认(10 分钟超时自动拒)→ user 点确认才真调 `chat.create({ data: { name, chat_type: public, user_id_list: [senderOpenId] } })` 拉 user 进群 + `chat.link({ validity_period: week })` 拿 share_link(团队群限制时降级 chat_id);**双门控** opt-in 配置 + confirm 二次确认防 prompt injection;关闭时 system prompt **不教** marker 避免"哑回复"。**架构决策**:① `confirm-card.ts` 独立成模块(非内嵌 permission-card)— 语义/类型分离(permission_reply once/always/reject 路由 SDK vs confirm_reply yes/no 路由 callback),复用同样的 "controller + pending map + 超时 + 卡片"架构模式;② `system prompt` 改成 `getSystemPrompt()` 方法动态拼接,按 `account.enableAutoGroupCreate` 决定是否拼 CREATE_GROUP 段;③ `MessagePipeline` 接受 `larkClient` / `attachWorkspaceRoot` 可选注入对齐 PermissionCardController 风格,生产默认仍内部 new Client / 默认 FEISHU_WORKSPACE 零行为变化。**v1 草稿 15 处假设错配**已逐条修订:chatSessionStore.delete 已存在 / 图片走 image.create 非 file.create / chat.create({data,params}) 真实签名 / chat.link 单独调拿 invite link / user_id_list 拉人 / opt-in + confirm 双门控 / system prompt 扩段 / marker regex strip / file_type 枚举受限走 stream 兜底 / image 10MB vs file 30MB size 限制分开 / /new 群聊明确拒绝 / /new stripMentions @bot 形态 / [ATTACH:] 跟 [CREATE_GROUP:] 一致 marker 协议 / 先 merge main 拉齐(含已 push 的 401-fix)。**Phase 0 merge** main 进 feat/feishu-bridge 解 INDEX.md + 改动日志.md 二处冲突,merge commit 4215 行触发 large-diff exception(merge commit 本质不可拆,用户授权一次)。**测试**:391 pass / 0 fail / 803 expect(Phase 0 基线 289 → +102 新测试 / 241 新 expect);7 新源文件(reply-actions 162 / file-uploader 98 / confirm-card 297 / group-creator 75 + 4 测试文件)+ 5 改文件(config-schema +7 / account-store +2 / message-pipeline +260/-13 / plugin +26/-10 / config-schema.test +6 + message-pipeline.test +461)。Medium+,~1800 行 / 12 文件 / 0 R4 / 0 上游侵入(全 fork-only adapter-feishu-lark 包 + plugin.ts routing 扩)/ **未做的留 backlog**:群聊 per-user session 隔离 / AI tool 化非 marker 协议 / dmPolicy 死配置唤醒 / Phase 2-3 真机回归测试(等下次 ship 才能在飞书 IM 实测) |
| [feishu-create-group-toggle-gui](docs/features/feishu-create-group-toggle-gui/3-changelog.md) | `80f81774c` + `06863e8e4` + `773b710c4` + `f812e2d2a` + `ffbfd2ae2`(本笔)| 把 feishu-bridge-light 的 `enableAutoGroupCreate` opt-in flag 从 JSON 配置文件升级为 GUI 开关(2026-05-24)— **触发**:user 实测 2026-05-24 暴露 3 个问题 ① user 必须手编 `~/.opencode/feishu-config.json` 才能启用 ② LLM 通过 imbot edit 默认 allow 默改 JSON user 不知道 ③ sidecar 无文件 watcher 改完不 hot-reload。**方案**:"编辑账号模型" dialog 升级为"编辑账号设置",加"模型 / 高级能力"两个分隔块,高级段含 "允许 AI 自动创建新群" checkbox + 副标说明 + hot-reload 立即生效。**API 设计 user 拍板 Option A**:扩 `updateAccountModel` → `updateAccountSettings` partial update,单 endpoint 服务多个 settings,未来加新 flag 0 endpoint 改动;旧 endpoint + Tauri 命令 + 前端 helper 保留 thin wrapper 向后兼容。**全链路改动**:① account-store.ts 新加 `updateAccountSettings(id, patch)` 严格白名单 partial(其他 schema 字段 0 影响)② server `POST /accounts/update-settings` endpoint 加 4 道闸校验(missing accountId / unknown_fields / empty_patch / invalid_field 类型)+ 触发 onAccountsChanged hot reload ③ Tauri 命令 `feishu_update_account_settings` 用 `Option<Option<ModelRef>>` 表 model 三态(`undefined`不动 / `null`清 / `obj`设,对齐 server `"model" in body` 检测)④ 前端 `feishuUpdateAccountSettings` 新 API + 旧 `feishuUpdateAccountModel` 标 `@deprecated` 但保留 thin wrapper ⑤ dialog 加 checkbox + i18n 4 keys × 3 locale + `AccountSummary` 全链路加 `enable_auto_group_create` 字段。Medium ~290 行 / 13 文件 / 10 新单测(account-store partial 7 + 兼容 3)/ 401 pass 0 fail / 16/16 typecheck / 0 R4 / 0 上游侵入(全 fork-only)。**测试取舍**:server endpoint HTTP 集成测试撤掉,因 Bun 缓存 `os.homedir()` 首次值 HOME env 改了不生效(PoC 验证)→ 测试会污染 user 真实 `~/.opencode/`(实施期间已发生过一次:`acc1` 进 config + 11 个 secret 文件落地;立即清理 + 文档化教训 + backlog "writeSecret 抽 path option")。account-store 10 单测远超 R5 Medium ≥ 3 unit 达标。HTTP 层是薄壳无独立业务逻辑,留 backlog: `validateUpdateSettingsBody()` 抽 pure helper extract 模式独立单测 |
| [feishu-create-group-hard-block](docs/features/feishu-create-group-hard-block/3-changelog.md) | `90857b4dd` + `1df6c0392` + `74e4a5860` | Pipeline 层关键字硬拦截 — 接力 `feishu-create-group-toggle-gui` 修 provider-agnostic 漏洞(2026-05-24,同日发车)。**起因**:claude-code(及其他 spawn-based)provider 跳过 `role=system` 消息,soft constraint system prompt 失效,LLM 翻 fork 源码撞 imbot read permission 卡 user。**修法 3 commits**:① 1-spec+2-plan(实施前文档+开发中 regex 决策 append)② `reply-actions.ts` 新加 `isGroupCreationIntent(text)` 纯函数 helper extract 模式 — 中文 regex `/(?:开\|建\|创建\|新建\|拉\|搞\|做)[^群]{0,20}群/`(动词+字符+群,精准识别"建一个项目讨论群"等真实表达 + 避开"群是怎么建的"动词在群后)+ 英文 lowercase substring 8 keyword + 14 单测覆盖中英命中/不命中/边界/regex 精准对比 ③ `message-pipeline.ts handle()` 在 `/new` 早退之后加 3 道门控硬拦截块(`flag=false` + `p2p` + `keyword命中`)— 命中则不调 LLM,直接 sendFeishuText GUI 引导(文案跟既有 system prompt soft constraint 完全一致,UX 在所有 provider 下统一)+ 6 集成测覆盖正交矩阵。**3 道门控顺序保证不误拦**:`flag=true` 跳过让 LLM 走 marker confirm 路径 / 群聊跳过避免误拦"群是怎么建的"学术问题 / 关键字命中才拦。**误拦权衡 documented**:user 主动关 flag 时少量误拦(如"如何创建一个群")回复 GUI 引导成本低,远好于漏拦让 LLM 撞 imbot read permission 卡。Medium- ~190 行 / 4 文件 / 20 新测试 / 429 pass 0 fail / 16/16 typecheck / 0 R4 / 0 上游侵入(全 fork-only adapter-feishu-lark)|
| [feishu-group-mention-policy](docs/features/feishu-group-mention-policy/3-changelog.md) | `4c038e179` + `cdcf77cd2` + `b106f7286` + `2c7a63c1c` | 激活 dead 的 `FeishuAccount.requireMention` 配置 + GUI 暴露 + 顺手把 chat.create 默认 chat_type "public" → "private"(2026-05-24,主题"飞书桥接 privacy 默认值整治")。**起因**:user 研发场景"同一个 bot 多项目并行,每个项目一个群当独立 session 用,希望群里不用 @ 就响应";同时撞 chat.create 硬编码 "public" 跟 user 主观预期"我建的群我决定谁加入"不符。**主修法 4 commits**:① 1-spec+2-plan(实施前文档)② `reply-actions.ts` 新加 `isBotMentioned(mentions, botOpenId)` 纯函数 helper(防御性 botOpenId 空串返 false 保守拒响应)+ `message-pipeline.ts handle()` 加 3 条件早退(`chatType !== "p2p"` + `requireMention=true` + 不含 bot @ → 跳过 LLM)+ 13 单测(helper 7 + 集成 6 正交矩阵)③ `group-creator.ts` chat.create `chat_type` 改 `"private"` + 测试同步 ④ 全链路扩 `requireMention` 字段:account-store updateAccountSettings 加 partial 处理 + server endpoint 白名单加字段 + GET /accounts 响应加字段 + Tauri AccountSummary/ListAccountWireItem/UpdateAccountSettingsRequest 全链路加 require_mention + 前端 UpdateAccountSettingsPatch + AccountSummary 加字段 + dialog "高级能力" 段加 checkbox "群里需要 @ 后再响应" + i18n 2 keys × 3 locale + 3 新 account-store 单测。**前置条件 documented**:requireMention=false 实际生效需 user 先在飞书开放平台改订阅模式为"全量群消息"(GUI hint 含警示文字)。**复用 updateAccountSettings partial endpoint 架构回报点**:0 新加 endpoint / 0 新加 Tauri 命令 / 0 新文件,只加 schema 字段 + UI 一行 checkbox。Medium- ~220 行 / 11 文件 / 16 新单测 / 481 pass 0 fail / 16/16 typecheck / 0 R4 / 0 上游侵入(全 fork-only)|
| [feishu-llm-strip-mention-placeholders](docs/features/feishu-llm-strip-mention-placeholders/3-changelog.md) | `97b104b2b` | 修自 `feishu-bridge-light` 起的潜在 bug — `message-pipeline.handle()` 传 raw text 进 LLM,**@bot 飞书占位符 `@_user_N` 没 strip** → LLM 误当另一个联系人,reply 含"我不是 @_user_1"类幻觉(2026-05-24 user 实测灵狐 p2p + DeskFox-Mac 群 test030 各 1 case 复现)。**根因**:`cleaned = stripMentions(text, mentions)` 已算,`/new` 检测和 CREATE_GROUP intent 都用 cleaned,**但 `runOpencode(sessionID, text, ...)` 漏改 cleaned**。**修法**:1 行 `text` → `cleaned`(`message-pipeline.ts:483`)+ 3 行 FORK 注释。Tiny ~4 行代码 + 37 行 2 bug-repro 单测(群 + p2p 各 1)/ 485 pass 0 fail / 16/16 typecheck / 0 R4 / 0 上游侵入(纯 fork-only 单文件改)。bug-repro 先写 → 修代码 → pass 验证修法。[bug-repro: LLM 收 raw text 含 @_user_N 占位符 — 应传 stripMentions 后的 cleaned text]|
| [office-选中加聊天](docs/features/office-选中加聊天/3-changelog.md) | 主交付 `6e3eb2b3c` + `afa27565e` + `9dc090355` + `9dc8e2ddd` + `7d1d491dc` + `9f0a73352` + `7749509d2` + `45f0e8a58` + `6c797d27b` / 第 1 轮 QA `52734f9d9` + `899c0254b` + `d5d27cb0b` + `a4aac3265` + `72b204d36` + `5238f8d93` + `33f4283d9` + `58dd2be3d` + `648b79991` / 第 2 轮 QA #1-#4 `ca09359e2` + `3564f54c6` + `ea03331e9` + 本笔 docs | **统一选区菜单架构 v1 含 2 轮 QA 跟进**(2026-05-24~2026-05-25)。**架构方案 B**:`ContextMenuHost` + 可插拔 `SelectionProvider` first-match-wins。**主交付**:chat + PDF/office 两处(MD 留 v2),wrapper 替代避 R4,跨页 inline hint 不 toast,顶栏"用本机软件打开"常驻兜底,chat-selection-menu 薄壳化透明迁移。**1-spec 固化"office WYSIWYG 编辑不重启" 决议**(语义上本 feat 吃掉 office 编辑需求 — 选中加聊天 → AI 用 python-docx/openpyxl/python-pptx 改原文件 → 重走 office→PDF 预览)。**第 1 轮 QA 暴露 → 第 1 笔 R4 override**:layout `select-none` 继承 → textLayer 选不到字(hot-fix select-text);选两行扩整页/中间漏字/pptx 选不到 → 改用 `TextLayerBuilder` 替代 raw TextLayer;视觉漏字 → DomSelectionProvider 加视觉 bbox 算法;字间天窗 → 行级 rect 合并 + 不过滤 whitespace span;选区双色干扰 → 藏 native + overlay 改 chat 同色调。**第 2 轮 QA #1-#4 关键修法**:①`--total-scale-factor` 修行末漏字(R4 第 2 笔满配额) ② pointerdown snapshot 修行间右键 selection collapse ③ `sel.anchor/focus` 视觉坐标修拖几行选中整页(DOM 顺序 ≠ 视觉顺序的根本对策) ④ `commentOrigin: "quote"` 改卡片不附文件二进制(B 方案 LLM token 干净,formatCommentNote 复用,build-request-parts isQuote 分支跳过 filePart)。**经验沉淀 T1-T7** 见 3-changelog § 经验沉淀:R4 改前读完整 side effect 链 / DOM 顺序 ≠ 视觉顺序 / 事件链 default 时机用 pointerdown snapshot / 复用 UI vs 复用数据通路(语义维度) / LLM "引用一段" vs "读全文" / CDP 验数据非验视觉 / 调研撞墙换层面。Medium ~600 行主交付 + Follow-up ~250 行 / 24 commits / 72 新单测 / Logic 清单 ≥ 80% 达标 / **R4 2 笔满配额** / 0 产品代码上游侵入(全 fork-only)/ View 清单 e2e 留 backlog(Phase ③ ready 后补)|
| [聊天选区-卡片化-换行](docs/features/聊天选区-卡片化-换行/3-changelog.md) | `27cdc7208` + `3b74e93a2` + `c8fc84702`(merge `79758e485`)| **office-选中加聊天 后续 v1.5 同日 ship**(2026-05-25)— 解决 v1 ship 后两个遗留:① chat 区选区仍走老 markdown blockquote 塞 textarea ② 卡片溢出横向滚 user 看不到下一张。**方案 B `kind` 字段贯穿**:`FileContextItem.kind?: "chat" \| "file"` 数据维度统一 5 层(prompt.tsx / submit.ts / history.ts / build-request-parts.ts / comment-note.ts),卡片 UI 按 kind 分流(chat 显 `bubble-5` icon + "聊天引用",file 仍 FileIcon + 文件名),`formatCommentNote` LLM 模板按 kind 分流(chat 走 "user is quoting earlier in this conversation" 让 LLM 明确引文来自同一对话历史继承上下文,file 走原 "regarding this file of {path}")。**Step 2 先 1 后**(user 拍板):纯 CSS `flex-wrap + max-h-[180px]` 卡片自动换行先 ship(2 commits 含 user QA 调整 140→180) → chat 卡片化大改稳扎稳打 1 笔。**dom-provider chat 区返 sourceMeta**(`{kind:"chat", path:"<chat selection>"}`)+ host.tsx 整合 submitToChat 分流。i18n 加 `prompt.context.{chatQuoteLabel, removeChatQuote}` 三语。3 commits / 12 文件 / +179 / -32 / 6 新单测(formatCommentNote 4 分支 + metadata round-trip + 非法 kind 兜底)/ 87 单测全过 / typecheck 16/16 / 0 R4 / 0 上游侵入。**经验 L1-L3**:Medium 起手建三文档(本次直接 ship 后才补,违例反思)/ kind 字段贯穿优于双 union 类型(字段共享率 90%+ 时)/ UI 与 LLM 模板同 kind 驱动避免分流逻辑分散 |

| [feishu-attach-upload-robustness](docs/features/feishu-attach-upload-robustness/3-changelog.md) | `983f54646` + `645bf2a31` + `83295c161` + `de3904512` + `8e83f91e6` + `6f8fb3746` | 修飞书 ATTACH 上传 100% 失败(2026-05-24,user 实测 4.7KB notes.md 都断,关 Clash 仍发生)。**iter 0~3 全部死在 axios + Node form-data 调用链同款 bug 但症状各异**:createReadStream → "socket connection closed unexpectedly" / raw Buffer → `source.on is not a function`(plugin bundle 内 `Buffer.isBuffer(buf) === false`,Bun + CJS interop 怪相)/ Readable.from(buffer) → 30s timeout × 3 hang(form-data 算不出 Content-Length 走 chunked,飞书 server hang)。**iter 4 解法**:**完全绕开 SDK image.create / file.create**,改用 Bun-native `fetch + FormData + Blob` 直接 POST `/open-apis/im/v1/{images,files}`,tenant_access_token 从 SDK 内部 `tokenManager` 借(SDK cached)。`message.create` 仍走 SDK(非 multipart,工作正常)。**iter 4 follow-up**:移除 iter 3 误移植的 `sanitizeFileNameForUpload`(那是 OpenClaw 给 Node form-data 用的兜底,Bun-native FormData 自动按 RFC 8187 处理 UTF-8 filename,sanitize 反而让中文显示成 %E6%8A%A5%E5%91%8A.md raw)。**完整 7 文档教训**:plugin/bundle work 先读 stack trace 看错误在哪里(plugin.js 不是 sidecar);Bun + Node CJS bundle Buffer 互操作有坑;OpenClaw 修法移植前先弄清调用链一致;sidecar / plugin `need_rebuild` 时间戳判断范围太窄要手动删 bundle 才能进新代码。**测试**:43 新单测 file-uploader + makeAttachFakes 改 fetch mock — 全套 518/518 + 16/16 typecheck。Medium ~210 行代码 + ~340 行测试 / 0 R4 / 0 上游侵入 / 留 backlog `build-deskfox.sh` 时间戳判断扩 adapter-feishu-lark + Layer 2 proxy dispatcher + 方案 D confirm card 白名单。[bug-repro: ATTACH upload 100% 失败 — Bun + axios + Node form-data + Buffer 互操作 bug,绕开整条链才修]|
| [feishu-group-slash-command](docs/features/feishu-group-slash-command/3-changelog.md) | `dcc15ee71` + `298628e62` | 飞书桥接建群入口从自然语言切到 `/group <群名>` slash command(2026-05-24,user 反馈"自然语言误触率不可接受")。**方案 C(三选)**:`/group X` 主路径 0 LLM 调用 + 自然语言关键字白名单缩窄 + 引导降级。**主改动 reply-actions.ts + message-pipeline.ts**:① 新加 `parseGroupCommand(text)` 解析 /group 命令(空 / 超长 30 / 大小写敏感等错误分类)② 改写 `isGroupCreationIntent` 宽容 regex `(?:开|建|创建...)[^群]{0,20}群` → **18 个白名单短语**(中文 Tier 1+2 共 14 + 英文 4)+ **查询后缀排除**(中文 7 个 + 英文 new group noun-phrase 7 + 通用 2)③ pipeline.handle() 加 /group 分支(p2p only / 命中弹 confirm card / 群聊拒绝 / 用法错误友好提示)④ 自然语言关键字命中 → 不调 LLM reply 引导用 /group ⑤ **删整条 LLM marker 路径**(`[CREATE_GROUP:name]` regex / `parseCreateGroupMarkers` / `processGroupMarkers` / `CREATE_GROUP_MARKER_PROMPT` / `CREATE_GROUP_DISABLED_PROMPT`)⑥ 删 `parseCreateGroupShortForm` + `extractGroupName` 半结构化路径 ⑦ `getSystemPrompt` 简化:加 `GROUP_CREATION_GUIDE_PROMPT` 替换原 marker / disabled 双 prompt,不再按 flag 分支。**provider-agnostic 0 LLM 解耦**:/group 路径在 runOpencode() 之前拦截,claude-code / imbot / default 行为一致。**已知漏拦透明记录**:"建一个X群" / "创建一个X群" 类中间字插入断开 contiguous match,user 改用 /group 显式或换"建群叫 X"(Tier 1)。**测试**:新 56 case(parseGroupCommand 12 + isGroupCreationIntent 45 + /group 集成 11 + getSystemPrompt 4)/ 删 30 case 旧 marker 测 / 505 全过。Medium 净 -378 行(508 + / 886 -,主要删 ~900 行旧 LLM marker + regex + 测试)/ 16/16 typecheck / 0 R4(large-diff override:测试 + 文档 + 删代码同 commit 不拆)/ 0 上游侵入。[bug-repro: 自然语言宽容 regex 误拦"建立群体精神"/"新群规"/"如何建群"等查询]|
| [feishu-group-new-cmd-and-mention-rename](docs/features/feishu-group-new-cmd-and-mention-rename/3-changelog.md) | `80a04487c` + `092fd106b` + `1092eca52` + `a1bef9d05` | 飞书桥接"高级能力"面板全面整顿(2026-05-25,user 反馈"高级能力应该默认关闭"+ 群里独占用法需要 /new + checkbox 文案 stale)。**改动 1 — `/new` 群里启用条件**:p2p 永远允许;group + `requireMention=true`(默认)→ 拒绝引导;group + `requireMention=false`(免@ 模式)→ 允许清 session reply 含"影响所有成员"。行业调研(Discord per-channel LLM bot 模式)支持方案 C(无 admin 门控,依赖 user 主动开 requireMention=false 的信任 intent)。**改动 2 — GUI checkbox 语义反转 + 默认改关**:"群里需要 @ 后再响应"(默认勾)→ "允许 AI 免@ 读取群里所有信息"(默认不勾);后端 `requireMention` 字段不变只反 UI 一层 0 兼容性风险;i18n key `requireMention.*` → `allowReadAll.*`(zh/zht/en 同步,hint 文案改写含 /new 群里说明)。**改动 3 — 彻底删 `enableAutoGroupCreate` 死开关全栈**(实施中扩展):`feishu-group-slash-command` 删 LLM marker 路径后 flag 在 pipeline 0 引用是个 GUI 死开关;清 12 文件 6 层(config-schema 删字段 / account-store 删 constructor+patch / server 删 endpoint / Tauri 删 AccountSummary+Wire+ListItem 4 处字段 / GUI utils 删字段 / dialog 删 checkbox 加 info paragraph / settings-feishu 删 prop / 3 i18n locale 删 key 加 groupCommand.info / account-store.test + config-schema.test 删/改 case)。**老配置兼容**:zod 默认 strip unknown,老 `enableAutoGroupCreate` 字段自动消失无 migration 风险。**Info paragraph 文案**:"建群方式:私聊发 `/group <群名>`(例:`/group 项目讨论`),AI 弹确认卡片,点确认才真建。"**测试**:505/505 全过(原 518 - 删 13 + 新 5 - 修 5 = 505) + 16/16 typecheck + Rust cargo check 过。Medium 净 -2 行(162 + / 164 -)/ 16 文件 / 0 R4 / 0 上游侵入。**闭环 backlog**:`feishu-group-slash-command` 留的 "enableAutoGroupCreate flag 是否彻底删" — 本次彻底删关闭。|
| [imbot-workspace-rename](docs/features/imbot-workspace-rename/3-changelog.md) | `3ad07cefb` + `11f6b739e` | home base 路径 `~/.opencode/feishu-workspace/` → `~/.opencode/imbot-workspace/`(2026-05-25,跟 ADR `OPENCODE-PLAN/架构决策/im桥接-imbot单一架构.md` 跨 IM 共享 home base 语义对齐;未来 telegram/钉钉 加入时不再叫 feishu-workspace 困惑)。**只改 home base**(其他 4 个 feishu-*config/sessions/server/dedup 真 feishu-specific 保留)。**migration helper**:`migrateLegacyWorkspace(legacyPath, newPath, fs, logger)` DI 友好,启动时 init 调用,行为表 5 种(migrated / noop-already-new / noop-no-legacy / skipped-both-exist / failed),原子 `renameSync` 跨子目录 + 子文件全跟着。**测试 6 case**(T1-T6):5 个 DI mock + 1 个真实 tmp fs 验证 mv 行为。**实施后必跑 C1-C10 测试清单**全列入 1-spec,实施完逐一验证(C1 typecheck 16/16 / C2-C5 511/511 测试 / C6 Tier 3 build 出 .app / C10 Rust 跳过无引用)。Medium 净 +180 行(238 + / 39 -)/ 9 代码 + 测试文件 / 0 R4 / 0 上游侵入。**配套**:OPENCODE-PLAN 仓 ADR + opencode-fork imbot-定制指南.md 全文路径同步;CLAUDE.md governance table 同步;历史 feat 1-spec/2-plan/3-changelog 保留 feishu-workspace 字眼作为快照不动。|
| [imbot-workspace-rename-followup](docs/features/imbot-workspace-rename-followup/3-changelog.md) | `39a9b2760` + `c8f3b2434` + (本笔) | `imbot-workspace-rename` 落地后 user 实测发现 bot reply 仍引用老路径 `~/.opencode/feishu-workspace/notes.md` ENOENT(2026-05-25)。**根因**:`feishu-chat-sessions.json` 里持久化的 opencode session ID 是 rename **之前** 创建的,session.directory 字段绑死 `feishu-workspace` + session.messages[] 含老 system prompt,复用老 session ID 时 opencode 仍按老 session 状态走 — user 必须每个 chat 手动 `/new` 才能用新路径,11+ chat 不可接受。**修法**:plugin `initBackground` 加 `applyStaleSessionsCleanup(markerPath, chatSessionStorePath, fs, logger)` helper(DI 友好,5 状态 applied/noop-already-applied/noop-no-sessions/failed),启动时一次性 unlink chatSessionStore + 写 marker `~/.opencode/.imbot-workspace-rename-cleanup-applied`(JSON 含 appliedAt + feat,**幂等** — 同一 user 第二次启动 noop)。**Trade**:user 失去 chat multi-turn memory(one-time),比手动 `/new` × 11+ 体验好。**沿这个问题排查的其他 stale ref 候选**:`feishu-config.json` / `feishu-bridge-server.json` / `feishu-message-dedup.json` 3 个 plugin 持久化文件都不引 workspace 路径,无需迁移;CLAUDE.md / docs / OPENCODE-PLAN 历史快照保留老字眼。**测试 6 case**(T1-T6):T1 marker 已存在 noop / T2 首装 noop-no-sessions / T3 升级 applied / T4 unlink EACCES failed 不写 marker / T5 写 marker ENOSPC failed / T6 真实 tmp fs 集成验 unlink + marker JSON 内容。**回归**:517/517 全过(基线 511 + 新 6),16/16 typecheck。Medium- 净 +280 行(2 文件:plugin.ts +111 / apply-stale-sessions-cleanup.test.ts 新 169)/ 0 R4 / 0 上游侵入 / 0 Rust 改动。[bug-repro: rename 后 bot reply 引用老 feishu-workspace 路径 ENOENT — chatSessionStore 持久化 session ID 绑死老 directory] |
| [vite-config-loopback-r6-fix](#vite-config-loopback-r6-fix) | `baf586df1` | 3 vite/astro dev server 默认 `host: "0.0.0.0"` → `"127.0.0.1"` 修 R6 违规(`packages/app/vite.config.ts` + `packages/enterprise/vite.config.ts` + `packages/web/astro.config.mjs`)+ pre-commit §4.5 扩 vite/astro `host: "0.0.0.0"` 第三类模式覆盖防再回归。**起源**:2026-05-25 ship Win prod `2026.5.25.1` 时 pre-push e2e gate 自动起 `bun run dev:e2e-mock`,vite 默认绑 `0.0.0.0:3000` 暴露 LAN — user 实时 audit 命中。**测试**:`bun run typecheck` 16/16 ✅ + 实测 `bun run dev:e2e-mock` netstat 仅 `127.0.0.1:3000` 监听 ✅。**R4 override 第 2 笔本季** — wrapper 不可行性论证:① vite plugin `configResolved` hook 不能改 `server.host`(server preload 后 plugin 才介入)② `vite --config <path>` 包一层需触动 `package.json` scripts(也是黑名单,等量转嫁不解约束)③ astro 无 `--config` flag 等价机制 ④ 直接改 9 行 + 3 个 FORK marker vs wrapper 100+ 行,显然更轻、回滚 0 成本 | Tiny ~40 行 / 4 文件(3 黑名单 vite/astro + 1 hook 白名单).husky/pre-commit / 0 上游侵入(全 fork-only marker)/ 0 新 SDK 依赖 / R4 override 论证已记 |
| [md-editing-iter-3](docs/features/md-editing-iter-3/3-changelog.md) | `1b9f4c0ef` + `52978936d` + `e44d40c80` + `f7b8c8bf8` + `9e92cda44` + `390356ed1` + `429bcc942` + `46a4ad72f` + `68ea12d77` + `f9f15673b` + `7d8f830c5` + `4e58b5e30` + merge | MD 编辑态语义样式增强(白领基线,2026-05-25,13 commits + merge / 6 轮矫正)— iter-1(2026-05-05)/ iter-2(2026-05-09)之后第 3 迭代,user 反馈"编辑态丑陋无层次"。**最终 copy 方案**:GitHub MD CSS heading 比例(h1=2em / h2=1.5em / h3=1.25em / h4=1em / h5=0.9em / h6=0.85em 公众号/知乎/简书/Notion 同款)+ iA Writer 源模式 opacity 0.7 标记符弱化 + **GitHub Primer 单蓝 accent 仅给链接**(`var(--text-interactive-base)` 跟 `markdown.css:48` 预览侧统一)+ inline code 无 chip(源模式靠反引号自识别 iA Writer/GitHub source view/Notion 同处理)+ strikethrough line-through + **heading 无下划线 + 链接无下划线**。**6 轮矫正深坑**:① user 提"白领画像非程序员"反转初版程序员风(1.6em h1)→ GitHub MD CSS(2em h1);② `var(--primary)` theme.css 未定义 bug,换 `--text-interactive-base`;③ lezer-markdown 把 fenced code block 内容也 tag `t.monospace`,我们 chip 套每 token 视觉灾难 → 完全删 monospace spec;④ 加 strikethrough + 加 Prec.high 尝试(后被 ⑤ 修正);⑤ default `{fallback:true}` 语义陷阱(仅当无其他 highlighter 时才用,我们一注册 default 整体 bail → 代码块全单色)+ default `tags.heading` underline 走 CSS cascade 注入位置赢我们的 textDecoration:none → 三步联动:去 default fallback + 加 Prec.high + 每 heading1-6 + url + link 显式 textDecoration:none 反 default;⑥ user 反馈 Python 单色不符合"代码都该有高亮"标准,扩 codeLanguages 注册 Python/SQL/JSON/YAML/CSS 5 个语言(动态 import 懒加载 0 启动 bundle penalty);⑦ Cmd+Shift+E keybind workaround Tauri WKWebView + SolidJS Portal 互操作限制(cliclick/osascript/CGEventPost 全部打不到 Portal menu button,但 keyboard event 通)。**架构最终态**:`markdownHighlightStyle` HighlightStyle.define 14 条 tag rule + `markdownSyntaxHighlight = Prec.high(syntaxHighlighting(...))` + `code-mirror-view.tsx:33` 去掉 `{fallback:true}` 让 default 与 markdown 平级共存(CM 按 tag 逐个 resolve)+ `lang-from-ext.ts` codeLanguages 8 语言。**自测路径完整**:Playwright e2e 视觉断言 spec + 滚动 tour 15 张截图(12-22s)+ 真 .app + Python Quartz CGEvent 滚动 + screencapture 兜底(20 张视觉档案)。**测试**:typecheck 16/16 / app 699 + 4 H1-H4 单测 / e2e spec + tour 双过 / 真 .app 视觉 20 张全干净。**2 R4 override**(本季超上限 ≤ 2/季叠加 vite-config-loopback 的 2 笔,user 授权):① `@lezer/highlight 1.2.3` direct dep — HighlightStyle.define 直接吃 Tag 对象 wrapper 不可行;② 5 个 `@codemirror/lang-*` direct deps — codeLanguages 注册唯一方式。Medium ~+800 行 / 9 文件 / 0 上游侵入。**已知边界**:Bash/Diff/Mermaid 单色(无官方 CM6 lang-*,留 backlog)。**10 个深坑沉淀到 3-changelog 经验段**(用户画像必须先于美学决策 / CSS 变量验证 / lezer-markdown tag 范围 / CM6 fallback:true 全或无语义 / CSS cascade 与 Prec.high 翻盘 / e2e 断言阈值必须按用户视觉感知精度 / e2e mock vs 真 .app 是两套环境最终必真 .app / Tauri WebView + SolidJS Portal 合成 click 限制 / vite minifier `!==` ↔ `===` 反转 grep / cliclick `t:` vs `kp:` 修饰键差异)。**配套** OPENCODE-PLAN 沉淀 `需求池/自动化测试-iter3-视觉测试经验.md`(给方案 v2 → v3 升级 4 个补丁)|
| [feishu-image-recognition](docs/features/feishu-image-recognition/3-changelog.md) | `f967ed5cb` + `60174ac35` + `8039a70d9` + `f0f096c78` + `daf4974ad` + `3784dc65b` + `a436f3bf1` + `c20d34ad4` + (本笔) | 飞书 user 发图给 bot 识别(2026-05-26,8 commits / 4 fix follow-up)。**D 方案落地**:image 下载到 `~/.opencode/imbot-workspace/feishu-images/<chatId>/`,`FilePartInput.url=file://<absolutePath>` 由 opencode-cli `prompt.ts:1230` 自动 readFile + base64 inline 给 LLM(0 临时 server / 0 R6 / 0 改上游)。**最终落地范围**:text + image + post 三 messageType(post = 图+文字混合,飞书拖图+输文字默认走 post);**vision 预检**:收图前查 `model.capabilities.input.image`,不支持 model 直接友好提示换模型避免卡死"识别中...",`visionCapCache` Map 10min TTL。**关键架构选型**:1-spec 4 选项(A/B/C/D)对比 — A localhost HTTP server 撞 R6 / B base64 data URL JSON 体 27MB+ 膨胀 / C 改上游 R3 / D workspace 持久化 + file:// 0 摩擦 ✓。**S1-S5 加固层**:① 大小硬限 20MB(Content-Length 预检 + 真 buffer 复检 + 空 buffer 拒)② 30s AbortController fetch timeout ③ mime 白名单 7 种 image ④ 落盘路径 resolve 越界 assert ⑤ error 不含 tenantAccessToken(只暴 image_key/message_id)。**helper extract 模式** 3 个新纯函数:`parseMessageContent` / `extractVisionSupport` / `isVisionCacheFresh`(全部 export 供单测,绕开 class 私有方法 + opencode RPC mock)。**测试 57 新增**:I1-I5 + S1-S5 + U5 + U1 + U2-U4 全过 — 574/574 全包 0 fail / 16/16 typecheck / 0 R4 / 0 上游侵入。**5 个 fix follow-up 踩坑沉淀** changelog:① `/images/{image_key}` 对 user 上传图返 400(必用 `/messages/{message_id}/resources/{file_key}?type=image`)② post messageType 不是 image(默认 user 拖图 + 输文字走 post,先期只收 text/image 致"图文消息无响应")③ vision capability ≠ attachment(claude-code/haiku attachment=true 但 capabilities.input.image=false 仍会 LLM 端报错)④ plugin → message-pipeline → image-downloader 循环 import(downloader 自己重算 IMBOT_WORKSPACE 路径而非 import)⑤ sidecar binary 时间戳陷阱(message-pipeline.ts 改完必须 build 前 ls 时间戳确认重 build)。Medium 1.5d / 1687 行 / 8 文件 / 0 R4 / 0 上游侵入。**留 backlog**:merge_forward(合并转发,user 已提)/ file / audio / video / sticker / 多图聚合 — 由独立 feat 接。[bug-repro: ① 飞书 user 发图 400 Bad Request for image_key=img_v3_xxx ② 飞书图+文 post 消息无响应] |
| [feishu-merge-forward](docs/features/feishu-merge-forward/3-changelog.md) | `2bdc90a5b` + `0cf0d9bca` + (本笔) | 飞书 user 合并转发消息识别(2026-05-26,3 commits)。**最具鲁棒性方案落地**(user 拍板,选 D1=C 全图展开 + R1-R4 鲁棒补强):D1 全图展开 MAX_IMAGE_COUNT=5(继承 image-recognition S1-S5)/ D2 SDK `client.im.v1.message.get` 拉子消息 + Promise.race 30s timeout / D3 MAX_SUB_MESSAGES=50 末尾省略 / D4 嵌套 1 层递归 + depth≥2 占位 / D5 群聊带 sender(open_id 前 6 位)+ p2p 不带 / D6 中文 user 视角占位 + 元信息(`[文件: 月报.docx 1.1MB]` / `[语音 12s]` / `[分享: 群 oc_xxx]`)。**R1-R4 鲁棒补强**:30s timeout / `sortByCreateTime` R2 时间序 / 0 子消息+拉取失败+深度超限 友好兜底 / vision 预检 textOnly 降级。**核心调研**:WSS event content 不含子消息必须二次 `GET /open-apis/im/v1/messages/:id` 拉 items + filter `upper_message_id`;子消息资源访问必须用**子消息自己的 message_id**(跟 image-recognition 撞过的 API 二分同款风险点,提前规避)。**架构**:`merge-forward-flatten.ts` 纯函数 helper(340 行,8 种 msg_type 占位 + 嵌套 depth 计数 + 全局 image index 独立计数器防 maxImages 截断后停增)+ `merge-forward-fetcher.ts` SDK 调用层(85 行,Promise.race timeout + filter upper_message_id + 业务错 code 兜底)+ `message-pipeline.ts` `handleMergeForward` 独立路径(140 行)+ `expandNestedMergeForward` 嵌套展开(`  ↳ ` 缩进 + 占位 replace + 失败兜底)+ `runOpencode.imageOpts.imageParts[]` 多 file part 注入。**测试 48 新增**:M1-M12 + sortByCreateTime + hasAnyImage + renderSubMessage(37 case)+ F1-F5(11 case);**622/622 全过**(基线 574 + 新 48)/ 16/16 typecheck / 0 R4 / 0 上游侵入。**5 个经验沉淀** changelog:① global image index 必须独立计数器(初版 `images.length+1` 截断后停增)② 嵌套递归不能放纯函数(IO 违反纯函数约束,搬到 pipeline 层做)③ SDK timeout 用 Promise.race 兜底(SDK 不支持 AbortController)④ 子消息资源用子消息自己的 message_id ⑤ SDK get_message 普通 JSON 0 multipart 风险(只 multipart upload 撞 axios+form-data 链)。Medium 1.5d / 1891 行 / 10 文件 / 0 R4 / 0 上游侵入。**留 backlog**:file/audio/video/sticker 多模态展开 / 嵌套≥3 层 / sender_id→用户名缓存(需查飞书 contact API)/ 结构化 JSON pattern D |
| [media-creation-mode](docs/features/media-creation-mode/3-changelog.md) | `73902adbd` → `a30f1f94c`(14 commits) + (本笔 follow-up) | 创作模式(2026-05-26~27,REQ-030)— 否决"AI 自动调工具"改显式模式菜单(Chat + 文生图/图片编辑/文生视频/图生视频/语音合成/语音识别/专业翻译)+ 左侧随模式换模型/音色 + 输入即生成;结果融入聊天滚动流(轻量卡:提示词+输入缩略图+小结果+已保存路径+打开)+ 产出落当前项目根 `creations/`。fork-frontend(prompt-input/message-timeline 注入)+ 本地生成服务(Bun.serve 51737 R6 SSE)+ 模块级 store,引擎复用 media-gen-alibaba。CDP 真机端到端自测 + user 桌面 QA。无模型引导/通用引擎留 backlog。**follow-up(2026-05-27,macOS GUI 测试,4 笔)**:修 6 bug —— ① 首页(无 session)生成后卡片不显示(`NewSessionView` 漏挂 `MediaCreationResults`)② TTS/视频卡播放器 Error(远端 OSS url 喂 `<audio>` 跨源/过期失败,改走 `localasset://` 播本地落盘文件;抽 `creationMediaSrc`)③ 启动过创作后旧记录在新会话冒出来(`cards` 全局未隔离 → 改按 session 作用域 + 首页 draft 过继)④ 视频生成完成但卡片卡在"正在生成…"(生成中途发 chat,adoption 移走卡,patch 固定 scope 落空 → 改 patch 按卡 id 跨作用域查找)⑤ home→home 再点新建会话不清 draft(同路由 effect 不触发 → 4 个新建会话入口显式调 `resetDraft()`);+9 单测(creationMediaSrc 4 + 作用域 5);[bug-repro] 见 3-changelog Follow-up 段 | Medium-Large,三文档,14 commits + 4 follow-up |
| [window-resizable](docs/features/window-resizable/3-changelog.md) | `44180a261` | 修窗口无法鼠标拖拽改大小 — 真因 `tauri-plugin-window-state` 持久化恢复 fullscreen/maximized(残留 `.window-state.json` `fullscreen:true` 反复恢复→钉死全屏抓不到边);修 `constants.rs` flags 减 MAXIMIZED+FULLSCREEN + `windows.rs` 去 `.maximized(true)` 改窗口态 + 清残留状态文件。native 拖拽 user 真桌面验通 | Tiny,只 changelog,2 上游文件 FORK marker |
| [media-gen-bundle](docs/features/media-gen-bundle/3-changelog.md) | `b977f4130` | media-gen 创作插件打包进安装包(同飞书机制)— Tauri resources + DeskFox.iss [Files] 装到 `{app}\plugin\media-gen` + `feishu_plugin_install.rs` 通用化加 `ensure_media_gen_plugin_in_config` 开机注入;装完连 Alibaba (China) 填 key 即自动可用、零手动配。注入逻辑通用化(`inject_plugin` 末两段自动推标识,签名不变测试零改)+ `media_gen_and_feishu_coexist` 单测;顺带修 build-deskfox 清理块 PS5.1 编码 bug(Get-Content/Set-Content ANSI → .NET UTF-8 无 BOM,否则写坏含中文 opencode.jsonc 致注入全废)。真机端到端验通(build→清理→注入→/healthz+8模型→文生图落项目根) | Medium,三文档,1 commit + 编码 bug 修 |
| [feishu-win-test-compat](docs/features/feishu-win-test-compat/3-changelog.md) | `b6e2f2f4a` | 飞书单测 Windows 兼容修复 — 飞书那批 Mac 验 622 全过、Win 跑 607/15 fail(media-gen 合并前体检发现)。① **真 Win 代码 bug** `image-downloader.ts` 路径越界 assert `resolve(p)+"/"` 在 Win 反斜杠路径前缀永不匹配 → 飞书图片下载全失败,改 `+path.sep`;② classifyAttachment 测试 ROOT Unix 路径 → `resolve(...)`;③ defaultFilePath 断言写死 `/` → `join(...)`;④ dedup 2 个 TTL 刷新测试时序裕度太窄(Win ~16ms 粒度)→ 放大 ttl/sleep。修后 622 pass/0 fail(Win)+ typecheck 17/17 | Tiny,只 changelog,4 文件净 +6 行,[bug-repro: 飞书单测 Win 15 fail] |
| [feishu-merge-forward-image-400](docs/features/feishu-merge-forward-image-400/3-changelog.md) | `aadbd245c` | 合并转发图片 graceful 降级 — 真机发现合并转发子图下载全 400(飞书 234043:**官方不支持读取合并转发内子消息资源**,平台硬限制;原机制单测 mock fetch 掩盖、真实从没成功)。下载本就不可能,改 graceful:flatten `maxImages=0`(不建下载列表+不渲染假"已展开识别"+加 `imageCount`)+ 停死下载(省 5×400)+ prompt 告知 LLM 只看文字 + **回复头部诚实提示**「📷 合并转发里的 N 张图我读不了(飞书限制),需识别请直接转发图」。飞书 622 pass + 真机 UX user 验通 | Tiny+,只 changelog,3 文件净 -13 行 |
| [file-tree-test-client-env](docs/features/file-tree-test-client-env/3-changelog.md) | `b073c5fa5` | file-tree.test.ts Windows 测试修复 — 远程同步代码后 app 单测在 Win 跑出 1 fail(`Client-only API called on the server side`)。根因:bun test 解析 solid-js 服务端构建,`import ./file-tree` 连带加载 `@opencode-ai/ui/context-menu`(Kobalte)→ `@kobalte/core` 求值期调 client-only API 抛错(纯测试环境问题,真实 app 有浏览器正常)。测试已 mock 其余 UI 独缺 context-menu,补一条同款 mock 隔离。上游测试 #12468 加 FORK marker。app 711/0(原 708/1)| Tiny,只 changelog,1 文件 +6 行 |
| [feishu-plugin-bundle-fs2](docs/features/feishu-plugin-bundle-fs2/3-changelog.md) | (本笔 commit) | 飞书插件 "failed to load plugin (fs.existsSync undefined)" 修复(2026-05-27,mac ship 验证发现)— prod 启动 sidecar 4× `feishu-bridge ... fs.existsSync undefined failed to load plugin`,但 feishu 功能其实全正常(WSS 2/2/server/migrate),`2026.5.25.1` 无此错=回归。**根因**:opencode plugin loader 的 `getLegacyPlugins` 遍历插件模块**所有 export** 当 plugin server 调 `fn(input,options)`;plugin.ts 为单测 export 了 `migrateLegacyWorkspace`/`applyStaleSessionsCleanup` 两个裸 helper → 被错参调用 → 第 3 参 fs=undefined → 抛。media-gen 不 export 裸 helper 故同 sidecar 加载干净;helper 是 imbot-workspace-rename(2026-05-25 后)新加 export 的=回归点。**排查关键**:typeof fs 日志证实 cleanup 被调 5 次(1 次 initBackground fs=对象 + 4 次 fs=undefined 不经调用点)+ sidecar 插桩拿堆栈定位 `applyStaleSessionsCleanup`;曾误判 `--target=bun` 打包(red herring,改 target 只换错名没修)。**修法**:helper 挪到新模块 `workspace-migrate.ts`,plugin.ts import 用不 re-export → 入口 export 面只剩真插件。+守卫单测 `plugin-exports.test.ts`(断言入口不 export 裸 helper)。真机验:4→0 错、feishu 干净加载;飞书 622 pass / typecheck 17/17。**⚠️ Win prod `2026.5.27.1` 大概率同款,需重发盖掉** | Medium,新 1 文件(移动)+ plugin.ts/2 测/1 probe import 改 + 1 守卫测,[bug-repro: feishu 插件 export 裸 helper 被 opencode 当插件调致 fs undefined] |
| [req-031-tray-icon-relaunch-show](docs/features/req-031-tray-icon-relaunch-show/3-changelog.md) | (本笔 commit) | REQ-031 关闭到托盘后点桌面/Dock/程序列表图标重开窗口(2026-05-28)— 关 GUI 后 hide 到托盘常驻,再点图标启动 single-instance 回调拦截,原实现只 `set_focus + unminimize` 对被 `hide()` 的窗口无效 → 用户点图标没反应。修法:`system_tray::show_main_window_impl` 改 `pub`(顺序 `show → unminimize → set_focus`,被 hide 的必须先 show),lib.rs single-instance 回调复用之,与 tray 菜单"打开 DeskFox"行为完全对齐。Tiny ~8 行,0 上游 TS / 0 R4。e2e 基础设施未 ready 前由 user 真桌面抽查 Win/Mac(spec 标 🟡)。[bug-repro: 托盘常驻态下点图标重开窗口无反应] | Tiny,只 changelog,2 桌面 rs 文件 |
| [req-032-menu-clamp-viewport](docs/features/req-032-menu-clamp-viewport/3-changelog.md) | (本笔 commit) | REQ-032 选区菜单贴近窗口边沿被遮挡(2026-05-28)— 两处手写"选区加聊天"菜单(file-tabs.tsx mdMenu + context-menu-host/host.tsx ContextMenuHost)绕开 Kobalte,自己 `fixed` + raw 坐标无视口保护,贴边沿时按钮溢出看不到点不到(user 实报:md 文件结尾右键「取消/加入聊天」被裁)。**修法**:新建共享 helper `utils/menu-position.ts` 含纯函数 `clampMenuToViewport`(下溢 flip 上方/右溢左收/各方 clamp margin) + glue `repositionMenu`(读 getBoundingClientRect 真实尺寸,不能用常量,input 卡 360px+textarea 多行高);两处菜单各加 `createEffect(menu()) + queueMicrotask → repositionMenu`,4 个 Match div 加 `ref + visibility:hidden`(防闪)。**+9 helper 单测**(4 边沿 + 角部双向溢出 + flip 后 clamp + 自定义 margin + 极端菜单 > 视口)。typecheck 17/17 / app 738 pass / 0 R4 / 0 上游侵入。需求池审计:全仓其他右键菜单全走 Kobalte/相对定位,不受影响。**长期**:随 office-选中加聊天 v2 迁 Kobalte 后本 helper 可删。[bug-repro: 选区菜单贴近窗口边沿溢出视口看不到点不到] | Small,~190 行 / 4 文件(新 helper+测 2 文件 + 2 接入)|
| bun-lock-media-gen-reconcile | (本笔 commit) | **bun.lock 补 `@deskfox/media-gen` workspace 条目**(2026-05-27,macOS 适配测试中发现)— media-gen-alibaba/bundle 合入 main 时 `packages/media-gen/package.json` 进仓但 bun.lock 漏同步,致干净 checkout 不先 `bun install` 直接 typecheck 失败(`bun:test`/`Bun`/`zod` 类型找不到)、CI `--frozen-lockfile` 会报错。`bun install` 补 16 行 workspace 自引用条目(zod/@types/bun 等均已在 catalog,**依赖图 0 变化**,自报 `Checked 2589 installs across 2916 packages (no changes)`)。**R4 override(bun.lock 黑名单,本季第 2 笔,user 批准)**——**① wrapper 不可行性**:lockfile 由 Bun 自动生成且必须如实反映 workspace 包集合,无 wrapper / 新文件可替,唯一修法即补 lockfile 本身;**② 风险**:零供应链(无新包 / 无版本变更 / 无 registry 变更),回退 `git checkout main -- bun.lock`;**③ 同 #61 md-export-pdf-word bun.lock override 场景**(lockfile 随 dep/workspace 变动自然重生) | Tiny,+16 行 / 1 文件(bun.lock)/ **R4 override 第 2 笔本季** / 0 上游侵入 |
| [stuck-working-indicator-fix](docs/features/stuck-working-indicator-fix/3-changelog.md) | `c7eb95ce2` | 会话列表"运行中"旋转图标永久卡死(2026-06-06,user 报 DeskFox 任务点停止无效 + 杀重开仍转)— **根因**:进程被硬杀(SIGKILL/崩溃/休眠)时正在生成的 assistant 消息漏盖 `time.completed`(`processor.ts:641` 的 `Effect.ensuring(cleanup)` finalizer 来不及跑),留下永久"未完成"残骸消息;前端 `isWorking()` 用 `findLast(任意缺 completed 的 assistant)` 判运行中 → 扫到埋在历史里的残骸 → 永久转、杀重开仍在(残骸持久化 DB)、点停止无效(后端无活跃任务)。provider 无关(飞书桥接 session 同样中招),全库审计 7 个残骸 session。**双层修复**(DeskFox 机制,不碰 claude-code 插件):① 后端 `messages` handler idle 时自愈补盖 `completed=created`(新 fork-only `heal-interrupted.ts`,busy 守卫不误伤在途)② 前端 `isWorking` 改纯函数 `deriveSessionWorking` 只看最后一条消息。14 新单测全过 + session/server 302+12 回归绿 + 两端 typecheck / 上游改 2 文件(均加 FORK marker)。**R4 override 1 笔**(`[override-blacklist]`,user 复核批准):pre-commit 路径黑名单覆盖整个 `packages/opencode/` 目录 → 本笔触动其下 3 文件触发。逐文件:`handlers/session.ts` 真上游改动(messages handler 唯一咽喉点,无 wrapper 可走,R1 二级 ~5 行注入);`heal-interrupted.ts` + `.test.ts` 纯 fork-only 新文件(P1),仅因落在 opencode 目录被路径黑名单误伤。[bug-repro: 硬杀漏盖 time.completed 致残骸消息令"运行中"图标永久卡死] | Medium,378 行(2 新 fork-only + 2 测 + 2 上游 FORK 块)|
| [upstream-sync-2026-08](docs/features/upstream-sync-2026-08/3-changelog.md) | `ea3ea31315` / `3faa8a76f4` / `76c1340c11` / `c1909dbb92` + 本笔 | **上游同步 v1.17.4 → v1.18.16**(2026-08-11,Large)— 漂移 1281 commits 分 4 段 merge(时间线虚拟化重写 / markdown→session-ui 搬家 / v2 默认翻转 / i18n 全球化)。整体验收(local 档真机 CDP)发现 v2 换代致 fork 定制在用户路径失效 7 项,🔴 两项(创作模式入口、飞书设置页)本分支回植(0 行上游改动)+ 3 条守卫 e2e;其余 5 项立 REQ-106。 | 4 笔 merge 各 1 R4;回植 0 R4 |
| [keep-legacy-layout](docs/features/keep-legacy-layout/3-changelog.md) | `b86d15779b` / `98a8bbaf9b` / `d837f71536` / `d7be6c9d3d` + 本笔 | **不跟随上游 v2 换代,默认回经典布局**(2026-08-11/12,Large 口径)— **推翻上一笔的 D1**:user 真机试用 v2 后决定不采用。上游有 4 条独立路径推用户去 v2(渠道默认 / 新档案默认 / 2026-09-14 退役日强制 / 升级迁移强切),**必须一次全堵**(漏一条 = 过一阵子自己变回去,排查成本极高),逐条留单测;上游原判定改名保留 `upstreamShouldEnableNewLayout` 且继续被测试覆盖(下次 merge 能正常接上游改动),v2 开关保留供自愿试用。修双渠道徽标(段3 上游外层重复加 `ChannelIndicator`)+ 标题栏工具组锚左(web 端回落防整组消失)。**三层机器比对**(命令注册表 / FORK marker 按 feat 聚合 / i18n·右键菜单·handler 逐文件,基准 sync 前 main `e77443750e`)补回 8 处丢失定制 —— **教训:上游 merge 冲掉的是调用点而非定义**(定义还在 → grep 找不出、e2e 照绿、静默失效);5 处判为有意撤销不恢复。顺带修「聊天引用点开空白预览页」,过滤**必须解码后比对**(存量 tab 是 `%3Cchat%20selection%3E`,单测绿真机不生效)。守卫 `classic-layout-default.spec.ts` +2 条。 | Large 口径,13 文件 +271/−31;**0 R4 override**;1-spec 事后追认 |
---

## 仓库基线信息

| 项 | 值 |
|---|---|
| upstream | `github.com/sst/opencode`（→ 实际 `anomalyco/opencode`,GitHub redirect；只读，仅 fetch） |
| origin | `github.com/zoulukuang/deskfox`（**主仓**,真 fork 关系 `forked from anomalyco/opencode`,2026-05-03 迁移自 `yuesoue/opencode-for-office-deskfox`,2026-05-04 user 改名 `yuesoue` → `zoulukuang`(详见 `user-rename-zoulukuang` 行)；PR / Issue / Star / Release 都在这里；`git push origin` 推这里） |
| origin-legacy | `github.com/zoulukuang/opencode-for-office-deskfox`（**老主仓,逐步放弃**;只读不再 push;archive 待新仓稳定后再做;历史 Release `ship-prod-2026.5.1.2` 留在这里不动；URL 中 `zoulukuang` 是 user 改名后,GitHub 自动 owner 替换） |
| gitee | `gitee.com/zoulukuang/opencode-for-office-deskfox`（**国内镜像,待跟随主仓迁** — Gitee 侧待手动新建 `zoulukuang/deskfox` + 配 auto-sync from `zoulukuang/deskfox`,完成前 Gitee 仍同步老 GitHub 仓） |
| upstream-baseline tag | `upstream-baseline`（clone 时打，对应 sst/opencode 的 `dev` 分支某一时刻） |
| 默认工作分支 | `dev`（与上游同名） |
| feature 分支命名 | `feat/<feature-name>`，例 `feat/editable-file-viewer` |

## 上游同步流程

```bash
git fetch upstream
git tag pre-rebase-$(date +%Y-%m-%d) dev   # rebase 前打 tag
git checkout dev
git rebase upstream/dev                    # 把上游新提交并到本地 dev
git push origin dev --force-with-lease     # 推到 GitHub（feature 分支会基于这个；gitee 由 GitHub 后台自动同步）
```

冲突 → 走 09 规则的回退流程，不强推。

---

## 模板（每次改动复制下方结构填写）

```markdown
## #N <YYYY-MM-DD> <一句话标题>

**关联 commit**: `<short-hash>`
**所在分支**: `<branch-name>`
**baseline tag**: `<tag-name>`
**触发原因**: <为什么改：phase 步骤 / bug 报告 / 用户指令>

**改前预清单**（动手前填）:
- 预计触动文件: `<path>`
- 预计 diff 行数上限: ~XX 行
- 预期影响功能点: <列举>

**实际改动**:
- `<file>:<line>` 改前 `<片段>` → 改后 `<片段>`
- `<file>:<line>` 新增 `<内容>`

**事后比对**:
- 实际 diff 行数: XX 行 ✅/❌
- 多出的文件: 无 ✅ / 有：`<file>`（解释：...）

**影响范围**:
- <功能点>: 预计无影响（理由：...）

**回归测试点**:
- R1: <步骤> → <期望> → ✅/❌/挂账（原因）
- R2: ...

**review 自检**:
- [ ] 仅触动白名单目录
- [ ] git diff --stat 在预期内
- [ ] 大小写检查通过：`git ls-files | sort -fu | uniq -di` 输出空
- [ ] 新增依赖：无 / 有（已说明理由）
- [ ] 是否有"顺手改"未记录：否
- [ ] baseline tag 已打并记录在 commit

**回退方法**:
\`\`\`
git reset --hard <baseline-tag>
\`\`\`
```

---

## 改动记录

> **v1 时期(2026-04-23 → 2026-04-26)的 #1-#12 详细条目已归档**:
> [`docs/history/changelog-pre-v2.md`](docs/history/changelog-pre-v2.md)(只读保留,1000 行)
>
> **v2(2026-04-27)起**,改动记录 = 上方"规范 v2 起的索引"表 + `docs/features/<feat-id>/3-changelog.md` 详细。

---

## R4 黑名单 override 记录

- **2026-06-14 — `packages/desktop/electron-builder.deskfox.config.ts`**(version 修复 commit):
  - **命中规则**:pre-commit §4.1 黑名单 `.*\.config\.(ts|js|mjs)$`(本意保护构建配置)。
  - **wrapper 不可行性**:该文件是 electron-builder 打包配置**本体**(fork 自建 `FORK-ONLY`),按 channel×平台注入 `extraMetadata.version` 的逻辑必须写在配置内,无处可 wrapper;黑名单 `*.config.ts` 过宽规则误伤 fork 自有配置(同类:office-installer.ts 曾被 `packages/opencode/` 前缀误伤)。
  - **风险**:低。只读 `installer-versions.json` 注入版本号,影响安装包名/app 版本/updater 比较,不碰上游/运行逻辑,单 commit 可 revert。
  - **理由**:补回换基座漏迁的版本注入逻辑,使 electron 版符合既定版本规范 §3.2bis(Dev 领先独立号线)。user 复核已批。

- **2026-06-14 — `packages/desktop/electron.vite.config.ts`**(版本号归拢 + 渠道注入根治 commit):
  - **命中规则**:pre-commit §4.1 黑名单 `.*\.config\.(ts|js|mjs)$`(本 session 第 2 次同类误伤,上次为 fork-only electron-builder 配置)。
  - **wrapper 不可行性**:`import.meta.env.VITE_OPENCODE_CHANNEL` 是 build 期注入,无运行时 wrapper;唯一注入点是 renderer 段 `define`。改动为 FORK-marked 几行 define 块,不碰上游现有逻辑。
  - **风险**:低。新增一个从单一源 `OPENCODE_CHANNEL` 派生的 define,让渲染层徽标/版本号确定生效;单 commit 可 revert。
  - **理由**:根治隐患——原渲染层无此 define,徽标/版本号只靠 build 时碰巧设了 `VITE_OPENCODE_CHANNEL` env 才对(漏设则徽标消失+版本号回落 prod 线)。user 复核已批。

- **2026-07-05 — REQ-069 非 git 文件夹稳定身份 `75ca4f573d`**(R4 squash 变体单笔,原流水线 6 单元 commit 压 1,配额记 1 笔):
  - **命中规则**:pre-commit §4.1 路径黑名单 `packages/core/` + `packages/opencode/`(含纯新增文件,REQ-048 教训)。
  - **逐文件 wrapper 不可行性**:
    - `packages/core/src/project.ts`(+25 −14):项目身份优先级链(`remote > .git/opencode > 锚id > root`)是 `ProjectV2.resolve` 的内在职责——resolve 是全系统唯一身份判定点(session.ts/location.ts/move-session.ts 直接调用),在外层 wrapper 改写返回值 = 造第二个身份真相源,必然漂移。读锚主体已抽 fork-only `anchor.ts`,resolve 体内仅读锚调用 + 两分支,FORK-BEGIN/END。
    - `packages/opencode/src/project/project.ts`(+100):`fromDirectory` 是唯一「显式打开项目」编排点,铸锚触发/flag 门控/M6 反查必须在 migrateProjectId 与 DB upsert 之间的固定次序里,外置 = resolve 直接调用方(move-session 等)会误触发铸造写盘(违反「显式打开才写」不变量)。写锚/exclude 主体在 core anchor.ts,此处仅编排注入,FORK-BEGIN/END。
    - `packages/opencode/src/effect/runtime-flags.ts`(+2):RuntimeFlags 集中式 Service,flag 注册无法外置;≤5 行 + 单点 FORK marker(R1-Level2)。
    - 新增 6 文件(anchor.ts + 5 测试):锚模块须与 ProjectV2 同包(core 内部 Effect/FSUtil 依赖),测试按包内惯例落 test/ 目录——纯新增被路径黑名单误伤,非深改。
  - **风险**:中低。M8 flag `OPENCODE_EXPERIMENTAL_NONGIT_IDENTITY` **默认关**,发布行为零变化;flag 关路径经「无锚 bit-identical」断言 + 现有盘根断言原样通过双保险;单笔 revert 干净(P4)。
  - **理由**:REQ-069 架构特性(v2026.6.25 版本主体),1-spec user 2026-07-04 审签;交付测试 102/102 + 集成终关全绿。squash 变体依 2026-07-04 user 拍板(逐单元 commit 保流水线 checkpoint,合 main 前压 1 笔复核)。

- **2026-07-05 — REQ-072 会话侧栏项目维度 `(待提交)`**(v2026.8.4,feat 分支 project-continuity-v2026-8-4;R4 squash 变体逐单元 commit):
  - **命中规则**:pre-commit §4.1 路径黑名单 `packages/opencode/`(含 `session.ts` 主体 + 新增 test 文件,REQ-048 教训:纯新增落该目录也触发)。
  - **逐文件 wrapper 不可行性**:
    - `packages/opencode/src/session/session.ts`(+21 含注释):global 门控判据 `ctx.project.id === ProjectV2.ID.global` 依赖 `InstanceState.context` 的 **resolve 后 projectID**,该值只在 `Session.list` 运行时(Effect layer 内)可得。handler(`handlers/session.ts`)拿到的是**未 resolve** 的 HTTP query,projectID 尚未从 directory 解析 → 门控无法上移 handler、更无法 fork-only 外部 wrapper(会造第二身份真相源)。已把侵入压到极致:纯函数 `gateProjectScope` + `list` 内 1 行调用,`handlers/session.ts`、`listByProject`、HTTP schema **全零改**(scope=project plumbing 是上游原生 #24853/#25217/#30804)。FORK-BEGIN/END 包裹。
    - 新增 `test/session/gate-project-scope.test.ts`、改 `test/server/session-list.test.ts`:测试按包内惯例落 `packages/opencode/test/`,被路径黑名单误伤,非深改。
    - 前端 4 文件(session-load/types/tabs/新 tabs-dedup)+ 前端 2 测试均 **fork-only 或非黑名单**,不计入 R4。
    - **(真机修复追加)`packages/opencode/src/project/project.ts`**:REQ-072 真机验收发现 flag-on 后 bootstrap stale 条目会「重建已删文件夹」→ relocate 失效丢会话;加 `openDirMissing` 守卫(确切 ENOENT 时不 mint/writeAnchor)。守卫内在于 REQ-069 fromDirectory 铸锚编排(与 REQ-069 override 同源不可外置),归本 feat **同 1 笔 R4**(squash 变体合并计)。Fix1(sidecar.ts 开 flag)+ Fix2 锚扫描 relocate 主体(desktop-main fs-probe + 前端 layout/home/server)全 **fork-only/非黑名单**,不计入 R4。
  - **风险**:低。`gateProjectScope` 只在 `input.scope==="project"` 改变行为,不传 scope 的所有既有 caller 零影响(TC-B3 验);global 反例强制降级守大杂烩(TC-B2/B4);`openDirMissing` 守卫只在确切 ENOENT 改行为、复用三态判定不误伤离线盘;13 门控测试 + fs-probe 16 + REQ-069 项目族 141 + 真机 relocate 11/11 全绿无回归;纯 FORK 包裹单笔可 revert,不改持久化/DB schema/HTTP 契约。
  - **理由**:REQ-072(v2026.8.4 版本主体之一),user 2026-07-05 拍板方案(项目维度 + 身份门控 + 门控落后端)。**配额提示**:本季(Q3)R4 累计 = REQ-069 + REQ-072 **2 笔**,处健康基线 ≤2 上限;若本版 REQ-071 也需触黑名单,将超基线,届时先报备 user。复核报告见 `docs/features/project-continuity-v2026-8-4/3-changelog.md`「R4 override 复核报告」段。

- **2026-07-11 — REQ-081 macOS Intel x64 交叉打包 `74b1b64a40`**(feat 分支 macos-intel-x64-build):
  - **命中规则**:pre-commit §4.1 黑名单 `.*\.config\.(ts|js|mjs)$`(护上游配置;`.deskfox.config.*` 才豁免)。**唯一命中文件** `packages/desktop/electron.vite.config.ts`;其余 3 文件(`build-deskfox-electron.sh`/`prepare-lo-bundle.sh` 在 branding 非黑名单、`electron-builder.deskfox.config.ts` 走 `.deskfox` 例外)不计入 R4。
  - **逐文件 wrapper 不可行性**:
    - `packages/desktop/electron.vite.config.ts`(+1 改 +5 注释):`nodePtyPkg` 改读 `process.env.DESKFOX_TARGET_ARCH || process.arch`。它同时喂给同文件内的 `node-pty-narrower` 插件 `resolveId`(重写 `@lydell/node-pty`→宿主子包)和 `externalizeDeps.include`,两处都是 electron-vite 加载 config 时内联求值、**无对外注入点供 fork-only 覆盖**;走 fork 副本 config 需改 package.json script(亦黑名单)+ 整份复制上游 config → 造长期双轨(违「稳定>简洁」,漂移风险远高于 3 行注入)。属 R1 三级跳第 2 级(上游 ≤5 行接口注入)标准形态,带 FORK marker + `[feat:]` tag。
  - **风险**:低。`DESKFOX_TARGET_ARCH` 未设时回落 `process.arch` = 上游原逻辑,arm64/prod 现有发布主路径零影响(TC-7 验);仅 `--arch x64` 交叉打包才注入该 env;单点改 `git revert` 干净;无持久化/schema/契约变更。
  - **理由**:REQ-081 交叉打 Intel x64 包能在真机启动的**充分必要条件之一**(bundle 写死构建机架构是真机崩溃真凶),TC-3/4/6 直接验证,真 Intel Mac(2020 i5/15.7.7)安装启动成功。**配额提示**:本季(Q3)R4 累计 = REQ-069 + REQ-072 + **本笔 = 3 笔,已超健康基线 ≤2**,已就此向 user 专门报备并取得 override 同意。复核报告见 `docs/features/macos-intel-x64-build/3-changelog.md`「R4 override 复核报告」段。
| renderer-snapshot-oom | 2026-08-02 | REQ-087 renderer 快照 OOM/磁盘写入超限治理(节流+历史剥图+熔断+连环崩自愈) | [docs/features/renderer-snapshot-oom/3-changelog.md](docs/features/renderer-snapshot-oom/3-changelog.md) |
| sidecar-oom-brake | 2026-08-02 | REQ-049 sidecar OOM L1 硬帽+软刹车 / L3 断连提示(含 watchdog 通道修复) | [docs/features/sidecar-oom-brake/3-changelog.md](docs/features/sidecar-oom-brake/3-changelog.md) |
| permission-filter-concurrency | 2026-08-02 | REQ-078 权限过滤签名 refetch + canResolve 下沉(修并发藏死/幻影徽标) | [docs/features/permission-filter-concurrency/3-changelog.md](docs/features/permission-filter-concurrency/3-changelog.md) |
| popup-enter-passthrough | 2026-08-02 | REQ-085 浮层 Enter 穿透 toggle 预览区(target 守卫修复) | [docs/features/popup-enter-passthrough/3-changelog.md](docs/features/popup-enter-passthrough/3-changelog.md) |
| session-content-search | 2026-08-07 | REQ-095 会话记录内容搜索(⌘K 会话内容分组 + trigram FTS5 + 锚点跳转定位) | [docs/features/session-content-search/3-changelog.md](docs/features/session-content-search/3-changelog.md) |
| session-list-ux | 2026-08-07 | REQ-096 会话列表操作体验(标题 blur 保存 + 行右键菜单 + 归档撤销,含投影层取消归档持久化修复) | [docs/features/session-list-ux/3-changelog.md](docs/features/session-list-ux/3-changelog.md) |
| in-session-find | 2026-08-07 | REQ-097 会话内查找 ⌘F(查找条+高亮+跳转+⌘K 联动定位) | [docs/features/in-session-find/3-changelog.md](docs/features/in-session-find/3-changelog.md) |
| native-menu-i18n | 2026-08-07 | 原生右键菜单标签跟随 app 语言设置(19 语言,IPC 重挂即时生效) | [docs/features/native-menu-i18n/3-changelog.md](docs/features/native-menu-i18n/3-changelog.md) |
| fork-param-merge-guard | 2026-08-09 | REQ-104 fork 关键参数 `--conditions=browser` 三道锁防上游 merge 丢失(pre-commit §4.6 + `_fork_notes` + merge guide §3.6/§5.7) | [docs/features/fork-param-merge-guard/3-changelog.md](docs/features/fork-param-merge-guard/3-changelog.md) |
| pre-commit-forkfile-exempt | 2026-08-10 | REQ-048 最小子集:黑名单对 fork 自建文件动态豁免(tag `upstream-base` 基准树判定,fail-closed,merge guide §5.8 移 tag) | [docs/features/pre-commit-forkfile-exempt/3-changelog.md](docs/features/pre-commit-forkfile-exempt/3-changelog.md) |
| opencode-test-baseline | 2026-08-10 | REQ-105 opencode 单测可信基线 Mac 段(确定性失败 2 条入快照,flaky 附录;Win 段待接力) | [docs/features/opencode-test-baseline/3-changelog.md](docs/features/opencode-test-baseline/3-changelog.md) |
| voice-preclear-batch | 2026-08-18 | REQ-084① 迁移污染检测 + REQ-117 performance e2e 套件复活(6 条红全部定位到「测试前提过期」,含真 Electron resize 真机复核)+ submit.test 修复 + i18n CRLF/死 key 清理 + REQ-068 归档 | [docs/features/voice-preclear-batch/3-changelog.md](docs/features/voice-preclear-batch/3-changelog.md) |
| req-119-chat-selection-pseudo-path | 2026-08-19 | REQ-119 聊天引用伪路径不再泄漏进后端(前端源头不发 + 后端短路兜底 + 常量提到 core 共享),消除每次引用注入的假 Read 调用与会话 Error | [docs/features/req-119-chat-selection-pseudo-path/3-changelog.md](docs/features/req-119-chat-selection-pseudo-path/3-changelog.md) |
| req-123-revert-pure-quote-message | 2026-08-19 | REQ-123 纯引用消息没有撤回按钮 + 撤回保真回填引用卡片(动作条判定抽 fork-only 纯函数;metadata 逆函数 + replaceComments 四点对称收敛;顺带修 ↑ 历史找回的聊天引用退化) | [docs/features/req-123-revert-pure-quote-message/3-changelog.md](docs/features/req-123-revert-pure-quote-message/3-changelog.md) |
